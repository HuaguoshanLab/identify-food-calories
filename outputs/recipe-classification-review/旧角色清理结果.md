# 旧角色清理结果（2026-09-17）

已删除菜谱候选旧 meal_role 字段、维护接口、批量设置弹窗、列表列及相关运行代码。当前分类统一使用配餐用途、餐内角色、食材标签。历史餐单组成项的角色快照、历史审计和旧迁移保留，用于历史追溯。

开发数据库已执行 0031。迁移前备份见 before-drop-legacy-role.json；校验见 drop-legacy-role-verification.json。623 条记录保留，除删除字段外，其他字段逐项一致。

CSV 新模板为 11 列，包含三维分类及分类依据；支持无分类的七列文件，拒绝旧八列角色模板。填写分类时用途、角色、依据必填。

验证结果：

- 后端管理、规划及定向 API/图测试：311 项通过。
- 日志回归测试：6 项通过。
- 隔离 PostgreSQL 集成测试：25 项通过，包含迁移验证。
- 后台菜谱与审计组件：10 项通过。
- 后台菜谱管理 E2E、H5 组合餐 E2E：各 1 项通过，使用隔离数据库和 Fake Provider。
- 后台类型检查、构建、Ruff、差异检查通过；构建仍提示现有产物超过 500 KB。
- 内置浏览器登录已过期；使用 Chrome 现有管理员会话刷新真实开发页面，确认总数 623、旧按钮和旧列消失、三维分类显示正常；打开并取消导入弹窗，确认新模板说明。真实开发库未通过该弹窗新增数据；CSV 导入保存由上述 E2E 验证。

E2E 还定位并修复已有响应错误：餐单保存后的日志代码把字符串状态当枚举读取，导致成功提交返回 500。现已直接记录字符串状态，并补充回归测试。

本轮未验证真实模型或 Mem0 效果；分类覆盖及份量质量的既有限制不因删除旧字段而消失。
