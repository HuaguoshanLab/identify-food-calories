# 旧菜谱三维分类回填结果

2026-09-17，真实本地开发库 `food_agent_dev`；策略 `recipe-classification.v1`。

已通过内置浏览器后台“补齐三维分类”预览并保存全部 623 条非删除候选。新增配餐用途、餐内角色、食材标签及分类依据；不重导菜谱。0030 迁移已执行。

| 维度 | 结果 |
|---|---|
| 配餐用途 | 整餐候选 130、组合组成项 342、待确认 151；两者皆可 0 |
| 餐内角色 | 蛋白质菜 224、混合主餐 130、其他配菜 50、汤羹 21、主食 20、蔬菜菜肴 10、奶及替代品 10、饮品 6、水果 1、待确认 151 |
| 食材标签 | 351 条有名称线索标签，272 条暂无足够标签证据 |
| 审计 | 623 条逐项分类记录及 1 条批次记录 |

三个字段均已回填，但不等于每条都已人工确认。151 条用途／角色待确认与 272 条标签待确认有重叠，不应相加。名称回填不证明完整配料，也不证明过敏原缺失。没有为了填满而猜测地方菜的隐藏配料。

已逐字段核对修改前后：只改变分类、修订号和更新时间；原名称、份量、说明、目录关联、启停状态、旧配餐角色等保持不变。当前生成仍使用旧角色，新分类尚未切换进配餐算法。历史餐单不执行任何回写。

文件：`before.json` 为回填前快照；`after.json` 为回填后快照；`verification.json` 为计数和核对结果；`needs-review.json` 包含用途／角色或食材标签待复核条目。

验证：后台领域及 API 合约测试 42 项、真实隔离 PostgreSQL 测试 25 项、后台组件测试 10 项、后台端到端 1 项通过；Ruff、类型检查、构建、差异检查通过。内置浏览器真实页面确认“已补齐 623 条”及三列分类展示；端到端另验证刷新、重复跳过、旧角色修改、生命周期和审计可读。

限制：当前只提供自动回填预览及保存，不提供三维字段逐条人工编辑表单；旧 CSV 不携带新分类。新分类未参与规划，不应据此宣称配餐规则已经升级。回退须基于审计前值另做带版本检查的修正，不能通过降级迁移删列或直接覆盖已有分类。
