# 目录草稿 HTTP 合约

## 职责

`catalog/api/` 是目录草稿请求、严格 Zod DTO 与安全错误分类的唯一所有者。页面只能调用这里导出的命令，不能自行 `fetch`、拼接后台 URL 或信任未校验的响应。

## 允许依赖

- 可依赖 Zod、浏览器 `fetch` 和经构建验证的 `__ADMIN_API_BASE_URL__`。
- 可接受调用方临时传入的内存 access token；不得保存、记录或返回 token。
- 生命周期预览和命令支持可选 AbortSignal；批量调用设置单请求超时，超时不代表服务端未提交，必须原键重试。
- 禁止依赖 React 组件、用户 H5、后端源码、数据库、Provider SDK 或密钥。

## 文件索引

| 路径 | 职责 |
| --- | --- |
| `README.md` | HTTP 合约边界、允许依赖和文件索引。 |
| `index.ts` | 严格草稿/生命周期/分页/CSV DTO、筛选读取、文件预览/导入/下载，以及 publication 级嵌入聚合状态读取与带理由、幂等键的批量重试命令；所有响应均拒绝额外字段。 |
