# Phase 3: 多模态餐食分析闭环 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-31
**Phase:** 3-多模态餐食分析闭环
**Areas discussed:** 图片入口与隐私提示、识别不确定时的追问策略、模型失败、重试与防重复计费

---

## 图片入口与隐私提示

| 问题 | 备选 | 用户选择 |
| --- | --- | --- |
| 上传入口 | 同时提供拍照和相册选择；二级来源选择；默认相机 | 同时提供拍照和相册选择 |
| 隐私说明 | 上传区可展开简短承诺；首次强制确认；仅隐私页说明 | 上传区可展开简短承诺 |
| 不合规图片 | 前端预提示、服务端最终裁决；全由服务端；浏览器转码 | 前端预提示、服务端最终裁决 |
| 完成后的原图 | 完成即删；关闭结果页再删；短时保留 | 完成即删 |

**User's choice:** A、A、A、A。
**Notes:** 定向修正不依赖原图，重新上传须建立新分析。

---

## 识别不确定时的追问策略

| 问题 | 备选 | 用户选择 |
| --- | --- | --- |
| 何时确认 | 仅阻塞项集中追问；逐项确认；模型全部自行决定 | 仅阻塞项集中追问 |
| 模糊菜名 | 最多 3 个候选并给出线索；仅重新描述；尽可能多候选 | 最多 3 个候选并给出线索 |
| 份量不足 | 受控份量/补充信息；一律克数；视觉估重直接计算 | 视觉估重直接计算 |
| 估重边界 | 标记不确定性且阈值追问；永不追问；全部先确认 | 标记不确定性且阈值追问 |

**User's choice:** A、A、C、A。
**Notes:** 视觉估重可参与计算，但报告必须标记其估算性质；映射不唯一或低于阈值仍集中追问。

---

## 模型失败、重试与防重复计费

| 问题 | 备选 | 用户选择 |
| --- | --- | --- |
| 瞬时失败 | 自动重试一次并幂等；无限重试；完全手动 | 自动重试一次并幂等 |
| 结果状态未知 | 先对账且不自动重调；直接重调；直接判失败 | 先对账且不自动重调 |
| 持续视觉失败 | 降级文字分析；只允许稍后再试；通用失败页 | 降级文字分析 |
| 错误透明度 | 稳定失败类别；暴露 Provider 原文；统一“失败” | 稳定失败类别 |

**User's choice:** A、A、A、A。
**Notes:** 降级必须明确说明图片没有被识别。

---

## the agent's Discretion

- 可信阈值、支持格式和尺寸上限、暂存/删除实现、Provider 对账协议、错误码、Qwen-VL 官方资料核验、冻结评测门槛与报告控件细节。

## Deferred Ideas

None.
