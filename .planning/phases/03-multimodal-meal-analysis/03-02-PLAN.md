---
phase: 03-multimodal-meal-analysis
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [backend/app/providers/vision/qwen.py, backend/app/providers/vision/factory.py, backend/app/core/config.py, backend/.env.example, backend/tests/test_qwen_vision_provider.py, backend/tests/unit/test_runtime_foundation.py, backend/app/providers/vision/README.md, backend/README.md]
autonomous: false
user_setup:
  - service: Alibaba Cloud Model Studio
    why: production Qwen-VL requires a user-owned workspace, chosen data region/deployment scope and uncommitted API key
requirements: [VIS-03, VIS-05, VIS-06]
decision_ids: [D-08, D-09]
must_haves:
  truths: ["Qwen-VL adapter only receives validated temporary input and returns Pydantic-validated observations.", "网络结果未知绝不自动二次调用；真实 transient 最多一次且在预算内重试。", "隐私文案只能使用已验证的区域/部署范围事实。"]
  artifacts:
    - path: backend/app/providers/vision/qwen.py
      provides: Qwen-VL HTTP adapter with safe accounting and error classification
  key_links:
    - from: backend/app/providers/vision/qwen.py
      to: backend/app/providers/vision/dto.py
      via: strict response extraction then Pydantic validation
      pattern: VisionMealResult
---
<objective>接入真实 Qwen-VL adapter，但把它限制在可验证地区、结构化输出、计量和安全重试边界内。Output: 真实 adapter、Fake transport 测试和需用户提供的生产配置说明。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03-multimodal-meal-analysis/03-RESEARCH.md @backend/app/providers/reasoning/deepseek.py @backend/app/providers/vision/dto.py @backend/app/images/service.py</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现 Qwen-VL 结构化观察 adapter 和安全计量</name>
  <read_first>
    - backend/app/providers/reasoning/deepseek.py
    - backend/app/providers/vision/dto.py
    - backend/app/providers/vision/ports.py
    - .planning/phases/03-multimodal-meal-analysis/03-RESEARCH.md
    - backend/app/core/config.py
  </read_first>
  <files>backend/app/providers/vision/qwen.py, backend/app/providers/vision/factory.py, backend/app/core/config.py, backend/tests/test_qwen_vision_provider.py, backend/app/providers/vision/README.md</files>
  <action>以 `httpx.AsyncClient` 和可注入 transport 实现 `QwenVisionModelProvider`。仅把经过 ImageSafetyService 的临时文件按选定官方 API 支持的受限方式传给 Qwen；设置固定非-thinking、版本化 JSON 输出指令、显式模型 ID、max completion tokens 和版本化 min/max pixels。把响应 content 解析为 JSON 后由 `VisionMealResult` 校验；任何空 content、非对象、未知字段、超界/缺失字段都成为 `PROVIDER_SCHEMA_INVALID`。仅记录 provider request id、model alias、image/prompt/completion tokens、计算成本、延迟、版本和安全 digest。429/5xx 可在同一 request key、未标记 outcome unknown 的情况下最多重试一次；timeout/transport loss 一律 `OUTCOME_UNKNOWN`，不重调。适配器不得输出原始响应、prompt、图片或 key。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m pytest tests/test_qwen_vision_provider.py -q</automated></verify>
  <acceptance_criteria>
    - fake HTTP transport 证明正确的图文请求、非 thinking JSON 模式、versioned model/prompt/pixel budget 和 provider request ID/usage 映射。
    - malformed JSON、Pydantic 失败、4xx、429/5xx、timeout 分别映射稳定安全错误；timeout/transport 失败调用数恰为 1。
    - logger、异常和 metadata 不出现 base64、原图、API key、prompt 或原始模型输出。
  </acceptance_criteria>
  <done>真实视觉调用的可变供应商语义被隔离在一个可替换、可计量 adapter 内。</done>
</task>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint 2: 选择 Qwen 生产数据区域并提供未提交配置</name>
  <files>backend/.env.example, backend/app/core/config.py, backend/app/providers/vision/README.md</files>
  <action>用户在 Alibaba Cloud Model Studio 选择 workspace region 与 deployment scope，核实所选 Qwen-VL model 在该范围可用，并确认该区域的 request/result storage 与服务条款。只在未提交 `backend/.env` 提供 `VISION_PROVIDER_MODE=qwen`、Model Studio API key、model ID、region/deployment scope、价格快照版本及每百万 token 价格；不得把 key 或真实图片加入 Git。</action>
  <what-built>Qwen adapter 的 production 配置、隐私边界和测试隔离。</what-built>
  <how-to-verify>使用 Model Studio console/当前官方 docs 对照 `.env.example`，确认图片由第三方按所选区域处理；应用应在任一缺失/无效设置时拒绝 production 启动。可选地用非敏感测试图执行一次人工 smoke，但不得把响应、原图或 key 写入测试 fixture。</how-to-verify>
  <resume-signal>提供“区域、部署范围、模型 ID 已核实；私钥已在未提交环境变量配置”的确认，或报告无法满足的数据处理限制。</resume-signal>
  <done>生产视觉调用的区域、费用版本和第三方处理告知有真人核验，且敏感配置未进入仓库。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| 超时后重复付费 | Repudiation | Critical | provider cost/run integrity | app→Qwen | Task 1 | timeout causes OUTCOME_UNKNOWN and one HTTP call |
| 选错区域/错误的零留存承诺 | Information disclosure | High | image processing facts | product→provider | Checkpoint 2 | human region/scope confirmation |
| 模型输出绕过结构化验证 | Tampering | High | graph input | provider→DTO | Task 1 | invalid JSON/schema fixtures fail closed |
</threat_model>
<verification>adapter transport tests通过；production 真实调用仅在 Checkpoint 2 完成人工核验后执行。</verification>
<success_criteria>Qwen-VL 可用但不被信任：区域事实、响应结构、费用和重复调用均由受控边界约束。</success_criteria>
<output>Create 03-02-SUMMARY.md when done.</output>
