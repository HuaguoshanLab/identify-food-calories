---
phase: 03-multimodal-meal-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/images/README.md, backend/app/images/schemas.py, backend/app/images/service.py, backend/app/images/repository.py, backend/app/providers/vision/README.md, backend/app/providers/vision/dto.py, backend/app/providers/vision/ports.py, backend/app/providers/vision/fake.py, backend/app/providers/vision/factory.py, backend/app/providers/README.md, backend/app/core/config.py, backend/.env.example, backend/tests/unit/test_image_safety.py, backend/tests/unit/test_vision_provider.py, backend/tests/unit/README.md, backend/app/README.md, backend/README.md]
autonomous: true
requirements: [VIS-02, VIS-03, VIS-06]
decision_ids: [D-02, D-03, D-04]
must_haves:
  truths: ["未验证的上传永远不能到达 Vision Provider。", "数据库、日志、Fake trace 和 Provider DTO 都不保存原图、base64 或 EXIF。", "Fake Vision Provider 能脚本化多菜、低置信度、schema 无效、transient、permanent 与 outcome_unknown。"]
  artifacts:
    - path: backend/app/images/service.py
      provides: 有界真实解码、元数据剥离、最小临时引用和 finally/过期删除接口
    - path: backend/app/providers/vision/dto.py
      provides: 与 API/ORM/Graph State 分离的 Pydantic 多菜视觉契约
  key_links:
    - from: backend/app/images/service.py
      to: backend/app/providers/vision/ports.py
      via: 仅传递已验证临时引用
      pattern: ImageSafetyService
---
<objective>交付可单测的安全图片临时处理与可替换 Vision Provider 基座。Output: 不含敏感原图的临时引用、受限 DTO/port/fake/factory 和 fail-closed 配置。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03-multimodal-meal-analysis/03-CONTEXT.md @.planning/phases/03-multimodal-meal-analysis/03-RESEARCH.md @.planning/phases/03-multimodal-meal-analysis/03-PATTERNS.md @backend/app/providers/reasoning/dto.py @backend/app/core/config.py</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 建立安全图片临时处理 Service 与最小引用</name>
  <read_first>
    - AGENTS.md
    - backend/AGENTS.md
    - backend/app/core/config.py
    - backend/app/agent/service.py
    - backend/app/agent/retention.py
    - backend/tests/unit/README.md
  </read_first>
  <files>backend/app/images/README.md, backend/app/images/schemas.py, backend/app/images/service.py, backend/app/images/repository.py, backend/app/core/config.py, backend/.env.example, backend/tests/unit/test_image_safety.py, backend/tests/unit/README.md, backend/app/README.md, backend/README.md</files>
  <action>创建带 README 的 `app/images/`。实现 `ImageSafetyService` 和 `ValidatedImageReference`：只接受明确 allowlist 的 JPEG、PNG、WebP；先限制上传字节数，再用 Pillow/等价真实解码确认格式、宽高和总像素，拒绝损坏文件、声明 MIME 与解码格式不符、超限和解压炸弹。剥离 EXIF/其他 metadata 后以随机 UUID 文件名写入受配置控制的私有临时目录；引用只含 SHA-256 digest、规范 MIME、宽高、字节数、创建/到期时间和不可猜测临时 locator。实现 `delete(reference)` 与过期扫描，成功、验证失败、Provider 异常、超时和用户删除均走 `finally`/补偿路径。Settings 增加显式 image limits/temporary-directory/TTL，生产环境缺失或不安全目录 fail closed。不得返回、日志化、持久化或写 SSE 的 bytes、base64、EXIF、原始文件名或公开 URL。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m pytest tests/unit/test_image_safety.py -q</automated></verify>
  <acceptance_criteria>
    - JPEG、PNG、WebP 在限制内被规范化为无 EXIF 的私有临时文件；解码后引用不包含图片 bytes、base64、原名或坐标 metadata。
    - 伪造 MIME、损坏图、超字节、超像素和压缩炸弹稳定失败且 Provider 调用数为 0。
    - 成功、异常和过期分支结束后临时路径不存在或由可测试过期清理删除。
  </acceptance_criteria>
  <done>上传输入先变为可删除、最小化、受校验的临时引用。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 按既有 Provider 模式建立 Vision DTO、Port、Fake 与 Factory</name>
  <read_first>
    - backend/app/providers/reasoning/dto.py
    - backend/app/providers/reasoning/ports.py
    - backend/app/providers/reasoning/fake.py
    - backend/app/providers/reasoning/factory.py
    - backend/app/images/schemas.py
    - backend/tests/test_reasoning_provider.py
  </read_first>
  <files>backend/app/providers/vision/README.md, backend/app/providers/vision/dto.py, backend/app/providers/vision/ports.py, backend/app/providers/vision/fake.py, backend/app/providers/vision/factory.py, backend/app/providers/README.md, backend/app/core/config.py, backend/tests/unit/test_vision_provider.py, backend/tests/unit/README.md, backend/app/README.md</files>
  <action>创建带 README 的 `app/providers/vision/`，镜像 reasoning 的 Protocol/factory/Fake 分层而不复用其文字请求 DTO。定义 `VisionMealRequest`（仅接收 `ValidatedImageReference` 和版本化 prompt/model/pixel budget）、`VisionMealItemDTO`（菜名、做法/份量线索、可空估计 grams、字段/项目 confidence）和 `VisionMealResult`（items、safe Provider metadata、prompt/schema 版本），所有模型 `extra="forbid"`、数量/文本/grams/置信度有上界。复用稳定 `ProviderFailureKind`/安全错误 envelope；Fake queue API 可精确编排 success、schema-invalid、TRANSIENT、PERMANENT、OUTCOME_UNKNOWN，并且 trace 只记录 call id、版本、usage、cost、latency。Settings 与 factory 提供 `VISION_PROVIDER_MODE=fake|qwen`、模型、区域/部署范围、超时、像素预算和价格快照；测试环境强制 fake，生产在没有完整 Qwen 设置时拒绝启动。同步父 README 索引。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m pytest tests/unit/test_vision_provider.py -q &amp;&amp; .venv/bin/python -m mypy app/images app/providers/vision app/core/config.py</automated></verify>
  <acceptance_criteria>
    - Vision Provider DTO、HTTP schema、ORM model 和 `MealAgentState` 没有共享类。
    - fake 调用 trace 没有图片路径、原名、bytes、base64、识别原文或 EXIF。
    - test 强制 fake；production 选择 fake 或缺 Qwen region/model/key/price 设置均 fail closed。
  </acceptance_criteria>
  <done>Graph 可依赖可脚本化视觉观察而不依赖云 SDK 或不可信图片。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| 伪造/畸形或压缩炸弹图片 | Tampering/DoS | High | worker memory、文件系统 | browser→image service | Task 1 | bad MIME/decode/size/pixel fixtures zero Provider calls |
| EXIF/GPS、原图或 base64 泄漏 | Information disclosure | Critical | 用户图片与位置 | upload→temp/log/DTO | Task 1/2 | metadata/trace/SSE source assertions and deletion tests |
| 测试意外调用付费模型 | Repudiation | High | API key/cost | test→factory | Task 2 | APP_ENV=test always Fake assertion |
</threat_model>
<verification>安全图片单测、Vision fake/contract 单测、mypy 和目录 README 合同全部通过。</verification>
<success_criteria>任何图像输入都先经受限解码/清理，Vision 输出是独立、可测试、无敏感 payload 的结构化观察。</success_criteria>
<output>Create 03-01-SUMMARY.md when done.</output>
