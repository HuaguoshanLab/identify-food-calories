---
phase: 03-multimodal-meal-analysis
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [backend/migrations/versions/0006_multimodal_images.py, backend/migrations/versions/README.md, backend/app/agent/models.py, backend/app/agent/ports.py, backend/app/agent/repository.py, backend/app/agent/service.py, backend/app/agent/state.py, backend/app/agent/graph.py, backend/app/agent/schemas.py, backend/app/agent/api.py, backend/app/main.py, backend/openapi-agent-v1.json, backend/tests/unit/test_agent_multimodal.py, backend/tests/integration/test_agent_multimodal.py, backend/tests/integration/README.md, backend/app/agent/README.md, backend/README.md, frontend/src/features/agent/api/generate-contracts.mjs, frontend/src/features/agent/api/client.generated.ts, frontend/src/features/agent/api/schemas.generated.ts, frontend/src/features/agent/api/README.md]
autonomous: true
requirements: [VIS-03, VIS-04, VIS-05, VIS-06, NUT-06, NUT-07]
decision_ids: [D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11]
must_haves:
  truths: ["登录用户可将一张安全图片以同一 thread_id 进入视觉节点，模型候选仍经受控目录和确定性工具。", "未知/低可信项集中 interrupt，视觉估重在报告中标记且不伪造为测量。", "持久化最小图片元数据、调用状态与删除链均隔离用户，永不保存原图/base64。"]
  artifacts:
    - path: backend/migrations/versions/0006_multimodal_images.py
      provides: 最小图片/视觉调用 metadata schema
    - path: backend/tests/integration/test_agent_multimodal.py
      provides: 真实 PostgreSQL 的图片到可信报告、恢复、删除与幂等证据
  key_links:
    - from: backend/app/agent/graph.py
      to: backend/app/providers/vision/ports.py
      via: VisionModelProvider observation then NutritionToolAdapter
      pattern: analyze_meal_image
---
<objective>把安全图片、Vision observation、已有 LangGraph/营养工具、Postgres ledger 和版本化 API 接成一个可恢复的后端垂直切片。Output: migration、状态/图/API、真实 PG integration 和 generated OpenAPI contract。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03-multimodal-meal-analysis/03-CONTEXT.md @.planning/phases/03-multimodal-meal-analysis/03-UI-SPEC.md @backend/app/agent/{api,service,state,graph,models,repository,schemas}.py @backend/migrations/versions/0004_agent_core.py</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 迁移并实现图片/调用最小审计、用户隔离和删除链</name>
  <read_first>
    - backend/migrations/versions/0004_agent_core.py
    - backend/app/agent/models.py
    - backend/app/agent/repository.py
    - backend/app/agent/service.py
    - backend/app/agent/retention.py
    - backend/tests/integration/test_agent_retention.py
  </read_first>
  <files>backend/migrations/versions/0006_multimodal_images.py, backend/migrations/versions/README.md, backend/app/agent/models.py, backend/app/agent/ports.py, backend/app/agent/repository.py, backend/app/agent/service.py, backend/tests/integration/test_agent_multimodal.py, backend/tests/integration/README.md, backend/app/agent/README.md, backend/README.md</files>
  <action>新建 Alembic `0006_multimodal_images.py`，只增加用户/线程/运行绑定的最小图片处理与视觉 invocation metadata：SHA-256 digest、规范 MIME、dimensions、bytes、lifecycle status、expiry/deleted timestamps、safe locator 或受控删除 handle、provider request id、attempt/status/cost/safe result digest/failure code；使用 FK/cascade、status check、tenant/expiry index 和 idempotency unique constraint。禁止原图/base64/EXIF/原名/公开 URL/text prompt 字段。扩展 repository/service，以 owner filter 获取、创建/复用、claim/update、完成/unknown 和删除这些记录；删除 intent、失败补偿和过期 worker 都调用 ImageSafetyService 删除且在真实 PG 事务边界保持可重试。迁移和 integration tests 证明 owner A 看不到/删不掉 B，任何路径不存敏感图像，且过期/用户删除清理文件和 metadata。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python tests/run_pg.py --env-file .env.test.example -- .venv/bin/python -m pytest tests/integration/test_agent_multimodal.py -q -k "migration or ownership or deletion or expiry"</automated></verify>
  <acceptance_criteria>
    - 从 0001 迁移到 head 后图片/视觉 metadata schema 存在且无 binary/base64/EXIF/original-name 列。
    - 跨用户读取、上传线程、重试、删除均为统一不可发现结果；A 的删除不会影响 B。
    - 成功、Provider 异常、过期与用户删除都删除临时文件，并清理/最小化相关 metadata。
  </acceptance_criteria>
  <done>图片生命周期具备权威、可审计、可删除且租户隔离的最小存储边界。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 接通 Vision 节点、确定性营养、multipart API 与 OpenAPI</name>
  <read_first>
    - backend/app/agent/graph.py
    - backend/app/agent/state.py
    - backend/app/agent/api.py
    - backend/app/agent/schemas.py
    - backend/app/agent/tools.py
    - backend/app/providers/vision/ports.py
    - backend/app/nutrition/service.py
    - frontend/src/features/agent/api/generate-contracts.mjs
  </read_first>
  <files>backend/app/agent/state.py, backend/app/agent/graph.py, backend/app/agent/service.py, backend/app/agent/schemas.py, backend/app/agent/api.py, backend/app/main.py, backend/openapi-agent-v1.json, backend/tests/unit/test_agent_multimodal.py, backend/tests/integration/test_agent_multimodal.py, frontend/src/features/agent/api/generate-contracts.mjs, frontend/src/features/agent/api/client.generated.ts, frontend/src/features/agent/api/schemas.generated.ts, frontend/src/features/agent/api/README.md</files>
  <action>扩展 `MealAgentState` 为版本化、JSON-safe 的视觉摘要（digest/reference status、结构化 items、grams estimate/uncertainty、safe provider metadata），不保存图片或 Provider DTO 原文。给 `AgentNextAction` 增加受限 vision 分支；只有 validated、未完成、未 outcome-unknown 的同一 invocation 才调用 `VisionModelProvider`。将视觉 item 变成 `StateMealItem` 后继续现有受控 `search_food_catalog → calculate_nutrition → validate_nutrition_result`；目录外项明确 `unaccounted_items`，未授权/无资格目录数据绝不进入公开结果。映射不唯一或低估重可信度时一次性生成全部 clarification；估重进入计算时报告每项带 `is_estimated`/uncertainty。API 新增认证 multipart 图片创建/重试/状态查询所需 operation，先验证 thread owner 和 command key，再调用 Service；SSE 只安全阶段/summary。OpenAPI-first 生成 frozen contract、Zod schemas 和 TS client，禁止手改 generated 文件。测试覆盖 Fake Vision 多菜、候选上限 3、低可信追问、partial report、outcome unknown 零盲重调与 snapshot resume。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m pytest tests/unit/test_agent_multimodal.py -q &amp;&amp; .venv/bin/python tests/run_pg.py --env-file .env.test.example -- .venv/bin/python -m pytest tests/integration/test_agent_multimodal.py -q -k "graph or upload or resume or outcome" &amp;&amp; cd ../frontend &amp;&amp; node src/features/agent/api/generate-contracts.mjs check-all</automated></verify>
  <acceptance_criteria>
    - Graph never imports agent ORM/repository and never uses a model value as final nutrition; report numbers originate from NutritionToolAdapter output.
    - one image creates/reuses one idempotent invocation; transient failures retry once, outcome unknown does not automatically produce a second provider call.
    - public snapshot/SSE/OpenAPI excludes images, base64, EXIF, prompt, model raw text and chain-of-thought while exposing safe status, report and recovery code.
  </acceptance_criteria>
  <done>后端可安全地把多菜视觉观察转为可追问、可校验、可恢复的营养报告。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| 跨用户图片/线程访问 | Spoofing/Information disclosure | Critical | image metadata/checkpoint | API→Service→repository | Task 1/2 | cross-user 404 before provider/saver |
| 模型伪造营养或未授权数据进入报告 | Tampering | Critical | nutrition result | vision→graph→tools | Task 2 | deterministic tool/provenance and unqualified catalog tests |
| unknown 结果盲重调 | Repudiation | Critical | cost/invocation ledger | Service→Provider | Task 1/2 | outcome unknown one-call integration test |
</threat_model>
<verification>迁移、真实 PG 多模态 integration、unit graph/API、OpenAPI drift check 均通过。</verification>
<success_criteria>一个登录用户能提交安全图片并以同一线程获得安全、可追问、确定性营养支持的多菜报告。</success_criteria>
<output>Create 03-03-SUMMARY.md when done.</output>
