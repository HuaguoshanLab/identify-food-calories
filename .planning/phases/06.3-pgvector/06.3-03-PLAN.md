---
phase: 06.3-pgvector
plan: "03"
type: execute
wave: 3
depends_on: ["06.3-02"]
files_modified: [backend/app/core/config.py, backend/.env.example, backend/app/providers/embedding/dashscope.py, backend/app/providers/embedding/factory.py, backend/app/providers/embedding/README.md, backend/tests/unit/test_embedding_provider.py, backend/tests/unit/README.md]
autonomous: true
requirements: [HFS-06, HFS-09]
must_haves:
  truths: ["Test always selects Fake and production requires validated DashScope configuration.", "Semantic search may be explicitly disabled without breaking exact/text search."]
  artifacts: [{path: backend/app/providers/embedding/dashscope.py, provides: "Bounded production HTTP adapter"}, {path: backend/app/providers/embedding/factory.py, provides: "Environment-safe Provider selection"}]
  key_links: [{from: backend/app/providers/embedding/factory.py, to: backend/app/core/config.py, via: "validated settings", pattern: "text-embedding-v4|1024"}, {from: backend/app/providers/embedding/dashscope.py, to: backend/app/providers/embedding/ports.py, via: "strict Protocol implementation", pattern: "EmbeddingProvider"}]
---
<objective>Wire Provider configuration and factory without coupling it to application lifespan.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-02-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-CONTEXT.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement and safely select the production DashScope Provider</name>
  <read_first>backend/app/providers/vision/factory.py, backend/app/core/config.py, backend/.env.example, backend/app/providers/embedding/README.md, backend/tests/unit/test_embedding_provider.py</read_first>
  <files>backend/app/core/config.py, backend/.env.example, backend/app/providers/embedding/dashscope.py, backend/app/providers/embedding/factory.py, backend/app/providers/embedding/README.md, backend/tests/unit/test_embedding_provider.py, backend/tests/unit/README.md</files>
<behavior>- `APP_ENV=test` always creates Fake without credentials or network.
- Production enabled mode requires key, pinned host/scope, model, dimension, timeout and cost caps.
- DashScope uses one bounded retry for 429/5xx, propagates cancellation, enforces 1.5 seconds, and maps response/status failures to body-free safe codes.
- Invalid/missing production configuration fails closed; explicitly disabled semantic mode is allowed.</behavior>
  <action>Implement the HTTPX DashScope adapter against Plan 02 contracts, pinning HTTPS host/model/dimension/adapter identity and validating every response before return. Add validated settings and `create_embedding_provider`, following the vision factory. Prevent request-level endpoint override and production Fake. Document environment keys, add `dashscope.py` and `factory.py` to the embedding child README file index with their responsibilities/allowed dependencies, and update the unit-test index.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/unit/test_embedding_provider.py -k factory -q &amp;&amp; uv run pytest tests/architecture/test_directory_contract.py -q &amp;&amp; uv run mypy app/providers/embedding app/core/config.py</automated></verify>
  <acceptance_criteria>MockTransport covers timeout/status/retry/count/dimension/NaN/Inf without response-body leakage; factory matrix proves Fake-only tests, production fail-closed behavior and explicit text-fallback mode; directory-contract validation sees `dashscope.py` and `factory.py` in `providers/embedding/README.md`.</acceptance_criteria>
  <done>Runtime selection is deterministic and safe.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-03-01 | Spoofing | provider endpoint | mitigate | Pinned HTTPS host/scope is settings-owned, never request-controlled. |
| T-063-03-SC | Tampering | factory imports | mitigate | Only factory imports concrete adapter; tests assert boundary. |
</threat_model>
<verification>Run factory matrix and mypy.</verification>
<success_criteria>Provider construction is explicit, offline-testable and fail closed in production.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-03-SUMMARY.md`.</output>
