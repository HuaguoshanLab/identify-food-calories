---
phase: 06.3-pgvector
plan: "09"
type: execute
wave: 6
depends_on: ["06.3-06", "06.3-08"]
files_modified: [backend/app/admin/api.py, backend/app/admin/schemas.py, backend/app/admin/service.py, backend/app/admin/repository.py, backend/app/admin/README.md, backend/tests/unit/test_admin_catalog_api.py, backend/tests/integration/test_catalog_search_admin.py, backend/tests/integration/README.md]
autonomous: true
requirements: [HFS-05, HFS-06]
must_haves:
  truths: ["Only a DB-confirmed admin can read/retry indexing or curate versioned relation evidence.", "Controlled relation evidence is production-reachable, immutable by version and revocable without changing candidate eligibility."]
  artifacts: [{path: backend/app/admin/api.py, provides: "RBAC status, retry and relation-evidence routes"}, {path: backend/tests/integration/test_catalog_search_admin.py, provides: "DB-backed relation governance proof"}]
  key_links: [{from: backend/app/admin/api.py, to: backend/app/admin/service.py, via: "DB-authorized control-plane methods", pattern: "embedding-status|embedding-retries|relation-evidence"}, {from: backend/app/admin/service.py, to: backend/app/admin/repository.py, via: "versioned relation and audit transaction", pattern: "relation_evidence"}]
---
<objective>Expose the search-index control plane through strict, DB-authorized and audited admin APIs.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-08-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add publication status and batch retry endpoints</name>
  <read_first>backend/app/admin/api.py, backend/app/admin/schemas.py, backend/app/admin/service.py, backend/tests/unit/test_admin_catalog_api.py</read_first>
  <files>backend/app/admin/api.py, backend/app/admin/schemas.py, backend/app/admin/service.py, backend/app/admin/README.md, backend/tests/unit/test_admin_catalog_api.py</files>
  <behavior>- GET `/catalog-publications/{publication_id}/embedding-status` returns aggregate counts and explicit job rows.
- POST `/catalog-publications/{publication_id}/embedding-retries` requires Idempotency-Key and bounded reason and retries the failed batch.
- Both reload ADMIN role from PostgreSQL; user gets 403, missing gets 404, lifecycle/concurrency mismatch gets 409.
- Responses never expose submitted names, vectors, provider bodies, endpoints or secrets.</behavior>
  <action>Add strict routes and stable exception mapping over the Plan 08 service methods. Keep authorization in service as DB-backed `require_role(ADMIN)`; API must not operate on job IDs or mutate repository directly.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/unit/test_admin_catalog_api.py -k embedding -q</automated></verify>
  <acceptance_criteria>Unit tests assert exact paths, aggregate/job schema, RBAC, idempotency headers, 403/404/409 and forbidden-field absence.</acceptance_criteria>
  <done>The admin HTTP contract is strict, publication-scoped and auditable.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Curate versioned relation evidence through an audited admin boundary</name>
  <read_first>backend/app/admin/api.py, backend/app/admin/schemas.py, backend/app/admin/service.py, backend/app/admin/repository.py, backend/tests/integration/test_catalog_publish_eligibility.py</read_first>
  <files>backend/app/admin/api.py, backend/app/admin/schemas.py, backend/app/admin/service.py, backend/app/admin/repository.py, backend/app/admin/README.md, backend/tests/unit/test_admin_catalog_api.py, backend/tests/integration/test_catalog_search_admin.py, backend/tests/integration/README.md</files>
  <behavior>- Create/update/revoke accepts only current controlled source/target publication versions and D-02 relation values; invalid values or stale versions fail without mutation.
- Service reloads actor role from PostgreSQL and requires ADMIN for every read/write; USER and unknown actors are denied.
- Update creates a new immutable evidence version and revoke appends revocation evidence; neither overwrites history.
- Each command requires bounded reason and idempotency key, records before/after/version audit, and replay returns the original result.</behavior>
  <action>Add strict relation-evidence schemas/routes and application methods following existing catalog publication RBAC/audit/idempotency patterns. Keep candidate eligibility authoritative: relation evidence can rank only, never make an ineligible publication searchable. Update the new integration-test README entry with the RBAC, publication-version, invalid-value, revoke and audit assertions.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/unit/test_admin_catalog_api.py -k relation_evidence -q &amp;&amp; uv run python tests/run_pg.py --env-file .env.test.example -- uv run pytest tests/integration/test_catalog_search_admin.py -k relation -q</automated></verify>
  <acceptance_criteria>Real PostgreSQL tests prove ADMIN create/version/revoke/replay, USER/unknown denial, stale-version and invalid-relation rejection, immutable audit, and no eligibility mutation.</acceptance_criteria>
  <done>Controlled relation layers have a production-reachable, versioned and governed source.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-09-01 | Elevation of Privilege | admin routes | mitigate | Service reloads role from DB for read and write. |
| T-063-09-02 | Information Disclosure | API response | mitigate | Strict safe response models reject extras. |
| T-063-09-SC | Tampering | retry request | mitigate | Idempotency key, bounded reason and stable 409 mapping. |
| T-063-09-03 | Tampering | relation evidence | mitigate | Immutable version/revoke records, constrained values, current publication FKs and audit. |
</threat_model>
<verification>Run focused admin API tests.</verification>
<success_criteria>Backend enforces RBAC for status/retry/relation operations and produces governed relation evidence consumed by production retrieval.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-09-SUMMARY.md`.</output>
