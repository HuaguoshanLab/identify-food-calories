---
phase: 06.3-pgvector
verified: 2026-09-12T00:00:00+08:00
status: passed
score: 9/9 requirements verified
overrides_applied: 0
---

# Phase 06.3：混合菜品检索最终倒推验证

**Phase Goal：** 受控目录以唯一 canonical/alias 精确匹配优先；无唯一精确时以确定性 PostgreSQL 文本与隔离 pgvector 召回最多三个当前合格、版本明确、可解释候选；两个 Agent 流程均要求确认；语义故障安全降级、索引可审计，发布结论只来自冻结确定性评测。

**结论：VERIFICATION PASSED**

这不是对 SUMMARY 的复述。结论来自当前实现、冻结 release、测试/CI 定义、代码复审、安全审计以及已有的真实 PostgreSQL 与浏览器验收记录。Phase Goal 与 HFS-01..09 均有实现和可追溯验证支撑；没有发现会让候选自动误选、绕过资格过滤、混用向量空间或伪造发布结论的阻断缺口。

## 目标倒推结果

| Requirement | 必须为真的结果 | 判断 | 当前代码与验证证据 |
|---|---|---|---|
| HFS-01 | 唯一当前合格 canonical/alias 精确匹配优先，`米饭` 只选米饭。 | PASS | `SqlAlchemyHybridFoodSearchRepository.find_current_qualified_exact()` 在 `backend/app/nutrition/search_repository.py:46-62` 走 current-qualified SQL；`NutritionService.search_food_catalog()` 在 `backend/app/nutrition/service.py:63-76` 仅 `len(exact_matches)==1` 才 `PASS`。冻结 release 有 7 个 exact SQL cases，`米饭 → food:rice-v1`，且排除蛋炒饭。 |
| HFS-02 | 非唯一精确时由 pg_trgm 与 pgvector 确定性融合，最多三个、去重、可靠且可解释的候选；五个锁定表达目标在 Top 3。 | PASS | 文本/向量查询分别在 `search_repository.py:64-88`、`90-122`；`search.py:35-66` 先过滤阈值、按 relation layer/RRF/稳定 ID 排序并截断 3。release 的 `target_recall=1.0`，含五个锁定中文表达。 |
| HFS-03 | 所有非精确候选都必须 `ASK`，不能按 Top 1 或分数自动选。 | PASS | `FoodSearchFusionResult` 强制 `ASK`，`FoodSearchResult` 只允许 `PASS` 携带 selected（`schemas.py:137-187`）；服务融合路径总是构造 `ASK`（`service.py:117-143`）。冻结 release 的非精确投影 selected 均为空，52 个相关 unit tests 通过。 |
| HFS-04 | 餐食分析和饮食规划经同一 Tool/Service，得到一致的 action、候选顺序、关系标签；确认后重新读取权威 ID + version。 | PASS | 两个 Tool adapter 都暴露同一 `search_food_catalog` port（`agent/tools.py:50-75,126-127,191-204`）；餐食图 resume 会重新搜索并验证 `food_id + catalog_version`（`agent/graph.py:528-550`）。release 实际执行两图，24/24 entry，`flow_semantics_parity=1.0`。 |
| HFS-05 | 三通道实时排除失格/撤销/旧版本/不可计算记录；向量空间隔离并只在完整、评测合格后原子激活。 | PASS | exact/text/vector 都以 `_current_qualified_statement()` 为起点；vector 查询加入 active vector-space 与 1024 维条件（`search_repository.py:98-111`）。immutable build、完成证据与 activation 的实现和真实 PostgreSQL 集成记录由 Plan 21、Plan 17 及 `test_catalog_embedding_jobs.py` 覆盖；安全审计将相应威胁标为 CLOSED。 |
| HFS-06 | Embedding/向量故障不破坏唯一 exact 或文本 `ASK`；索引任务有有限租约/重试；后台状态和 retry 强制 RBAC、幂等、审计。 | PASS | `service.py:89-116` 对 timeout/transient/outcome-unknown 降级文本，永久真实 provider 错误 fail-closed；worker/job 与管理员服务有有限 attempts、lease、稳定 retry key。Plan 20 的真实 Playwright 记录覆盖 partial failure、管理员重试到 ready、普通用户 GET/POST 403；安全审计未发现开放项。 |
| HFS-07 | 至少 24 个去标识、冻结、版本化案例能稳定产生可复现的证据。 | PASS | `backend/evals/phase_06_3/cases.jsonl` 与 `evaluate.py` 使用 hash chain、严格字段/敏感字段拒绝和 synthetic-only fixture；当前 `release.json` 为 24 cases。验证器本次运行 `evaluate.py --verify-release` 成功；release 绑定 dataset/evaluator/search-policy hashes。 |
| HFS-08 | Langfuse 只作为显式、本地隔离、payload-free 的实验镜像，PASS/FAIL 等价保留 30 天，不影响离线验收。 | PASS | `langfuse_publish.py` 只在 `--publish-langfuse` 后、严格验证 release 后才懒加载 SDK；`langfuse_retention.py` 仅按名称/时间做有界的 30 天 purge/requery，且不在应用 runtime 导入。`docker-compose.langfuse.yml` 为 loopback/internal 配置；导出/留存 unit contracts 已覆盖。 |
| HFS-09 | 发布结论只能来自真实 PostgreSQL 的冻结确定性评测；hash/version/privacy 合同不可绕过。 | PASS | `evaluate.py:712-786` 重算 evidence、case、metrics 和当前源 hash，`--verify-release` 要求 PASS；CI workflow 重新生成并验证 release、运行真实 PostgreSQL contracts 与 Ruff。当前 release 为 `PASS`，且 code review 对 release/Langfuse hash-bound 规则结论为 clean。 |

**评分：9/9 HFS requirements verified。**

## 关键链路与数据流

| 链路 | 验证 |
|---|---|
| 用户查询 → 唯一 exact 或 hybrid `ASK` | `NutritionService` 先 exact 短路；否则 text/vector evidence 经确定性 `fuse_food_search_evidence()`，再 re-materialize 当前合格 authority。没有数据为空的伪组件或静态 API 返回。 |
| candidate → 两张 Graph → 用户确认 → 营养计算 | 图只通过 Tool adapter 调用服务；checkpoint 的 candidate 不是权限，resume 后重新检查当前可见 ID/version，之后 `calculate_nutrition()` 再按 authority read。 |
| publication → immutable build → worker → completion proof → activation | build snapshot、独立 completion proof、active pointer 和 release validator 分离；不能以完成 job 或 release 文本单独替代其余证据。 |
| release → optional Langfuse mirror | evaluator/release 是唯一决策源；publisher 只能镜像已验证的安全 projection，且不会修改 release bytes。 |

## 本次独立自动检查

| 检查 | 结果 |
|---|---|
| `python evals/phase_06_3/evaluate.py --verify-release` | PASS（当前 hash-bound frozen release 可验证） |
| `pytest tests/unit/test_phase063_eval_contract.py tests/unit/test_hybrid_food_search.py tests/unit/test_langfuse_export.py -q` | PASS，52 passed |
| CI-gated backend surface Ruff | PASS |
| `git diff --check` | PASS |

未在本次最终验证进程启动 PostgreSQL 或浏览器服务。因此“fresh real-PG 15 个 integration tests”和真实浏览器路径不是本次新跑的事实，而是已审阅的可复查验收记录：`06.3-EVAL-REVIEW.md` 记录隔离 PostgreSQL fresh release、15 个 integration tests 均通过；`06.3-20-SUMMARY.md` 记录真实 Playwright `1 passed (26.9s)` 与内置浏览器的注册、Mailpit 验证、管理员发布、partial failure、重试 ready 路径。它们支撑跨栈验收，但不被表述成本次重新执行。

## 复审与安全证据

- `06.3-REVIEW.md`：deep targeted re-review，critical/warning/info 均为 0，状态 `clean`。
- `06.3-SECURITY.md`：66 个阶段威胁全部 CLOSED，0 open；唯一 accepted risk 是“前端菜单不是授权边界”，并由后端 PostgreSQL role re-read 补偿，符合本阶段 RBAC 设计。
- Phase 20 真实 UI/Playwright 证据显示普通用户 status GET/retry POST 都得到 403；这验证了授权不依赖前端隐藏。

## 残余风险：非阻断，但不等于生产就绪

`06.3-EVAL-REVIEW.md` 的总分为 77/100、结论为 `NEEDS WORK`。这不是 HFS-01..09 或 Phase Goal 的实现失败，而是四项生产运营/治理缺口，必须在生产发布前闭环：

1. 冻结集 relation、排除项和高后果判断缺少目录管理员、营养师、中文菜系专家、隐私负责人的版本化签核 artifact。
2. allowlist/DTO invariant 拒绝尚无可路由的无 payload 计数/告警；语义降级率、零候选率、确认后失效、索引滞后、预算/任务耗尽也尚未按版本聚合和阈值告警。
3. 没有按 exact/text fallback/hybrid 分通道记录 p50/p95/p99，并将 AI-SPEC 的 50ms/150ms/1.5s 初始 SLO 变成明确 PASS/WARN/FAIL 门禁。
4. 没有本地隔离 Langfuse 的真实 publish → 30 天 purge → requery 演练证据；当前是严格代码/单元合同，不是对 Langfuse 服务的端到端运行证明。

这些项不改变“安全检索和冻结发布机制已实现”的结论；但若要宣称生产运营已验证，当前证据不够。Phase 7 的上线/CI/安全目标也不能被本阶段替代。

---

_Verified: 2026-09-12T00:00:00+08:00_  
_Verifier: gsd-verifier (goal-backward, code-first)_
