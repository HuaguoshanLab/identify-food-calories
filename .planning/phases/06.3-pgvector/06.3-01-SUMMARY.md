---
phase: 06.3-pgvector
plan: "01"
subsystem: infra
tags: [uv, pgvector, langfuse, supply-chain, dependency-lock]
requires:
  - phase: 06.2
    provides: Controlled catalog publication and current-qualified food data boundaries
provides:
  - Exact pgvector runtime and Langfuse dev-only dependency pins
  - Fail-closed registry evidence and direct-dependency drift validation
affects: [06.3 hybrid retrieval, offline Langfuse experiments]
tech-stack:
  added: [pgvector 0.5.0, langfuse 4.14.0]
  patterns: [Exact approved package set with registry-response SHA-256 evidence]
key-files:
  created: []
  modified:
    - backend/pyproject.toml
    - backend/uv.lock
    - backend/supply-chain-evidence.json
    - backend/validate_supply_chain.py
    - backend/tests/unit/test_supply_chain.py
key-decisions:
  - "pgvector==0.5.0 is runtime-only; langfuse==4.14.0 remains in the uv dev dependency group."
  - "The supply-chain CLI defaults to validating checked-in evidence so the plan's verification command is an actual gate."
patterns-established:
  - "Use exact PyPI response hashes plus source-repository metadata for audited direct packages."
  - "Reject direct dependency lock drift and missing SHA-256 artifact hashes before accepting lock reports."
requirements-completed: [HFS-08, HFS-09]
duration: not-recorded
completed: 2026-09-11
---

# Phase 06.3 Plan 01: Supply-chain dependency pins Summary

**Reproducible pgvector runtime support and dev-only Langfuse experiment tooling, backed by exact PyPI evidence and fail-closed dependency-drift tests.**

## Performance

- **Duration:** Not recorded precisely; execution completed in this session.
- **Started:** Not recorded precisely.
- **Completed:** 2026-09-11T02:36:13Z
- **Tasks:** 1/1
- **Files modified:** 5

## Accomplishments

- Added `pgvector==0.5.0` to runtime dependencies and regenerated `uv.lock`.
- Added `langfuse==4.14.0` only to the uv `dev` dependency group.
- Recorded exact PyPI registry-response hashes, publication metadata, and authoritative source repositories for both packages.
- Extended the approval set and test coverage so package/version/group drift and missing SHA-256 lock data fail closed.

## Task Commits

1. **Task 1: Pin audited retrieval and experiment dependencies** - `9ce96f5` (chore)

## Files Created/Modified

- `backend/pyproject.toml` - Declares pgvector runtime dependency, Langfuse dev group, and stable pytest root import path.
- `backend/uv.lock` - Locks the complete resolved graph and package artifact hashes.
- `backend/supply-chain-evidence.json` - Adds exact official PyPI evidence for pgvector and Langfuse.
- `backend/validate_supply_chain.py` - Expands the approved set, provides the default evidence gate, and implements direct-lock validation helpers.
- `backend/tests/unit/test_supply_chain.py` - Covers dependency grouping, fixed package versions, default CLI execution, and lock drift rejection.

## Decisions Made

- `pgvector==0.5.0` is runtime support for PostgreSQL vector operations; `langfuse==4.14.0` is isolated to development experiments and is not an application runtime dependency.
- PyPI raw JSON response SHA-256 values and official source repositories are stored as evidence; the uv mirror lock still records artifact SHA-256 values for reproducible installs.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Made the documented supply-chain command executable**
- **Found during:** Task 1
- **Issue:** `uv run python validate_supply_chain.py` required a subcommand and always exited with argparse usage error.
- **Fix:** Defaulted the CLI to checked-in schema/evidence validation while retaining explicit `verify` and `self-test` commands.
- **Files modified:** `backend/validate_supply_chain.py`, `backend/tests/unit/test_supply_chain.py`
- **Verification:** Default CLI returns `SUPPLY-CHAIN GATE: PASS`.
- **Committed in:** `9ce96f5`

**2. [Rule 3 - Blocking] Restored the missing direct-lock validation implementation**
- **Found during:** Task 1
- **Issue:** The existing supply-chain unit test imported a nonexistent `lock_dependencies.py`, preventing collection and leaving drift validation unavailable.
- **Fix:** Implemented the tested direct dependency, SHA-256, and pip-report validation helpers in the existing supply-chain validator module.
- **Files modified:** `backend/validate_supply_chain.py`, `backend/tests/unit/test_supply_chain.py`
- **Verification:** The full supply-chain unit test suite passes.
- **Committed in:** `9ce96f5`

**3. [Rule 3 - Blocking] Stabilized root gate-module imports under the required pytest command**
- **Found during:** Task 1
- **Issue:** `uv run pytest` did not place `backend/` on `sys.path`, so the existing root-level validator could not be imported.
- **Fix:** Declared `pythonpath = ["."]` in the repository's pytest configuration.
- **Files modified:** `backend/pyproject.toml`
- **Verification:** `uv run pytest tests/unit/test_supply_chain.py -q` passes.
- **Committed in:** `9ce96f5`

**Total deviations:** 3 auto-fixed (1 Rule 1, 2 Rule 3).
**Impact on plan:** All fixes are required for the plan's stated verification command and dependency-drift gate; no application behavior or architecture changed.

## Issues Encountered

- The sandbox initially blocked the uv cache and network DNS. Using an isolated `/tmp` uv cache and approved package downloads resolved the environment limitation; no dependency substitution occurred.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Hybrid retrieval can import the pinned pgvector SQLAlchemy adapter.
- Offline Langfuse experiment work can use the dev dependency without adding it to the online application runtime.

## Self-Check: PASSED

- Confirmed all five modified task files exist.
- Confirmed task commit `9ce96f5` exists and contains no tracked-file deletions.
- Re-ran supply-chain CLI, 19 unit tests, targeted Ruff, `uv lock --check`, and `git diff --check` before this summary.

---
*Phase: 06.3-pgvector*
*Completed: 2026-09-11*
