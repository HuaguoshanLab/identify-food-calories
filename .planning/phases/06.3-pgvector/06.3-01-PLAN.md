---
phase: 06.3-pgvector
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [backend/pyproject.toml, backend/uv.lock, backend/supply-chain-evidence.json, backend/validate_supply_chain.py, backend/tests/unit/test_supply_chain.py]
autonomous: true
requirements: [HFS-08, HFS-09]
must_haves:
  truths: ["pgvector is a pinned runtime dependency and Langfuse is dev-only.", "Dependency drift fails the existing supply-chain gate."]
  artifacts: [{path: backend/uv.lock, provides: "Reproducible dependency graph"}, {path: backend/supply-chain-evidence.json, provides: "Registry and integrity evidence"}]
  key_links: [{from: backend/validate_supply_chain.py, to: backend/pyproject.toml, via: "exact approved package set", pattern: "pgvector|langfuse"}]
---
<objective>Lock the two audited packages independently from Provider and application wiring.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-RESEARCH.md @.planning/phases/06.3-pgvector/06.3-AI-SPEC.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Pin audited retrieval and experiment dependencies</name>
  <read_first>backend/pyproject.toml, backend/uv.lock, backend/supply-chain-evidence.json, backend/validate_supply_chain.py, backend/tests/unit/test_supply_chain.py</read_first>
  <files>backend/pyproject.toml, backend/uv.lock, backend/supply-chain-evidence.json, backend/validate_supply_chain.py, backend/tests/unit/test_supply_chain.py</files>
  <action>Add exact `pgvector==0.5.0` to runtime and `langfuse==4.14.0` to dev only, regenerate the lock, and extend the existing exact registry/repository/integrity evidence and approved-package assertions. Do not add a DashScope SDK; the Provider uses existing HTTPX.</action>
  <verify><automated>cd backend &amp;&amp; uv run python validate_supply_chain.py &amp;&amp; uv run pytest tests/unit/test_supply_chain.py -q</automated></verify>
  <acceptance_criteria>Exact versions and hashes match; Langfuse is absent from runtime dependencies; unexpected direct packages fail closed.</acceptance_criteria>
  <done>Supply-chain prerequisites are reproducible and independently verified.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-01-SC | Tampering | uv packages | mitigate | Exact locks, registry evidence and approved-set validation. |
</threat_model>
<verification>Run supply-chain validation and its unit test.</verification>
<success_criteria>Only audited exact packages enter their intended dependency groups.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-01-SUMMARY.md`.</output>
