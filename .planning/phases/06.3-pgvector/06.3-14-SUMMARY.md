---
phase: 06.3-pgvector
plan: "14"
subsystem: agent-graphs
tags: [langgraph, hybrid-search, clarification, checkpoint]
requires: [06.3-13]
provides: [safe-meal-and-planning-clarification]
affects: [meal-analysis, diet-planning]
tech-stack:
  added: []
  patterns: [offered-id-version-validation, authoritative-reread]
key-files:
  modified: [backend/app/agent/graph.py, backend/app/agent/state.py, backend/app/agent/tools.py, backend/app/planning/service.py]
decisions:
  - "Nonexact planning substitutions persist only a safe candidate projection and re-read the offered identity before use."
metrics:
  tasks_completed: 1
---

# Phase 06.3 Plan 14: Graph Clarification Summary

餐食分析和饮食规划现在都将非精确检索强制停在安全确认边界；恢复只接受原候选的 `(food_id, catalog_version)`，并在使用前重新检查当前目录资格。

## Accomplishments

- 餐食恢复拒绝版本不匹配的候选，并在计算前重新检索当前合格条目。
- 规划替换为非精确查询保存有界安全候选；确认后重新检索，再将 ID/版本传到替换服务。
- 规划服务在替换前通过确定性 Nutrition Port 权威复读，并将指定菜品约束到目标餐次候选，避免“伪确认”。

## Verification

- `uv run --extra dev pytest tests/unit/test_agent_multimodal.py tests/unit/test_diet_planning_graph.py -q` — 17 passed.
- `uv run --extra dev ruff check app/agent app/planning/service.py tests/unit/test_agent_multimodal.py tests/unit/test_diet_planning_graph.py` — passed.
- `uv run python tests/run_pg.py --env-file .env.test.example -- uv run --extra dev pytest tests/integration/test_agent_checkpoint.py -q` — 3 passed.
- 计划指定的 `-k hybrid_search` 当前没有匹配用例（3 deselected）；完整 checkpoint 文件已补跑。

## Deviations from Plan

### Auto-fixed Issues

1. **[Rule 4 - Approved architectural extension]** 规划替换接口原本不接收已确认菜品身份，无法保证确认结果实际约束替换。
   - 扩展了安全 State 投影、Planning Tool 和 PlanningService 的最小参数面，未增加新图或重复检索逻辑。

## Known Stubs

None.

## Self-Check: PASSED

- 已确认本计划修改文件存在；提交后由父执行器合并。
