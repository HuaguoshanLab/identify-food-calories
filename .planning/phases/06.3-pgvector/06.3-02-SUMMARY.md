---
phase: 06.3-pgvector
plan: "02"
subsystem: providers
tags: [pydantic, protocol, embeddings, privacy, testing]
requires:
  - phase: 06.3-01
    provides: locked pgvector and embedding dependency foundation
provides:
  - strict, frozen embedding request/result DTOs
  - async replaceable EmbeddingProvider Protocol and offline Fake
affects: [06.3-03, dashscope-adapter, hybrid-food-search]
tech-stack:
  added: []
  patterns: [provider-only DTOs, finite 1024-vector validation, privacy-safe Fake accounting]
key-files:
  created: [backend/app/providers/embedding/dto.py, backend/app/providers/embedding/ports.py, backend/app/providers/embedding/fake.py, backend/tests/unit/test_embedding_provider.py]
  modified: [backend/app/providers/README.md, backend/tests/unit/README.md]
key-decisions:
  - "Embedding requests contain only normalized food names and text_type; result matching uses scalar input_count rather than echoing names."
  - "The Fake exposes only operation, text type and input count in its retained trace."
patterns-established:
  - "Provider contracts are frozen Pydantic DTOs with extra fields forbidden."
  - "A query has exactly one normalized name; document batches contain at most ten names."
requirements-completed: [HFS-06, HFS-09]
duration: 24min
completed: 2026-09-11
---

# Phase 06.3 Plan 02: Embedding Provider Contracts Summary

**Strict 1024-dimensional finite-float embedding contracts with a context-free async Protocol and offline scripted Fake.**

## Performance

- **Duration:** 24 min
- **Started:** 2026-09-11T02:22:00Z
- **Completed:** 2026-09-11T02:46:32Z
- **Tasks:** 1/1
- **Files modified:** 8

## Accomplishments

- Added frozen, provider-only DTOs that accept only normalized food names, enforce query/document cardinality, and reject extra context.
- Validated every embedding response for matching item count, exactly 1024 dimensions, and finite numeric values.
- Added an async Protocol and zero-network scripted Fake whose retained calls contain safe counters only.
- Created the required embedding directory README and synchronized parent/test indexes.

## Task Commits

1. **Task 1: Build strict embedding contracts and offline Fake** - `d3ddce5` (feat)

## Files Created/Modified

- `backend/app/providers/embedding/dto.py` - frozen boundary DTOs and response invariants.
- `backend/app/providers/embedding/ports.py` - runtime-checkable async `EmbeddingProvider` Protocol.
- `backend/app/providers/embedding/fake.py` - scripted offline provider with privacy-safe trace records.
- `backend/tests/unit/test_embedding_provider.py` - DTO, Fake and no-retention contract coverage.
- `backend/app/providers/embedding/README.md` - directory responsibility, allowed dependencies and file index.
- `backend/app/providers/README.md` and `backend/tests/unit/README.md` - parent indexes for new source/test artifacts.

## Decisions Made

- Embedding output carries only scalar `input_count`; input names are not reflected into outputs or call traces.
- The Protocol is `@runtime_checkable`, allowing contract tests to verify a concrete Fake without coupling production callers to it.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Made the Protocol runtime-checkable**
- **Found during:** Task 1
- **Issue:** `isinstance(fake, EmbeddingProvider)` raised `TypeError`, so the concrete Fake could not be verified against its declared Protocol.
- **Fix:** Added `@runtime_checkable` to `EmbeddingProvider` and reran the provider test suite.
- **Files modified:** `backend/app/providers/embedding/ports.py`, `backend/tests/unit/test_embedding_provider.py`
- **Verification:** `uv run --extra dev pytest tests/unit/test_embedding_provider.py -q` passed (5 tests).
- **Committed in:** `d3ddce5`

**Total deviations:** 1 auto-fixed (1 Rule 1 bug)
**Impact on plan:** Necessary correctness fix; no scope expansion.

## Issues Encountered

- The full directory-contract command remains red on this branch because pre-existing directories `admin-frontend/src/features/recipes/api`, `admin-frontend/src/features/system/api`, and `outputs` have no required README. The new `embedding/` directory itself has its README and parent index; unrelated paths were not modified.
- Initial verification required the existing locked `dev` extra because the default environment has no `pytest` executable.

## User Setup Required

None - production DashScope credentials are declared by the plan but no production adapter is introduced in this plan.

## Next Phase Readiness

Plan 06.3-03 can implement the DashScope adapter behind the validated `EmbeddingProvider` Protocol without importing nutrition schemas or graph state.

## Self-Check: PASSED

- Required source and test artifacts exist in the worktree.
- Task commit `d3ddce5` exists in Git history.

---
*Phase: 06.3-pgvector*
*Completed: 2026-09-11*
