---
phase: 06.3-pgvector
asvs_level: 1
block_on: high
register_authored_at_plan_time: true
threats_total: 66
threats_closed: 66
threats_open: 0
audited_at: 2026-09-11
---

## SECURED

**Phase:** 06.3 — pgvector  
**Threats Closed:** 66/66  
**ASVS Level:** 1

本报告只把当前 `main` 的实现、迁移、冻结测试契约和已落盘的可复现实验/浏览器验收记录作为证据；未以计划文字代替实现。所有 PLAN 均含可解析的 `<threat_model>`，不存在“无计划而默认安全”的情况。

## Threat Register and Verification

| Threat ID | Category | Disposition | Status | Verified evidence |
|---|---|---:|---:|---|
| T-063-01-SC | Tampering | mitigate | CLOSED | `backend/pyproject.toml:15,28,51` pins direct packages; `backend/uv.lock:845,851,866,2274` fixes resolutions and hashes. |
| T-063-02-01 | Information Disclosure | mitigate | CLOSED | `backend/app/providers/embedding/dto.py:19-75` is frozen, `extra=forbid`, and accepts only normalized names plus fixed scalar metadata. |
| T-063-02-02 | Tampering | mitigate | CLOSED | `dto.py:76-91` enforces one vector/input, exactly 1024 finite dimensions; `dashscope.py:223-266` validates vendor shape before DTO construction. |
| T-063-02-SC | Tampering | mitigate | CLOSED | `pyproject.toml:15` reuses pinned HTTPX; no new HTTP client package is imported by the adapter. |
| T-063-03-01 | Spoofing | mitigate | CLOSED | `backend/app/providers/embedding/dashscope.py:25-29,152-158` uses one constant HTTPS endpoint; request data cannot provide a host. |
| T-063-03-SC | Tampering | mitigate | CLOSED | `backend/app/providers/embedding/factory.py:42-48` is the sole dynamic import of the concrete DashScope adapter; tests cover factory modes in `tests/unit/test_embedding_provider.py:101-124`. |
| T-063-04-01 | Tampering | mitigate | CLOSED | `backend/app/nutrition/search_models.py:30-54,76-88` defines identity unique constraints, FKs and the unique active pointer. |
| T-063-04-02 | Denial of Service | mitigate | CLOSED | `search_models.py:116-140`; `search_repository.py:124-168`; `index_worker.py:25-154` enforce finite attempts, due index, lease and bounded backoff. |
| T-063-04-SC | Tampering | mitigate | CLOSED | `backend/migrations/versions/0025_hybrid_food_search.py` is on the single `0027` Alembic head; real-PG schema/rollback coverage is in `tests/integration/test_hybrid_food_search.py:136-143`. |
| T-063-05-01 | Tampering | mitigate | CLOSED | `backend/app/nutrition/schemas.py:113-164` forbids impossible PASS/ASK states; `nutrition/service.py:71-80,121-140` only creates PASS from one exact qualified row. |
| T-063-05-02 | Information Disclosure | mitigate | CLOSED | `nutrition/search.py:59-77` projects only discriminator fields; `tests/unit/test_hybrid_food_search.py:160-194` asserts no scores/vectors enter the public candidate. |
| T-063-05-SC | Tampering | mitigate | CLOSED | `nutrition/search.py:15-23` has versioned constants; `tests/unit/test_hybrid_food_search.py` fixes the deterministic fusion contract. |
| T-063-06-01 | Tampering | mitigate | CLOSED | `search_repository.py:46-53,114-122,170-215` applies current qualification to exact/text/vector paths and rechecks before completion; `nutrition/service.py:121-132` rematerializes candidates. |
| T-063-06-02 | Denial of Service | mitigate | CLOSED | `search_repository.py:55-70,81-112` bounds each channel; `search_models.py:64,82,125` defines trigram/HNSW/due indexes. |
| T-063-06-03 | Tampering | mitigate | CLOSED | `search_repository.py:62-79` obtains relation evidence through `_current_relation_for_text_query`; the constrained evidence models are in `search_models.py:91-114`. |
| T-063-06-SC | Tampering | mitigate | CLOSED | Untrusted query input reaches `bindparam` only (`search_repository.py:50,58-61`); SQL text in the ledger is static with named bind parameters (`core/embedding_budget.py:86-103`). |
| T-063-07-01 | Tampering | mitigate | CLOSED | `nutrition/service.py:71-80,121-140` keeps exact-only PASS and requires current-qualified rematerialization. |
| T-063-07-02 | Information Disclosure | mitigate | CLOSED | Query vector is local in `nutrition/service.py:89-102`; `search_models.py:76-77` documents that query vectors are never stored. |
| T-063-07-03 | Information Disclosure | mitigate | CLOSED | `backend/app/core/tracing.py:20-40,146-158` has a central exact-key allowlist; `tests/unit/test_hybrid_food_search.py:338-362` rejects payload canaries. |
| T-063-07-04 | Denial of Service | mitigate | CLOSED | Service has no tracing lifecycle calls (`nutrition/service.py`); the one owner constructs and closes it in `main.py:76-83,166-176`. |
| T-063-07-SC | Denial of Service | mitigate | CLOSED | `nutrition/service.py:89-119` applies a two-second wait and only transient/outcome-unknown text fallback; permanent provider errors remain fail-closed. |
| T-063-08-01 | Denial of Service | mitigate | CLOSED | `admin/service.py:1553-1613` locks replay-safe retries; finite job limits/leases are in `search_models.py:116-140`. |
| T-063-08-02 | Information Disclosure | mitigate | CLOSED | `admin/service.py:1256-1264` returns the safe status projection; `tests/integration/test_catalog_embedding_jobs.py:160` asserts names are absent. |
| T-063-08-SC | Tampering | mitigate | CLOSED | `admin/service.py:1282-1363` creates pointer-independent work, audit and jobs under the build lock then commits/rolls back as one application transaction. |
| T-063-09-01 | Elevation of Privilege | mitigate | CLOSED | `admin/service.py:185-191` reloads active DB role; every Phase 06.3 status/retry/build/activation entry calls it (`1256,1282,1414,1562`). |
| T-063-09-02 | Information Disclosure | mitigate | CLOSED | `admin/schemas.py` response DTOs are strict; HTTP endpoint binds `response_model` at `admin/api.py:709-730,752-762`; safe projection is integration-tested. |
| T-063-09-SC | Tampering | mitigate | CLOSED | `admin/api.py:730-750` requires bounded idempotency header and stable 409; `admin/service.py:1553-1613` derives a stable replay/audit key under lock. |
| T-063-09-03 | Tampering | mitigate | CLOSED | `search_models.py:91-114` constrains immutable/revocable relation records; `admin/service.py:1633-1730` requires admin, uses audit and appends revocation rather than mutating prior evidence. |
| T-063-10-01 | Denial of Service | mitigate | CLOSED | `index_worker.py:25-154` has fixed lease, max backoff and finite job attempt behavior; supervisor cancellation is in `agent/supervisor.py:73-125`. |
| T-063-10-02 | Tampering | mitigate | CLOSED | `search_repository.py:170-215` locks/rechecks before provider I/O and again before writeback. |
| T-063-10-03 | Tampering | mitigate | CLOSED | `index_worker.py:167-180` derives separate completion evidence; `admin/service.py:1513-1551` checks manifest hash, completion hash, jobs and embeddings before activation. |
| T-063-10-SC | Information Disclosure | mitigate | CLOSED | Worker persists only truncated safe error codes (`search_repository.py:217-250`); provider errors expose stable codes only (`dashscope.py:160-181`). |
| T-063-11-01 | Denial of Service | mitigate | CLOSED | `agent/supervisor.py:55-125` creates at most one named task, validates `(0,5m]` polling and awaits stop; lifecycle test is `tests/integration/test_agent_bootstrap.py:87-109`. |
| T-063-11-SC | Tampering | mitigate | CLOSED | `main.py:76-83,139-149` obtains the provider only through the factory; `embedding/factory.py:24-48` makes test mode Fake-only. |
| T-063-12-01 | Information Disclosure | mitigate | CLOSED | `admin-frontend/src/features/catalog/api/index.ts:98-116` uses strict Zod status/job schemas; UI receives no provider body/vector/name in job projection. |
| T-063-12-02 | Denial of Service | mitigate | CLOSED | `admin-frontend/src/features/catalog/CatalogLifecyclePage.tsx` serializes batch retry/refresh UI state; server remains idempotency-enforced at `admin/api.py:730-750`. |
| T-063-12-SC | Elevation of Privilege | accept | CLOSED (accepted risk) | See **Accepted Risks**: UI is not an authorization control; DB-side role re-read is verified under T-063-09-01. |
| T-063-13-01 | Information Disclosure | mitigate | CLOSED | `agent/state.py:123-181` freezes narrow checkpoint state and omits rank/score/vector/query/provider payload; `tests/unit/test_agent_multimodal.py:169-188` verifies exclusion. |
| T-063-13-SC | Tampering | mitigate | CLOSED | Both graph ports are adapters over `NutritionService` (`agent/tools.py:86-127,155-204`); no graph repository access is exposed by the protocols. |
| T-063-13-02 | Information Disclosure | mitigate | CLOSED | Production uses `create_tracing_runtime` once (`main.py:76-83`) and `core/tracing.py:20-40,146-158` filters all exported attributes. |
| T-063-13-03 | Denial of Service | mitigate | CLOSED | `main.py:166-176` stops supervisor before the sole tracing flush/shutdown owner; `agent/supervisor.py:73-86` awaits worker stop. |
| T-063-14-01 | Tampering | mitigate | CLOSED | Meal resume validates offered ID/version (`agent/graph.py:403-447`) and resolution re-searches/re-reads current qualification (`graph.py:528-562`). |
| T-063-14-02 | Information Disclosure | mitigate | CLOSED | Clarification builds `StateCandidate` from allowlisted fields only (`agent/graph.py:803-824`); state DTO has no scores/vector/query evidence. |
| T-063-14-SC | Denial of Service | mitigate | CLOSED | Existing graph loop/tool limits remain represented in state budgets (`agent/state.py`) and resumptions are no-ops for invalid input (`agent/graph.py:168-186`). |
| T-063-15-01 | Tampering | mitigate | CLOSED | `evals/phase_06_3/evaluate.py:126-171` validates exact field order, sequence and hash chain before execution. |
| T-063-15-02 | Information Disclosure | mitigate | CLOSED | `evaluate.py:84-98,141-146` rejects sensitive fields and permits only `synthetic:` fixtures; `tests/unit/test_phase063_eval_contract.py` exercises rejection. |
| T-063-15-SC | Repudiation | mitigate | CLOSED | `evaluate.py:163-171` requires all case categories and locked mappings; release metrics require nonzero channel/graph denominators (`631-632`). |
| T-063-16-01 | Tampering | mitigate | CLOSED | `evaluate.py:488-550` executes real repository exact/text/vector channels and graph entries, then asserts channel/exclusion/mode/version evidence; `tests/integration/test_hybrid_food_search.py:146-170` requires all counters. |
| T-063-16-02 | Repudiation | mitigate | CLOSED | `evaluate.py:584-632` recomputes evidence hash, binds current dataset/evaluator/policy hashes and preserves PASS/FAIL semantics. |
| T-063-16-SC | Information Disclosure | mitigate | CLOSED | `evaluate.py:225-297` seeds only synthetic fixture rows with `FakeEmbeddingProvider`; release schema validates only labels/hashes/counters. |
| T-063-17-01 | Elevation of Privilege | mitigate | CLOSED | `evals/phase_06_3/activate.py:19-57` resolves email only to UUID; `admin/service.py:1414` re-reads DB ADMIN role. |
| T-063-17-02 | Tampering | mitigate | CLOSED | `admin/service.py:1491-1511` calls strict `verify_release` then recomputes each source hash and checks the target identity. |
| T-063-17-03 | Tampering | mitigate | CLOSED | `admin/service.py:1513-1551` demands immutable manifest/hash, separately stored completion evidence and exact completed-job/embedding coverage. |
| T-063-17-SC | Repudiation | mitigate | CLOSED | `admin/service.py:1415-1483` locks activation, writes approval/pointer/audit and commits atomically; replay validates original actor/build/reason. |
| T-063-18-01 | Information Disclosure | mitigate | CLOSED | `langfuse_publish.py:38-60` validates exact frozen release before lazy SDK construction; only fixed metadata fields are exported at `60-80`. |
| T-063-18-02 | Tampering | mitigate | CLOSED | `langfuse_publish.py:56-80` validates hash-bound release before export and rejects changed source bytes afterward; PASS/FAIL release contract is checked in `evaluate.py:584-632`. |
| T-063-18-SC | Spoofing | mitigate | CLOSED | `docker-compose.langfuse.yml:2-43` pins images, uses an internal network and exposes only `127.0.0.1:3001`; required credentials have no defaults. |
| T-063-19-01 | Information Disclosure | mitigate | CLOSED | `langfuse_retention.py:103-125,143-193` filters only trace name plus age, bounds pages/batches/retries/deadline, and never reads payload. |
| T-063-19-02 | Repudiation | mitigate | CLOSED | `langfuse_retention.py:43-57,181-213` emits exact ephemeral JSON counts/status and exits nonzero on unverifiable deletion. |
| T-063-19-SC | Tampering | mitigate | CLOSED | Selection is timestamp-only (`langfuse_retention.py:112-125`); PASS/FAIL symmetry is tested in `tests/unit/test_langfuse_export.py:189-230`. |
| T-063-20-01 | Repudiation | mitigate | CLOSED | `06.3-20-SUMMARY.md` records executed focused automated gates and real Playwright/browser public-page path; E2E contract is `admin-frontend/tests/e2e/admin-management.spec.ts`. |
| T-063-20-02 | Tampering | mitigate | CLOSED | `evaluate.py:584-632` permits only source/hash-bound release evidence; `admin/service.py:1491-1511` consumes it by recomputation, not claim text. |
| T-063-20-SC | Information Disclosure | mitigate | CLOSED | `docs/learning/phase-06.3-hybrid-food-search.md` and `evals/phase_06_3/README.md:17-57` describe only synthetic/safe examples; publisher enforces it in code. |
| T-063-21-01 | Elevation of Privilege | mitigate | CLOSED | `admin/cli.py:44-63` passes only resolved UUID; `admin/service.py:1282` re-reads ADMIN role before lock/snapshot/enqueue. |
| T-063-21-02 | Repudiation | mitigate | CLOSED | `admin/service.py:1315-1363` writes stable manifest SHA-256/idempotency/audit; completion proof remains separately verified by `1513-1551`. |
| T-063-21-03 | Denial of Service | mitigate | CLOSED | `search_models.py:80-82,121-125,145-158` has unique identities/business keys; `admin/service.py:1283-1350` locks and replay-checks build before enqueuing. |

## Accepted Risks

| Threat ID | Risk accepted | Scope and compensating control |
|---|---|---|
| T-063-12-SC | Frontend route/menu state is not an authorization boundary. | This is deliberate. Every read/write administrative service re-reads the active PostgreSQL role; see `backend/app/admin/service.py:185-191` and the API/service integration contracts. |

## Summary Threat Flags

| Flag | Mapping | Classification | Evidence |
|---|---|---|---|
| Local Langfuse experiment endpoint (`06.3-18-SUMMARY.md`) | T-063-18-SC | Informational, registered | `docker-compose.langfuse.yml` pins images, uses `langfuse_internal`, loopback-only `127.0.0.1:3001`, and required local credentials. |

No unregistered flags were found.

## Security Audit 2026-09-11

| Metric | Count |
|---|---:|
| Threats found | 66 |
| Closed | 66 |
| Open | 0 |
| Accepted risks documented | 1 |
| Unregistered flags | 0 |

### Audit Trail

- Parsed every `<threat_model>` in `06.3-01` through `06.3-21` PLAN artifacts (66 entries).
- Parsed every `## Threat Flags` section in available summaries; the sole flag maps to T-063-18-SC.
- Verified implementation incrementally: provider DTO/factory/adapter, SQLAlchemy retrieval and worker, persistent budget ledger/migrations, admin service/API/CLI, tracing/checkpoint boundaries, evaluator/release activation, Langfuse publisher/retention and local Compose.
- No implementation files were modified by this audit.
