---
phase: 06.3-pgvector
plan: "04"
type: execute
wave: 2
depends_on: ["06.3-01"]
files_modified: [backend/app/nutrition/search_models.py, backend/app/nutrition/README.md, backend/migrations/versions/0025_hybrid_food_search.py, backend/migrations/versions/README.md, backend/tests/integration/test_hybrid_food_search.py, backend/tests/integration/README.md]
autonomous: true
requirements: [HFS-02, HFS-05]
must_haves:
  truths: ["Catalog embeddings are isolated by immutable model/dimension/adapter vector space.", "A publication may own multiple name jobs, while immutable build snapshots/completion and activation approval evidence are persisted separately."]
  artifacts: [{path: backend/migrations/versions/0025_hybrid_food_search.py, provides: "pg_trgm, VECTOR(1024), relation, jobs, activation evidence"}, {path: backend/app/nutrition/search_models.py, provides: "Derived ORM models"}]
  key_links: [{from: backend/app/nutrition/search_models.py, to: catalog_publications, via: "publication/name/vector-space foreign keys", pattern: "publication_id|vector_space_id"}]
---
<objective>Create the single-head schema for versioned retrieval, multi-name jobs and controlled activation.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-RESEARCH.md @.planning/phases/06.3-pgvector/06.3-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Migrate versioned hybrid-search state</name>
  <read_first>backend/migrations/versions/0024_recipe_candidates_support_admin_catalog.py, backend/migrations/versions/0018_catalog_publication_eligibility.py, backend/migrations/versions/0008_retrieval_vectors.py, backend/app/admin/models.py, backend/migrations/versions/README.md</read_first>
  <files>backend/app/nutrition/search_models.py, backend/app/nutrition/README.md, backend/migrations/versions/0025_hybrid_food_search.py, backend/migrations/versions/README.md, backend/tests/integration/test_hybrid_food_search.py, backend/tests/integration/README.md</files>
  <behavior>- Revision 0025 has sole predecessor 0024 and one Alembic head.
- Spaces bind model, dimension 1024 and adapter; embeddings are unique by publication/name/space.
- Jobs are unique by publication/name/space with finite attempts, due and lease fields.
- Versioned controlled relation evidence alone grants upper relation layers.
- Each vector-space build has immutable identity, requested-by/reason/idempotency evidence, an exact snapshot manifest/hash of all currently eligible canonical names and controlled aliases, and separately immutable completion counts/hash/time.
- Activation approval stores release, dataset, code, retrieval and embedding hashes plus approver/time; pointer switch is a separate transaction.</behavior>
  <action>Install `pg_trgm`; add immutable content-derived search version (`publication_id + content_hash`) while keeping legacy snapshots readable; add vector spaces, active pointer, catalog embeddings, versioned relation evidence, multi-name jobs, immutable build snapshot/completion evidence, publication retry command evidence and activation approvals. Constrain relation values to the D-02 enum and model revoke as audited versioned evidence, never destructive overwrite. Add GiST trigram and partial cosine HNSW indexes with constraints and reverse-order downgrade. Do not modify old `Vector(3)` tables or mix vector spaces.</action>
  <verify><automated>cd backend &amp;&amp; uv run python tests/run_pg.py --env-file .env.test.example -- uv run pytest tests/integration/test_hybrid_food_search.py -k schema -q</automated></verify>
  <acceptance_criteria>Real PostgreSQL introspection proves extension, dimensions, opclasses, unique/check/FK constraints, one head, relation enum/revocation, immutable build snapshot/completion evidence, multi-job support and immutable approval hashes.</acceptance_criteria>
  <done>The database can represent all retrieval and activation invariants without storing query text/vectors.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-04-01 | Tampering | vector spaces | mitigate | Immutable identity, FK/unique constraints and single active pointer. |
| T-063-04-02 | Denial of Service | jobs | mitigate | Finite attempts, due index, lease and unique business key. |
| T-063-04-SC | Tampering | extension | mitigate | Single reviewed Alembic head and real PG introspection. |
</threat_model>
<verification>Upgrade/downgrade/upgrade in isolated PostgreSQL and verify one head.</verification>
<success_criteria>D-07, D-08, D-11 and D-12 have enforceable storage contracts.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-04-SUMMARY.md`.</output>
