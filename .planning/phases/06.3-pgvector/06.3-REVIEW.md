---
phase: 06.3-pgvector
reviewed: 2026-09-11T10:01:02Z
depth: deep
files_reviewed: 10
files_reviewed_list:
  - backend/app/core/config.py
  - backend/app/core/embedding_budget.py
  - backend/app/providers/embedding/dashscope.py
  - backend/migrations/versions/0027_embedding_period_budgets.py
  - backend/tests/integration/test_embedding_budget_ledger.py
  - backend/tests/unit/test_embedding_provider.py
  - backend/evals/phase_06_3/evaluate.py
  - backend/evals/phase_06_3/langfuse_publish.py
  - backend/tests/unit/test_phase063_eval_contract.py
  - backend/tests/unit/test_langfuse_export.py
findings:
  critical: 0
  warning: 0
  info: 0
  total: 0
status: clean
---

# Phase 06.3: 最终针对性复审报告

**Reviewed:** 2026-09-11T10:01:02Z
**Depth:** deep
**Files Reviewed:** 10
**Status:** clean

## Summary

此前 `NUMERIC(18,8)` 舍入导致 period budget 可被极小金额配置绕过的问题已闭合：配置入口、DashScope 构造边界和 ledger 事务入口均拒绝不可无损存储、溢出或非有限的 reservation/cap；计算出的供应商成本向上取整到账本量子，确保不会因存储精度少记费用。两次 HTTP attempt 的最坏成本仍在请求前被原子预留，首个可计费失败的完整 cap 会被保留。

冻结 release 与 Langfuse 的严格 hash-bound 规则未被这次修复破坏：PASS 仍要求当前受控源文件 hash、精确 schema 和全量 assertions；Langfuse 在创建 client 前复用相同校验，允许有效 FAIL 留作审计但拒绝伪造 PASS。

## Narrative Findings (AI reviewer)

未发现 blocker、warning 或新的安全/正确性缺陷。

## Verified Closure

- `require_ledger_amount()` 将可配置金额约束为正数、有限、最多 8 位小数，并限制在 `NUMERIC(18,8)` 的可表示范围；同时校验两次 retry reservation，避免倍增溢出。
- ledger 的 `reserve()` 和 `settle()` 都重新执行精度检查；`round_cost_up_for_ledger()` 对实际成本向上取整，因此不会出现低估费用的舍入路径。
- 真实 PostgreSQL 回归证明九位小数 reservation 在插入账本行之前被拒绝；最小可表示 reservation 只能成功一次，第二次会被 cap 拒绝。
- DashScope 边界在启动和实际 settle 前均保持 fail-closed；retry 的 `2 * single_call_cap` 预留和首失败成本保留仍成立。
- hybrid 测试前置条件仍只放宽隔离测试中的 seed 假设，不改变生产端“exact 唯一才 PASS、多个 exact 进入 ASK”的安全契约。

## Verification

- `tests/integration/test_embedding_budget_ledger.py`：2 passed（真实 PostgreSQL）。
- `tests/unit/test_embedding_provider.py tests/unit/test_phase063_eval_contract.py tests/unit/test_langfuse_export.py`：60 passed。
- `python evals/phase_06_3/evaluate.py --verify-release`：通过。
- 目标 Ruff：通过。

---

_Reviewed: 2026-09-11T10:01:02Z_
_Reviewer: the agent (gsd-code-reviewer)_
_Depth: deep_
