---
phase: 06.3-pgvector
plan: "21"
type: execute
wave: 7
depends_on: ["06.3-09"]
files_modified: [backend/app/admin/api.py, backend/app/admin/schemas.py, backend/app/admin/service.py, backend/app/admin/repository.py, backend/app/admin/README.md, backend/tests/unit/test_admin_catalog_api.py, backend/tests/integration/test_catalog_embedding_jobs.py, backend/tests/integration/README.md]
autonomous: true
requirements: [HFS-05, HFS-06, HFS-09]
must_haves:
  truths: ["A DB-confirmed ADMIN can create an immutable vector-space identity and exact current-qualified name snapshot.", "Repeated or concurrent build requests enqueue each immutable snapshot item once; Plan 10 alone reconciles those jobs into activation-verifiable completion evidence."]
  artifacts: [{path: backend/app/admin/service.py, provides: "DB-authorized vector-space build application command"}, {path: backend/tests/integration/test_catalog_embedding_jobs.py, provides: "Snapshot, replay, partial failure and completion-evidence proof"}]
  key_links: [{from: backend/app/admin/api.py, to: backend/app/admin/service.py, via: "actor ID, reason and idempotency key", pattern: "vector-space-builds"}, {from: backend/app/admin/service.py, to: backend/app/admin/repository.py, via: "locked Plan 04 schema transaction creates immutable snapshot/manifest, jobs and audit for Plan 10 reconciliation", pattern: "snapshot_hash|vector_space"}]
---
<objective>Create a production-reachable administrator-only build/backfill command whose immutable snapshot and completion evidence gate later activation.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-04-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-09-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-CONTEXT.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create and safely replay complete vector-space builds</name>
  <read_first>backend/app/admin/service.py, backend/app/admin/repository.py, backend/app/admin/api.py, backend/app/admin/schemas.py, backend/app/nutrition/search_models.py, backend/tests/integration/test_catalog_embedding_jobs.py</read_first>
  <files>backend/app/admin/api.py, backend/app/admin/schemas.py, backend/app/admin/service.py, backend/app/admin/repository.py, backend/app/admin/README.md, backend/tests/unit/test_admin_catalog_api.py, backend/tests/integration/test_catalog_embedding_jobs.py, backend/tests/integration/README.md</files>
  <behavior>- POST vector-space build requires pinned model/dimension/adapter/retrieval identity, bounded reason and idempotency key; AdminService re-reads PostgreSQL actor and requires ADMIN, denying USER/unknown actors.
- One locked transaction creates the immutable vector-space identity, snapshots every currently eligible canonical name and controlled alias with publication/content versions in stable order, stores manifest/count/hash, enqueues one unique job per snapshot item, and writes immutable audit.
- Repeated/concurrent invocation with the same key returns the same build and creates no duplicate space, job or provider cost; conflicting reuse fails closed.
- Partial failure remains incomplete and resumable through existing bounded retry; replay never replaces or expands the original snapshot.
- This plan does not write completion evidence: Plan 10 alone reconciles the immutable manifest after job writeback; Plan 17 later consumes both artifacts.</behavior>
  <action>Add an administrator-only application command and strict API/CLI-compatible route over Plan 04 schema. Derive the immutable identity from pinned model/dimension/adapter/retrieval version, acquire catalog/build locks, select the exact current-qualified canonical/alias snapshot, hash its canonical manifest, enqueue unique jobs idempotently, and append audit in one transaction. Expose safe immutable identity/manifest and expected/pending/failed counts for Plan 10 reconciliation; do not create completion evidence and never switch the active pointer. Update admin and integration-test README indexes with the new route, command and assertions.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/unit/test_admin_catalog_api.py -k vector_space_build -q &amp;&amp; uv run python tests/run_pg.py --env-file .env.test.example -- uv run pytest tests/integration/test_catalog_embedding_jobs.py -k 'build or snapshot or concurrent or replay or partial or completion or rbac' -q</automated></verify>
  <acceptance_criteria>Real PostgreSQL tests prove ADMIN success, USER/unknown denial, exact eligible-name snapshot coverage, immutable identity/manifest, unique jobs, safe resume/replay, partial/concurrent behavior and audit; they also prove Plan 21 creates no completion evidence and performs no activation.</acceptance_criteria>
  <done>A complete target vector space can be built, resumed and independently proven before release activation.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-21-01 | Elevation of Privilege | build command | mitigate | AdminService re-reads PostgreSQL role and requires ADMIN before lock/snapshot/enqueue. |
| T-063-21-02 | Repudiation | build snapshot | mitigate | Stable immutable manifest hash, idempotency command record and audit; completion evidence is separately owned by Plan 10. |
| T-063-21-03 | Denial of Service | repeated build | mitigate | Unique immutable identity/jobs plus locked idempotent replay prevent duplicate provider cost. |
</threat_model>
<verification>Run API contract and concurrent real-PostgreSQL build/replay/completion tests.</verification>
<success_criteria>Vector-space build/backfill is administrator-only, exact-snapshot-bound, restart-safe and activation-verifiable.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-21-SUMMARY.md`.</output>
