---
phase: 06.3-pgvector
plan: "11"
type: execute
wave: 9
depends_on: ["06.3-03", "06.3-10"]
files_modified: [backend/app/agent/supervisor.py, backend/app/agent/README.md, backend/app/main.py, backend/app/core/config.py, backend/.env.example, backend/tests/integration/test_agent_bootstrap.py, backend/app/nutrition/README.md]
autonomous: true
requirements: [HFS-05, HFS-06]
must_haves:
  truths: ["FastAPI lifespan owns exactly one worker and shuts it down cleanly.", "Tests inject Fake and disabled semantic mode leaves exact/text usable."]
  artifacts: [{path: backend/app/main.py, provides: "Worker runtime ownership"}]
  key_links: [{from: backend/app/main.py, to: backend/app/nutrition/index_worker.py, via: "supervised lifespan start/stop", pattern: "CatalogEmbeddingWorker"}]
---
<objective>Wire the completed worker into the existing supervised application lifespan.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-03-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-10-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Own worker configuration, startup and shutdown</name>
  <read_first>backend/app/main.py, backend/app/agent/supervisor.py, backend/app/agent/retention.py, backend/app/core/config.py, backend/.env.example, backend/tests/integration/test_agent_bootstrap.py</read_first>
  <files>backend/app/agent/supervisor.py, backend/app/agent/README.md, backend/app/main.py, backend/app/core/config.py, backend/.env.example, backend/tests/integration/test_agent_bootstrap.py, backend/app/nutrition/README.md</files>
  <behavior>- Positive lease/backoff/idle settings are validated.
- Lifespan starts one worker after supervisor readiness and stops it before Provider/tracing shutdown.
- Restart recovers abandoned leases; shutdown leaves no task/session.
- Test mode uses Fake; explicit semantic disable does not block app startup.</behavior>
  <action>Have `PersistedAgentRuntimeFactory` create one Provider and attach the worker to `PostgresLeaseSupervisor`. Follow existing lifecycle ordering; do not use `asyncio.run`, per-request workers or process-local coordination as authority.</action>
  <verify><automated>cd backend &amp;&amp; uv run python tests/run_pg.py --env-file .env.test.example -- uv run pytest tests/integration/test_agent_bootstrap.py -k embedding_worker -q</automated></verify>
  <acceptance_criteria>Lifecycle tests prove one start/stop, abandoned lease recovery, zero background tasks and zero network in test.</acceptance_criteria>
  <done>Indexing is operationally integrated without blocking fallback behavior.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-11-01 | Denial of Service | lifespan task | mitigate | Single supervised task, validated intervals and bounded cancellation. |
| T-063-11-SC | Tampering | runtime Provider | mitigate | Factory from Plan 03; Fake-only tests. |
</threat_model>
<verification>Run real-PG lifespan integration tests.</verification>
<success_criteria>Worker lifecycle is finite, leak-free and restart-safe.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-11-SUMMARY.md`.</output>
