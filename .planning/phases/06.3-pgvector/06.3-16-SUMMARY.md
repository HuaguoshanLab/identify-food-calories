---
phase: 06.3-pgvector
plan: "16"
subsystem: testing
tags: [postgresql, pgvector, pg_trgm, evaluation, fake-embedding]
requires:
  - phase: 06.3-06
    provides: production hybrid repository and service path
  - phase: 06.3-14
    provides: confirmation-safe Agent graph behavior
  - phase: 06.3-15
    provides: frozen hash-chained retrieval cases
provides:
  - Hash-bound, deterministic real-PostgreSQL retrieval release evidence
  - Offline synthetic catalog snapshot exercising exact/text/vector SQL channels
affects: [06.3-17, release-activation]
tech-stack:
  added: []
  patterns: [synthetic-authority-snapshot, atomic-release-evidence]
key-files:
  created: [backend/evals/phase_06_3/release.json]
  modified: [backend/evals/phase_06_3/evaluate.py, backend/tests/unit/test_phase063_eval_contract.py, backend/tests/integration/test_hybrid_food_search.py]
key-decisions:
  - "冻结评测只使用 Fake Embedding 和经 AdminService 发布的合成目录行，避免网络和真实用户数据。"
  - "release evidence 绑定冻结数据、评测器和检索策略 hash；运行时 UUID 不进入报告，保证重复字节一致。"
patterns-established:
  - "真实 PostgreSQL evaluator：先经治理服务写入合成权威快照，再通过生产 repository/service 读取。"
requirements-completed: [HFS-01, HFS-02, HFS-03, HFS-04, HFS-05, HFS-06, HFS-09]
duration: 28min
completed: 2026-09-11
---

# Phase 06.3 Plan 16: Frozen Real-PG Evaluation Summary

**冻结 24 个检索案例通过真实 PostgreSQL 的 exact、pg_trgm 与 pgvector 路径生成可验证、字节稳定的 release evidence。**

## Accomplishments

- 评测器以 `AdminService` 发布合成目录快照，再经 `SqlAlchemyHybridFoodSearchRepository` 和 `NutritionService` 执行检索。
- Fake Embedding 提供固定 1024 维向量；无网络、无真实用户数据、无查询向量持久化。
- `release.json` 绑定数据集、评测器、检索策略 hash，并拒绝 decision 或 evidence hash 篡改。
- 新增真实 PostgreSQL 评测 smoke test 与 unit fail-closed 合同，测试索引同步更新。

## Verification

- `uv run python tests/run_pg.py --env-file .env.test.example -- uv run --extra dev pytest tests/integration/test_hybrid_food_search.py -k eval_snapshot -q` → 1 passed
- 两次 `evaluate.py` 隔离 PostgreSQL 运行后 `cmp` → 字节一致
- `evaluate.py --verify-release` → passed
- `uv run --extra dev pytest tests/unit/test_phase063_eval_contract.py -q` → 14 passed
- `uv run ruff check ...` → All checks passed

## Task Commits

1. Task 1 (RED contracts): `080958a` — `test(06.3-16): add real PostgreSQL evaluation contracts`
2. Task 1 (GREEN evaluator): `c424e23` — `feat(06.3-16): emit deterministic real PostgreSQL release evidence`

## Deviations from Plan

### Auto-fixed Issues

1. [Rule 1 - Bug] Reused publication-created search versions and embedding rows
- **Found during:** Task 1
- **Issue:** Production publication flow already creates version/index/job records; evaluator attempted duplicate inserts.
- **Fix:** Reuse existing rows and update only the synthetic embedding payload.
- **Verification:** isolated PostgreSQL evaluator passes.

## Known Stubs

None.

## Next Phase Readiness

`release.json` is the deterministic local activation input for Plan 17.

## Self-Check: PASSED

- Evaluator, release evidence, unit and integration contracts exist.
- Verification commands above passed in the isolated PostgreSQL environment.
