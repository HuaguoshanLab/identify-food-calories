---
phase: 06.3-pgvector
plan: "10"
type: execute
wave: 8
depends_on: ["06.3-02", "06.3-06", "06.3-21"]
files_modified: [backend/app/nutrition/index_worker.py, backend/app/nutrition/search_repository.py, backend/app/nutrition/README.md, backend/tests/integration/test_catalog_embedding_jobs.py, backend/tests/integration/README.md]
autonomous: true
requirements: [HFS-05, HFS-06]
must_haves:
  truths: ["Concurrent workers lease each job once and recheck authority before and after Provider I/O.", "Worker completion cannot activate a vector space."]
  artifacts: [{path: backend/app/nutrition/index_worker.py, provides: "Finite leased embedding worker"}]
  key_links: [{from: backend/app/nutrition/index_worker.py, to: backend/app/nutrition/search_repository.py, via: "claim/recheck/writeback against Plan 04 schema and immutable Plan 21 manifest", pattern: "SKIP LOCKED|lease|snapshot_hash"}, {from: backend/app/nutrition/index_worker.py, to: backend/app/nutrition/search_models.py, via: "separate Plan 10 completion evidence reconciled from exact Plan 21 snapshot", pattern: "completion_hash|completion_count"}]
---
<objective>Implement bounded multi-process catalog embedding job execution.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-02-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-08-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-21-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Claim, execute and write back finite jobs</name>
  <read_first>backend/app/memory/repository.py, backend/app/records/models.py, backend/app/nutrition/search_repository.py, backend/app/providers/embedding/ports.py, backend/tests/integration/test_catalog_embedding_jobs.py</read_first>
  <files>backend/app/nutrition/index_worker.py, backend/app/nutrition/search_repository.py, backend/app/nutrition/README.md, backend/tests/integration/test_catalog_embedding_jobs.py, backend/tests/integration/README.md</files>
  <behavior>- Plan 04 provides the persistence schema; Plan 21 creates the immutable build snapshot/manifest and its unique jobs; this worker only claims and reconciles those jobs.
- Claim uses due order, `FOR UPDATE SKIP LOCKED`, lease owner/token and finite attempts.
- Provider runs outside claim transaction with document input only.
- Before call and writeback, stale publication/name/eligibility/space cancels safely.
- Transient failures back off to a fixed cap; permanent failures terminate.
- Duplicate workers cannot double-charge or duplicate vectors.
- After writeback, a locked reconciliation marks immutable build completion evidence only when every item in the original snapshot manifest has exactly one valid target-space embedding; partial/failed builds remain incomplete and resumable.</behavior>
  <action>Create `CatalogEmbeddingWorker` and repository job primitives following the existing outbox and Plan 04 schema. Persist safe failure/usage counters only. Claim only jobs created from the immutable Plan 21 build snapshot/manifest, upsert the claimed controlled catalog vector, reconcile that exact unchanged Plan 21 manifest, and write Plan 10 completion count/hash/time only after exact coverage. Never create, replace or expand the snapshot, and never update active-space pointers or approval evidence. Update the integration-test README assertions for lease, duplicate-cost, Plan 21 manifest identity and Plan 10 completion-evidence coverage.</action>
  <verify><automated>cd backend &amp;&amp; uv run python tests/run_pg.py --env-file .env.test.example -- uv run pytest tests/integration/test_catalog_embedding_jobs.py -k 'claim or worker or lease or backoff or duplicate' -q</automated></verify>
  <acceptance_criteria>Concurrent real-PG tests show one charge/write, fixed attempt cap, stale cancellation, partial-build incompleteness, exact reconciliation of the immutable Plan 21 snapshot into separately stored Plan 10 completion evidence, and no snapshot mutation or self-activation.</acceptance_criteria>
  <done>Async indexing is finite, idempotent and authority-aware.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-10-01 | Denial of Service | retry loop | mitigate | Fixed attempt/backoff/lease bounds and cancellation. |
| T-063-10-02 | Tampering | stale writeback | mitigate | Lease and live authority rechecks before I/O and commit. |
| T-063-10-03 | Tampering | build/completion evidence | mitigate | Reconcile immutable Plan 21 manifest exactly; Plan 10 writes separate completion evidence and cannot alter snapshot or activation. |
| T-063-10-SC | Information Disclosure | error state | mitigate | Safe codes and accounting only. |
</threat_model>
<verification>Run concurrent worker tests through isolated PostgreSQL.</verification>
<success_criteria>D-06/D-07 worker behavior is bounded and cannot authorize search.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-10-SUMMARY.md`.</output>
