---
phase: 06.3-pgvector
plan: "13"
type: execute
wave: 10
depends_on: ["06.3-07", "06.3-11"]
files_modified: [backend/app/agent/tools.py, backend/app/agent/state.py, backend/app/agent/supervisor.py, backend/app/main.py, backend/tests/unit/test_agent_multimodal.py, backend/tests/unit/test_diet_planning_graph.py, backend/tests/integration/test_agent_bootstrap.py]
autonomous: true
requirements: [HFS-03, HFS-04, HFS-05, HFS-06]
must_haves:
  truths: ["Both Tool ports await the same NutritionService.search_food_catalog method.", "PersistedAgentRuntimeFactory owns one TracingRuntime shared by supervisor, adapters and every NutritionService, then flushes and shuts it down exactly once.", "Graph state stores safe discriminators but no score/vector/provider payload."]
  artifacts: [{path: backend/app/agent/state.py, provides: "Backward-compatible safe candidate projection"}, {path: backend/app/agent/tools.py, provides: "Shared async service adapter"}]
  key_links: [{from: backend/app/agent/tools.py, to: backend/app/nutrition/service.py, via: "NutritionService.search_food_catalog", pattern: "await.*search_food_catalog"}, {from: backend/app/main.py, to: backend/app/core/tracing.py, via: "PersistedAgentRuntimeFactory calls create_tracing_runtime(settings) exactly once", pattern: "create_tracing_runtime"}, {from: backend/app/main.py, to: backend/app/agent/supervisor.py, via: "same TracingRuntime injected into PostgresLeaseSupervisor, SessionNutritionToolAdapter and every NutritionService", pattern: "tracing_runtime"}, {from: backend/app/main.py, to: backend/tests/integration/test_agent_bootstrap.py, via: "application lifespan spy-exporter flush/shutdown proof", pattern: "flush|shutdown"}]
---
<objective>Upgrade shared Tool/state contracts and give the production runtime factory sole ownership of one Phoenix lifecycle before graph branch changes.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-07-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-11-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Wire one async service adapter and safe candidate state</name>
  <read_first>backend/app/agent/tools.py, backend/app/agent/state.py, backend/app/agent/supervisor.py, backend/app/main.py, backend/app/core/tracing.py, backend/app/nutrition/service.py, backend/tests/unit/test_agent_multimodal.py, backend/tests/unit/test_diet_planning_graph.py, backend/tests/integration/test_agent_bootstrap.py</read_first>
  <files>backend/app/agent/tools.py, backend/app/agent/state.py, backend/app/agent/supervisor.py, backend/app/main.py, backend/tests/unit/test_agent_multimodal.py, backend/tests/unit/test_diet_planning_graph.py, backend/tests/integration/test_agent_bootstrap.py</files>
  <behavior>- `PersistedAgentRuntimeFactory` calls `create_tracing_runtime(settings)` once and injects that object into `PostgresLeaseSupervisor`, `SessionNutritionToolAdapter`, and every `NutritionService` it constructs.
- Meal and planning Tool Protocols expose the same awaitable service signature and the same caller-owned runtime object.
- `StateCandidate` contains ID, version, canonical label, relation label, optional `prepared_state`, bounded `portion_hints`, and `source_name`.
- State excludes similarity/rank/vector/raw query/provider fields and decodes old checkpoints with absent optional discriminator fields.
- Sync Session is created/closed inside one `to_thread` closure.
- Application lifespan flushes and shuts down that runtime exactly once after supervised work stops; no service, adapter or supervisor duplicates ownership.
- A real-factory integration test with a spy exporter observes exactly `retrieval.version`, `match.channel`, `fallback.code`, `latency.bucket`, `index.health`, and `index.version`, observes no query/vector/candidate/user/meal/body/health fields, and records one flush plus one shutdown.</behavior>
  <action>Convert adapters async end-to-end and delegate only to existing `NutritionService.search_food_catalog`; do not introduce `HybridFoodSearchService`. Extend strict state DTOs with the exact D-04 allowlist. In `PersistedAgentRuntimeFactory`, create exactly one `TracingRuntime` through `create_tracing_runtime(settings)` and inject that same object into `PostgresLeaseSupervisor`, `SessionNutritionToolAdapter`, and every constructed `NutritionService`; adapters/services borrow it and never close it. Make the FastAPI application lifespan stop supervised workers first, then flush and shut down this one runtime exactly once on normal or exceptional exit. Add a real runtime-factory integration test with a spy exporter that executes hybrid search and proves the six safe attributes, forbidden-field absence, shared object identity, and one flush/one shutdown. Never pass a Session across threads or use `asyncio.run`.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/unit/test_agent_multimodal.py tests/unit/test_diet_planning_graph.py -k 'search_contract or checkpoint_projection or compatibility' -q &amp;&amp; uv run python tests/run_pg.py --env-file .env.test.example -- uv run pytest tests/integration/test_agent_bootstrap.py -k 'hybrid_tracing_runtime or tracing_lifecycle' -q</automated></verify>
  <acceptance_criteria>Unit tests prove identical service method, legacy decoding, discriminator preservation and serialized forbidden-field absence; real-factory integration proves a single shared TracingRuntime instance, all six safe hybrid-search attributes, zero forbidden query/vector/candidate/user/meal/body/health fields, and exactly one flush plus one shutdown.</acceptance_criteria>
  <done>Tool and checkpoint boundaries are shared, async-safe and explainable.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-13-01 | Information Disclosure | checkpoint | mitigate | Exact allowlist with bounded discriminator fields and forbidden serialization tests. |
| T-063-13-SC | Tampering | service bypass | mitigate | Both Tool ports call the same concrete NutritionService method. |
| T-063-13-02 | Information Disclosure | production tracing factory | mitigate | One factory-created runtime enforces central allowlist; spy-exporter integration rejects all payload/identity/health fields. |
| T-063-13-03 | Denial of Service | tracing lifecycle | mitigate | Application lifespan is sole owner and flushes/shuts down once after supervisor stop, including exceptional exit. |
</threat_model>
<verification>Run focused Tool/state compatibility tests and the real-factory tracing lifecycle integration slice.</verification>
<success_criteria>D-04/D-12 safe state, shared service ownership and one production Phoenix runtime lifecycle are explicit and tested.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-13-SUMMARY.md`.</output>
