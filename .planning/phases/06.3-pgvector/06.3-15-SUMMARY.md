---
phase: 06.3-pgvector
plan: "15"
subsystem: testing
tags: [evaluation, retrieval, hash-chain, privacy, pytest]
requires:
  - phase: 06
    provides: Versioned backend evaluation conventions and directory documentation contracts
provides:
  - Privacy-safe, hash-bound Phase 06.3 retrieval reference dataset
  - Fail-closed loader for schema, order, category, mapping, and privacy checks
affects: [06.3-pgvector, hybrid-retrieval, release-evaluation]
tech-stack:
  added: []
  patterns:
    - Ordered JSONL records use canonical SHA-256 parent/case hash chaining.
    - Frozen retrieval fixtures use exact-key allowlists and reject privacy fields before execution.
key-files:
  created:
    - backend/evals/phase_06_3/cases.jsonl
    - backend/evals/phase_06_3/evaluate.py
    - backend/tests/unit/test_phase063_eval_contract.py
  modified:
    - backend/evals/README.md
    - backend/tests/unit/README.md
key-decisions:
  - "Only unique exact cases can declare PASS; every non-exact fixture is ASK with auto-pass disabled."
  - "Dataset records are synthetic opaque snapshots with fixed schema/order and no user, image, prompt, provider response, or vector payload fields."
patterns-established:
  - "Phase 06.3 evaluation loader rejects malformed evidence before any retrieval implementation is invoked."
requirements-completed: [HFS-07, HFS-09]
duration: 10min
completed: 2026-09-11
---

# Phase 06.3 Plan 15: Frozen Retrieval Evaluation Contract Summary

**24 条以上合成检索用例以严格 schema、固定顺序和 SHA-256 父链冻结，并在执行前拒绝篡改及隐私字段。**

## Performance

- **Duration:** 10 min
- **Started:** 2026-09-11T02:26:00Z
- **Completed:** 2026-09-11T02:36:30Z
- **Tasks:** 1/1
- **Files modified:** 7

## Accomplishments

- 建立 24 条版本化合成 JSONL 案例：6 条 exact、6 条 non-exact、4 条 ambiguity、4 条 eligibility、4 条 failure/determinism。
- 锁定 `米饭 → 米饭` 的唯一精确 `PASS`，以及五个 Top-3 非精确 `ASK` 映射，禁止非精确自动选择。
- 离线 loader 在任何后续检索执行之前检查字段顺序、parent/case hash、类别覆盖、版本声明、锁定映射和敏感字段。

## Task Commits

1. **Task 1: Create privacy-safe hash-bound Phase 06.3 cases** — `f9fd920` (test), `eb7660f` (feat)

## Files Created/Modified

- `backend/evals/phase_06_3/cases.jsonl` — 24 条合成、hash-chain 绑定的检索案例。
- `backend/evals/phase_06_3/evaluate.py` — fail-closed 数据集 loader。
- `backend/evals/phase_06_3/README.md` — 本目录责任、依赖边界与文件索引。
- `backend/tests/unit/test_phase063_eval_contract.py` — schema/hash/privacy/category 合同测试。
- `backend/evals/README.md`、`backend/tests/unit/README.md` — 父目录索引更新。

## Decisions Made

- 任何非精确案例都固定为 `ASK` 且 `auto_pass_allowed=false`；高分或唯一候选不能扩大自动选择权限。
- 通过严格根字段 allowlist 保障 fixture 仅包含合成检索标签与版本标识，杜绝真实身份、餐食、身体/健康、图片、提示词、Provider 原文和向量内容。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking environment] `uv` 默认缓存目录无写权限**
- **Found during:** Task 1
- **Issue:** `/Users/mina/.cache/uv` 在隔离环境不可写，首次测试无法初始化。
- **Fix:** 使用 worktree 内的 `UV_CACHE_DIR` 运行锁定依赖与测试。
- **Files modified:** None
- **Verification:** Phase 06.3 合同测试 12/12 通过。
- **Committed in:** N/A（环境配置）

---

**Total deviations:** 1 auto-fixed (Rule 3: 1).
**Impact on plan:** 不改变产品代码、依赖版本或评测合同。

## Issues Encountered

- 计划要求的 `tests/architecture/test_directory_contract.py` 仍失败，但失败仅来自基线已跟踪目录：`admin-frontend/src/features/recipes/api`、`admin-frontend/src/features/system/api` 和 `outputs` 缺少 README；本计划新增的 `backend/evals/phase_06_3/README.md` 完整。按作用域边界未修改无关目录。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

后续混合检索实现可消费此固定数据集；必须继续保留 exact-only PASS、non-exact ASK 和 fail-closed hash/privacy 合同。

## Self-Check: PASSED

- 已确认 7 个计划内代码/文档文件存在。
- 已确认任务 commits `f9fd920` 与 `eb7660f` 位于当前 worktree 分支历史。
- `uv run --extra dev pytest tests/unit/test_phase063_eval_contract.py -k 'dataset or hash or privacy' -q`：12 passed。
- `validate_dataset(...)`：接受 24 条链式记录。

---
*Phase: 06.3-pgvector*
*Completed: 2026-09-11*
