---
phase: 06.3-pgvector
plan: "02"
type: execute
wave: 2
depends_on: ["06.3-01"]
files_modified: [backend/app/providers/embedding/__init__.py, backend/app/providers/embedding/dto.py, backend/app/providers/embedding/ports.py, backend/app/providers/embedding/fake.py, backend/app/providers/embedding/README.md, backend/app/providers/README.md, backend/tests/unit/test_embedding_provider.py, backend/tests/unit/README.md]
autonomous: true
requirements: [HFS-06, HFS-09]
user_setup:
  - service: DashScope
    why: "Production catalog/query embeddings"
    env_vars: [{name: DASHSCOPE_EMBEDDING_API_KEY, source: "Alibaba Cloud Model Studio API key"}]
must_haves:
  truths: ["D-10: the Provider receives only one normalized food name and never receives meal, portion, identity, body, health or conversation context.", "Strict 1024-finite-float embedding contracts exist behind a replaceable Protocol.", "The scripted Fake retains neither names nor vectors and supports deterministic offline tests."]
  artifacts: [{path: backend/app/providers/embedding/dto.py, provides: "Strict provider-only DTOs"}, {path: backend/app/providers/embedding/fake.py, provides: "Offline scripted provider"}]
  key_links: [{from: backend/app/providers/embedding/dashscope.py, to: backend/app/providers/embedding/ports.py, via: "Protocol implementation", pattern: "EmbeddingProvider"}]
---
<objective>Define the isolated Embedding DTO, Protocol and offline Fake before the production adapter consumes them.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-CONTEXT.md @.planning/phases/06.3-pgvector/06.3-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build strict embedding contracts and offline Fake</name>
  <read_first>backend/app/providers/vision/dto.py, backend/app/providers/vision/ports.py, backend/app/providers/vision/qwen.py, backend/app/providers/vision/fake.py, backend/app/providers/vision/README.md, backend/tests/test_qwen_vision_provider.py</read_first>
  <files>backend/app/providers/embedding/__init__.py, backend/app/providers/embedding/dto.py, backend/app/providers/embedding/ports.py, backend/app/providers/embedding/fake.py, backend/app/providers/embedding/README.md, backend/app/providers/README.md, backend/tests/unit/test_embedding_provider.py, backend/tests/unit/README.md</files>
  <behavior>- Query is one normalized 1–200-character name with `text_type=query`; catalog batches use `document` and max 10.
- Responses require exact count, dimension 1024 and finite floats.
- Fake records safe call counters only, never input names or vectors.</behavior>
  <action>Create physically separate frozen DTOs, async `EmbeddingProvider` and scripted Fake. Encode query/document count, exact dimension and finite-value invariants in provider-only DTOs. Add the required child README and update both the provider parent index and unit-test file index for the new test. Do not import nutrition schemas, Agent state or a concrete network adapter into the contracts.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/unit/test_embedding_provider.py tests/architecture/test_directory_contract.py -q</automated></verify>
  <acceptance_criteria>Tests cover count/dimension/NaN/Inf and scripted safe failures with zero network; source/runtime assertions prove input and vector non-retention.</acceptance_criteria>
  <done>The replaceable, privacy-bounded Provider core exists independently of runtime configuration.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-02-01 | Information Disclosure | provider request | mitigate | DTO admits only normalized names and no user/meal/health context. |
| T-063-02-02 | Tampering | provider result DTO | mitigate | Validate model, count, dimension and finite values before return. |
| T-063-02-SC | Tampering | provider package | mitigate | Uses Plan 01 locks and existing HTTPX only. |
</threat_model>
<verification>Run provider and directory-contract tests offline.</verification>
<success_criteria>D-09–D-12 are enforced at the external boundary.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-02-SUMMARY.md`.</output>
