---
phase: 06.3-pgvector
plan: "19"
type: execute
wave: 15
depends_on: ["06.3-18"]
files_modified: [backend/evals/phase_06_3/langfuse_retention.py, backend/evals/phase_06_3/README.md, backend/tests/unit/test_langfuse_export.py]
autonomous: true
requirements: [HFS-08, HFS-09]
must_haves:
  truths: ["A standalone explicit CLI purges successful and failed detailed records equally at the UTC 30-day boundary.", "Purge result is ephemeral stdout/JSON and never becomes a repository artifact."]
  artifacts: [{path: backend/evals/phase_06_3/langfuse_retention.py, provides: "Bounded purge and asynchronous verification CLI"}]
  key_links: [{from: backend/evals/phase_06_3/langfuse_retention.py, to: Langfuse Public API, via: "explicit bounded delete/requery", pattern: "older-than|verify"}]
---
<objective>Enforce D-14/D-15 retention with a precise ephemeral-report contract.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-18-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Purge and verify 30-day detailed experiment retention</name>
  <read_first>backend/evals/phase_06_3/langfuse_publish.py, backend/evals/phase_06_3/README.md, backend/app/agent/retention.py, backend/tests/unit/test_langfuse_export.py</read_first>
  <files>backend/evals/phase_06_3/langfuse_retention.py, backend/evals/phase_06_3/README.md, backend/tests/unit/test_langfuse_export.py</files>
  <behavior>- `purge --older-than 30d --verify --format json` uses one documented UTC cutoff; 29-day remains and records at/older than cutoff delete.
- PASS/FAIL traces and scores use identical age-only selection.
- Pagination/delete/requery are bounded by batch/page/retry/deadline; unverifiable partial deletion exits nonzero.
- stdout JSON schema is exactly `{cutoff_utc, scanned, requested, verified_deleted, remaining_overdue, status}` with safe integer/status values; stderr contains safe codes only.
- Command writes no purge file, database row or Git artifact and never deletes dataset/release files.</behavior>
  <action>Implement standalone official-API deletion and asynchronous requery verification. It is invoked manually, never by app lifespan or scheduler. Emit the report only to stdout (human redirection, if desired, is outside the application contract); do not add a persistent purge-report artifact.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/unit/test_langfuse_export.py -k retention -q</automated></verify>
  <acceptance_criteria>Mock clock/API covers 29/30/31 days, PASS/FAIL symmetry, pagination, delayed deletion, timeout/partial failure, exact JSON keys and no filesystem writes.</acceptance_criteria>
  <done>Detailed experiment retention is bounded, symmetric and independently verifiable.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-19-01 | Information Disclosure | expired traces | mitigate | Age-only purge plus bounded verification. |
| T-063-19-02 | Repudiation | purge outcome | mitigate | Exact ephemeral JSON counts/status and nonzero unverifiable exit. |
| T-063-19-SC | Tampering | outcome filtering | mitigate | Tests prove PASS/FAIL symmetry and forbid result-based selection. |
</threat_model>
<verification>Run all retention boundary and no-write tests.</verification>
<success_criteria>D-14/D-15 are operational without accidental persistent artifacts.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-19-SUMMARY.md`.</output>
