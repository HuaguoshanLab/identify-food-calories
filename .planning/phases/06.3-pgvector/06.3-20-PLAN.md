---
phase: 06.3-pgvector
plan: "20"
type: execute
wave: 16
depends_on: ["06.3-12", "06.3-14", "06.3-17", "06.3-18", "06.3-19"]
files_modified: [docs/learning/phase-06.3-hybrid-food-search.md, docs/learning/README.md, README.md, admin-frontend/tests/e2e/admin-management.spec.ts, admin-frontend/tests/e2e/README.md]
autonomous: true
requirements: [HFS-01, HFS-02, HFS-03, HFS-04, HFS-05, HFS-06, HFS-07, HFS-08, HFS-09]
must_haves:
  truths: ["A developer can reproduce every deterministic gate and optional experiment command from documentation.", "Real admin browser flow verifies status, partial failure and batch retry without DB/token shortcuts."]
  artifacts: [{path: docs/learning/phase-06.3-hybrid-food-search.md, provides: "Chinese architecture and testing guide"}, {path: admin-frontend/tests/e2e/admin-management.spec.ts, provides: "Real-login/public-API status, partial-failure, batch-retry, replay and ordinary-user denial gate"}]
  key_links: [{from: README.md, to: backend/evals/phase_06_3/evaluate.py, via: "real-PG release and activation commands", pattern: "run_pg.py|activate"}, {from: admin-frontend/tests/e2e/admin-management.spec.ts, to: /api/v1/admin/catalog-publications/*/embedding-status, via: "real browser login and public admin API", pattern: "embedding-status|embedding-retries"}]
---
<objective>Document the completed system and run the phase-wide deterministic and real-browser gate.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-SPEC.md @.planning/phases/06.3-pgvector/06.3-17-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-19-SUMMARY.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Teach architecture and execute final acceptance</name>
  <read_first>docs/learning/phase-03-multimodal-meal-analysis.md, docs/learning/README.md, README.md, backend/evals/phase_06_3/README.md, admin-frontend/tests/e2e/admin-management.spec.ts, admin-frontend/tests/e2e/README.md, admin-frontend/playwright.config.ts, admin-frontend/package.json</read_first>
  <files>docs/learning/phase-06.3-hybrid-food-search.md, docs/learning/README.md, README.md, admin-frontend/tests/e2e/admin-management.spec.ts, admin-frontend/tests/e2e/README.md</files>
  <action>Extend the existing-convention `admin-management.spec.ts` with a test named `admin embedding status and batch retry` and update its README index/flow contract. The Playwright path must register and verify users, perform real admin login, and use only product pages plus public `/api/v1/admin/*` APIs for every catalog/index state transition—never direct database state setup, token fabrication/injection/copying, browser-storage reads or internal service calls. The existing audited first-admin bootstrap may establish the initial role before real login, but it must not seed or mutate catalog/index state. Publish a synthetic multi-name item through the public UI/API so the test Fake deterministically completes one job and fails another, observe `partial_failure`, retry the failed batch through the UI, and repeat the user-visible retry/status action to prove no duplicate job or cost; an independently logged-in ordinary user must receive 403 from both status GET and retry POST boundaries. Write the required Chinese guide for exact-first reasoning, discriminator-safe ASK/resume, current-qualified SQL, multi-job aggregate/retry, worker lifecycle, evidence-bound activation, Phoenix versus Langfuse, ephemeral retention report and exact test commands. State performance numbers only as measured environment-bound evidence, never universal claims. Update indexes/root commands, run all gates, then perform the AGENTS-required built-in browser follow-up over the same real login → publication → status `partial_failure` → batch retry/idempotent repeat → ordinary-user status/retry 403 path and record what was observed in the summary. Optional Langfuse smoke stays separate and cannot satisfy release.</action>
  <verify><automated>cd backend &amp;&amp; uv run ruff check app tests evals &amp;&amp; uv run mypy app &amp;&amp; uv run python tests/run_pg.py --env-file .env.test.example -- uv run pytest &amp;&amp; uv run python tests/run_pg.py --env-file .env.test.example -- uv run python evals/phase_06_3/evaluate.py --verify-release &amp;&amp; cd ../admin-frontend &amp;&amp; npm test -- --run &amp;&amp; npm run build &amp;&amp; npm run test:e2e -- tests/e2e/admin-management.spec.ts --grep "embedding status and batch retry"</automated></verify>
  <human-check>After the automated Playwright slice passes, use the Codex built-in browser against the running product to repeat real admin login, catalog publication, visible `partial_failure`, failed-job batch retry, idempotent repeat, and ordinary-user 403; record the exact path/results and any remaining manual uncertainty in `06.3-20-SUMMARY.md`.</human-check>
  <acceptance_criteria>All offline deterministic gates and the targeted real cross-stack Playwright command pass; the test covers visible `partial_failure`, failed-job batch retry, idempotent repeat and ordinary-user 403 without DB/token shortcuts; documentation includes exact commands/contracts; built-in browser follow-up records the same real-page/public-API path; optional Langfuse evidence is clearly separate.</acceptance_criteria>
  <done>Phase implementation, security, evaluation and teaching contracts are reproducible end to end.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-20-01 | Repudiation | final acceptance | mitigate | Full automated gate plus real public-page browser evidence. |
| T-063-20-02 | Tampering | capability claims | mitigate | Only hash-bound and environment-bound measured evidence is documented. |
| T-063-20-SC | Information Disclosure | docs/examples | mitigate | Synthetic examples and safe fields only. |
</threat_model>
<verification>Run complete gate and browser path; record optional Langfuse separately.</verification>
<success_criteria>All HFS requirements are demonstrable without Langfuse or paid-provider dependence.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-20-SUMMARY.md`.</output>
