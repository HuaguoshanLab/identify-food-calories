---
phase: 06.3-pgvector
plan: "05"
subsystem: nutrition-search
tags: [pydantic, deterministic-fusion, hybrid-search, tdd]
requires:
  - phase: 06.3-03
    provides: versioned embedding provider boundary
  - phase: 06.3-04
    provides: controlled relation evidence and isolated vector schema
provides:
  - non-exact ASK-only fusion contract with bounded safe candidate projections
  - versioned reliability thresholds, controlled relation layers, stable deduplication and ordering
affects: [06.3-pgvector-06, 06.3-pgvector-07, agent-state]
tech-stack:
  added: []
  patterns: [internal-evidence-public-projection, relation-layer-first-fusion, exact-allowlist]
key-files:
  created: [backend/app/nutrition/search.py, backend/tests/unit/test_hybrid_food_search.py]
  modified: [backend/app/nutrition/schemas.py, backend/app/nutrition/ports.py]
key-decisions:
  - "Non-exact evidence can only produce an ASK result; no fusion path carries a selected food."
  - "Relation layer precedes every score; scores and reciprocal ranks only order candidates within the same layer."
  - "Public candidates expose only D-04 discriminators and never scores, ranks, vectors or provider fields."
patterns-established:
  - "Keep repository evidence DTOs separate from public candidate DTOs and project through one allowlist function."
  - "Drop weak evidence before Top-3 truncation instead of filling the candidate list."
requirements-completed: [HFS-01, HFS-02, HFS-03]
duration: 7min
completed: 2026-09-11
---

# Phase 06.3 Plan 05: Hybrid Fusion Contracts Summary

**版本化纯融合将非精确检索固定在 ASK 确认边界，并按受控关系层安全投影最多三个候选。**

## Performance

- **Duration:** 7 min
- **Started:** 2026-09-11T03:01:57Z
- **Completed:** 2026-09-11T03:08:20Z
- **Tasks:** 1/1
- **Files modified:** 7

## Accomplishments

- 建立内部 `FoodSearchEvidence` 与外部 `FoodSearchCandidate` 的强隔离，公共输出没有分数、排名、向量或 Provider 字段。
- 新增纯融合器：先丢弃低质量证据，再按名称变体、地域/做法变体、同类食物分层，层内以分数与 RRF 稳定排序并去重。
- 锁定唯一身份去重、少于三项不凑数、共享食材噪声排除、非精确单命中不自动选择及 D-04 discriminator 的单元测试。

## Task Commits

1. **Task 1: Define search DTOs and deterministic fusion policy (RED)** - `0c6fff1` (`test`)
2. **Task 1: Define search DTOs and deterministic fusion policy (GREEN)** - `63056b1` (`feat`)

## Files Created/Modified

- `backend/app/nutrition/schemas.py` - 严格的内部证据、公开候选及非精确融合结果 DTO。
- `backend/app/nutrition/ports.py` - 后续应用服务使用的权威 hybrid-search Repository 协议。
- `backend/app/nutrition/search.py` - 可靠性阈值、融合、稳定排序和安全投影。
- `backend/tests/unit/test_hybrid_food_search.py` - D-01 至 D-04 的纯合同测试。
- `backend/app/nutrition/README.md`、`backend/tests/unit/README.md` - 新模块与测试索引。

## Decisions Made

- 非精确融合结果的 `selected_food` 只能为 `None`，并由 Pydantic 强制 `ASK`；唯一精确 PASS 留给后续服务短路路径。
- 关系层绝不被跨层分数压过；分数只用于可靠性门槛和同层排序，RRF 与稳定身份键解决同分情况。
- 可见候选仅含 ID、版本、名称、关系、可选做法、最多三个受控份量提示及来源名称。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Contract gap] 让同层分数参与排序。**
- **Found during:** Task 1
- **Issue:** 初始融合实现仅把分数作为可靠性阈值，未让其在同一关系层内决定排序。
- **Fix:** 合并每通道最高分，并在关系层之后、RRF 之前作为稳定排序键。
- **Files modified:** `backend/app/nutrition/search.py`
- **Verification:** 聚焦融合合同测试与 Ruff 均通过。
- **Committed in:** `63056b1`

---

**Total deviations:** 1 auto-fixed (1 Rule 1)
**Impact on plan:** 修复直接满足 D-02 的层内排序要求，没有扩大功能范围。

## Issues Encountered

- 初次执行 `uv` 时受 sandbox 限制无法读取共享缓存；经批准使用项目锁定依赖后，测试正常运行。未新增或修改依赖。

## Known Stubs

None. 融合器是纯函数；数据库通道和应用服务编排由后续计划接入。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 06 可以实现三个 PostgreSQL 通道并返回 `FoodSearchEvidence`。
- Plan 07 可以保持 `NutritionService.search_food_catalog` 为唯一编排入口，在唯一精确短路后调用本融合器。

## Self-Check: PASSED

- Confirmed `backend/app/nutrition/search.py` and `backend/tests/unit/test_hybrid_food_search.py` exist.
- Confirmed task commits `0c6fff1` and `63056b1` exist.

---
*Phase: 06.3-pgvector*
*Completed: 2026-09-11*
