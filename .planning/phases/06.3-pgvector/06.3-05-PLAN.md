---
phase: 06.3-pgvector
plan: "05"
type: execute
wave: 4
depends_on: ["06.3-03", "06.3-04"]
files_modified: [backend/app/nutrition/schemas.py, backend/app/nutrition/ports.py, backend/app/nutrition/search.py, backend/app/nutrition/README.md, backend/tests/unit/test_hybrid_food_search.py, backend/tests/unit/README.md]
autonomous: true
requirements: [HFS-01, HFS-02, HFS-03]
must_haves:
  truths: ["Only one unique exact result can PASS; every nonexact result ASKs.", "D-03: fewer than three reliable candidates are returned as-is and unreliable candidates never fill Top 3.", "Candidates expose allowlisted relation, preparation, portion and source discriminators but no internal scores."]
  artifacts: [{path: backend/app/nutrition/search.py, provides: "Versioned deterministic fusion"}, {path: backend/app/nutrition/schemas.py, provides: "Strict internal and public candidates"}]
  key_links: [{from: backend/app/nutrition/search.py, to: backend/app/nutrition/schemas.py, via: "safe projection", pattern: "prepared_state|portion_hints|source_name"}]
---
<objective>Lock PASS/ASK, controlled relation ranking and D-04 discriminator projection as pure contracts.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-CONTEXT.md @.planning/phases/06.3-pgvector/06.3-AI-SPEC.md @.planning/phases/06.3-pgvector/06.3-04-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define search DTOs and deterministic fusion policy</name>
  <read_first>backend/app/nutrition/schemas.py, backend/app/nutrition/ports.py, backend/app/nutrition/service.py, backend/tests/unit/test_nutrition.py, backend/app/nutrition/search_models.py</read_first>
  <files>backend/app/nutrition/schemas.py, backend/app/nutrition/ports.py, backend/app/nutrition/search.py, backend/app/nutrition/README.md, backend/tests/unit/test_hybrid_food_search.py, backend/tests/unit/README.md</files>
  <behavior>- PASS requires channel exact, one selected current version and no candidates.
- ASK/none selects nothing and returns 0–3 unique current identities.
- Layer order is name variant, region/cooking variant, same-class food; scores order only within one layer.
- Public `FoodSearchCandidate` carries `food_id`, `catalog_version`, canonical label, relation label, optional `prepared_state`, bounded `portion_hints`, and `source_name`; no similarity/rank/vector/provider fields.
- Unreliable hits are dropped rather than used as filler.</behavior>
  <action>Extend existing schemas/ports and add pure versioned normalizer, dedupe, threshold, layer-first ordering and safe projection in `search.py`, per D-01–D-04. Standardize the application owner as existing `NutritionService.search_food_catalog`; do not introduce `HybridFoodSearchService`. Semantic-only evidence may only yield `同类食物`. Update the unit-test README index for the new contract test and name the behaviors it verifies.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/unit/test_hybrid_food_search.py -k 'invariant or fusion or discriminator or score' -q</automated></verify>
  <acceptance_criteria>Tests cover equal scores, duplicates, fewer than three, shared-ingredient noise, one nonexact hit, discriminator present/absent projection, and serialized absence of scores.</acceptance_criteria>
  <done>D-01–D-04 are executable runtime invariants.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-05-01 | Tampering | PASS/ASK DTO | mitigate | Pydantic impossible-state validation and exact-only authority. |
| T-063-05-02 | Information Disclosure | public candidate | mitigate | Exact allowlist includes safe discriminators and excludes scores/vectors. |
| T-063-05-SC | Tampering | policy inputs | mitigate | Versioned constants and frozen tests; no new dependency. |
</threat_model>
<verification>Run pure search contract tests.</verification>
<success_criteria>Candidate semantics are deterministic, explainable and non-authoritative unless exact.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-05-SUMMARY.md`.</output>
