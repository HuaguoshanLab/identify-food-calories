---
phase: 06.3-pgvector
plan: "14"
type: execute
wave: 11
depends_on: ["06.3-13"]
files_modified: [backend/app/agent/graph.py, backend/app/agent/README.md, backend/tests/unit/test_agent_multimodal.py, backend/tests/unit/test_diet_planning_graph.py, backend/tests/integration/test_agent_checkpoint.py]
autonomous: true
requirements: [HFS-03, HFS-04, HFS-05, HFS-06]
must_haves:
  truths: ["Every nonexact result interrupts in meal and planning flows with identical candidates and discriminators.", "Resume validates offered ID/version and re-reads current qualification."]
  artifacts: [{path: backend/app/agent/graph.py, provides: "Both-flow ASK/resume behavior"}]
  key_links: [{from: backend/app/agent/graph.py, to: backend/app/agent/tools.py, via: "await shared search Tool", pattern: "await.*search_food_catalog"}]
---
<objective>Enforce identical ASK, clarification and resume semantics in both real graphs.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-13-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-CONTEXT.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Route safe candidates through clarification and authoritative resume</name>
  <read_first>backend/app/agent/graph.py, backend/app/agent/state.py, backend/tests/unit/test_agent_multimodal.py, backend/tests/unit/test_diet_planning_graph.py, backend/tests/integration/test_agent_checkpoint.py, backend/app/planning/service.py</read_first>
  <files>backend/app/agent/graph.py, backend/app/agent/README.md, backend/tests/unit/test_agent_multimodal.py, backend/tests/unit/test_diet_planning_graph.py, backend/tests/integration/test_agent_checkpoint.py</files>
  <behavior>- Exact rice PASS continues; any nonexact result including one candidate interrupts in both flows.
- Clarification carries relation plus optional preparation, portion hints and source when available, never internal scores.
- Resume accepts only offered `(food_id,catalog_version)`, then authoritative reread; stale choice re-asks/stops.
- Same snapshot/query yields identical ordered IDs, labels, discriminators and action in both flows.
- Existing loop/tool/timeout limits remain effective.</behavior>
  <action>Await the shared Tool in meal resolution and planning named-food/substitution resolution. Build clarification content from the safe state projection, route ASK before calculation/recipe use, and reread confirmed identity. Do not create a new graph, duplicate search logic or trust browser-returned labels/nutrients.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/unit/test_agent_multimodal.py tests/unit/test_diet_planning_graph.py -q &amp;&amp; uv run python tests/run_pg.py --env-file .env.test.example -- uv run pytest tests/integration/test_agent_checkpoint.py -k hybrid_search -q</automated></verify>
  <acceptance_criteria>Tests cover exact, five mappings, one-candidate ASK, empty/fallback, D-04 discriminator rendering, stale resume, flow parity and loop termination.</acceptance_criteria>
  <done>Both Agent flows share safe, explainable confirmation semantics.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-14-01 | Tampering | resume payload | mitigate | Offered-set validation plus ID/version qualification reread. |
| T-063-14-02 | Information Disclosure | clarification | mitigate | Safe discriminator allowlist, no scores/vectors/query. |
| T-063-14-SC | Denial of Service | graph loop | mitigate | Preserve existing loop/tool/time budgets. |
</threat_model>
<verification>Run both graph suites and real checkpoint integration.</verification>
<success_criteria>HFS-03/04 parity is proven at Tool, graph and checkpoint boundaries.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-14-SUMMARY.md`.</output>
