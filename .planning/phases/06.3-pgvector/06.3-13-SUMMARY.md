---
phase: 06.3-pgvector
plan: "13"
subsystem: agent-runtime
tags: [langgraph, fastapi, tracing, phoenix, hybrid-search, postgres]
requires:
  - phase: 06.3-07
    provides: async NutritionService hybrid-search path and safe tracing attributes
  - phase: 06.3-11
    provides: supervised worker lifecycle
provides:
  - One lifespan-owned TracingRuntime shared by Agent runtime collaborators
  - Thread-confined hybrid-search adapter with async graph boundary
  - Backward-compatible, retrieval-evidence-free checkpoint candidates
affects: [06.3-14, agent-graphs, observability]
tech-stack:
  added: []
  patterns: [single-owner telemetry lifecycle, thread-confined synchronous sessions, safe state projection]
key-files:
  created: [.planning/phases/06.3-pgvector/06.3-13-SUMMARY.md]
  modified: [backend/app/main.py, backend/app/agent/tools.py, backend/app/agent/state.py, backend/app/agent/supervisor.py, backend/app/agent/graph.py, backend/tests/integration/test_agent_bootstrap.py]
key-decisions:
  - "PersistedAgentRuntimeFactory alone flushes and shuts down tracing after supervisor work stops."
  - "SessionNutritionToolAdapter creates, consumes, and closes each synchronous search Session inside one worker-thread closure."
  - "Checkpoint candidates retain only bounded explanation discriminators and remain compatible with legacy payloads."
patterns-established:
  - "Runtime collaborators borrow the factory-created TracingRuntime and never own its lifecycle."
  - "Hybrid search cannot persist query, scores, ranks, vectors, provider payloads, or identity data in graph state."
requirements-completed: [HFS-03, HFS-04, HFS-05, HFS-06]
duration: 45min
completed: 2026-09-11
---

# Phase 06.3 Plan 13: Shared Tool State and Tracing Runtime Summary

**The persisted Agent runtime now shares one application-owned tracing lifecycle while hybrid-search candidates remain explainable without persisting retrieval evidence.**

## Performance

- **Duration:** 45 min
- **Completed:** 2026-09-11T05:50:35Z
- **Tasks:** 1/1
- **Files modified:** 8

## Accomplishments

- Injected one `TracingRuntime` into the supervisor and session-backed nutrition adapter; the FastAPI factory flushes and shuts it down exactly once after supervised workers stop.
- Kept the graph-to-service search boundary awaitable while creating, using, and closing each SQLAlchemy Session in the same worker-thread closure.
- Extended `StateCandidate` with bounded safe discriminators and preserved decoding of old checkpoint payloads that lack them.
- Added real PostgreSQL lifecycle coverage with a spy tracing runtime, asserting the six hybrid-search attributes, forbidden-field absence, shared identity, and one flush/shutdown.

## Task Commits

1. **Task 1: Wire one async service adapter and safe candidate state** — `ee5f498` (`feat`)

## Files Created/Modified

- `backend/app/main.py` — creates and owns the sole production tracing runtime.
- `backend/app/agent/tools.py` — confines hybrid search Sessions to the worker thread and shares runtime dependencies.
- `backend/app/agent/state.py` and `backend/app/agent/graph.py` — persist bounded candidate explanations only.
- `backend/app/agent/supervisor.py` — stops supervised work without closing borrowed tracing.
- `backend/tests/integration/test_agent_bootstrap.py` — real factory lifecycle and safe telemetry proof.

## Decisions Made

- `asyncio.Runner` is scoped to the adapter worker thread so the async embedding provider and synchronous Session execute on the same thread; no Session crosses threads and no `asyncio.run` bridge is used.
- The generic relation label is `目录候选` because the current `FoodSearchResult` does not expose fusion-channel relation evidence; inventing a more specific relation would be false.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Corrected the hybrid repository injected by the session adapter.**
- **Found during:** Task 1 real PostgreSQL tracing test.
- **Issue:** `SqlAlchemyNutritionRepository` does not implement hybrid retrieval methods.
- **Fix:** Injected `SqlAlchemyHybridFoodSearchRepository` alongside the existing authority repository.
- **Files modified:** `backend/app/agent/tools.py`
- **Verification:** Focused real PostgreSQL tracing lifecycle test passed.
- **Committed in:** `ee5f498`

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Required for the runtime’s planned hybrid-search path; no scope expansion.

## Known Stubs

None.

## Next Phase Readiness

Plan 14 can consume a shared, lifecycle-safe tracing runtime and safe candidate state without adding ownership or payload leakage paths.

## Self-Check: PASSED

- Confirmed the task commit `ee5f498` exists and no tracked files were deleted.
- Confirmed all declared runtime, state, graph, and focused test files exist.
