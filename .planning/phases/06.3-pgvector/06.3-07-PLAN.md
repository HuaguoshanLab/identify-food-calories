---
phase: 06.3-pgvector
plan: "07"
type: execute
wave: 6
depends_on: ["06.3-02", "06.3-03", "06.3-05", "06.3-06"]
files_modified: [backend/app/nutrition/service.py, backend/app/core/tracing.py, backend/tests/unit/test_hybrid_food_search.py, backend/tests/unit/test_runtime_foundation.py, backend/tests/unit/README.md]
autonomous: true
requirements: [HFS-01, HFS-02, HFS-03, HFS-06]
must_haves:
  truths: ["NutritionService.search_food_catalog exact-short-circuits before embedding.", "Declared semantic failures preserve text-only ASK and never expand PASS authority.", "Phoenix receives payload-free retrieval channel, version, fallback, latency-bucket and index-health signals from the production search boundary."]
  artifacts: [{path: backend/app/nutrition/service.py, provides: "Single application orchestration owner"}]
  key_links: [{from: backend/app/nutrition/service.py, to: backend/app/providers/embedding/ports.py, via: "temporary query embedding", pattern: "search_food_catalog"}, {from: backend/app/nutrition/service.py, to: backend/app/core/tracing.py, via: "injected TracingRuntime allowlist-only Phoenix span; construction/lifecycle remain Plan 13 factory ownership", pattern: "nutrition.hybrid_search"}]
---
<objective>Extend the existing NutritionService with exact-first async orchestration, safe fallback and payload-free Phoenix production tracing.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-02-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-05-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-06-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Orchestrate unique exact, hybrid recall and fallback</name>
  <read_first>backend/app/nutrition/service.py, backend/app/nutrition/repository.py, backend/app/providers/embedding/ports.py, backend/app/providers/embedding/fake.py, backend/app/core/tracing.py, backend/tests/unit/test_nutrition.py, backend/tests/unit/test_runtime_foundation.py</read_first>
  <files>backend/app/nutrition/service.py, backend/app/core/tracing.py, backend/tests/unit/test_hybrid_food_search.py, backend/tests/unit/test_runtime_foundation.py, backend/tests/unit/README.md</files>
  <behavior>- `米饭` unique exact returns rice PASS with zero provider/vector calls.
- Exact miss executes text plus bounded semantic recall and always ASK/none.
- Timeout, unavailable, invalid vector and vector-query outage return valid text fallback and safe code.
- Only normalized single name reaches Provider; query vector is discarded after one call.
- An injected `TracingRuntime` opens one Phoenix span and exports only `retrieval.version`, `match.channel`, `fallback.code`, `latency.bucket`, `index.health`, `index.version` and existing safe status/tool attributes; normalized query, vector, candidate labels/body and user/meal/body/health fields are rejected.</behavior>
  <action>Make existing `NutritionService.search_food_catalog` async, inject the search repository, `EmbeddingProvider` and a caller-owned `TracingRuntime`, and keep it as the one concrete service symbol everywhere. Exact first; on miss gather text, await query embedding with a bound, gather active-space vectors, fuse via `search.py`, and rematerialize current qualified rows. Catch only declared availability failures. At this application orchestration boundary, open a `nutrition.hybrid_search` span on the injected runtime, extend the central tracing allowlist only with bounded payload-free retrieval/version/channel/fallback/latency-bucket/index-health keys, and never attach query/candidate/provider payloads or identity/meal/health context. Do not call `create_tracing_runtime`, flush or shutdown here: Plan 13 gives the production factory sole lifecycle ownership. Preserve all nutrition calculation/validation methods unchanged.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/unit/test_hybrid_food_search.py tests/unit/test_nutrition.py tests/unit/test_runtime_foundation.py -k 'hybrid or tracing or allowlist or nutrition' -q</automated></verify>
  <acceptance_criteria>Fake call tests prove short-circuit, no nonexact selection, safe fallback, query/vector non-retention and byte-stable candidates; tracing canaries prove every prohibited field is absent from the Phoenix exporter while all six safe retrieval signals are emitted.</acceptance_criteria>
  <done>One service owns all online search semantics.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-07-01 | Tampering | service decision | mitigate | Exact-only PASS invariant and rematerialization before output. |
| T-063-07-02 | Information Disclosure | transient query vector | mitigate | Local variable only; no log/cache/persistence. |
| T-063-07-03 | Information Disclosure | Phoenix span | mitigate | Central exact-key allowlist exports payload-free enums/versions/buckets only; forbidden canaries are tested. |
| T-063-07-04 | Denial of Service | tracing lifecycle | mitigate | Service never constructs, flushes or shuts down tracing; Plan 13 owns one runtime for the application lifespan. |
| T-063-07-SC | Denial of Service | Provider call | mitigate | Bounded wait and declared fallback only. |
</threat_model>
<verification>Run nutrition service suites offline.</verification>
<success_criteria>HFS-01/02/03/06 are complete at the application layer.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-07-SUMMARY.md`.</output>
