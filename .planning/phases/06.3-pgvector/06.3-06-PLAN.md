---
phase: 06.3-pgvector
plan: "06"
type: execute
wave: 5
depends_on: ["06.3-04", "06.3-05"]
files_modified: [backend/app/nutrition/search_repository.py, backend/app/nutrition/repository.py, backend/app/nutrition/README.md, backend/tests/integration/test_hybrid_food_search.py, backend/tests/integration/README.md]
autonomous: true
requirements: [HFS-01, HFS-02, HFS-05, HFS-06]
must_haves:
  truths: ["Exact, pg_trgm and pgvector queries share one live current-qualified SQL base and project only current-version controlled relation evidence.", "Confirmation re-reads food ID and immutable catalog version."]
  artifacts: [{path: backend/app/nutrition/search_repository.py, provides: "Authoritative three-channel PostgreSQL adapter"}]
  key_links: [{from: backend/app/nutrition/search_repository.py, to: backend/app/nutrition/repository.py, via: "shared qualified statement", pattern: "current_qualified"}]
---
<objective>Implement all retrieval channels against one authoritative PostgreSQL boundary.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-04-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-05-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build exact, trigram and isolated vector queries</name>
  <read_first>backend/app/nutrition/repository.py, backend/app/admin/models.py, backend/app/nutrition/ports.py, backend/app/nutrition/search_models.py, backend/tests/integration/test_catalog_publish_eligibility.py</read_first>
  <files>backend/app/nutrition/search_repository.py, backend/app/nutrition/repository.py, backend/app/nutrition/README.md, backend/tests/integration/test_hybrid_food_search.py, backend/tests/integration/README.md</files>
  <behavior>- Canonical/alias equality groups identities; only exactly one qualified identity is exact.
- Trigram uses bound expressions, calibrated pool and stable tie-breakers.
- Vector requires active model/dimension/adapter space, cosine opclass and finite 1024-vector.
- Every channel filters current publication, latest eligibility, source/license and calculability in SQL.
- Candidate projection left-joins only non-revoked controlled relation evidence matching both current source and target publication/content versions; invalid/old/revoked evidence cannot grant a relation layer.
- Missing active index/known vector outage maps to safe fallback; contract/data corruption propagates.</behavior>
  <action>Extract a shared current-qualified statement, then implement `find_current_qualified_exact`, `find_text_candidates`, `find_vector_candidates`, and `get_qualified_food` in repository adapters. In the text/vector projections, join controlled relation evidence by current source publication/content version, current target publication/content version and non-revoked status; map its constrained value to `relation_layer`, while absence falls back conservatively to `同类食物`. Apply D-07/D-08 live filters before materialization, use SQLAlchemy binds, and record real `EXPLAIN (ANALYZE, BUFFERS)` evidence on the fixed fixture. Do not swallow arbitrary SQL errors.</action>
  <verify><automated>cd backend &amp;&amp; uv run python tests/run_pg.py --env-file .env.test.example -- uv run pytest tests/integration/test_hybrid_food_search.py -k 'exact or text or vector or eligibility or explain' -q</automated></verify>
  <acceptance_criteria>Real PG tests prove no stale/ineligible leak, exact ambiguity cannot PASS, vector-space isolation, correct cosine operator, deterministic order, current relation projection, old/revoked relation exclusion and authoritative confirmation reread.</acceptance_criteria>
  <done>PostgreSQL remains the sole authority for every candidate channel.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-06-01 | Tampering | eligibility SQL | mitigate | Shared live qualification in each candidate query and confirmation reread. |
| T-063-06-02 | Denial of Service | candidate scans | mitigate | Indexed bounded pools and captured query plans. |
| T-063-06-03 | Tampering | relation layer projection | mitigate | Join only constrained, non-revoked evidence bound to both current publication versions. |
| T-063-06-SC | Tampering | SQL input | mitigate | Bound SQLAlchemy expressions only. |
</threat_model>
<verification>Run the complete hybrid repository suite through isolated real PostgreSQL.</verification>
<success_criteria>HFS-01/02/05 SQL behavior is authoritative, deterministic and safely degradable.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-06-SUMMARY.md`.</output>
