---
phase: 06.3-pgvector
plan: "08"
type: execute
wave: 3
depends_on: ["06.3-04"]
files_modified: [backend/app/admin/models.py, backend/app/admin/ports.py, backend/app/admin/repository.py, backend/app/admin/schemas.py, backend/app/admin/service.py, backend/app/admin/README.md, backend/tests/integration/test_catalog_embedding_jobs.py, backend/tests/integration/README.md]
autonomous: true
requirements: [HFS-05, HFS-06]
must_haves:
  truths: ["Publication atomically creates all required name/space jobs while exact/text become visible.", "Publication status aggregates explicit job rows and batch retry is idempotent under partial/concurrent failure."]
  artifacts: [{path: backend/app/admin/service.py, provides: "Publication-level aggregate and batch retry policy"}, {path: backend/tests/integration/test_catalog_embedding_jobs.py, provides: "Multi-job concurrency proof"}]
  key_links: [{from: backend/app/admin/service.py, to: backend/app/nutrition/search_models.py, via: "atomic publication enqueue and aggregate", pattern: "publication_id"}]
---
<objective>Define precise multi-job publication, aggregate status and idempotent batch retry semantics.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-CONTEXT.md @.planning/phases/06.3-pgvector/06.3-04-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement publication-level aggregate over explicit jobs</name>
  <read_first>backend/app/admin/models.py, backend/app/admin/ports.py, backend/app/admin/repository.py, backend/app/admin/schemas.py, backend/app/admin/service.py, backend/tests/integration/test_catalog_publish_eligibility.py</read_first>
  <files>backend/app/admin/models.py, backend/app/admin/ports.py, backend/app/admin/repository.py, backend/app/admin/schemas.py, backend/app/admin/service.py, backend/app/admin/README.md, backend/tests/integration/test_catalog_embedding_jobs.py, backend/tests/integration/README.md</files>
  <behavior>- Publish transaction creates one unique job per normalized canonical/alias name and target space; replay creates none.
- Aggregate status is `pending|processing|partial_failure|failed|ready`, derived from explicit safe job rows.
- Partial success plus retryable failure is `partial_failure`; every job row reports job ID, space ID, status, attempts, safe error and timestamps only.
- Publication-level retry locks all retryable failed rows, transitions them as one idempotent batch, audits before/after counts, and concurrent/replayed requests create no duplicate cost.
- Provider/job failure never rolls back publication or exact/text visibility.</behavior>
  <action>Extend existing admin repository/service publication transaction per D-05–D-08. Define `CatalogEmbeddingStatusResponse` as publication aggregate plus explicit job rows; define publication-scoped retry command with required idempotency key/reason. Lock publication and eligible retryable jobs in stable order, store command hash/result, reset only legal failed jobs, and commit audit once. Keep Provider I/O outside HTTP transaction. Update the integration-test README index for the new catalog embedding job suite and its publication/aggregate/retry assertions.</action>
  <verify><automated>cd backend &amp;&amp; uv run python tests/run_pg.py --env-file .env.test.example -- uv run pytest tests/integration/test_catalog_embedding_jobs.py -k 'publish or aggregate or partial or retry or concurrent or idempotent' -q</automated></verify>
  <acceptance_criteria>Real PG tests cover multiple names/spaces, partial failure, zero retryable conflict, concurrent retries, replay, rollback and immediate exact/text visibility.</acceptance_criteria>
  <done>The singular publication API has an unambiguous multi-job contract.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-08-01 | Denial of Service | batch retry | mitigate | Publication lock, command hash, finite job attempts and stable row locking. |
| T-063-08-02 | Information Disclosure | aggregate projection | mitigate | Safe scalar/job allowlist; no name/vector/provider body. |
| T-063-08-SC | Tampering | publication transaction | mitigate | Job enqueue, pointer, eligibility and audit commit atomically. |
</threat_model>
<verification>Run multi-job publication/retry tests in real PostgreSQL.</verification>
<success_criteria>D-05–D-08 work for partial and concurrent multi-job lifecycles.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-08-SUMMARY.md`.</output>
