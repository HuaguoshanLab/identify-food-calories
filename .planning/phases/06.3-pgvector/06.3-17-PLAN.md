---
phase: 06.3-pgvector
plan: "17"
type: execute
wave: 13
depends_on: ["06.3-10", "06.3-16", "06.3-21"]
files_modified: [backend/evals/phase_06_3/activate.py, backend/evals/phase_06_3/README.md, backend/app/admin/service.py, backend/app/admin/repository.py, backend/tests/integration/test_catalog_embedding_jobs.py, backend/tests/integration/README.md]
autonomous: true
requirements: [HFS-05, HFS-09]
must_haves:
  truths: ["Only a PostgreSQL-confirmed ADMIN using matching release and exact build-completion evidence may activate a complete space.", "Approval evidence is persisted before one atomic active-pointer switch and replay is idempotent."]
  artifacts: [{path: backend/evals/phase_06_3/activate.py, provides: "Thin controlled activation CLI"}, {path: backend/app/admin/service.py, provides: "DB-authorized activation application service"}]
  key_links: [{from: backend/evals/phase_06_3/activate.py, to: backend/app/admin/service.py, via: "actor ID, reason and idempotency command", pattern: "activate_vector_space"}, {from: backend/app/admin/service.py, to: backend/evals/phase_06_3/release.json, via: "hash and exact build-evidence validation before repository transaction", pattern: "dataset_sha256|retrieval|embedding|snapshot_hash"}, {from: backend/app/admin/service.py, to: backend/app/nutrition/search_models.py, via: "Plan 21 immutable snapshot plus separate Plan 10 completion evidence on Plan 04 schema", pattern: "snapshot_hash|completion_hash|completion_count"}, {from: backend/app/admin/service.py, to: backend/app/admin/repository.py, via: "approval insert and atomic pointer switch", pattern: "activate"}]
---
<objective>Turn release evidence into a controlled, auditable and atomic vector-space activation.</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md @.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/06.3-pgvector/06.3-10-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-16-SUMMARY.md @.planning/phases/06.3-pgvector/06.3-21-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Validate release evidence and atomically activate a complete space</name>
  <read_first>backend/evals/phase_06_3/evaluate.py, backend/evals/phase_06_3/release.json, backend/app/admin/service.py, backend/app/admin/repository.py, backend/tests/integration/test_catalog_embedding_jobs.py</read_first>
  <files>backend/evals/phase_06_3/activate.py, backend/evals/phase_06_3/README.md, backend/app/admin/service.py, backend/app/admin/repository.py, backend/tests/integration/test_catalog_embedding_jobs.py, backend/tests/integration/README.md</files>
  <behavior>- CLI requires target space, PASS release path, actor user ID, bounded approval reason and idempotency key, then delegates all authorization and mutation to AdminService.
- AdminService re-reads the actor from PostgreSQL and requires current ADMIN role; USER and unknown actor leave all evidence/pointers unchanged.
- It recomputes release, dataset, evaluator/code, retrieval policy, catalog snapshot, model/dimension/adapter hashes and matches the target space.
- It verifies the target's immutable build identity and Plan 21 manifest hash/count, plus the separate Plan 10 completion hash/count/time written against Plan 04 schema, then verifies every exact Plan 21 snapshot item has exactly one completed embedding and no failed/pending job.
- One transaction locks pointer/space, persists immutable approval evidence, and switches active space; replay is idempotent.
- Hash mismatch, incomplete index, concurrent pointer change or FAIL report leaves pointer unchanged.</behavior>
  <action>Create a thin activation CLI separate from worker completion. It accepts an actor identifier only as lookup input, never as authorization evidence, and invokes `AdminService.activate_vector_space`. The service must run existing DB-backed `require_role(ADMIN)`, recompute file/database evidence, validate the Plan 21 immutable snapshot/manifest without mutating it, validate the separate Plan 10 completion evidence against that exact manifest and Plan 04 schema, validate target identity/completeness, and call one repository transaction that stores approval/audit hashes and atomically switches the active pointer. Never trust CLI identity claims, a caller-supplied PASS boolean or dashboard score.</action>
  <verify><automated>cd backend &amp;&amp; uv run python tests/run_pg.py --env-file .env.test.example -- uv run pytest tests/integration/test_catalog_embedding_jobs.py -k 'activation or approval_hash or completeness or concurrent' -q</automated></verify>
  <acceptance_criteria>Real PostgreSQL tests prove USER/unknown denial, ADMIN successful switch, immutable approval/audit evidence, idempotent replay, exact Plan 21 snapshot plus Plan 10 completion-evidence verification, and no switch for snapshot/completion mismatch, partial build or concurrent pointer change.</acceptance_criteria>
  <done>D-11 activation is a reachable, evidence-bound operation.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-063-17-01 | Elevation of Privilege | activation CLI/service | mitigate | CLI identity is lookup-only; AdminService re-reads PostgreSQL role and requires ADMIN. |
| T-063-17-02 | Tampering | release hashes | mitigate | Recompute all hashes and compare target vector identity/completeness. |
| T-063-17-03 | Tampering | snapshot/completion chain | mitigate | Treat Plan 21 manifest as immutable input and require separately stored Plan 10 completion evidence to match it exactly before activation. |
| T-063-17-SC | Repudiation | pointer switch | mitigate | One locked transaction and idempotent approval record. |
</threat_model>
<verification>Run all activation cases in isolated real PostgreSQL.</verification>
<success_criteria>Evaluation approval is concretely linked to one complete vector space and atomic activation.</success_criteria>
<output>Create `.planning/phases/06.3-pgvector/06.3-17-SUMMARY.md`.</output>
