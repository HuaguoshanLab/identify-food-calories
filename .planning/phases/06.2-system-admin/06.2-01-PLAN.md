---
phase: "06.2"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/admin/schemas.py, backend/app/admin/ports.py, backend/app/admin/repository.py, backend/app/admin/service.py, backend/app/admin/api.py, backend/tests/admin/test_admin_user_management_service.py, backend/tests/unit/test_admin_user_management_api.py, backend/tests/integration/test_admin_audit.py]
autonomous: true
requirements: [AUTH-04, ADM-01, ADM-05, ARC-08]
must_haves:
  truths: ["管理员通过公开 API 查询账号和固定角色", "升降权原子写入角色与审计并支持幂等", "降权后普通会话保留但后台实时拒绝"]
  artifacts: ["backend/app/admin/service.py", "backend/app/admin/api.py", "backend/tests/integration/test_admin_audit.py"]
  key_links: ["admin API → service policy → repository locks → User/AdminAudit", "demotion → retained refresh session → live DB RBAC 403"]
---
<objective>扩展既有 admin 模块，交付最小公开账号治理 API，不建立第二套权限或审计系统。</objective>
<context>@AGENTS.md @backend/AGENTS.md @.planning/phases/06.2-system-admin/06.2-CONTEXT.md @.planning/phases/06.2-system-admin/06.2-RESEARCH.md @.planning/phases/06.2-system-admin/06.2-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 固定账号与角色 DTO、查询端口和策略测试</name><files>backend/app/admin/schemas.py, backend/app/admin/ports.py, backend/tests/admin/test_admin_user_management_service.py, backend/tests/unit/test_admin_user_management_api.py</files><action>定义分页筛选、账号最小投影、固定角色和角色命令 DTO；用 fake repository/HTTPX 覆盖管理员授权、筛选、角色统计、自操作、无效目标、重复提交与错误映射。</action><verify><automated>cd backend && .venv/bin/pytest tests/admin/test_admin_user_management_service.py tests/unit/test_admin_user_management_api.py -q</automated></verify></task>
<task type="auto" tdd="true"><name>Task 2: 接入 SQL、锁、事务、审计和公开路由</name><files>backend/app/admin/repository.py, backend/app/admin/service.py, backend/app/admin/api.py, backend/tests/integration/test_admin_audit.py</files><action>实现邮箱/角色/状态分页与角色计数；角色变更先做实时 DB RBAC，再锁定治理事务和用户行，禁止自操作、未验证提升及最后管理员降权，同事务写专用与通用审计。降权不撤销认证会话。</action><verify><automated>cd backend && .venv/bin/pytest tests/integration/test_admin_audit.py -q</automated></verify></task>
</tasks>
<verification>Ruff、策略/API 单测和真实 PostgreSQL 集成测试全部通过。</verification>
<success_criteria>直接调用公开接口也无法绕过后台 RBAC、并发保护、原因、审计和幂等要求。</success_criteria>
<output>Create 06.2-01-SUMMARY.md when done</output>
