---
phase: 02-agent
plan: 09
type: execute
wave: 6
depends_on: ["02-04", "02-05", "02-07", "02-08"]
files_modified: [backend/app/agent/schemas.py, backend/app/agent/api.py, backend/app/main.py, backend/app/agent/README.md, backend/README.md, backend/openapi-agent-v1.json, frontend/src/features/agent/api/generate-contracts.mjs, frontend/src/features/agent/api/schemas.generated.ts, frontend/src/features/agent/api/client.generated.ts, frontend/src/features/agent/api/README.md]
autonomous: true
requirements: [AGT-02, AGT-04, ARC-05, QLT-02]
must_haves:
  truths: ["首个闭环需要的公开 operations 全部预声明并统一返回 501。", "runtime OpenAPI→frozen JSON→TS→Zod→operation client 可 generate-all/check-all。"]
  artifacts:
    - path: "frontend/src/features/agent/api/generate-contracts.mjs"
      provides: "机械生成与临时逐字漂移检查"
    - path: "backend/openapi-agent-v1.json"
      provides: "完整 6-operation sentinel contract"
  key_links:
    - from: "frontend/src/features/agent/api/generate-contracts.mjs"
      to: "backend/app/main.py"
      via: "create_app().openapi()"
      pattern: "create_app.*openapi"
---
<objective>建立完整公开合同 sentinel 与不可手写的跨栈生成链。Output: 501 operations、四类 artifacts、真实 HTTP sentinel。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@backend/app/auth/api.py @backend/app/main.py @backend/app/agent/service.py @frontend/src/auth/api.ts @frontend/tests/e2e/auth-skeleton.spec.ts @frontend/src/features/agent/api/README.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 预声明完整 Agent v1 公共 operations</name>
  <read_first>
    - backend/app/auth/api.py
    - backend/app/auth/schemas.py
    - backend/app/main.py
    - backend/app/agent/service.py
    - .planning/phases/02-agent/02-CONTEXT.md
  </read_first>
  <files>backend/app/agent/schemas.py, backend/app/agent/api.py, backend/app/main.py, backend/app/agent/README.md, backend/README.md</files>
  <action>定义 extra=forbid 且 operationId 稳定的六个 operation。所有 operation 先复用权威 Bearer 认证并统一返回 501；生产实现必须验证真实 access token。schema 从一开始覆盖 waiting/partial/completed/retryable/terminal/deletion_pending；main lifespan 委托 runtime factory，当前 no-op。同步 agent README 与 backend README 公共 API 索引；frontend api README 已由 02-04 创建且由 Task 2 的生成文件索引更新。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -c "import uuid; from fastapi.testclient import TestClient; from app.main import create_app; from app.agent.api import get_agent_principal; a=create_app(); a.dependency_overrides[get_agent_principal]=lambda: uuid.UUID(int=1); c=TestClient(a); t='00000000-0000-0000-0000-000000000001'; calls=[('post','/api/v1/agent/threads',{'json':{'input_text':'米饭 100 克'}}),('post',f'/api/v1/agent/threads/{t}/input',{'json':{'kind':'description','text':'米饭 100 克'}}),('get',f'/api/v1/agent/threads/{t}',{}),('get',f'/api/v1/agent/threads/{t}/events',{}),('post',f'/api/v1/agent/threads/{t}/retry',{}),('delete',f'/api/v1/agent/threads/{t}',{})]; rs=[c.request(m,u,**k) for m,u,k in calls]; assert all(r.status_code==501 and r.json()['error']['code']=='AGENT_NOT_IMPLEMENTED' for r in rs); o=a.openapi(); required={'createAgentThread','submitAgentInput','getAgentThread','streamAgentEvents','retryAgentRun','deleteAgentThread'}; assert required &lt;= {v.get('operationId') for p in o['paths'].values() for v in p.values() if isinstance(v,dict)}"</automated></verify>
  <acceptance_criteria>
    - 6 个 operationId 完整，create/input/snapshot/events 覆盖首绿。
    - 未认证 401，已认证统一 501；retry/delete 也不伪造实现。
    - API Schema 与 State/Provider DTO/ORM 分离。
  </acceptance_criteria>
  <done>公开合同在实现前完整冻结，后续无需补字段漂移。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 建立 runtime OpenAPI 四类生成与 501 sentinel 合同</name>
  <read_first>
    - frontend/src/features/agent/api/README.md
    - frontend/src/auth/api.ts
    - frontend/src/features/agent/api/README.md
    - backend/app/main.py
  </read_first>
  <files>backend/openapi-agent-v1.json, frontend/src/features/agent/api/generate-contracts.mjs, frontend/src/features/agent/api/schemas.generated.ts, frontend/src/features/agent/api/client.generated.ts, frontend/src/features/agent/api/README.md</files>
  <action>实现 generate-all/check-all：子进程从实际 create_app().openapi() 取 Agent operations，稳定排序生成 frozen JSON、同一 schemas.generated.ts 内的 TS declarations+Zod runtime schemas，以及 operation client。check-all 必须在临时目录全量重生并 byte-for-byte 比较 runtime OpenAPI、frozen JSON、TS/Zod 和 client，禁止从 frozen JSON 反推 runtime 或手写字段；同步 api README 文件索引。用 TestClient 证明未认证 401 和已认证依赖后的统一 501/code。</action>
  <verify><automated>cd frontend &amp;&amp; node src/features/agent/api/generate-contracts.mjs generate-all &amp;&amp; node src/features/agent/api/generate-contracts.mjs check-all &amp;&amp; npm run typecheck &amp;&amp; cd ../backend &amp;&amp; .venv/bin/python -c "from fastapi.testclient import TestClient; from app.main import create_app; r=TestClient(create_app()).post('/api/v1/agent/threads',json={'input_text':'米饭 100 克'}); assert r.status_code==401"</automated></verify>
  <acceptance_criteria>
    - check-all 同时比较 runtime/frozen/TS/Zod/client，任一 mutation 非零。
    - operation client 只能用 Auth request，不使用 query token/localStorage。
    - 501 sentinel 由正常通过的 HTTP contract 断言，不靠 shell 反转。
  </acceptance_criteria>
  <done>跨栈合同完整、机械且 sentinel 无假绿。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| 手写客户端掩盖 drift | Tampering | High | API contract | FastAPI→browser | Task 2 | temp byte compare |
| 未认证线程入口 | Spoofing | Critical | thread | browser→API | Task 1 | 401/501 contract |
</threat_model>
<verification>6 operations、generate/check/typecheck 和 501 HTTP sentinel 通过。</verification>
<success_criteria>实际 Wave 7 只填实现，不再修改公共 schema。</success_criteria>
<output>Create 02-09-SUMMARY.md when done.</output>
