---
phase: 02-agent
plan: 14
type: execute
wave: 11
depends_on: ["02-13"]
files_modified: [backend/app/agent/retention.py, backend/app/agent/service.py, backend/app/agent/repository.py, backend/app/agent/supervisor.py, backend/app/agent/README.md, backend/app/core/config.py, backend/.env.example, backend/tests/integration/test_agent_retention.py, backend/tests/integration/README.md, backend/tests/unit/test_runtime_foundation.py]
autonomous: true
requirements: [AGT-04, AGT-07, QLT-02]
must_haves:
  truths: ["应用生命周期自动执行 7d checkpoint/events、30d minimal audit、24h deletion intent 清理。", "双 worker 单 claim、崩溃 lease 恢复、跨租户不误删。"]
  artifacts:
    - path: "backend/app/agent/retention.py"
      provides: "automatic PG-leased retention worker"
  key_links:
    - from: "backend/app/agent/supervisor.py"
      to: "backend/app/agent/retention.py"
      via: "lifespan registration"
      pattern: "RetentionWorker"
---
<objective>实现 D-18 自动保留和级联清理，不依赖手工 CLI。Output: lease worker、fail-closed 配置、真实 PG/fake-clock 证据。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-agent/02-CONTEXT.md @backend/app/agent/supervisor.py @backend/app/agent/service.py @backend/app/core/config.py @backend/tests/run_pg.py</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 锁定 retention 配置与生命周期合同</name>
  <read_first>
    - backend/app/core/config.py
    - backend/.env.example
    - backend/tests/unit/test_runtime_foundation.py
  </read_first>
  <files>backend/app/core/config.py, backend/.env.example, backend/tests/unit/test_runtime_foundation.py</files>
  <action>按 D-18 先写配置/lifecycle tests，再定义可注入 clock/batch/lease/scheduler 的 retention settings。production 必须显式配置 7d checkpoint/events、30d minimal audit、24h deletion SLA，以及最大 poll_interval（上限 5 分钟）；删除 intent 的 persisted due_at 必须不晚于 requested_at+24h-poll_interval。7d/30d 不允许提前删除：scheduler 以 `min(now+poll_interval, earliest_eligible_at)` 提前唤醒，精确到 eligibility boundary；缺失、无限、非正、poll_interval 超限或静默默认启动失败，test 环境仍需显式安全值。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m pytest tests/unit/test_runtime_foundation.py -q -k "retention_config or retention_lifecycle"</automated></verify>
  <acceptance_criteria>
    - production 7/30/24 任一未显式设置 fail closed。
    - fake clock/batch/lease 可直接注入，不需要 sleep。
    - 日志只含稳定计数/状态，无用户正文。
  </acceptance_criteria>
  <done>D-18 配置边界可执行且无静默无限保留。</done>
</task>
<task type="auto">
  <name>Task 2: 建立自动 lease retention worker</name>
  <read_first>
    - backend/app/core/config.py
    - backend/app/agent/supervisor.py
    - backend/app/agent/service.py
    - backend/app/agent/repository.py
    - backend/app/agent/models.py
    - backend/app/agent/README.md
  </read_first>
  <files>backend/app/agent/retention.py, backend/app/agent/service.py, backend/app/agent/repository.py, backend/app/agent/supervisor.py, backend/app/agent/README.md</files>
  <action>实现 RetentionWorker 并由实际 supervisor/lifespan 自动启动持续 scheduler，而不是手工 cycle。Repository 只 claim/query/delete/flush，Service 决定 tenant 范围、eligible 和事务；显式删除 saver namespace。worker 每次提交后按最早 7d/30d eligible_at、deletion due_at 或 now+poll_interval 计算下一唤醒；新 deletion intent 通过持久化通知/本进程 wake event 提前唤醒。删除 intent 在 persisted due_at 前被 claim，最迟 requested_at+24h 完成级联；7d/30d 在边界前不删并在 boundary wake 完成。README 同任务索引 worker与 SLA 算法。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m mypy app/agent/retention.py app/agent/service.py app/agent/repository.py app/agent/supervisor.py</automated></verify>
  <acceptance_criteria>
    - worker 位于实际 lifespan，不依赖人工 CLI 或仅内存保证。
    - claim/delete 经过 Service 事务且 saver namespace 显式清理。
    - 所有查询和删除都带 tenant/thread 范围。
  </acceptance_criteria>
  <done>D-18 清理由应用生命周期自动调度。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: 真实 PostgreSQL 证明 24h/7d/30d 与 lease</name>
  <read_first>
    - backend/app/agent/retention.py
    - backend/app/agent/service.py
    - backend/tests/run_pg.py
    - backend/tests/integration/README.md
    - backend/tests/integration/test_agent_checkpoint.py
  </read_first>
  <files>backend/tests/integration/test_agent_retention.py, backend/tests/integration/README.md</files>
  <action>真实测试库创建两 tenant、ledger/event 和真实 checkpointer rows；启动实际 lifespan automatic scheduler，使用 fake clock/可控 wake 驱动完整 lifecycle，不直接调用 cleanup/cycle API、不 sleep。参数化 24h-poll_interval due_at、requested_at+24h、7d/30d 边界前/精确边界，断言删除在 24h 前完成、7d/30d 不早删且边界唤醒；覆盖新 intent 提前唤醒、双 worker、crash lease expiry、重复调度幂等、跨租户完整保留。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python tests/run_pg.py --env-file .env.test.example -- .venv/bin/python -m pytest tests/integration/test_agent_retention.py tests/integration/test_agent_checkpoint.py -q</automated></verify>
  <acceptance_criteria>
    - automatic lifecycle 在 requested_at+24h 之前完成；7d/30d 边界前不早删并由最早期限唤醒。
    - 双 worker 无双删，crash 后最终完成。
    - 其他 user/thread 和 development DB 完整不变。
  </acceptance_criteria>
  <done>D-18 SLA 有真实数据库、并发和时钟证据。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| 无限保留敏感状态 | Information Disclosure | High | checkpoint/events | storage lifecycle | Task 1/3 | exact boundary tests |
| 跨租户误删 | Elevation of Privilege | Critical | tenant data | worker→repository | Task 2/3 | cross-tenant fixture |
</threat_model>
<verification>config/lifecycle 与真实 PG retention 套件通过。</verification>
<success_criteria>D-18 不需要人工触发且期限精确、隔离、可恢复。</success_criteria>
<output>Create 02-14-SUMMARY.md when done.</output>
