---
phase: 02-agent
plan: 05
type: execute
wave: 4
depends_on: ["02-02", "02-06", "02-07"]
files_modified: [backend/migrations/versions/0004_agent_core.py, backend/migrations/versions/README.md, backend/tests/integration/test_auth_migration.py, backend/tests/integration/test_agent_migration.py, backend/tests/integration/README.md, backend/evals/README.md, backend/evals/phase02-cases.jsonl, backend/evals/validate_dataset.py, backend/README.md]
autonomous: true
requirements: [AGT-04, AGT-06, AGT-07, NUT-01, ARC-05, QLT-02]
must_haves:
  truths: ["0004 只创建业务 schema，不包含营养 seed。", "首 5 happy cases 完整覆盖 AI-SPEC 主路径且 hash 不可静默修改。"]
  artifacts:
    - path: "backend/migrations/versions/0004_agent_core.py"
      provides: "Agent ledger 与 nutrition schema"
    - path: "backend/evals/phase02-cases.jsonl"
      provides: "5-case append-only happy prefix"
  key_links:
    - from: "backend/tests/integration/test_agent_migration.py"
      to: "backend/tests/run_pg.py"
      via: "guarded Alembic/pytest"
      pattern: "run_pg"
---
<objective>交付纯 schema 迁移与首批正确语义的冻结案例。Output: 0004、真实 PG 迁移证据、5-case hash chain。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@backend/migrations/versions/0003_admin_audit.py @backend/tests/integration/test_auth_migration.py @backend/tests/run_pg.py @.planning/phases/02-agent/02-AI-SPEC.md @.planning/phases/02-agent/02-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 创建可逆且无 seed 的 0004 migration</name>
  <read_first>
    - backend/migrations/versions/0003_admin_audit.py
    - backend/tests/integration/test_auth_migration.py
    - backend/tests/integration/test_auth_database_protocols.py
    - backend/tests/run_pg.py
    - backend/app/nutrition/models.py
    - backend/app/agent/models.py
    - .planning/phases/02-agent/02-PATTERNS.md
  </read_first>
  <files>backend/migrations/versions/0004_agent_core.py, backend/migrations/versions/README.md, backend/tests/integration/test_auth_migration.py, backend/tests/integration/test_agent_migration.py, backend/tests/integration/README.md</files>
  <action>以 0003_admin_audit 为 down_revision，从 02-06 Nutrition ORM 与 02-07 Agent ORM 的单一 SQLAlchemy metadata 真相生成/核对单一 0004，只建 metadata 声明的 thread/run/invocation/event/deletion-intent 与 catalog/version/source/food/alias/portion 表，显式 FK/check/unique/index、tenant/idempotency/seq/lease 约束并逆序 downgrade。不得 INSERT 业务 seed。真实 PG 测试经 02-02 wrapper 运行；child 始终保留互异的 DATABASE_URL 开发哨兵和 TEST_DATABASE_URL，迁移测试沿用 conftest/config 的 `validate_test_database_configuration()` 返回值显式选择 TEST_DATABASE_URL，禁止把 DATABASE_URL 重绑到测试库。覆盖空库到 head、0003↔0004 往返、旧 auth/admin 不变、catalog 0 行，并用 metadata-vs-live-schema 逐表/列/约束/索引断言防止 migration 与 ORM 漂移。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python tests/run_pg.py --env-file .env.test.example -- .venv/bin/python -m pytest tests/integration/test_auth_migration.py tests/integration/test_agent_migration.py -q</automated></verify>
  <acceptance_criteria>
    - 0004 为唯一 head，全部约束和索引名称精确断言，live schema 与两组 ORM metadata 无漂移。
    - migration source/SQL 无 seed INSERT，初始 catalog 0 行。
    - downgrade/upgrade 可重复且开发库逐字不变。
  </acceptance_criteria>
  <done>权威业务 schema 可从空库安全重建且与 seed 生命周期分离。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 建立语义严格的 5-case happy prefix</name>
  <read_first>
    - .planning/phases/02-agent/02-AI-SPEC.md
    - .planning/phases/02-agent/02-RESEARCH.md
    - backend/README.md
  </read_first>
  <files>backend/evals/README.md, backend/evals/phase02-cases.jsonl, backend/evals/validate_dataset.py, backend/README.md</files>
  <action>创建 append-only JSONL/hash-chain validator。首 5 例必须全部是完整主路径：明确克数直算；与 FDC 精确匹配的家庭份量；唯一受控别名；多菜；饮料+明确配料。目录外只允许进入 missing_ambiguity，异常值和工具校验失败只允许 validation_budget。validator 除 count/hash 外强制 category、required semantic tags、expected state/trace/report/events/forbidden assertions，并禁止 PII/CoT/secret。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python evals/validate_dataset.py --dataset evals/phase02-cases.jsonl --expected-count 5 --expected-composition happy=5 --require-happy-tags direct_grams,exact_household_portion,unique_alias,multi_item,beverage_and_condiment</automated></verify>
  <acceptance_criteria>
    - 5 个 happy case 各自具备一个必需语义标签且能直接 completed。
    - out-of-catalog/negative/tool-failure 被 validator 拒绝归入 happy。
    - case/parent hashes 可复算，旧 prefix 变更非零。
  </acceptance_criteria>
  <done>冻结集从语义正确而非凑数的 5 个 happy cases 开始。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| migration 误连开发库 | Tampering | Critical | PostgreSQL | Alembic→DB | Task 1 | guarded round-trip/dev unchanged |
| 错误分类掩盖主路径缺口 | Spoofing | High | eval evidence | author→dataset | Task 2 | required semantic tags |
</threat_model>
<verification>迁移套件与 5-case 语义/hash validator 通过。</verification>
<success_criteria>schema 和首批评测证据均可独立复现。</success_criteria>
<output>Create 02-05-SUMMARY.md when done.</output>
