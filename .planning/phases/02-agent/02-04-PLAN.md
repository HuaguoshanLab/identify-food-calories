---
phase: 02-agent
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [frontend/package.json, frontend/package-lock.json, frontend/src/features/README.md, frontend/src/features/agent/README.md, frontend/src/features/agent/api/README.md, frontend/src/features/agent/stream/README.md, frontend/src/features/agent/components/README.md, frontend/src/README.md, frontend/src/features/agent/stream/useAgentEventStream.ts, frontend/src/features/agent/components/AnalyzePage.tsx, frontend/src/features/agent/components/AnalyzePage.test.tsx, frontend/src/App.tsx]
autonomous: true
requirements: [ARC-05, QLT-02]
must_haves:
  truths: ["分析页位于既有受保护 AppShell，未接通前不伪造营养值。", "feature/api/stream/components 从创建时有 README 和父索引。"]
  artifacts:
    - path: "frontend/src/features/agent/components/AnalyzePage.tsx"
      provides: "可访问文字输入与诚实状态壳"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/features/agent/components/AnalyzePage.tsx"
      via: "protected route"
      pattern: "AnalyzePage"
---
<objective>建立用户可见但诚实的 Agent H5 外壳和 feature-first 目录。Output: 页面、路由、测试、同提交目录文档。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@frontend/AGENTS.md @frontend/src/App.tsx @frontend/src/App.test.tsx @frontend/src/README.md @frontend/src/auth/AuthProvider.tsx @docs/ui/h5-foundation.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 创建可访问的诚实分析页</name>
  <read_first>
    - frontend/src/App.tsx
    - frontend/src/App.test.tsx
    - frontend/src/auth/AuthProvider.tsx
    - frontend/src/app/PlaceholderTabPage.tsx
    - docs/ui/h5-foundation.md
  </read_first>
  <files>frontend/src/features/agent/components/AnalyzePage.tsx, frontend/src/features/agent/components/AnalyzePage.test.tsx, frontend/src/features/agent/components/README.md, frontend/src/App.tsx, frontend/src/README.md</files>
  <action>在现有 RequireAuthentication/AppShell 内以 AnalyzePage 替换占位页，并把 /app index replace 到 /app/analyze。提供有 label 的文字输入、44px 主按钮、focus、aria-live、idle/submitting/unavailable/error 状态；在 controller 未接通时诚实显示能力接线中，不硬编码 kcal 或未来图片/保存/规划能力。遵循 D-04 的完整输入直算语义，不加入固定确认步骤；同任务创建 components README 并同步 src 父索引。</action>
  <verify><automated>cd frontend &amp;&amp; npm test -- --run src/features/agent/components/AnalyzePage.test.tsx src/App.test.tsx &amp;&amp; npm run build</automated></verify>
  <acceptance_criteria>
    - 页面复用唯一滚动区和语义 token。
    - 空/超长/提交中/未接通状态有 Testing Library 断言。
    - 源码无营养常量、token 存储和未交付能力入口。
  </acceptance_criteria>
  <done>登录用户有真实输入入口但不会被假结果欺骗。</done>
</task>
<task type="auto">
  <name>Task 2: 同提交建立 feature 目录合同</name>
  <read_first>
    - frontend/AGENTS.md
    - frontend/src/README.md
    - frontend/src/app/README.md
  </read_first>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/features/README.md, frontend/src/features/agent/README.md, frontend/src/features/agent/api/README.md, frontend/src/features/agent/stream/README.md, frontend/src/features/agent/components/README.md, frontend/src/README.md, frontend/src/features/agent/stream/useAgentEventStream.ts</files>
  <action>严格限制在本计划 frontmatter 已列出的 12 个文件内，不得顺手新增、移动或修改任何其他文件；这 12 个文件是创建 `features/agent/{api,stream,components}` 时“新目录 README + 每一级直接父 README 索引 + 精确 npm lock + 可编译 transport skeleton”的不可拆原子合同，不授权任何 API 实现、SSE 业务接线、样式扩展或其他 feature 修改。按 `components→agent→features→src` 和 `stream/api→agent→features→src` 逐级建立职责、允许依赖、实际文件索引并同步每一级直接父 README，不能跳级只改 src。仅在 02-01 证据批准后安装并锁定 eventsource-parser==3.1.0；stream 合同明确 authenticated fetch 的 chunk 必须交给该 parser 处理分片 event/data/id 边界，禁止自制 split-on-newline parser、EventSource 和报告拼装。api 只接受 runtime OpenAPI 生成文件，components 不保存 Graph State、不做营养计算。同计划创建 disabled/abort-only transport skeleton，后续只填实现；执行时不可扩展文件范围或实现后续计划能力。</action>
  <verify><automated>cd frontend &amp;&amp; npm ls eventsource-parser@3.1.0 --depth=0 &amp;&amp; node -e "import('eventsource-parser').then(m=&gt;{if(typeof m.createParser!=='function')process.exit(1)})" &amp;&amp; cd ../backend &amp;&amp; .venv/bin/python -m pytest tests/architecture/test_directory_contract.py -q</automated></verify>
  <acceptance_criteria>
    - 每个新目录与 README/父索引同计划出现。
    - generated-only、authenticated fetch、eventsource-parser fragmented parsing、snapshot authority 写成明确合同。
    - package.json/package-lock.json 精确锁定 eventsource-parser 3.1.0 且可本地 import。
    - src/app 仍只做页面组合。
  </acceptance_criteria>
  <done>前端 Agent 边界可机器审查。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| UI 伪造未交付营养值 | Spoofing | High | 用户信任 | controller→view | Task 1 | no-kcal unavailable tests |
| token 经 feature 持久化 | Information Disclosure | High | access token | browser→API | Task 2 | directory/source contract |
</threat_model>
<verification>组件、路由、build 和目录合同通过。</verification>
<success_criteria>首个纵切可在不重建 UI/目录的情况下接通。</success_criteria>
<output>Create 02-04-SUMMARY.md when done.</output>
