---
phase: 02-agent
plan: 13
type: execute
wave: 10
depends_on: ["02-12"]
files_modified: [backend/pyproject.toml, backend/requirements.lock, backend/lock_dependencies.py, backend/.env.example, backend/app/core/config.py, backend/app/core/tracing.py, backend/app/core/README.md, backend/app/providers/reasoning/README.md, backend/app/agent/supervisor.py, backend/app/providers/reasoning/deepseek.py, backend/tests/unit/test_runtime_foundation.py]
autonomous: true
requirements: [AGT-05, AGT-06, AGT-07, ARC-06, QLT-02]
user_setup:
  - service: DeepSeek
    why: "production reasoning provider and paid eval"
    env_vars:
      - name: DEEPSEEK_API_KEY
        source: "DeepSeek console; uncommitted server environment"
must_haves:
  truths: ["DeepSeek 与 Fake 实现同一 Port；unknown outcome 永不自动重发。", "真实 Agent/Provider/tool 调用产生安全 Phoenix spans。"]
  artifacts:
    - path: "backend/app/providers/reasoning/deepseek.py"
      provides: "DeepSeek Responses adapter"
    - path: "backend/app/core/tracing.py"
      provides: "allowlist OTel/Phoenix lifecycle"
  key_links:
    - from: "backend/app/agent/supervisor.py"
      to: "backend/app/core/tracing.py"
      via: "real run spans"
      pattern: "trace"
---
<objective>接入真实 DeepSeek 与真实调用点 Phoenix 观测，同时保持费用和隐私边界。Output: adapter、fail-closed config、三类 spans/tests。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-agent/02-01-SUMMARY.md @.planning/phases/02-agent/02-RESEARCH.md @backend/app/providers/reasoning/ports.py @backend/app/main.py @backend/app/core/config.py</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 锁定 runtime 并实现 DeepSeek adapter</name>
  <read_first>
    - .planning/phases/02-agent/02-AI-SPEC.md
    - .planning/phases/02-agent/02-01-SUMMARY.md
    - backend/pyproject.toml
    - backend/requirements.lock
    - backend/lock_dependencies.py
    - backend/app/providers/reasoning/dto.py
    - backend/app/providers/reasoning/ports.py
    - backend/app/providers/reasoning/factory.py
    - backend/app/core/config.py
    - backend/.env.example
  </read_first>
  <files>backend/pyproject.toml, backend/requirements.lock, backend/lock_dependencies.py, backend/.env.example, backend/app/core/config.py, backend/app/providers/reasoning/README.md, backend/app/providers/reasoning/deepseek.py, backend/tests/unit/test_runtime_foundation.py</files>
  <action>先把现有 dev extra 中的 `httpx==0.28.1` 移除，并在 `[project].dependencies` 中只声明一次，使 DeepSeek adapter 所需 HTTP 客户端成为真实 runtime 依赖；不得在 runtime/dev 两处重复。经 02-01 供应链批准加入精确 Phoenix/OpenInference direct dependencies，使用同一个 lock_dependencies.py 重生成 hash-complete requirements.lock，并执行 direct/extras/hash/pyproject drift 检查。扩展同一生成器提供 `export-runtime`：从已校验的完整锁和 `[project].dependencies` 解析仅 runtime 的传递闭包，输出到临时目录。使用该 runtime HTTPX 实现 DeepSeek adapter：deepseek-v4-flash、thinking disabled、JSON Schema、max 800、20s timeout、Pydantic 二次校验；400/422 零重试，429/5xx/network/timeout 在总预算内一次；PREPARED 后未知崩溃窗标 PROVIDER_OUTCOME_UNKNOWN，supervisor/reconnect/restart 不重发，只有用户显式 retry 新 attempt。生产缺 key/model/price snapshot/预算 fail closed；日志只含 request id/model/fingerprint/tokens/cost/latency，并同步 reasoning README。创建全新 Python 3.11 clean env，不安装 dev extras，仅以 `--require-hashes` 安装临时 runtime lock；在该环境从源码 import 并实例化 DeepSeekReasoningModelProvider，同时证明 HTTPX/Phoenix/OpenInference 可用且 pytest 不存在。之后用 `.venv/bin/python -m pip install --require-hashes -r requirements.lock` 同步实际 backend/.venv，并用该解释器重复 adapter instantiate/import smoke 和 adapter 单测。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python lock_dependencies.py check --pyproject pyproject.toml --lock requirements.lock &amp;&amp; task_tmp=$(mktemp -d) &amp;&amp; .venv/bin/python lock_dependencies.py export-runtime --pyproject pyproject.toml --lock requirements.lock --output "$task_tmp/runtime.lock" &amp;&amp; python3.11 -m venv "$task_tmp/runtime-venv" &amp;&amp; "$task_tmp/runtime-venv/bin/python" -m pip install --require-hashes -r "$task_tmp/runtime.lock" &amp;&amp; PYTHONPATH=. "$task_tmp/runtime-venv/bin/python" -c "import httpx, phoenix, openinference; from app.providers.reasoning.deepseek import DeepSeekReasoningModelProvider; DeepSeekReasoningModelProvider(api_key='test', model='deepseek-v4-flash', timeout_seconds=20)" &amp;&amp; ! "$task_tmp/runtime-venv/bin/python" -c "import pytest" &amp;&amp; .venv/bin/python -m pip install --require-hashes -r requirements.lock &amp;&amp; .venv/bin/python -c "import httpx, phoenix, openinference; from app.providers.reasoning.deepseek import DeepSeekReasoningModelProvider; assert httpx.__version__ == '0.28.1'; DeepSeekReasoningModelProvider(api_key='test', model='deepseek-v4-flash', timeout_seconds=20)" &amp;&amp; .venv/bin/python -m pytest tests/unit/test_runtime_foundation.py -q -k "deepseek or provider_unknown or provider_config"</automated></verify>
  <acceptance_criteria>
    - httpx==0.28.1 只存在于 `[project].dependencies`，不在 dev extra 重复声明。
    - 同一生成器产出的 runtime-only hash 闭包可在不安装 dev extras 的 clean Python 3.11 env 中安装，并可使用 HTTPX 和 Phoenix/OpenInference。
    - backend/.venv 从更新后的完整 hash lock 同步，且该解释器可 import 全部新增 runtime 包。
    - clean runtime 和 backend/.venv 都能实例化 DeepSeek adapter；MockTransport 证明无 live network，permanent/transient/schema/unknown 的请求次数和稳定码精确。
    - adapter 不导入 Nutrition、State、API Schema 或 ORM。
  </acceptance_criteria>
  <done>DeepSeek 与 Phoenix 的生产依赖不再偷用 dev extra，且 adapter 在 clean runtime 与实际 backend/.venv 都可实例化。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 建立 Phoenix allowlist processor 与 runtime</name>
  <read_first>
    - .planning/phases/02-agent/02-AI-SPEC.md
    - .planning/phases/02-agent/02-01-SUMMARY.md
    - backend/pyproject.toml
    - backend/app/providers/reasoning/deepseek.py
    - backend/app/core/config.py
    - backend/app/core/README.md
  </read_first>
  <files>backend/app/core/config.py, backend/app/core/tracing.py, backend/app/core/README.md, backend/tests/unit/test_runtime_foundation.py</files>
  <action>只消费 Task 1 已锁定并同步的 Phoenix/OpenInference runtime，不再修改依赖。实现 tracing enabled/collector/HMAC/service/version 的显式生产配置和 allowlist processor；production 只 export 到显式 collector，不 launch local UI。processor 只允许版本、节点/工具、稳定状态/错误、计数/token/cost/latency 和 scoped HMAC，默认拒绝餐食文本、身份、提示词、模型响应与 CoT 等未声明 attribute；同步 core README 文件索引。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -c "import phoenix, openinference" &amp;&amp; .venv/bin/python -m pytest tests/unit/test_runtime_foundation.py -q -k "tracing_config or allowlist or redaction" &amp;&amp; .venv/bin/python -m mypy app/core/tracing.py</automated></verify>
  <acceptance_criteria>
    - 精确批准包可由 backend/.venv 导入，且不启动本地 Phoenix UI。
    - allowlist 默认拒绝未声明 attribute。
    - tracing enabled 缺 collector/HMAC/service/version 时 production fail closed。
  </acceptance_criteria>
  <done>Phoenix runtime 与安全 processor 可供真实调用点消费。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: 接线 lifecycle 并证明真实三类 spans</name>
  <read_first>
    - backend/app/core/tracing.py
    - backend/app/main.py
    - backend/app/agent/supervisor.py
    - backend/app/agent/tools.py
    - backend/app/providers/reasoning/deepseek.py
  </read_first>
  <files>backend/app/agent/supervisor.py, backend/tests/unit/test_runtime_foundation.py</files>
  <action>严格限制整个 02-13 计划只修改 frontmatter 已列出的 11 个文件，不得新增、移动或顺手修改其他文件；通过 runtime lifecycle 在 supervisor 初始化、flush、shutdown；supervisor 建 run span，Provider adapter 建 provider span，tracing runtime 对 Graph 已调用的 NutritionToolAdapter 做受控包装以产生 tool span，无需修改工具业务文件。用 in-memory exporter实际跑 Fake Agent、Mock DeepSeek、三个 Nutrition tools，证明三类 spans 和 Task 2 allowlist redaction，不得伪造 self-test span。11 文件范围仅因锁文件/配置/README 与真实三类 span 必须原子接线而保留，执行时不可扩展。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m pytest tests/unit/test_runtime_foundation.py -q -k "tracing_lifespan or real_agent_provider_tool_spans or redaction"</automated></verify>
  <acceptance_criteria>
    - run/provider/tool spans 均来自实际调用路径且 parent links 正确。
    - forbidden 字段全集在 exporter 中不存在。
    - shutdown 会 flush，disabled 模式零 exporter side effect。
  </acceptance_criteria>
  <done>Phoenix 覆盖真实运行路径且默认抑制敏感数据。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| unknown outcome 自动重发 | Repudiation | Critical | Provider cost | supervisor→provider | Task 2 | request-count crash tests |
| trace 泄露餐食/身份/CoT | Information Disclosure | Critical | telemetry | runtime→collector | Task 1/3 | actual-span redaction |
</threat_model>
<verification>Provider/config 与真实 span/redaction 套件通过。</verification>
<success_criteria>真实 Provider 和可观测性均不削弱预算、隐私或确定性真值。</success_criteria>
<output>Create 02-13-SUMMARY.md when done.</output>
