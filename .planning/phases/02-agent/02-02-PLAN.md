---
phase: 02-agent
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [backend/.env.test.example, backend/tests/run_pg.py, backend/tests/unit/test_test_database_guards.py, backend/tests/README.md, backend/scripts/run_initialized_app.py, backend/scripts/README.md, backend/README.md, frontend/playwright.config.ts, frontend/playwright.config.test.ts, frontend/README.md]
autonomous: true
requirements: [AGT-04, QLT-02]
must_haves:
  truths: ["所有真实 PG 动作在子进程前 fail closed。", "启动固定为 migration→checkpointer setup→seed apply→uvicorn。"]
  artifacts:
    - path: "backend/tests/run_pg.py"
      provides: "安全且互异的测试库 wrapper"
    - path: "backend/scripts/run_initialized_app.py"
      provides: "唯一有序初始化入口"
  key_links:
    - from: "frontend/playwright.config.ts"
      to: "backend/scripts/run_initialized_app.py"
      via: "webServer command"
      pattern: "run_initialized_app"
---
<objective>建立真实 PostgreSQL 与启动链的可执行安全边界。Output: guard、启动器、Playwright 接线。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@backend/app/core/config.py @backend/tests/conftest.py @backend/tests/unit/test_test_database_guards.py @frontend/playwright.config.ts @backend/README.md @frontend/README.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 建立唯一 fail-closed PG wrapper</name>
  <read_first>
    - backend/app/core/config.py
    - backend/tests/conftest.py
    - backend/tests/unit/test_test_database_guards.py
    - backend/tests/README.md
  </read_first>
  <files>backend/.env.test.example, backend/tests/run_pg.py, backend/tests/unit/test_test_database_guards.py, backend/tests/README.md</files>
  <action>创建受版本控制的 backend/.env.test.example，严格匹配 docker-compose.yml：APP_ENV=test、DATABASE_URL=postgresql+psycopg://postgres:postgres@127.0.0.1:5432/food_agent_dev（仅作互异 loopback 开发哨兵）、TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@127.0.0.1:55432/food_agent_test。创建唯一入口 `python tests/run_pg.py --env-file .env.test.example -- COMMAND`，只从该显式文件装载变量。启动 child 前拒绝文件/变量缺失、两 URL 同目标、SQLite、非 psycopg PostgreSQL、TEST 端口/库名不等于 compose 合同或非 loopback，错误不回显密码。child 必须原样保留三个正式变量：APP_ENV=test、DATABASE_URL 指向 5432/food_agent_dev、TEST_DATABASE_URL 指向 55432/food_agent_test；不得改写任一 URL，也不得添加替代性的数据库 URL 环境合同。真实 child 测试必须在 wrapper 启动的 Python 子进程内从环境实例化 `Settings(_env_file=None)`，调用 `validate_test_database_configuration()`，断言校验通过且返回/选择 127.0.0.1:55432/food_agent_test；同一子进程还要断言两个正式 URL 仍互异、DATABASE_URL 仍为 5432/food_agent_dev、APP_ENV 仍为 test。后续 PG 命令一律显式走 wrapper，并从该受保护函数的返回值取得真实测试目标。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m pytest tests/unit/test_test_database_guards.py -q</automated></verify>
  <acceptance_criteria>
    - 所有危险矩阵都证明 child 未执行。
    - 合法真实 child 同时保留互异的正式 DATABASE_URL 与 TEST_DATABASE_URL，受保护解析结果唯一指向 55432/food_agent_test。
    - URL 同目标、编码与脱敏有自动测试。
  </acceptance_criteria>
  <done>真实数据库命令默认拒绝且只可进入隔离测试库。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 固定初始化顺序并接入 Playwright</name>
  <read_first>
    - frontend/playwright.config.ts
    - docker-compose.yml
    - frontend/README.md
    - backend/README.md
    - backend/tests/run_pg.py
    - backend/tests/unit/test_test_database_guards.py
  </read_first>
  <files>backend/scripts/run_initialized_app.py, backend/scripts/README.md, backend/README.md, backend/tests/unit/test_test_database_guards.py, frontend/playwright.config.ts, frontend/playwright.config.test.ts, frontend/README.md</files>
  <action>在 test_test_database_guards.py 新增 startup_order/failure 用例；启动器在 APP_ENV=test 下必须先实例化现有 `Settings(_env_file=None)` 并调用现有 `validate_test_database_configuration()`，把其返回的规范化 55432/food_agent_test URL 显式传给安全 schema reset、Alembic test-target 接口、setup_checkpointer 和 seed importer，绝不让这些步骤读取 DATABASE_URL 作为测试目标，也不修改 config.py/conftest.py 的安全不变量。顺序严格为安全 schema reset→alembic upgrade head→setup_checkpointer→seed apply→uvicorn，任一步失败停止，并提供 --prepare-only；uvicorn 的 test initializer/runtime factory 同样消费上述受保护解析结果。Playwright 配置以 frontend 为 cwd，webServer command 先从仓库根执行 `docker compose up -d --wait postgres-test mailpit`，再切 backend 通过 run_pg wrapper 执行初始化器；backend webServer cwd 明确为 `../backend`，前端 cwd 保持 frontend，reuseExistingServer=false。playwright.config.test.ts 必须实际运行 provisioning child contract，断言 child 内 APP_ENV=test、DATABASE_URL=5432/food_agent_dev、TEST_DATABASE_URL=55432/food_agent_test 且二者互异，Settings 校验返回 55432 测试库；同时断言 compose 服务、健康等待、cwd、安全 reset 顺序和失败时 uvicorn 不启动。不得只做 typecheck，不得读取 shell/.env，不得重绑 DATABASE_URL。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m pytest tests/unit/test_test_database_guards.py -q -k "child_env or startup_order or migration_failure or checkpointer_failure or seed_failure or uvicorn_not_started" &amp;&amp; cd ../frontend &amp;&amp; npm test -- --run playwright.config.test.ts</automated></verify>
  <acceptance_criteria>
    - mock trace 精确为 guarded TEST_DATABASE_URL resolution→migration→checkpoint→seed→uvicorn。
    - 任一步失败不执行后续。
    - Playwright 不裸跑 Alembic/uvicorn、不复用未知进程。
  </acceptance_criteria>
  <done>开发、测试和 Playwright 共用同一可审计启动链。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| 测试误删开发库 | Tampering | Critical | PostgreSQL | runner→DB | Task 1 | dangerous matrix |
| 跳过初始化造成假绿 | Repudiation | High | schema/seed/checkpoint | launcher→app | Task 2 | exact child trace |
</threat_model>
<verification>guard、启动顺序和 Playwright 配置门通过。</verification>
<success_criteria>后续真实 PG 计划无需自造环境变量或启动协议。</success_criteria>
<output>Create 02-02-SUMMARY.md when done.</output>
