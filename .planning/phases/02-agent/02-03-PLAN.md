---
phase: 02-agent
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/providers/reasoning/dto.py, backend/app/providers/reasoning/ports.py, backend/app/providers/reasoning/fake.py, backend/app/providers/reasoning/factory.py, backend/app/providers/reasoning/README.md, backend/app/providers/README.md, backend/app/README.md, backend/app/core/config.py]
autonomous: true
requirements: [AGT-02, AGT-06, AGT-07, ARC-06]
must_haves:
  truths: ["首绿前存在独立 Provider DTO/Port/Fake/factory。", "Provider DTO、Graph State、API Schema、ORM 不复用。"]
  artifacts:
    - path: "backend/app/providers/reasoning/fake.py"
      provides: "可编程调用、费用和错误轨迹"
  key_links:
    - from: "backend/app/providers/reasoning/factory.py"
      to: "backend/app/core/config.py"
      via: "validated provider mode"
      pattern: "provider_mode"
---
<objective>建立 Provider 物理边界与后端 Phase 2 目录合同。Output: DTO/Port/Fake/factory 和同提交 README。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@backend/app/notifications/ports.py @backend/app/notifications/smtp.py @backend/app/core/config.py @backend/app/README.md @.planning/phases/02-agent/02-AI-SPEC.md @.planning/phases/02-agent/02-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 定义独立 Provider 合同与 Fake</name>
  <read_first>
    - backend/app/notifications/ports.py
    - backend/app/notifications/smtp.py
    - backend/app/core/config.py
    - .planning/phases/02-agent/02-AI-SPEC.md
  </read_first>
  <files>backend/app/providers/reasoning/dto.py, backend/app/providers/reasoning/ports.py, backend/app/providers/reasoning/fake.py, backend/app/providers/reasoning/factory.py, backend/app/core/config.py</files>
  <action>定义 async parse_meal/apply_correction Protocol、extra=forbid DTO、usage/error 元数据和可编程 Fake；DTO 禁止 kcal/P/F/C、API Schema、State、ORM。APP_ENV=test 只返回 Fake；production 只接受显式 DeepSeek 且 adapter 未装配时 fail closed。记录安全 call_id/token/cost/latency，不存原始正文或思维链。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m mypy app/providers app/core/config.py &amp;&amp; .venv/bin/python -c "from app.providers.reasoning.factory import create_reasoning_provider; assert create_reasoning_provider(app_env='test').__class__.__name__ == 'FakeReasoningModelProvider'"</automated></verify>
  <acceptance_criteria>
    - Fake 可脚本化结果、瞬时/永久/未知错误和费用。
    - 四类模型物理分离且 test mode 零网络。
    - production 缺显式配置立即失败。
  </acceptance_criteria>
  <done>Graph 可依赖稳定 Provider Port 而测试零付费。</done>
</task>
<task type="auto">
  <name>Task 2: 同提交建立目录 README 与父索引</name>
  <read_first>
    - AGENTS.md
    - backend/AGENTS.md
    - backend/app/README.md
  </read_first>
  <files>backend/app/providers/reasoning/README.md, backend/app/providers/README.md, backend/app/README.md</files>
  <action>只为本计划实际创建的 providers/reasoning 写职责、允许依赖和实际文件索引，并同步直接父 providers README 与 app README；agent、nutrition 目录由各自首次创建计划负责，避免 Wave 1 共享写与空目录占位。锁定 Provider DTO/State/API/ORM 分离。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m pytest tests/architecture/test_directory_contract.py -q</automated></verify>
  <acceptance_criteria>
    - 每个新目录创建时即有 README。
    - app README 只索引实际目录。
    - 文档不冒充功能已实现。
  </acceptance_criteria>
  <done>模块目录从创建时满足项目合同。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| 测试误触付费模型 | Denial of Service | High | 模型预算 | tests→provider | Task 1 | no-network factory test |
| production 回退 Fake | Tampering | Critical | 用户报告 | config→provider | Task 1 | fail-closed matrix |
</threat_model>
<verification>mypy、Fake 工厂和目录合同通过。</verification>
<success_criteria>Provider 与目录边界可供首个纵切消费。</success_criteria>
<output>Create 02-03-SUMMARY.md when done.</output>
