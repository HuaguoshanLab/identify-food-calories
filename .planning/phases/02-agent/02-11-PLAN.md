---
phase: 02-agent
plan: 11
type: execute
wave: 8
depends_on: ["02-10"]
files_modified: [backend/app/agent/state.py, backend/app/agent/graph.py, backend/app/agent/tools.py, backend/app/agent/service.py, backend/app/agent/api.py, backend/tests/integration/test_agent_vertical.py, backend/tests/unit/test_runtime_foundation.py, frontend/src/features/agent/components/AnalyzePage.tsx, frontend/src/features/agent/components/AnalyzePage.test.tsx, backend/evals/phase02-cases.jsonl]
autonomous: true
requirements: [AGT-02, AGT-03, AGT-04, AGT-06, NUT-02, NUT-03, NUT-04, NUT-05, QLT-02]
must_haves:
  truths: ["缺失/歧义一次集中 interrupt，同 thread resume 不重复已完成调用。", "partial 与定向修正只重算 dirty items。", "冻结集累计 14 例且旧 5 hash 不变。"]
  artifacts:
    - path: "backend/evals/phase02-cases.jsonl"
      provides: "5 happy + 5 missing/ambiguity + 4 correction"
  key_links:
    - from: "backend/app/agent/api.py"
      to: "backend/app/agent/graph.py"
      via: "same thread_id + Command(resume)"
      pattern: "Command"
---
<objective>纵向交付集中追问、消歧、partial 和定向修正。Output: Graph/API/UI/真实 PG 测试与 14-case chain。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-agent/02-10-SUMMARY.md @backend/evals/phase02-cases.jsonl @backend/evals/validate_dataset.py @frontend/src/features/agent/components/AnalyzePage.tsx</context>
<tasks>
<task type="auto">
  <name>Task 1: 实现可重入集中追问与局部重算</name>
  <read_first>
    - backend/app/agent/state.py
    - backend/app/agent/graph.py
    - backend/app/agent/tools.py
    - backend/app/agent/service.py
    - backend/app/agent/api.py
    - backend/tests/integration/test_agent_vertical.py
    - .planning/phases/02-agent/02-CONTEXT.md
  </read_first>
  <files>backend/app/agent/state.py, backend/app/agent/graph.py, backend/app/agent/tools.py, backend/app/agent/service.py, backend/app/agent/api.py</files>
  <action>落实 D-01、D-02、D-03、D-05、D-06、D-09、D-12、D-13：副作用节点与 interrupt 节点分开，interrupt 只组装已理解项目、全部阻塞问题和最多 3 个 qualified candidates；同 thread Command(resume=validated payload)，invalid answer 保持 waiting。稳定 item_id+input_version+dirty_item_ids 支持改量/改名/选候选/排除，只重跑受影响 search/calc/validate；unknown 不补 0，known 可生成 is_partial=true 且列出未计入项。完成线程允许修正，新餐必须新 thread。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m mypy app/agent/state.py app/agent/graph.py app/agent/tools.py app/agent/service.py app/agent/api.py</automated></verify>
  <acceptance_criteria>
    - 多项缺失只有一个 interrupt，候选不超过 3 且不自动选 top-1。
    - resume 后 interrupt 前调用/费用不增。
    - 三类修正均证明未受影响 DTO/invocation/cost/hash 不变。
  </acceptance_criteria>
  <done>追问和修正可恢复、诚实且不会重复已确认工作。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 接通 H5 并追加 9 个语义案例</name>
  <read_first>
    - frontend/src/features/agent/api/client.generated.ts
    - frontend/src/features/agent/api/schemas.generated.ts
    - frontend/src/features/agent/components/AnalyzePage.tsx
    - frontend/src/features/agent/components/AnalyzePage.test.tsx
    - backend/evals/phase02-cases.jsonl
    - backend/evals/validate_dataset.py
  </read_first>
  <files>backend/tests/integration/test_agent_vertical.py, backend/tests/unit/test_runtime_foundation.py, frontend/src/features/agent/components/AnalyzePage.tsx, frontend/src/features/agent/components/AnalyzePage.test.tsx, backend/evals/phase02-cases.jsonl</files>
  <action>用真实 PG 与 unit tests 覆盖 Task 1 的 clarification/ambiguity/partial/correction/resume；UI 一次展示 understood items、全部 questions 和候选差异，键盘可选且默认无候选；支持自然语言修正/排除，partial/warn 用文字+图标，报告仍只读 snapshot。只追加 5 missing_ambiguity（集中追问、复合菜做法、生熟/可食部、歧义、目录外）和 4 correction（改量、改名、排除、连续修正），不改旧 5。运行 check-all 证明预声明合同无需 schema 变更。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python tests/run_pg.py --env-file .env.test.example -- .venv/bin/python -m pytest tests/integration/test_agent_vertical.py tests/unit/test_runtime_foundation.py -q -k "clarification or ambiguity or partial or correction or resume" &amp;&amp; .venv/bin/python evals/validate_dataset.py --dataset evals/phase02-cases.jsonl --expected-count 14 --expected-composition happy=5,missing_ambiguity=5,correction=4 &amp;&amp; cd ../frontend &amp;&amp; node src/features/agent/api/generate-contracts.mjs check-all &amp;&amp; npm test -- --run src/features/agent/components/AnalyzePage.test.tsx</automated></verify>
  <acceptance_criteria>
    - UI 不逐字段盘问、不自动选候选、不浏览器重算。
    - 旧 5 case hashes 完全不变。
    - category semantic tags 与 AI-SPEC composition 精确。
  </acceptance_criteria>
  <done>D-01..13 的交互边界在 UI、Graph、PG 和冻结集同步成立。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| 自动采用歧义候选 | Tampering | Critical | food_id | catalog→state | Task 1/2 | candidate/resume tests |
| unknown 零填伪装完整 | Spoofing | Critical | report | miss→report | Task 1 | partial member assertions |
</threat_model>
<verification>Graph/API/UI、check-all 和 14-case validator 通过。</verification>
<success_criteria>用户能集中补充与局部修正，且任何未知项被明确披露。</success_criteria>
<output>Create 02-11-SUMMARY.md when done.</output>
