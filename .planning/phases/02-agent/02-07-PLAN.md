---
phase: 02-agent
plan: 07
type: execute
wave: 3
depends_on: ["02-03", "02-06"]
files_modified: [backend/app/agent/models.py, backend/app/agent/ports.py, backend/app/agent/repository.py, backend/app/agent/service.py, backend/app/agent/state.py, backend/app/agent/tools.py, backend/app/agent/graph.py, backend/app/agent/README.md, backend/tests/unit/test_runtime_foundation.py, backend/tests/unit/README.md]
autonomous: true
requirements: [AGT-01, AGT-02, AGT-04, AGT-06, AGT-07, ARC-05, QLT-02]
must_haves:
  truths: ["业务 ledger 与 Graph State 分离，所有用户资源查询带 user_id。", "Graph 只能通过 tool adapter 调领域 Service，不能导入 ORM/Repository。"]
  artifacts:
    - path: "backend/app/agent/state.py"
      provides: "JSON-safe versioned AgentState"
    - path: "backend/app/agent/service.py"
      provides: "thread/run/event/invocation/lease transaction boundary"
  key_links:
    - from: "backend/app/agent/service.py"
      to: "backend/app/agent/ports.py"
      via: "Protocol injection"
      pattern: "AgentRepository"
---
<objective>建立首绿所需 ledger、State、tool/graph/supervisor 合同骨架。Output: 分层模块与确定性 foundation tests。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@backend/app/auth/models.py @backend/app/auth/repository.py @backend/app/admin/service.py @backend/app/README.md @.planning/phases/02-agent/02-AI-SPEC.md @.planning/phases/02-agent/02-PATTERNS.md</context>
<tasks>
<task type="auto">
  <name>Task 1: 定义 ledger、State 和运行合同</name>
  <read_first>
    - backend/app/auth/models.py
    - backend/app/auth/repository.py
    - backend/app/admin/service.py
    - backend/app/README.md
    - .planning/phases/02-agent/02-AI-SPEC.md
  </read_first>
  <files>backend/app/agent/models.py, backend/app/agent/ports.py, backend/app/agent/repository.py, backend/app/agent/service.py, backend/app/agent/state.py</files>
  <action>复用单一 Base、显式 constraint/index naming、Repository flush-only/with_for_update 和 Service commit/rollback。threads/runs/events/invocations 的 user resource 方法必须接收 user_id；命令 key 绑定 canonical hash，event seq/lease/invocation 使用锁或唯一约束。State 包含 AGT-02 所列字段与版本/预算/状态，图片字段 Phase 2 只允许空；不得含 token、ORM、Provider 原文、CoT 或无限消息。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m mypy app/agent/models.py app/agent/ports.py app/agent/repository.py app/agent/service.py app/agent/state.py</automated></verify>
  <acceptance_criteria>
    - owner filter 在单 SQL 条件中，Repository 无 commit/HTTP/Graph 语义。
    - State/ORM/API/Provider DTO 为不同模块。
    - 最小审计仅含版本、状态、计数、费用、耗时和安全摘要。
  </acceptance_criteria>
  <done>权威 ledger 和短期 State 职责清晰且 tenant-safe。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 建立可接线的 Graph/tool/supervisor 合同</name>
  <read_first>
    - backend/app/agent/service.py
    - backend/app/agent/state.py
    - backend/app/providers/reasoning/ports.py
    - .planning/phases/02-agent/02-AI-SPEC.md
    - .planning/phases/02-agent/02-RESEARCH.md
  </read_first>
  <files>backend/app/agent/tools.py, backend/app/agent/graph.py, backend/app/agent/README.md, backend/tests/unit/test_runtime_foundation.py, backend/tests/unit/README.md</files>
  <action>定义 NutritionToolAdapter port、主图 MEAL_ANALYSIS/DIET_PLANNING 路由合同和 lifespan runtime factory 接口；Postgres lease supervisor 的具体实现由 02-08 在真实初始化链中创建。此任务只建立类型/依赖方向与确定性无副作用骨架，DIET_PLANNING 固定 CAPABILITY_NOT_AVAILABLE。明确 Graph 文件禁止 SQLAlchemy/Model/Repository import；test_runtime_foundation 同时覆盖 Task 1 的 ledger/State 约束和本任务导入边界，并同步 agent README、unit README 文件索引。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m pytest tests/unit/test_runtime_foundation.py -q -k "agent_import_boundaries or runtime_contract" &amp;&amp; .venv/bin/python -m mypy app/agent</automated></verify>
  <acceptance_criteria>
    - Graph→tool adapter→Nutrition Service 依赖可由 import test 证明。
    - planning intent 不伪造 Phase 5。
    - README 同计划索引新增文件和禁止依赖。
  </acceptance_criteria>
  <done>首绿只需填充既有合同，不再临时发明层次。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| 跨用户读取 ledger | Spoofing | Critical | thread/run/event | service→repository | Task 1 | owner-filter tests |
| Graph 绕过领域服务 | Elevation of Privilege | Critical | PostgreSQL | graph→ORM | Task 2 | import-boundary test |
</threat_model>
<verification>foundation tests、mypy 和目录合同通过。</verification>
<success_criteria>ledger/State/Graph 边界可由实际 Wave 7 初始化与纵切计划消费。</success_criteria>
<output>Create 02-07-SUMMARY.md when done.</output>
