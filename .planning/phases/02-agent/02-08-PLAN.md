---
phase: 02-agent
plan: 08
type: execute
wave: 5
depends_on: ["02-02", "02-05", "02-06", "02-07"]
files_modified: [backend/app/nutrition/importer.py, backend/app/nutrition/data/fdc-seed-v1.json, backend/app/nutrition/data/README.md, backend/app/nutrition/README.md, backend/scripts/setup_checkpointer.py, backend/scripts/README.md, backend/tests/integration/test_agent_bootstrap.py, backend/tests/integration/README.md, backend/app/agent/supervisor.py, backend/app/agent/README.md]
autonomous: true
requirements: [AGT-04, NUT-01, NUT-02, QLT-02]
must_haves:
  truths: ["migration→checkpoint setup→seed apply 可在真实 PG 重复执行且幂等。", "目录存在 qualified rice，checkpointer 表真实存在。"]
  artifacts:
    - path: "backend/app/nutrition/importer.py"
      provides: "版本/hash 校验与幂等 --apply"
    - path: "backend/scripts/setup_checkpointer.py"
      provides: "显式 AsyncPostgresSaver setup CLI"
  key_links:
    - from: "backend/scripts/run_initialized_app.py"
      to: "backend/scripts/setup_checkpointer.py"
      via: "ordered child command"
      pattern: "checkpointer_setup"
---
<objective>把 schema、Checkpointer 和 FDC seed 变成真实可重复的初始化链。Output: importer、setup CLI、真实 PG 幂等证据。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@backend/scripts/run_initialized_app.py @backend/tests/run_pg.py @backend/app/nutrition/service.py @backend/migrations/versions/0004_agent_core.py @.planning/phases/02-agent/02-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 固化 FDC seed 与幂等 apply</name>
  <read_first>
    - .planning/phases/02-agent/02-RESEARCH.md
    - backend/app/nutrition/service.py
    - backend/app/nutrition/models.py
    - backend/app/nutrition/README.md
    - backend/tests/run_pg.py
  </read_first>
  <files>backend/app/nutrition/importer.py, backend/app/nutrition/data/fdc-seed-v1.json, backend/app/nutrition/data/README.md, backend/app/nutrition/README.md</files>
  <action>按 D-07/D-08 只使用 USDA FDC CC0 24–30 条记录；manifest 保存 FDC id/dataset/release/source/license/prepared-state/四项营养/qualification/content hash。--check 离线校验，--apply 只通过 Service/Repository 幂等写入 immutable catalog_version，重复相同 version/hash 零变更，冲突 hash 拒绝；null 不转 0。家庭份量只允许来源精确匹配或双角色签署，否则 unqualified 并追问克数。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m app.nutrition.importer --check app/nutrition/data/fdc-seed-v1.json</automated></verify>
  <acceptance_criteria>
    - --apply 连续两次行数/hash/版本不变，冲突 manifest 非零。
    - qualified cooked rice 可由 Repository 查询且 provenance 完整。
    - 运行时不访问 FDC 网络，migration 仍为 0 seed。
  </acceptance_criteria>
  <done>合法小型目录可离线、幂等、可追溯地装入真实 PostgreSQL。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 建立显式 Checkpointer setup 与全链幂等验证</name>
  <read_first>
    - backend/scripts/run_initialized_app.py
    - backend/tests/run_pg.py
    - backend/scripts/README.md
    - .planning/phases/02-agent/02-AI-SPEC.md
  </read_first>
  <files>backend/scripts/setup_checkpointer.py, backend/scripts/README.md, backend/app/agent/supervisor.py, backend/app/agent/README.md, backend/tests/integration/test_agent_bootstrap.py, backend/tests/integration/README.md</files>
  <action>实现 scripts/setup_checkpointer.py AsyncPostgresSaver CLI：在 APP_ENV=test 下实例化现有 Settings 并只连接 `validate_test_database_configuration()` 返回的 TEST_DATABASE_URL，绝不读取或重绑 DATABASE_URL；strict msgpack，不在请求或每 worker lifespan 隐式 setup。创建 Postgres lease supervisor 具体实现并同步 agent/scripts README。02-02 启动器按稳定命令名把同一个已验证测试 URL 显式传给 CLI/importer，无需再次修改；真实 PG 测试须额外断言 child 的 DATABASE_URL 仍为 5432/food_agent_dev、TEST_DATABASE_URL 为 55432/food_agent_test 且互异，再覆盖 Task 1 --apply、连续两次 --prepare-only，断言 alembic head、checkpoint/checkpoint_blobs/checkpoint_writes 表、qualified rice、catalog hash、supervisor 可启动，且顺序精确、开发库无变化。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python tests/run_pg.py --env-file .env.test.example -- .venv/bin/python scripts/run_initialized_app.py --prepare-only &amp;&amp; .venv/bin/python tests/run_pg.py --env-file .env.test.example -- .venv/bin/python -m pytest tests/integration/test_agent_bootstrap.py -q</automated></verify>
  <acceptance_criteria>
    - prepare-only 重复运行幂等。
    - checkpointer setup 独立于 Alembic 业务 migration。
    - 任一步失败启动器不执行 uvicorn。
  </acceptance_criteria>
  <done>部署/Playwright 启动前的三步初始化链有真实数据库证据。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| seed hash 冲突被覆盖 | Tampering | Critical | catalog truth | manifest→DB | Task 1 | repeated/conflict apply |
| 缺 checkpoint 表仍启动 | Repudiation | High | resumability | setup→app | Task 2 | real tables/order test |
</threat_model>
<verification>真实 PG --prepare-only 与 bootstrap 集成套件通过。</verification>
<success_criteria>实际 Wave 7 纵切可假设 schema、checkpoint 和 qualified seed 确实存在。</success_criteria>
<output>Create 02-08-SUMMARY.md when done.</output>
