---
phase: 02-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/pyproject.toml, backend/requirements.lock, backend/lock_dependencies.py, backend/supply-chain-evidence-v1.schema.json, backend/supply-chain-evidence.json, backend/validate_supply_chain.py, backend/tests/unit/test_supply_chain.py, backend/tests/unit/README.md, backend/README.md]
autonomous: false
requirements: [AGT-01, ARC-05, ARC-06, QLT-02]
must_haves:
  truths:
    - "所有研究中标为 [ASSUMED] 的包在安装前由真人核验精确版本、发布者和源仓库。"
  artifacts:
    - path: "backend/supply-chain-evidence.json"
      provides: "固定版本 scanner 输出或字段完整人工证据的 fail-closed 供应链门"
    - path: "backend/requirements.lock"
      provides: "首个 LangGraph 安装前的 hash-complete Python 3.11 锁"
  key_links:
    - from: ".planning/phases/02-agent/02-RESEARCH.md"
      to: "PyPI/npm registry"
      via: "精确包版本合法性核验"
      pattern: "\\[ASSUMED\\]"
---
<objective>先建立可执行的 fail-closed 供应链证据门，再锁定首绿所需 Python runtime。Output: schema/validator/evidence、精确批准信号、hash-complete lock。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-agent/02-RESEARCH.md @.planning/phases/02-agent/02-AI-SPEC.md @backend/pyproject.toml @frontend/package.json</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 建立版本化供应链 evidence validator</name>
  <read_first>
    - .planning/phases/02-agent/02-RESEARCH.md
    - backend/pyproject.toml
    - frontend/package.json
    - backend/tests/unit/README.md
    - backend/README.md
  </read_first>
  <files>backend/supply-chain-evidence-v1.schema.json, backend/supply-chain-evidence.json, backend/validate_supply_chain.py, backend/tests/unit/test_supply_chain.py, backend/tests/unit/README.md, backend/README.md</files>
  <action>实现二选一且 fail-closed 的自动门。首选 scanner 分支只接受 schema 中固定的 scanner_name、scanner_version、source_repository、release_artifact_sha256、invocation 和逐包结构化结果；固定身份之外的 `slopcheck` 命令即使 PATH 可见也拒绝。当前无法证明可信 scanner 来源时走人工 manifest 分支：每个精确包必须绑定 ecosystem、registry URL、version、owner/publisher、repository URL、license、published_at、checked_at、reviewer、registry response/page SHA-256；manifest 自身含 schema_version、生成时间和整体 SHA-256。validator 校验 7 个批准包全集、禁止额外包、字段/URL/hash/时间格式和重复项，并用失败 fixtures 覆盖缺字段、错版本、假 scanner、过期/变造 hash；同步直接父 README 索引。初始 evidence 只能是 schema-valid 的 `status: pending`，validator 对 pending 非零退出，不得伪造批准。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python -m pytest tests/unit/test_supply_chain.py -q &amp;&amp; .venv/bin/python validate_supply_chain.py self-test</automated></verify>
  <acceptance_criteria>
    - 不存在无条件调用 PATH 中 slopcheck 的路径。
    - 固定 scanner 结构化输出或字段完整人工 manifest 恰有一个分支通过。
    - pending、字段缺失、版本/registry/repository/license/time/hash 不匹配全部失败。
  </acceptance_criteria>
  <done>供应链审核不依赖不存在或未固定身份的扫描器。</done>
</task>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint 2: 核验全部新增依赖</name>
  <files>backend/supply-chain-evidence.json</files>
  <action>逐项核验 langgraph 1.2.11、langgraph-checkpoint-postgres 3.1.2、eventsource-parser 3.1.0、arize-phoenix 20.3.0、arize-phoenix-otel 0.17.1、openinference-instrumentation-langchain 0.1.72、promptfoo 0.122.0 的 registry、发布者、仓库、发布时间和活跃度；任一 SLOP、SUS、版本不存在或来源不一致即阻塞。</action>
  <what-built>02-RESEARCH Package Legitimacy Audit 中的精确清单。</what-built>
  <how-to-verify>若能提供固定 source/version/hash 的可信 scanner，则保存其结构化输出；否则逐包填写版本化人工 manifest 的 registry、版本、owner、repository、license、发布时间、核验时间、reviewer 与 registry 页面/响应 SHA-256。运行本地 validator；任何 pending、缺字段或不匹配都不得批准。</how-to-verify>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python validate_supply_chain.py verify --schema supply-chain-evidence-v1.schema.json --evidence supply-chain-evidence.json</automated></verify>
  <resume-signal>输入精确批准文本，或指出拒绝的包和版本。</resume-signal>
  <done>7 个精确依赖通过固定 scanner 证据或字段完整人工 manifest，并取得不可自动替代的人类批准。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: 首绿前生成并洁净验证 hash-complete Python lock</name>
  <read_first>
    - backend/supply-chain-evidence.json
    - backend/pyproject.toml
    - backend/tests/unit/test_supply_chain.py
    - backend/README.md
  </read_first>
  <files>backend/pyproject.toml, backend/requirements.lock, backend/lock_dependencies.py, backend/tests/unit/test_supply_chain.py, backend/README.md</files>
  <action>仅在 Task 2 evidence validator 通过后，把 langgraph==1.2.11 与 langgraph-checkpoint-postgres==3.1.2 加入 pyproject direct dependencies。实现 lock_dependencies.py，从 pyproject 的 project.dependencies 与 dev extra 解析全部 direct deps，使用 Python 3.11/pip 的下载报告生成包含全部传递依赖且每个 artifact 至少一个 `--hash=sha256:` 的排序稳定 requirements.lock；`check` 必须验证 direct deps/extras 全覆盖、精确版本、每行 hash 完整、无额外顶层依赖、pyproject drift。先创建临时干净 Python 3.11 venv，仅执行 `python -m pip install --require-hashes -r requirements.lock` 并 import 首绿依赖，证明锁可从零重建；该临时环境只用于独立证明。随后创建或验证后续计划唯一使用的 `backend/.venv` 确为 Python 3.11，并用 `.venv/bin/python -m pip install --require-hashes -r requirements.lock` 把同一份经供应链批准且 hash-complete 的锁同步到实际执行环境；最后必须用 `.venv/bin/python` import `langgraph`、`AsyncPostgresSaver` 及首绿所需 FastAPI/SQLAlchemy/Pydantic 模块。禁止后续计划混用系统 Python、临时证明环境或未按该锁同步的解释器。</action>
  <verify><automated>cd backend &amp;&amp; task_tmp=$(mktemp -d) &amp;&amp; python3.11 -m venv "$task_tmp/venv" &amp;&amp; "$task_tmp/venv/bin/python" lock_dependencies.py check --pyproject pyproject.toml --lock requirements.lock &amp;&amp; "$task_tmp/venv/bin/python" -m pip install --require-hashes -r requirements.lock &amp;&amp; "$task_tmp/venv/bin/python" -c "import fastapi, sqlalchemy, pydantic, langgraph; from langgraph.checkpoint.postgres.aio import AsyncPostgresSaver" &amp;&amp; if [ ! -x .venv/bin/python ]; then python3.11 -m venv .venv; fi &amp;&amp; .venv/bin/python -c "import sys; assert sys.version_info[:2] == (3, 11)" &amp;&amp; .venv/bin/python -m pip install --require-hashes -r requirements.lock &amp;&amp; .venv/bin/python -c "import fastapi, sqlalchemy, pydantic, langgraph; from langgraph.checkpoint.postgres.aio import AsyncPostgresSaver"</automated></verify>
  <acceptance_criteria>
    - lock 覆盖全部 runtime/dev direct deps 和完整传递闭包，每个 artifact 有 SHA-256。
    - pyproject 任一 direct dependency 漂移使 check 非零。
    - 临时干净 Python 3.11 venv 仅靠 requirements.lock 可安装并 import smoke。
    - backend/.venv 是 Python 3.11，使用同一 requirements.lock 完成 `--require-hashes` 安装，并由该解释器实际 import LangGraph、PostgresSaver 和全部首绿 runtime 依赖。
  </acceptance_criteria>
  <done>任何 LangGraph 安装和首绿之前已有可重建、可漂移检测的 hash lock。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| 依赖投毒/拼写混淆 | Tampering | High | lockfile/运行环境 | registry→project | Task 1/Checkpoint 2 | 固定 scanner 或 manifest validator |
| 锁文件遗漏/漂移 | Tampering | High | Python runtime | pyproject→venv | Task 3 | direct/hash/drift + clean venv |
</threat_model>
<verification>validator self-test、人工批准、lock drift、临时 clean Python 3.11 rebuild 与实际 backend/.venv 同锁同步全部通过。</verification>
<success_criteria>未批准前不安装；批准后临时证明环境和所有后续计划实际使用的 backend/.venv 都只能从同一 hash-complete lock 重建。</success_criteria>
<output>Create 02-01-SUMMARY.md when done.</output>
