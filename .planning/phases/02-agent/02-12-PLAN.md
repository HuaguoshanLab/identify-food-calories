---
phase: 02-agent
plan: 12
type: execute
wave: 9
depends_on: ["02-11"]
files_modified: [backend/app/agent/supervisor.py, backend/app/agent/service.py, backend/app/agent/graph.py, backend/tests/integration/test_agent_checkpoint.py, backend/tests/integration/README.md, frontend/src/features/agent/stream/useAgentEventStream.ts, frontend/src/features/agent/stream/useAgentEventStream.test.ts, frontend/src/features/agent/stream/README.md, frontend/tests/e2e/agent.spec.ts, backend/evals/phase02-cases.jsonl]
autonomous: true
requirements: [AGT-04, AGT-05, AGT-06, AGT-07, ARC-05, QLT-02]
must_haves:
  truths: ["进程重建、lease 过期、SSE gap/reconnect 不重复已完成调用。", "五类预算在下一调用前阻断，只有 transient 自动重试一次。", "冻结集完成 24 例。"]
  artifacts:
    - path: "backend/tests/integration/test_agent_checkpoint.py"
      provides: "真实 saver reopen/lease/budget tests"
    - path: "frontend/src/features/agent/stream/useAgentEventStream.test.ts"
      provides: "fragment/duplicate/gap/401/abort tests"
  key_links:
    - from: "frontend/src/features/agent/stream/useAgentEventStream.ts"
      to: "/api/v1/agent/threads/{id}/events"
      via: "snapshot-first authenticated fetch"
      pattern: "Last-Event-ID"
---
<objective>完成崩溃/断线恢复、有界自治和最终 24-case 数据集。Output: 真实 Checkpointer、SSE hook/E2E、预算矩阵与追加 10 例。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-agent/02-11-SUMMARY.md @backend/tests/run_pg.py @frontend/src/auth/AuthProvider.tsx @backend/evals/phase02-cases.jsonl @.planning/phases/02-agent/02-AI-SPEC.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 证明 Checkpointer/lease/预算恢复边界</name>
  <read_first>
    - backend/app/agent/supervisor.py
    - backend/app/agent/service.py
    - backend/app/agent/graph.py
    - backend/tests/run_pg.py
    - backend/tests/integration/README.md
    - .planning/phases/02-agent/02-AI-SPEC.md
  </read_first>
  <files>backend/app/agent/supervisor.py, backend/app/agent/service.py, backend/app/agent/graph.py, backend/tests/integration/test_agent_checkpoint.py, backend/tests/integration/README.md</files>
  <action>按 D-15、D-16、D-17，真实 PG 每例唯一 thread/namespace，关闭 saver/graph/app 后重建并 resume；双 supervisor 竞争 lease 只有一个执行，owner crash 后 fake clock 到期恢复。每次 node/Provider/tool 前检查 steps≤12、model≤4、tool≤12、active≤45s、cost≤0.02；恰好到限制可完成，下一调用前 LIMIT_REACHED，终态后零新增。waiting 时间不计 active；仅 429/5xx/network/timeout 自动重试一次，400/422/catalog miss/validation 零重试。GraphRecursionError 映射稳定失败且不泄露。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python tests/run_pg.py --env-file .env.test.example -- .venv/bin/python -m pytest tests/integration/test_agent_checkpoint.py -q</automated></verify>
  <acceptance_criteria>
    - saver reopen 从 checkpoint 继续，不从首节点重跑。
    - 双 worker/lease crash 无双调用或双事件。
    - 五类边界、等待排除和 retry 分类全部使用 fake clock，不 sleep。
  </acceptance_criteria>
  <done>恢复、并发和预算终止均有真实 PostgreSQL 证据。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 完成 snapshot-first SSE 与最后 10 个案例</name>
  <read_first>
    - frontend/src/auth/AuthProvider.tsx
    - frontend/src/features/agent/stream/useAgentEventStream.ts
    - frontend/tests/e2e/agent.spec.ts
    - backend/evals/phase02-cases.jsonl
    - backend/evals/validate_dataset.py
  </read_first>
  <files>frontend/src/features/agent/stream/useAgentEventStream.ts, frontend/src/features/agent/stream/useAgentEventStream.test.ts, frontend/src/features/agent/stream/README.md, frontend/tests/e2e/agent.spec.ts, backend/evals/phase02-cases.jsonl</files>
  <action>hook 用 Auth request 做 snapshot-first，并把 fetch 返回的任意 Uint8Array chunk 通过 streaming TextDecoder 后交给 02-04 已安装锁定的 eventsource-parser 3.1.0；正确处理 fragmentation、CRLF、多行 data、id/event 和 UTF-8 边界，禁止 split-on-newline 自制协议解析。实现 Last-Event-ID、seq 去重、gap 立即重取、401 一次 refresh/replay、unmount 只 abort 流。E2E 真实验证刷新/断流同 thread/report 且调用计数不增。追加 5 persistence_isolation、3 validation_budget、2 adversarial；异常值/工具校验失败只在 validation_budget，提示注入/医疗越界只在 adversarial。旧 14 hash 不变。</action>
  <verify><automated>cd frontend &amp;&amp; npm test -- --run src/features/agent/stream/useAgentEventStream.test.ts &amp;&amp; npm run test:e2e -- --grep "phase 2 reconnect" &amp;&amp; cd ../backend &amp;&amp; .venv/bin/python evals/validate_dataset.py --dataset evals/phase02-cases.jsonl --expected-count 24 --expected-composition happy=5,missing_ambiguity=5,correction=4,persistence_isolation=5,validation_budget=3,adversarial=2</automated></verify>
  <acceptance_criteria>
    - eventsource-parser 对 fragment/CRLF/multiline/UTF-8、duplicate/gap/401/abort/terminal 均有 hook tests。
    - E2E 不直写 DB/伪造 token/MSW。
    - 24 cases semantic tags、hash chain 和 forbidden assertions 精确。
  </acceptance_criteria>
  <done>D-14..17 与 24-case composition 完整。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| 崩溃恢复重复费用 | Repudiation | Critical | invocation/cost | supervisor→provider | Task 1 | reopen/lease call counts |
| 无限循环/成本 DoS | Denial of Service | Critical | runtime | user→graph | Task 1 | five-budget matrix |
| SSE token URL 泄露 | Information Disclosure | High | access token | browser→stream | Task 2 | authenticated-fetch source/tests |
</threat_model>
<verification>真实 PG checkpoint、hook/E2E 与 24-case validator 通过。</verification>
<success_criteria>恢复、重连、预算和安全边界均可重复且数据集完整。</success_criteria>
<output>Create 02-12-SUMMARY.md when done.</output>
