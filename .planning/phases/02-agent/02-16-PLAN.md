---
phase: 02-agent
plan: 16
type: execute
wave: 13
depends_on: ["02-15"]
files_modified: [backend/evals/README.md, backend/evals/expert-signoff-v1.schema.json, backend/evals/evaluate_phase2.py, backend/evals/promptfooconfig.yaml, backend/evals/release-failures.json, backend/evals/phase2-code-eval.json, package.json, package-lock.json, README.md]
autonomous: true
requirements: [AGT-06, AGT-07, ARC-06, QLT-02]
must_haves:
  truths: ["Fake Provider 实际执行 24 cases，observed 证据绑定实现/dataset hashes。", "每 case 强制两名不同 pseudonym、不同规定角色签署。", "所有 release 阈值和缺输入均有失败 fixture。"]
  artifacts:
    - path: "backend/evals/phase2-code-eval.json"
      provides: "实际 Graph/Fake Provider 24-case evidence"
    - path: "backend/evals/expert-signoff-v1.schema.json"
      provides: "per-case dual-role contract"
  key_links:
    - from: "backend/evals/phase02-cases.jsonl"
      to: "backend/evals/phase2-code-eval.json"
      via: "run-code-eval"
      pattern: "dataset_hash"
---
<objective>生成实际 24-case code eval 并锁定专家/Promptfoo 发布合同。Output: evaluator、machine evidence、role schema、失败 fixtures、已批准 promptfoo CLI。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-agent/02-AI-SPEC.md @.planning/phases/02-agent/02-01-SUMMARY.md @backend/evals/phase02-cases.jsonl @backend/app/providers/reasoning/fake.py @backend/app/agent/graph.py</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实际运行 24-case Fake Provider code eval</name>
  <read_first>
    - .planning/phases/02-agent/02-AI-SPEC.md
    - backend/evals/phase02-cases.jsonl
    - backend/evals/validate_dataset.py
    - backend/app/providers/reasoning/fake.py
    - backend/app/agent/graph.py
  </read_first>
  <files>backend/evals/README.md, backend/evals/evaluate_phase2.py, backend/evals/phase2-code-eval.json</files>
  <action>实现 run-code-eval/verify-code-eval；每 case 以隔离 thread/run 实际执行 Fake Provider→Graph→tools（含 resume/reconnect/budget/security），所有真实 PostgreSQL/Checkpointer 操作仍由 02-02 run_pg wrapper 包住。保存 observed state/trace/report/events/forbidden outcomes，禁止复制 expected 冒充 observed。result 绑定 dataset、24 case、rubric、graph/provider/prompt/tool/catalog/schema 版本 hashes；Critical/High 从 observed assertions 计算。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python tests/run_pg.py --env-file .env.test.example -- .venv/bin/python evals/evaluate_phase2.py run-code-eval --dataset evals/phase02-cases.jsonl --output evals/phase2-code-eval.json &amp;&amp; .venv/bin/python tests/run_pg.py --env-file .env.test.example -- .venv/bin/python evals/evaluate_phase2.py verify-code-eval --dataset evals/phase02-cases.jsonl --result evals/phase2-code-eval.json</automated></verify>
  <acceptance_criteria>
    - 精确 24 observed results，静态 expected-only 文件失败。
    - 每条包含 state/trace/report/events/forbidden outcomes。
    - 任一实现或 dataset hash 变化要求重跑。
  </acceptance_criteria>
  <done>冻结集有来自实际状态图运行的机器证据。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 锁定双角色、阈值、Promptfoo 配置与失败 fixtures</name>
  <read_first>
    - .planning/phases/02-agent/02-AI-SPEC.md
    - backend/evals/phase02-cases.jsonl
    - backend/evals/phase2-code-eval.json
    - frontend/tests/e2e/h5-visual.spec.ts-snapshots/analyze-phase2-candidate-430-chromium-darwin.png
  </read_first>
  <files>backend/evals/README.md, backend/evals/expert-signoff-v1.schema.json, backend/evals/evaluate_phase2.py, backend/evals/promptfooconfig.yaml, backend/evals/release-failures.json</files>
  <action>schema 强制全部 24 case 至少由两名不同真实评审人共同签署：一名营养师和一名食物成分数据管理员，两者分别确认食物编码、阻塞字段、家庭份量可审计性、权威数值与硬校验；产品/隐私角色可按 category 追加但绝不能替代这两名专家。每条 review 强制 role、pseudonym、rubric_version、dataset_hash、code_eval_hash；每个 Medium rubric 样本还必须含人工 1–5 数值分，并绑定 case/hash。Judge 对同一 Medium 样本产出对应 1–5 分，release 每次从原始 per-case 数值重算 Spearman≥.70，禁止消费预填相关系数。Promptfoo 固定 12 cases×3、machine JSON、network/product 分账；Critical100%、High≥95%、Medium avg≥4、无 score1。为缺营养师、缺数据管理员、伪重复 reviewer/pseudonym、缺人工分、缺 Judge 分、hash/rubric 不匹配、Spearman 不足及其他阈值建立独立失败 fixture。</action>
  <verify><automated>cd backend &amp;&amp; .venv/bin/python evals/evaluate_phase2.py self-test --fixtures evals/release-failures.json &amp;&amp; .venv/bin/python evals/evaluate_phase2.py validate-contracts --dataset evals/phase02-cases.jsonl --code-eval evals/phase2-code-eval.json --signoff-schema evals/expert-signoff-v1.schema.json --promptfoo-config evals/promptfooconfig.yaml</automated></verify>
  <acceptance_criteria>
    - 一人双角色、同/伪重复 pseudonym、缺营养师或数据管理员、缺 case/hash/rubric 均失败。
    - 每个 Medium 样本具备 hash-bound 人工/Judge 1–5 分，Spearman 从逐 case 原值重算。
    - 每个发布阈值和 Promptfoo 缺失有专属 fail fixture。
    - eval README 记录固定 machine JSON 与 release 输入合同。
  </acceptance_criteria>
  <done>人工、Judge、code eval 与付费执行的机器合同全部锁定。</done>
</task>
<task type="auto">
  <name>Task 3: 安装精确批准的本地 Promptfoo CLI</name>
  <read_first>
    - .planning/phases/02-agent/02-01-SUMMARY.md
    - README.md
  </read_first>
  <files>package.json, package-lock.json, README.md</files>
  <action>仅在 02-01 精确批准后安装固定 promptfoo 0.122.0，但不运行付费 eval；更新 lockfile 和根 README 固定 `npx --no-install promptfoo` 命令，禁止浮动版本、临时下载或把批准范围扩大到其他包。</action>
  <verify><automated>npx --no-install promptfoo --version | grep -Fx "0.122.0"</automated></verify>
  <acceptance_criteria>
    - package/lock 精确锁定 0.122.0。
    - CLI 只从本地 lockfile 解析，不访问 registry。
    - 付费 eval 仍由 02-17 独立人工门阻塞。
  </acceptance_criteria>
  <done>最终评测拥有已批准且可复现的本地 CLI。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Asset | Boundary | Mitigation Task | Verification Test |
|---|---|---:|---|---|---|---|
| static expected 冒充 eval | Spoofing | Critical | release evidence | dataset→evaluator | Task 1 | observed execution/hash |
| 单 reviewer 冒充多角色 | Spoofing | High | human labels | reviewer→signoff | Task 2 | role failure fixtures |
| 阈值失败被放行 | Tampering | Critical | release decision | results→report | Task 2 | exhaustive self-test |
| 未批准 CLI 进入 lockfile | Tampering | High | eval runtime | registry→project | Task 3 | exact local version |
</threat_model>
<verification>24-case code eval、双角色合同、Promptfoo 配置和失败 fixtures 通过。</verification>
<success_criteria>最终发布只能消费真实机器/人工/付费证据。</success_criteria>
<output>Create 02-16-SUMMARY.md when done.</output>
