---
phase: 4
plan: "07"
type: execute
wave: 6
depends_on: ["04-04", "04-06"]
files_modified:
  - backend/app/agent/tools.py
  - backend/app/agent/graph.py
  - backend/app/agent/state.py
  - backend/tests/unit/test_agent_memory_context.py
  - backend/tests/integration/test_direct_memory_public_api.py
  - backend/tests/integration/README.md
  - frontend/tests/e2e/agent.spec.ts
  - frontend/tests/e2e/README.md
  - docs/learning/04-meal-records-and-long-term-memory.md
  - docs/learning/README.md
autonomous: true
gap_closure: true
requirements: [MEM-03, MEM-04, MEM-05]
decisions_addressed: [D-08, D-09, D-10, D-11, D-13, D-14, D-16, D-17]
must_haves:
  truths:
    - "分析页输入“我不吃辣”经唯一 typed Graph tool 自动写入长期记忆；resume/replay 不重复。"
    - "A 仅经公开注册、Mailpit、验证、登录获得 token 后可管理记忆；B 的 GET/PATCH/DELETE 均不可用。"
    - "用户可从实际 H5 查看、编辑并确认删除自动写入的记忆，删除后 list 和后续 context 均为空。"
  artifacts:
    - path: backend/app/agent/tools.py
      provides: "Graph 唯一 direct preference tool boundary"
      contains: "capture_explicit"
    - path: backend/tests/integration/test_direct_memory_public_api.py
      provides: "不伪造 token 的公共认证与双用户 API 合同"
      contains: "Mailpit"
    - path: frontend/tests/e2e/agent.spec.ts
      provides: "真实 H5 从分析到记忆编辑删除"
      contains: "不吃辣"
  key_links:
    - from: backend/app/agent/graph.py
      to: backend/app/agent/tools.py
      via: "fresh text typed capture call, safe state marker on resume"
      pattern: "capture_explicit"
    - from: backend/tests/integration/test_direct_memory_public_api.py
      to: /api/v1/auth/register and /api/v1/memories
      via: "public cookie/origin verification and login-issued Bearer token"
      pattern: "access_token"
---

<objective>
把已验证的 direct-memory state machine 接入 Graph，并以公共认证 API、Playwright 和内置浏览器闭合 UAT 4、5。

Purpose: 根因是 Graph 从未调用 MemoryService；前端已有管理页，必须用实际用户路径而非内部调用证明。
Output: typed tool 接线、公共 A/B API 测试、H5/E2E/教学证据。
</objective>

<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@backend/AGENTS.md
@frontend/AGENTS.md
@docs/ui/h5-foundation.md
@backend/app/agent/graph.py
@backend/app/agent/tools.py
@backend/app/agent/state.py
@backend/app/memory/service.py
@backend/app/auth/api.py
@frontend/tests/e2e/auth-helpers.ts
@.planning/phases/04-meal-records-and-memory/04-UAT.md
@.planning/phases/04-meal-records-and-memory/04-06-PLAN.md

<interfaces>
Graph consumes only `NutritionToolAdapter.capture_explicit_preferences(user_id, run_id, statement) -> tuple[CapturedPreferenceSummary, ...]`. Summary has category/canonical text only—never ledger ID, external ID, request key, score, raw provider body or raw statement. Graph must not import MemoryService/ORM/Repository/Mem0/FastAPI.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 在 fresh text graph path 通过 typed tool 捕获直接偏好</name>
  <files>backend/app/agent/tools.py, backend/app/agent/graph.py, backend/app/agent/state.py, backend/tests/unit/test_agent_memory_context.py</files>
  <behavior>
    - Test 1: `米饭 100 克，我不吃辣` capture 一次，report totals 仍 130.0 kcal，无 ID/key/score。
    - Test 2: non-explicit text、model parse、vision、history 无 capture；推测仍需 confirm。
    - Test 3: completed replay、waiting resume、checkpoint reload 不重复 capture；安全 marker 和 tool budget charge 遵守既有上限。
  </behavior>
  <action>扩展三个 tool adapter 的 typed method；只有 Session adapter 构造 04-05 的 MemoryService/Repository/Provider。MealAnalysisGraph 对 fresh text 在 parse/context read 前调用一次，state 仅存不含原句/ID 的 completion marker；resume/replay 复用 marker。计入既有 tool/time/cost/loop budget。不得改变 Nutrition Service 数值权威，capture 结果/失败细节不得进 SSE、AgentEvent 或 report；D-17 仅允许安全 context reference。</action>
  <verify><automated>cd backend && uv run pytest tests/unit/test_agent_memory_context.py -q && uv run ruff check app/agent/graph.py app/agent/tools.py app/agent/state.py tests/unit/test_agent_memory_context.py</automated></verify>
  <done>Graph 只有窄 tool 写入路径，重放无第二 side effect，营养结果与安全输出不变。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 以公开注册、Mailpit 验证和登录 Bearer 测试 A/B memory API</name>
  <files>backend/tests/integration/test_direct_memory_public_api.py, backend/tests/integration/README.md</files>
  <behavior>
    - Test 1: A 经 public register→Mailpit code→verify→login 获 token；Agent 直接忌口后 GET memories 返回安全 avoidance DTO。
    - Test 2: B 经同链得到 token，对 A 的 memory GET/PATCH/DELETE 均安全不可用，A 不变。
    - Test 3: A PATCH 后 source=user_maintained；A DELETE 后 list 为零，另一公开 Agent run 的 context_references 也无该偏好。
  </behavior>
  <action>新增真实 PostgreSQL+Mailpit integration。启动命令及 test environment 必须显式使用 `SMTP_HOST=127.0.0.1 SMTP_PORT=1025 CORS_ORIGINS='["http://127.0.0.1:5178"]' docker compose up -d --wait postgres-test mailpit`，并以同样 SMTP/CORS env 运行 app/test；不要依赖本机残留服务。禁止 import/call `issue_access_token()`、principal dependency override、直接插入 User/AuthSession/Ledger、伪造 JWT、SQLite 或内部 service 建账号。

为 A/B 各建独立 HTTP client/cookie jar，base URL/Origin 统一为 CORS allowlist `http://127.0.0.1:5178`：公开 POST register，轮询 `http://127.0.0.1:8025/api/v1/messages` 与 message body 取 6 位码，带同一 client 的 registration context cookie、正确 `/api/v1/auth/register/verify` 路径和 Origin 调用 verify，再 public login，只用 response access_token 作 Bearer。A POST agent thread `米饭 100 克，我不吃辣` 后 list；实施三项断言。DELETE 后不得私下读 Repository：A 再 public Agent 分析 `米饭 100 克`，assert report context_references 不含忌口且 public list 仍空。更新 README 索引/运行 guard。</action>
  <verify><automated>cd backend && SMTP_HOST=127.0.0.1 SMTP_PORT=1025 CORS_ORIGINS='["http://127.0.0.1:5178"]' docker compose up -d --wait postgres-test mailpit && SMTP_HOST=127.0.0.1 SMTP_PORT=1025 CORS_ORIGINS='["http://127.0.0.1:5178"]' uv run python tests/run_pg.py --env-file .env.test.example -- uv run python -m pytest tests/integration/test_direct_memory_public_api.py -q && uv run ruff check tests/integration/test_direct_memory_public_api.py</automated></verify>
  <done>测试通过公开 cookie、Origin 和 login token 证明 A 写/list/edit/delete、B 无法访问、删除后公开 retrieval 为零。</done>
</task>

<task type="auto">
  <name>Task 3: 走真实 H5 路径复验 UAT 4、5并更新教学材料</name>
  <files>frontend/tests/e2e/agent.spec.ts, frontend/tests/e2e/README.md, docs/learning/04-meal-records-and-long-term-memory.md, docs/learning/README.md</files>
  <action>在 agent.spec.ts 增加独立 Phase 4 浏览器回归，复用页面注册/公开 Mailpit/登录 helper，禁止 DB seed、token 注入、内部函数。分析页输入 `米饭 100 克，我不吃辣` 后沿真实“我的 → 饮食偏好与记忆”验证忌口、用户直接表达、时间；编辑后验证用户手动维护；AlertDialog 确认删除后刷新与深链为空态。更新 E2E README。

更新中文教学文档：`Graph → typed tool → MemoryService → ledger/outbox → infer=False request-key Provider`；D-08 自动直接写与推测确认、删除/provisioning 竞争、公共 register/Mailpit/Origin/cookie/login A/B、Playwright/浏览器覆盖和常见错误（Graph 直连 ORM、semantic 去重、伪造 JWT、只查 list 不查 context）。自动化后用 Codex 内置浏览器走同一实际路径，检查成功/空态、编辑、删除确认、历史、320px 无横向溢出；不可用须记录原因。</action>
  <verify><automated>cd frontend && npm run typecheck && npm run lint && npm exec playwright test tests/e2e/agent.spec.ts</automated><human-check>Codex 内置浏览器：注册/登录 → 分析直接忌口 → 我的记忆 → 编辑 → 删除 → 刷新/深链空态；检查 320px、历史和无敏感字段。</human-check></verify>
  <done>UAT 4、5 有公开 API、Playwright 与真实浏览器证据，用户看到的只有安全来源/时间/偏好。</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-07-01 | Tampering | Graph trigger | mitigate | fresh text typed tool only; no model/vision/history auto-write. |
| T-04-07-02 | DoS | graph tool | mitigate | capture charges existing bounded budget and state marker prevents repeats. |
| T-04-07-03 | Elevation | public API | mitigate | A/B public register/Mailpit/verify/login contract; no forged principal. |
| T-04-07-04 | Information disclosure | state/SSE/H5 | mitigate | summary/DTO omit IDs, keys, score, raw input and provider body. |
</threat_model>

<source_audit>
SOURCE | ID | Coverage | Status
--- | --- | --- | ---
GOAL | Phase 4 preference usable in product | 04-05/06 + Tasks 1–3 | COVERED
REQ | MEM-01/MEM-02 | 04-01/03 existing | COVERED
REQ | MEM-03/MEM-04/MEM-05 | Tasks 1–3 | COVERED
REQ | MEM-06 | 04-03 existing + Task 2 delete retrieval regression | COVERED
RESEARCH | API→Service→Repository, browser public API | Tasks 1–3 | COVERED
CONTEXT | D-01..D-07 | existing 04-01/04 | COVERED
CONTEXT | D-08..D-17 | 04-05/06 + Tasks 1–3 | COVERED
</source_audit>

<verification>
- `cd backend && uv run pytest tests/unit/test_agent_memory_context.py -q`
- `cd backend && SMTP_HOST=127.0.0.1 SMTP_PORT=1025 CORS_ORIGINS='["http://127.0.0.1:5178"]' docker compose up -d --wait postgres-test mailpit && SMTP_HOST=127.0.0.1 SMTP_PORT=1025 CORS_ORIGINS='["http://127.0.0.1:5178"]' uv run python tests/run_pg.py --env-file .env.test.example -- uv run python -m pytest tests/integration/test_direct_memory_public_api.py -q`
- `cd frontend && npm run typecheck && npm run lint && npm exec playwright test tests/e2e/agent.spec.ts`
- Codex 内置浏览器重验 UAT 4、5。
</verification>

<success_criteria>Graph、公共认证/API 与真实 H5 共同证明“我不吃辣”可管理；越权和删除后 context 残留均被阻断。</success_criteria>
<output>Create `.planning/phases/04-meal-records-and-memory/04-07-SUMMARY.md` when done.</output>
