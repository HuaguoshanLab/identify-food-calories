---
phase: 4
plan: "05"
type: execute
wave: 4
depends_on: ["04-02", "04-03"]
files_modified:
  - backend/app/records/models.py
  - backend/migrations/versions/0009_direct_memory_provisioning.py
  - backend/migrations/README.md
  - backend/app/memory/README.md
  - backend/app/memory/ports.py
  - backend/app/memory/providers.py
  - backend/app/memory/repository.py
  - backend/app/memory/service.py
  - backend/tests/memory/test_memory_service.py
  - backend/tests/unit/test_memory_api.py
  - backend/app/README.md
autonomous: true
gap_closure: true
requirements: [MEM-03, MEM-04]
decisions_addressed: [D-08, D-09, D-11]
must_haves:
  truths:
    - "同一用户的直接偏好有可审计的 run 溯源和稳定 Provider request key；同 run 或跨 run 的重复陈述不会重复创建长期记忆。"
    - "Mem0 的直接写不触发 inference/extraction，且每个 request key 只解析为一个 canonical direct memory ID。"
    - "删除可取消尚未开始的 provisioning，且 Provider/DTO 不泄露原句、request key 或外部 ID。"
  artifacts:
    - path: backend/migrations/versions/0009_direct_memory_provisioning.py
      provides: "source-run、provision/delete 状态及 PostgreSQL 幂等约束"
      contains: "memory_provision_outbox"
    - path: backend/app/memory/providers.py
      provides: "infer=False 的 exact request-key direct-write contract"
      contains: "infer=False"
    - path: backend/tests/memory/test_memory_service.py
      provides: "直接创建、同 run 并发模型、跨 run 去重和 no-inference fake tests"
  key_links:
    - from: backend/app/memory/service.py
      to: preference_memory_ledger and memory_provision_outbox
      via: "local fact and durable intent are one transaction"
      pattern: "source_run_id"
    - from: backend/app/memory/providers.py
      to: Mem0 metadata
      via: "user-scoped exact request_key resolve before direct add"
      pattern: "request_key"
---

<objective>
定义直接偏好的持久化和 Provider 协议：本地 ledger/outbox 是权威与幂等边界，Mem0 仅以 `infer=False` 保存一条 canonical direct memory。

Purpose: UAT 缺口需要的不是另一条无键 `create()`，而是能抵御跨 run、并发、未知结果和删除竞争的可恢复写入合同。
Output: 0009 migration、直接偏好 Service/Provider contract、unit/DTO 行为证据。
</objective>

<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@backend/AGENTS.md
@backend/app/memory/service.py
@backend/app/memory/providers.py
@backend/app/memory/repository.py
@backend/app/records/models.py
@.planning/phases/04-meal-records-and-memory/04-UAT.md
@.planning/debug/phase-04-direct-memory-write.md
@.planning/phases/04-meal-records-and-memory/04-CONTEXT.md

<interfaces>
Only `MemoryService` may call the Provider. Add a direct-write contract, separate from semantic search:
```python
class MemoryProvider(Protocol):
    def resolve_direct_by_request_key(self, *, user_id: UUID, request_key: str) -> str | None: ...
    def create_direct(self, *, user_id: UUID, category: str, canonical_text: str, request_key: str) -> str: ...
```
`request_key` is the stable SHA-256 of `direct-memory.v1|user|source-run|category|canonical-text-digest`; never return or log it. `resolve` is exact, user-scoped metadata lookup, never semantic search.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 创建直接偏好账本、provision/delete 协调状态及数据库幂等约束</name>
  <files>backend/app/records/models.py, backend/migrations/versions/0009_direct_memory_provisioning.py, backend/migrations/README.md, backend/app/memory/ports.py, backend/app/memory/repository.py, backend/tests/memory/test_memory_service.py, backend/tests/unit/test_memory_api.py</files>
  <behavior>
    - Test 1: `我不吃辣` 创建一条 `avoidance/user_statement` 账本与一条 pending provision intent；公开 DTO 仍无 run ID、external ID、request key、score 或原句。
    - Test 2: 相同 user/run/category/canonical text 的并发 capture 返回同一 ledger/intent；同用户新 run 重复同文本也复用 active memory；另一个用户不复用。
    - Test 3: 删除发生在 worker claim 前时取消 pending provision intent，且不会调用 Provider；已删 ledger 永不重新 active。
  </behavior>
  <action>新增 Alembic `0009_direct_memory_provisioning`，不得改 0007/0008。给 `PreferenceMemoryLedger` 增加 nullable 内部 `source_run_id`（FK agent_runs、`ON DELETE SET NULL`）、`provisioning_status`（`pending|claimed|provisioned|outcome_unknown|failed|cancelled`）和 request-key digest；新增 `MemoryProvisionOutbox`，含 user/ledger FK、`provision_external_memory`、request key、attempt/not_before/status/audit times。给同 run 候选、active user/category/canonical text、ledger provision operation 建 PostgreSQL unique/partial-unique 约束。

扩展 deletion intent：删除事务锁定 ledger；若 provision 未 claim，原子标记它 cancelled 且不外呼；若 claim/unknown 或 remote ID 尚未 bind，则保存一个 durable deletion intent（可用 request key resolve remote ID）而非遗漏清理。Repository 的 get-or-create 以 user_id 条件、row lock、savepoint unique-race recovery 运行；不得存完整输入。先写失败的“创建/同 run 并发/跨 run/跨用户/DTO”测试，再实现。DTO 测试只验证既有 `/memories` 的安全 contract，不新增手动写 API。</action>
  <verify><automated>cd backend && uv run python tests/run_pg.py --env-file .env.test.example -- uv run alembic upgrade head && uv run pytest tests/memory/test_memory_service.py tests/unit/test_memory_api.py -q && uv run ruff check app/records/models.py app/memory/ports.py app/memory/repository.py migrations/versions/0009_direct_memory_provisioning.py tests/memory/test_memory_service.py tests/unit/test_memory_api.py</automated></verify>
  <done>migration、创建、同 run 并发、跨 run 去重、tenant 隔离及安全 DTO 均有自动化证明；未 claim 的写入可被删除取消。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现白名单提取和 infer=False 的 canonical direct Provider 合同</name>
  <files>backend/app/memory/providers.py, backend/app/memory/service.py, backend/app/memory/README.md, backend/app/README.md, backend/tests/memory/test_memory_service.py</files>
  <behavior>
    - Test 1: `我不吃辣`、`今天不想吃辣` 直接保存为 D-08 avoidance；没有明确一人称表达、视觉结果、meal parse、历史或模型猜测不能自动写。
    - Test 2: Fake Provider 记录 `infer=False`、stable key 和一条 exact canonical direct record；它不会做 inference/extraction，也不会返回多条 ID。
    - Test 3: Mem0 adapter 传 `infer=False`，结果恰好一个 canonical direct ID；零条或多条结果 fail closed。
  </behavior>
  <action>实现 versioned deterministic `explicit-preference.v1` 和 `capture_explicit_preferences(user_id, source_run_id, statement)`。只从当前用户明确目标/忌口/稳定偏好句式生成白名单 canonical text；D-08 临时直接忌口允许。模型 inference 必须继续 inactive proposal + confirm，不能用 parse/vision/history 推断替代直接陈述。

Mem0 `add` 必须使用 `infer=False`、一个 canonical user-role content、user_id 和 metadata `{category, source, request_key}`；不允许 Mem0 再抽取/拆分该用户事实。解析响应时严格要求单一 direct memory ID，0/多结果或缺 ID 是安全失败。每次 retry 先 exact-resolve metadata request key；禁止 semantic search 去重。Fake Provider 必须按 request key 映射、保存 infer flag、模拟 single-ID response，并测试没有 inference/extraction。若 endpoint 无精确 metadata resolve 能力，settings/startup fail closed，不可退化成可能重复的 create。更新 README 索引和最小数据边界。</action>
  <verify><automated>cd backend && uv run pytest tests/memory/test_memory_service.py -q && uv run ruff check app/memory/providers.py app/memory/service.py tests/memory/test_memory_service.py</automated></verify>
  <done>直接写永远是一次 infer=False canonical record；allowlist、推测确认和 Provider 结果单值性均受测试保护。</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-05-01 | Tampering | extractor | mitigate | D-08 deterministic allowlist, model/vision/history never auto-write. |
| T-04-05-02 | Repudiation | ledger/outbox | mitigate | source-run + request-key digest + DB uniqueness, no raw statement. |
| T-04-05-03 | Tampering | Mem0 direct add | mitigate | infer=False, exact metadata resolver, require exactly one ID, fail closed. |
| T-04-05-04 | Information disclosure | DTO/events | mitigate | public schemas exclude internal provenance, key, external ID and score. |
</threat_model>

<source_audit>
SOURCE | ID | Coverage | Status
--- | --- | --- | ---
GOAL | Phase 4 safe cross-session preference | 04-02/03 existing + 04-05..07 | COVERED
REQ | MEM-01/MEM-02/MEM-05 | 04-01/03/04 existing | COVERED
REQ | MEM-03/MEM-04 | 04-05 Tasks 1–2 | COVERED
REQ | MEM-06 | 04-03 existing + 04-06 delete/retrieval race proof | COVERED
RESEARCH | Provider port / fail-closed / outbox | Tasks 1–2 | COVERED
CONTEXT | D-01..D-07 | existing 04-01/04 | COVERED
CONTEXT | D-08/D-09/D-11 | Tasks 1–2 | COVERED
CONTEXT | D-10/D-12..D-17 | existing 04-02/03/04 + 04-06/07 revalidation | COVERED
</source_audit>

<verification>
- `cd backend && uv run python tests/run_pg.py --env-file .env.test.example -- uv run alembic upgrade head`
- `cd backend && uv run pytest tests/memory/test_memory_service.py tests/unit/test_memory_api.py -q`
</verification>

<success_criteria>持久化与 Provider 合同本身已证明 direct preference 的单值 infer=False、idempotency、DTO 安全性和删除前取消语义。</success_criteria>
<output>Create `.planning/phases/04-meal-records-and-memory/04-05-SUMMARY.md` when done.</output>
