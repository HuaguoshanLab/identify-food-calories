---
phase: 4
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - backend/app/retrieval/README.md
  - backend/app/retrieval/models.py
  - backend/app/retrieval/ports.py
  - backend/app/retrieval/repository.py
  - backend/app/retrieval/service.py
  - backend/app/agent/tools.py
  - backend/app/agent/graph.py
  - backend/app/agent/service.py
  - backend/migrations/versions/0008_retrieval_vectors.py
  - backend/tests/retrieval/test_retrieval_service.py
  - backend/tests/integration/test_retrieval_isolation.py
  - backend/tests/integration/test_agent_checkpoint.py
  - backend/tests/unit/test_agent_memory_context.py
  - backend/app/README.md
  - backend/tests/README.md
autonomous: true
requirements:
  - MEM-02
  - MEM-06
requirements_addressed:
  - MEM-02
  - MEM-06
decisions_addressed:
  - D-14
  - D-15
  - D-16
  - D-17
---

<objective>
实现带关系型过滤的三来源检索，并把安全偏好上下文经 Agent 工具接入，同时复核 PostgreSQL Checkpointer 的短期线程恢复。

Purpose: 让未来分析/规划可参考当前偏好、历史餐食和受控营养知识，而不让向量分数、跨用户数据或模型输出成为事实来源。
Output: pgvector retrieval 模块、migration、Agent tool adapter、隔离/recall/checkpointer 测试。
</objective>

<read_first>
- AGENTS.md
- backend/AGENTS.md
- backend/app/records/models.py
- backend/app/memory/service.py
- backend/app/agent/state.py
- backend/app/agent/tools.py
- backend/app/agent/graph.py
- backend/app/agent/service.py
- backend/scripts/setup_checkpointer.py
- backend/tests/integration/test_agent_checkpoint.py
- backend/migrations/versions/0007_meal_records_memory_ledger.py
- .planning/phases/04-meal-records-and-memory/04-CONTEXT.md
- .planning/phases/04-meal-records-and-memory/04-RESEARCH.md
</read_first>

<tasks>
<task type="execute">
  <name>Task 1: 建立 pgvector 检索数据模型与严格 tenant 过滤 repository</name>
  <read_first>backend/app/records/models.py, backend/app/nutrition/models.py, backend/migrations/versions/0007_meal_records_memory_ledger.py, backend/migrations/README.md, .planning/phases/04-meal-records-and-memory/04-RESEARCH.md</read_first>
  <files>backend/app/retrieval/README.md, backend/app/retrieval/models.py, backend/app/retrieval/ports.py, backend/app/retrieval/repository.py, backend/migrations/versions/0008_retrieval_vectors.py, backend/app/README.md</files>
  <action>创建 `app.retrieval`，为历史餐食和受控营养知识定义独立 embedding records；个人记录必须存 `user_id`、business record FK、embedding model/version、source type、active/deleted eligibility，catalog knowledge 存 food/catalog/version/eligibility metadata。migration 启用/复用 pgvector extension，建立关系型 owner/status/version 索引与仅在确有规模必要时的 vector index；第一版个人检索必须在 SQL `WHERE user_id = :user_id AND deleted_at IS NULL` 内执行并优先 exact scan，禁止先 ANN 再 Python filter。Repository 返回 source-tagged DTO，不能向上暴露 raw vector 或 similarity score。</action>
  <verify>cd backend && uv run alembic upgrade head && uv run pytest tests/integration/test_retrieval_isolation.py -q</verify>
  <acceptance_criteria>
    - personal vector query 的 SQLAlchemy statement 同时含 `user_id` 和 active/deleted predicate。
    - catalog query 同时含目录版本与可公开/可计算资格 predicate。
    - 返回 DTO 只有安全 source label、业务引用、文本摘要和时间；不含 embedding/similarity score。
    - migration 在现有 pgvector PostgreSQL 环境成功运行。
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: 组合三来源、优先当前偏好并接入 Agent 工具</name>
  <read_first>backend/app/memory/service.py, backend/app/records/service.py, backend/app/agent/tools.py, backend/app/agent/graph.py, backend/app/agent/state.py</read_first>
  <files>backend/app/retrieval/service.py, backend/app/agent/tools.py, backend/app/agent/graph.py, backend/app/agent/service.py, backend/tests/retrieval/test_retrieval_service.py, backend/tests/unit/test_agent_memory_context.py</files>
  <action>实现 `PersonalContextService.retrieve(user_id, query)`：从 MemoryService 获取仍 active 的偏好、从 records/retrieval 获取同用户历史、从 nutrition retrieval 获取受控知识；各来源独立查询和来源标注。current avoidance/goal/stable preference 必须在排序/冲突处理中优先于过往行为；历史不得自动编辑 ledger。Graph 只能通过 `agent.tools` 的窄 adapter 调用该 Service，state 仅保存安全的短依据和 source type；最终用户可见建议只可呈现“已参考你的忌口：{text}”类摘要，禁止 embedding、内部 ID、检索分数和 reasoning trace。保留 Nutrition Service 作为所有数值的唯一计算路径。</action>
  <verify>cd backend && uv run pytest tests/retrieval/test_retrieval_service.py tests/unit/test_agent_memory_context.py -q</verify>
  <acceptance_criteria>
    - 同一 query 的组合结果可区分 `preference`、`meal_history`、`nutrition_knowledge`。
    - 新的 avoidance 在与历史消费冲突时优先，历史不能调用 MemoryService update。
    - Agent graph/tool source 不 import retrieval ORM/repository，且输出不含 vector score/internal ID。
    - nutrition total tests 证明 contextual retrieval 不改变确定性 calculation/validation 结果。
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: 验证 Checkpointer 和检索隔离的真实 PostgreSQL 合同</name>
  <read_first>backend/tests/integration/test_agent_checkpoint.py, backend/scripts/setup_checkpointer.py, backend/app/retrieval/repository.py, backend/app/agent/service.py, backend/tests/integration/README.md</read_first>
  <files>backend/tests/integration/test_retrieval_isolation.py, backend/tests/integration/test_agent_checkpoint.py, backend/tests/README.md</files>
  <action>扩展真实 PostgreSQL integration tests：创建两个用户、相同/相似历史与偏好，断言所有 personal vector/history/memory context 查询只返回 owner 数据；soft-deleted meal/memory 无论 external deletion worker 状态都零召回。复用正式 `PostgresSaver` setup/reopen 路径，以同一 `thread_id` 验证 interrupt 后跨 Service/runtime 恢复，不以 in-memory checkpointer 或直接写 checkpoint 表伪造成功。将测试数据库 guard、迁移和 required checkpointer setup 写入测试 README。</action>
  <verify>cd backend && uv run pytest tests/integration/test_retrieval_isolation.py tests/integration/test_agent_checkpoint.py -q</verify>
  <acceptance_criteria>
    - 两用户检索 case 对每种个人来源均为零交叉，且已删 case 为零召回。
    - checkpointer test 在重新打开 PostgreSQL saver 后用同一 thread ID resume，而非重新跑整图。
    - 测试不读取或写入原图、base64、原始 provider response 或完整推理内容。
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation | Verification |
|---|---|---|
| 向量相似导致跨用户召回 | SQL 强制 user/status filters、分来源 retrieval、双用户 integration | zero-cross-user recall cases |
| ANN filter 导致错误/不足结果 | 小 tenant set exact query first；未来索引需显式 benchmark | source/repository assertion + recall test |
| 检索内容改写营养事实 | Graph tool 只取上下文，Nutrition Service 仍计算/校验 | deterministic totals test |
| checkpoint 被内存 fake 掩盖 | 正式 PostgresSaver setup/reopen 测试 | process/runtime reopen resume test |
</threat_model>

<must_haves>
<truths>
- 当前长期偏好、同用户历史餐食和受控营养知识分别检索并带来源边界。
- 任何个人检索都在 SQL 级别按 `user_id` 和 active 状态过滤，已删数据零召回。
- 同一 thread ID 可经真实 PostgreSQL Checkpointer 跨 runtime 恢复，检索不改变确定性营养结论。
</truths>
<artifacts>
- `backend/app/retrieval/repository.py`
- `backend/app/retrieval/service.py`
- `backend/migrations/versions/0008_retrieval_vectors.py`
- `backend/tests/integration/test_retrieval_isolation.py`
</artifacts>
</must_haves>

<verification>
- `cd backend && uv run alembic upgrade head`
- `cd backend && uv run pytest tests/retrieval/test_retrieval_service.py tests/unit/test_agent_memory_context.py tests/integration/test_retrieval_isolation.py tests/integration/test_agent_checkpoint.py -q`
- `cd backend && uv run ruff check app/retrieval app/agent/tools.py app/agent/graph.py tests/retrieval`
</verification>

<success_criteria>
- MEM-06 的三来源检索始终保留 relational filters、来源边界和删除零召回。
- MEM-02 的短期线程状态仍由真实 PostgreSQL Checkpointer 管理并可跨 runtime 恢复。
- 记忆/历史只影响安全建议上下文，不能成为营养数值真相。
</success_criteria>
