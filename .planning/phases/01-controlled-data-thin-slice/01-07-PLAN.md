---
phase: 01-controlled-data-thin-slice
plan: "07"
type: execute
wave: 4
depends_on: [01-01, 01-05, 01-06]
files_modified: [backend/app/api/v1/router.py, backend/app/api/v1/dishes.py, backend/app/api/v1/calculations.py, backend/app/api/v1/errors.py, backend/app/main.py, backend/tests/api/test_dishes.py, backend/tests/api/test_calculations.py, backend/tests/api/test_openapi_and_cors.py]
autonomous: true
requirements: [ARCH-05, CAL-01]
must_haves:
  truths: ["`/api/v1` 搜索和计算 API 调 service 而非 ORM，并返回 D-06 最小治理字段。", "OpenAPI、CORS 和错误响应可自动验证且不泄漏内部字段。"]
  artifacts:
    - path: backend/app/api/v1/dishes.py
      provides: "GET /api/v1/dishes service-only route"
    - path: backend/app/api/v1/calculations.py
      provides: "POST /api/v1/calculations service-only route"
    - path: backend/tests/api/test_openapi_and_cors.py
      provides: "versioned contract/CORS/route inventory test"
  key_links:
    - from: backend/app/api/v1/calculations.py
      to: backend/app/services/calculation_service.py
      via: "FastAPI dependency injection"
      pattern: "Depends"
---
<objective>把 domain 层接入严格版本化 FastAPI 契约。Purpose: Router 只处理 HTTP；其余做法都会破坏 D-14/D-15。Output: routes, safe error handler, CORS/OpenAPI and API contract tests.</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/01-controlled-data-thin-slice/01-CONTEXT.md
@.planning/phases/01-controlled-data-thin-slice/01-06-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add service-only versioned catalog and calculation routes</name>
  <files>backend/app/api/v1/router.py, backend/app/api/v1/dishes.py, backend/app/api/v1/calculations.py, backend/app/api/v1/errors.py, backend/tests/api/test_dishes.py, backend/tests/api/test_calculations.py</files>
  <behavior>- GET query length 1..50/limit 1..20 calls DishService and returns search items with `sourceReference`, `licenseStatus`, `dataVersion`.
- POST body `{dishId, grams}` calls CalculationService and returns kcal plus the same D-06 trio.
- unknown/invalid/ineligible cases become safe structured 4xx error envelope with no SQL/ORM/trace data.</behavior>
  <action>Write API tests first using FastAPI dependency overrides, then implement `/api/v1` router and endpoint modules. Use request/response Pydantic schemas from Plan 01-06; set response_model and public camelCase aliases. Router code must contain no `select`, Session, commit, formula, seed or policy. Error translator returns `{code,message,details?}` only. The D-06 trio is mandatory on API responses even though Phase 1 frontend will neither explicitly request nor render it. Do not add detail/auth/upload/model/multi-dish endpoints.</action>
  <verify><automated>cd backend && APP_ENV=test python -m pytest tests/api/test_dishes.py tests/api/test_calculations.py -q</automated></verify>
  <done>Both business endpoints have service-only code paths and exact safe response/error contracts including D-06 governance metadata.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Register narrow CORS and prove OpenAPI/route exposure</name>
  <files>backend/app/main.py, backend/tests/api/test_openapi_and_cors.py</files>
  <behavior>- `/docs` and `/openapi.json` remain available; OpenAPI contains only required `/api/v1/dishes` and `/api/v1/calculations` business paths and D-06 fields.
- CORSMiddleware permits only `http://localhost:5173`, explicit needed methods/headers, no credentials/wildcard, and rejects foreign origin.
- API route inventory has no multipart/upload/image/model/multi-dish route.</behavior>
  <action>Register the v1 router and project exception handler in `main.py`; configure validated CORS solely for localhost:5173, exact methods/headers and `allow_credentials=False`. Keep FastAPI docs defaults. Write contract/snapshot-style tests for OpenAPI request/response fields including sourceReference/licenseStatus/dataVersion, CORS allowed/foreign origins, error envelope, and route inventory. Never run migration/seed on startup or broaden CORS from unvalidated env input.</action>
  <verify><automated>cd backend && APP_ENV=test python -m pytest tests/api/test_openapi_and_cors.py -q</automated></verify>
  <done>OpenAPI, docs and only two business routes are publicly exposed; CORS is narrow and D-06 fields are contract-tested.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser HTTP → router | query/body are untrusted. |
| router → services | router must not gain persistence authority. |
| API response → browser | D-06 metadata is exposed but internals stay private. |
| future image/multi-dish | route inventory prevents accidental phase expansion. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-21 | Tampering | API request | mitigate | Pydantic bounds, service validation, no client nutrition inputs. |
| T-01-22 | Information Disclosure | errors/response/OpenAPI | mitigate | explicit response/error schemas, D-06 minimal trio only, contract tests exclude ORM/stack/URL. |
| T-01-23 | Spoofing / Information Disclosure | CORS | mitigate | localhost-only explicit CORS, no credentials/wildcard, preflight tests. |
| T-01-24 | Spoofing / Tampering | image/model/multi-dish | accept — out of phase | OpenAPI inventory asserts none exists; Phase 2 owns all image validation/deletion. |
| T-01-SC | Tampering | supply chain | mitigate | no install. |
</threat_model>
<verification>- API status/schema/error tests.
- OpenAPI D-06/CORS/route inventory test and manual curl to both endpoints.</verification>
<success_criteria>- strict API→Service boundary and `/api/v1` contract are proven.
- D-06 API metadata returns without exposing internals or building later-phase routes.</success_criteria>
<output>Create `.planning/phases/01-controlled-data-thin-slice/01-07-SUMMARY.md` when done.</output>
