---
phase: 01-controlled-data-thin-slice
plan: "05"
type: tdd
wave: 3
depends_on: [01-01, 01-04]
files_modified: [backend/app/services/release_policy.py, backend/app/scripts/release_preflight.py, backend/tests/unit/test_release_policy.py, backend/tests/integration/test_release_preflight.py]
autonomous: true
requirements: [DATA-04]
must_haves:
  truths: ["local demo_only 数据可被明确报告，生产 preflight 必须拒绝它。", "缺少来源、授权或推导链的服务数据不能通过生产资格检查。"]
  artifacts:
    - path: backend/app/services/release_policy.py
      provides: "repository-injected release eligibility evaluator"
    - path: backend/app/scripts/release_preflight.py
      provides: "machine-readable non-zero production gate"
  key_links:
    - from: backend/app/scripts/release_preflight.py
      to: backend/app/services/release_policy.py
      via: "manifest plus DB eligibility report"
      pattern: "production"
---
<objective>以 fake repository 单测和真实 PostgreSQL preflight 把数据许可变成硬门禁。Purpose: 数据字段不是治理；可失败的发布判定才是。Output: ReleasePolicy, preflight command and unit/integration evidence.</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/01-controlled-data-thin-slice/01-CONTEXT.md
@.planning/phases/01-controlled-data-thin-slice/01-04-SUMMARY.md</context>
<feature>
  <name>Data release eligibility policy</name>
  <files>backend/app/services/release_policy.py, backend/app/scripts/release_preflight.py, backend/tests/unit/test_release_policy.py, backend/tests/integration/test_release_preflight.py</files>
  <behavior>- Fake-repository unit tests cover a passing production-approved record and independently reject demo_only, missing sourceReference, missing licenseStatus/reference, and missing derivation chain.
- Local reports demo-only status; production returns machine-readable reasons and non-zero for violations in either manifest or DB.
- Only fully approved fixture passes production preflight.</behavior>
  <implementation>RED: write the five fake-repository cases in `tests/unit/test_release_policy.py` and run them with no database. GREEN: implement a repository protocol injected into ReleasePolicy; it alone evaluates status/provenance rules. Add the explicit module command to inspect manifest and database, emit safe JSON counts/reasons, and non-zero exit for production failures. Integration tests seed isolated PostgreSQL fixtures and prove DB plus source checks. Do not implement deployment, public endpoint, auth, image/model logic, or a cross-table CHECK pretending to replace policy.</implementation>
</feature>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| manifest/DB → release policy | unapproved metadata must fail publication. |
| policy command → CI/runbook | a release caller consumes machine-readable exit status. |
| future image/multi-dish | absent and irrelevant to this policy. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-14 | Tampering / Repudiation | data eligibility | mitigate | fake-repo unit matrix plus manifest+DB integration preflight and non-zero production exit. |
| T-01-15 | Information Disclosure | preflight report | mitigate | counts/reason codes only; no secrets/full records exposed publicly. |
| T-01-16 | Spoofing / Tampering | image/model/multi-dish | accept — out of phase | no provider/analysis path exists; Phase 2 owns it. |
| T-01-SC | Tampering | supply chain | mitigate | no package install. |
</threat_model>
<verification>- `cd backend && python -m pytest tests/unit/test_release_policy.py -q` uses fake repo only.
- `APP_ENV=test python -m pytest tests/integration/test_release_preflight.py -q` uses isolated PostgreSQL.
- production demo-only fixture exits non-zero; approved fixture passes.</verification>
<success_criteria>- all five ReleasePolicy cases are unit-tested without DB.
- production cannot pass with unresolved Phase 1 demo data or missing provenance.</success_criteria>
<output>Create `.planning/phases/01-controlled-data-thin-slice/01-05-SUMMARY.md` when done with RED/GREEN evidence.</output>
