---
phase: 01-controlled-data-thin-slice
plan: "03"
type: execute
wave: 1
depends_on: [01-01]
files_modified: [backend/app/models/base.py, backend/app/models/data_version.py, backend/app/models/dish.py, backend/app/models/dish_alias.py, backend/app/models/standard_recipe.py, backend/app/models/nutrition_record.py, backend/alembic/env.py, backend/alembic/versions/20260826_0001_initial_catalog.py, backend/tests/integration/test_catalog_schema.py]
autonomous: true
requirements: [ARCH-04, DATA-01]
must_haves:
  truths: ["空 PostgreSQL 可仅经 Alembic 重建受控 catalog 结构。", "稳定 dishId、别名、配方、营养与数据版本的关系和约束存在。"]
  artifacts:
    - path: backend/alembic/versions/20260826_0001_initial_catalog.py
      provides: "DDL-only catalog migration"
    - path: backend/tests/integration/test_catalog_schema.py
      provides: "real PostgreSQL migration/constraint tests"
  key_links:
    - from: backend/alembic/env.py
      to: backend/app/models
      via: "metadata migration parity"
      pattern: "target_metadata"
---
<objective>建立受控目录模型与仅 DDL 的 Alembic migration。Purpose: 所有可计算数据必须有可审计的 PostgreSQL 关系，而不是 JSON 直连 API。Output: models, migration and true-PostgreSQL schema evidence.</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/01-controlled-data-thin-slice/01-CONTEXT.md
@.planning/phases/01-controlled-data-thin-slice/01-RESEARCH.md
@.planning/phases/01-controlled-data-thin-slice/01-01-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define catalog models and PostgreSQL invariants</name>
  <files>backend/app/models/base.py, backend/app/models/data_version.py, backend/app/models/dish.py, backend/app/models/dish_alias.py, backend/app/models/standard_recipe.py, backend/app/models/nutrition_record.py</files>
  <behavior>- public dishId is immutable/unique while surrogate keys remain private.
- normalized aliases are unique; grams and kcal values are positive Decimal/Numeric; nutrition references version and recipe relations.
- DataVersion status permits only `demo_only` or `production_approved`.</behavior>
  <action>Write declarative SQLAlchemy 2 mappings for the controlled relation graph per D-09/D-10. Keep one Phase-1 standard recipe relation while allowing later multiplicity. Use PostgreSQL NUMERIC/Decimal, foreign keys, unique/check/index constraints. Do not make ORM models public API schemas, add analysis/image tables, or embed business rows.</action>
  <verify><automated>cd backend && APP_ENV=test python -m pytest tests/integration/test_catalog_schema.py -q</automated></verify>
  <done>The model graph represents dish, alias, standard recipe, nutrition and release-version provenance with database invariants.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Write and verify the reviewed DDL-only initial migration</name>
  <files>backend/alembic/env.py, backend/alembic/versions/20260826_0001_initial_catalog.py, backend/tests/integration/test_catalog_schema.py</files>
  <behavior>- `APP_ENV=test alembic upgrade head` creates all five catalog tables in the isolated test DB.
- duplicate/negative fixtures are rejected and the revision contains no catalog inserts.
- `alembic check` detects metadata/revision drift.</behavior>
  <action>First make the schema integration tests fail against the empty test DB, then manually review an immutable initial Alembic revision containing DDL/index/constraint only. Configure env.py to select TEST_DATABASE_URL only for `APP_ENV=test`; never select dev URL in test mode. Test fresh upgrade, constraints, and no startup `create_all`/migration/seed. Do not use SQLite.</action>
  <verify><automated>cd backend && APP_ENV=test alembic upgrade head && APP_ENV=test python -m pytest tests/integration/test_catalog_schema.py -q && alembic check</automated></verify>
  <done>An empty isolated PostgreSQL DB gains the complete schema exclusively through a checked Alembic revision.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Alembic/models → PostgreSQL | migrations define durable catalog invariants. |
| future API → model | ORM fields must stay internal. |
| future image/multi-dish → catalog | no writer consumes images/model output in this phase. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-08 | Tampering | schema/migration | mitigate | reviewed DDL-only revision, FK/unique/check constraints, Alembic check and real PostgreSQL tests. |
| T-01-09 | Information Disclosure | ORM | mitigate | API schemas are deferred and models are never response types. |
| T-01-10 | Spoofing / Tampering | image/multi-dish | accept — out of phase | no HTTP/image/model schema or catalog writer; Phase 2 owns validation. |
| T-01-SC | Tampering | dependencies | mitigate | no new package beyond Plan 01 pinned environment. |
</threat_model>
<verification>- fresh test DB migration and `alembic check`.
- PostgreSQL-only constraint suite.</verification>
<success_criteria>- catalog schema is reproducible from empty test PostgreSQL through DDL-only migration.
- no seed, API, frontend or later-phase table is introduced.</success_criteria>
<output>Create `.planning/phases/01-controlled-data-thin-slice/01-03-SUMMARY.md` when done.</output>
