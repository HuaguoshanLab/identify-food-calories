---
phase: 01-controlled-data-thin-slice
plan: "06"
type: tdd
wave: 3
depends_on: [01-01, 01-04]
files_modified: [backend/app/schemas/common.py, backend/app/schemas/dishes.py, backend/app/schemas/calculations.py, backend/app/schemas/errors.py, backend/app/repositories/dish_repository.py, backend/app/services/dish_service.py, backend/app/services/calculation_service.py, backend/tests/integration/test_dish_repository.py, backend/tests/unit/test_dish_service.py, backend/tests/unit/test_calculation_service.py]
autonomous: true
requirements: [CAL-01, DATA-01]
must_haves:
  truths: ["受支持 dishId 与克数由唯一后端 Decimal 公式计算 kcal。", "中文标准名/别名搜索只返回可计算、受控数据和 D-06 最小治理字段。"]
  artifacts:
    - path: backend/app/repositories/dish_repository.py
      provides: "sole SQLAlchemy search/calculable-dish query boundary"
    - path: backend/app/services/calculation_service.py
      provides: "sole deterministic calorie authority"
  key_links:
    - from: backend/app/services/calculation_service.py
      to: backend/app/repositories/dish_repository.py
      via: "repository protocol controlled record lookup"
      pattern: "get_calculable_dish"
---
<objective>实现可测试的 domain/repository 层：可搜索受控菜品及唯一确定性 kcal 规则。Purpose: API 层不能绕过这一边界，浏览器也不能复制公式。Output: DTOs, repository, services, unit and real PostgreSQL integration tests.</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/01-controlled-data-thin-slice/01-CONTEXT.md
@.planning/phases/01-controlled-data-thin-slice/01-04-SUMMARY.md</context>
<feature>
  <name>Governed dish search and deterministic calculation domain layer</name>
  <files>backend/app/schemas/common.py, backend/app/schemas/dishes.py, backend/app/schemas/calculations.py, backend/app/schemas/errors.py, backend/app/repositories/dish_repository.py, backend/app/services/dish_service.py, backend/app/services/calculation_service.py, backend/tests/integration/test_dish_repository.py, backend/tests/unit/test_dish_service.py, backend/tests/unit/test_calculation_service.py</files>
  <behavior>- Fake-repository service tests cover positive bounded Decimal grams, fixed `ROUND_HALF_UP` kcal, unknown/ineligible dish, and rejection of client kcal/per100/source/license/model/image fields.
- Search fake tests map standard name/alias to public DTO including `dishId`, `typicalServingGrams`, `dataStatus`, `sourceReference`, `licenseStatus`, and `dataVersion`; calculation response includes the same three D-06 governance fields.
- Independent true PostgreSQL repository tests seed an isolated DB and assert Chinese standard-name search, alias fragment `宫保`, stable public dishId, and exclusion of non-calculable/ineligible nutrition records.</behavior>
  <implementation>RED: create fake-repository service tests and a separate PostgreSQL `test_dish_repository.py`; no service test imports a Session. GREEN: define separate Pydantic DTOs with explicit camelCase aliases; repository alone issues SQLAlchemy queries `search_by_normalized_text` and `get_calculable_dish`. It must load the eligible nutrition/version relation and filter unavailable/ineligible records. Services accept repository protocols, own business validation and Decimal half-up formula only. Return D-06's minimal API governance fields from controlled DB records; they are public API metadata, not ORM serialization. Do not create routers, direct SQL in services, client formula, multi-dish/image/interval/history behavior.</implementation>
</feature>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| repository → PostgreSQL | only repository can issue SQL. |
| service → API DTO | governed DB fields are deliberately narrowed into D-06 public metadata. |
| client inputs → service | dishId/grams and forbidden nutrition fields are untrusted. |
| future image/multi-dish | no source exists. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-17 | Injection | dish search | mitigate | bounded/normalized input, parameterized SQLAlchemy repository, quote-like integration test. |
| T-01-18 | Tampering | kcal input | mitigate | service accepts governed repository data only and rejects client nutrition/model fields; fake-repo tests. |
| T-01-19 | Information Disclosure | DTO mapping | mitigate | explicit schemas expose D-06 trio only, never ORM PK/relations/DB URL; API plan contract test verifies. |
| T-01-20 | Spoofing / Tampering | image/model/multi-dish | accept — out of phase | no multipart/model/array analysis contract. |
| T-01-SC | Tampering | supply chain | mitigate | no install. |
</threat_model>
<verification>- fake-repository-only `test_dish_service.py` and `test_calculation_service.py`.
- dedicated `APP_ENV=test pytest tests/integration/test_dish_repository.py -q` against PostgreSQL, never SQLite.</verification>
<success_criteria>- one service owns calculation; repository is the only SQL boundary.
- D-06 governance trio is returned from DB-derived DTOs; search eligibility is independently PostgreSQL-proven.</success_criteria>
<output>Create `.planning/phases/01-controlled-data-thin-slice/01-06-SUMMARY.md` when done with TDD commits.</output>
