---
phase: 01-controlled-data-thin-slice
plan: "04"
type: execute
wave: 2
depends_on: [01-01, 01-03]
files_modified: [backend/data/catalog/v1.json, backend/app/repositories/catalog_repository.py, backend/app/scripts/seed_catalog.py, backend/tests/integration/test_seed_catalog.py]
autonomous: true
requirements: [DATA-01]
must_haves:
  truths: ["显式种子命令可从版本化 manifest 事务性导入 D-08 的 15 菜并完成约100道 demo_only 开发目录。", "错误 manifest 不会留下部分数据，重复导入幂等。"]
  artifacts:
    - path: backend/data/catalog/v1.json
      provides: "approximately-100 demo_only catalog with source/license/derivation/version fields"
    - path: backend/app/scripts/seed_catalog.py
      provides: "explicit transactional seed command"
  key_links:
    - from: backend/app/scripts/seed_catalog.py
      to: backend/data/catalog/v1.json
      via: "validated transactional upsert"
      pattern: "v1.json"
---
<objective>以显式、可重放、事务安全的 manifest seed 建立本地约100菜目录。Purpose: 先验证 15 菜 schema，再完成明确 demo_only 开发数据，不伪称商业授权关闭。Output: manifest, seed repository/command and PostgreSQL integration tests.</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/01-controlled-data-thin-slice/01-CONTEXT.md
@.planning/phases/01-controlled-data-thin-slice/01-03-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Author a governed demo-only approximately-100 dish manifest</name>
  <files>backend/data/catalog/v1.json, backend/tests/integration/test_seed_catalog.py</files>
  <behavior>- manifest contains all D-08 15 dishes and approximately 100 unique stable dishIds.
- every record has aliases, typical grams, one standard recipe, Decimal-compatible kcal/100g, sourceReference, licenseStatus/reference, derivation chain, dataVersion and `demo_only` status.
- no record claims unresolved commercial approval.</behavior>
  <action>Write manifest-validation tests first. Create `v1.json` with D-08 values first, then expand to approximately 100 entries. Every Phase 1 development entry is `demo_only`; source/license/derivation fields are retained but cannot be described as production approved. No scraped/copyright-uncertain data may be mislabelled. Do not create a multi-recipe behavior or API-facing copy of the catalog.</action>
  <verify><automated>cd backend && APP_ENV=test python -m pytest tests/integration/test_seed_catalog.py -q</automated></verify>
  <done>Manifest has traceable development metadata, fixed stable IDs, all required 15 dishes, and an approximately-100 final count.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement idempotent transactional manifest seeding</name>
  <files>backend/app/repositories/catalog_repository.py, backend/app/scripts/seed_catalog.py, backend/tests/integration/test_seed_catalog.py</files>
  <behavior>- valid manifest upserts all rows in one transaction and a second run preserves count/IDs.
- a malformed, duplicate or incomplete row causes full rollback and a safe non-sensitive report.
- only explicit `python -m app.scripts.seed_catalog --source data/catalog/v1.json` writes catalog data.</behavior>
  <action>Implement manifest validation and repository intent method `upsert_catalog`; normalize aliases deterministically and upsert by stable external ID in one transaction. The module command emits count/status summary and rolls back every row on one failure. Keep migration and FastAPI startup DDL/DML-free. Use the isolated PostgreSQL fixture and never SQLite.</action>
  <verify><automated>cd backend && APP_ENV=test python -m pytest tests/integration/test_seed_catalog.py -q && APP_ENV=test python -m app.scripts.seed_catalog --source data/catalog/v1.json</automated></verify>
  <done>Seed is explicit, idempotent, atomic and produces governed demo-only local data.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| versioned manifest → seed | source data becomes governed DB records. |
| seed → test PostgreSQL | batch writer has database authority. |
| future image/model data → catalog | not accepted as a catalog writer. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-11 | Tampering | manifest/seed | mitigate | strict validation, stable-ID upsert, atomic rollback, idempotency/invalid-row tests. |
| T-01-12 | Repudiation | provenance | mitigate | retain sourceReference/licenseStatus/derivation/dataVersion and mark all development values demo_only. |
| T-01-13 | Spoofing / Tampering | images/model/multi-dish | accept — out of phase | only checked manifest feeds seed; no HTTP/model writer exists. |
| T-01-SC | Tampering | dependencies | mitigate | no added packages. |
</threat_model>
<verification>- true PostgreSQL seed suite; seed twice; malformed fixture proves rollback.</verification>
<success_criteria>- about 100 governed demo_only dishes are imported only by explicit command.
- no production approval claim, automatic seed, or image/model path exists.</success_criteria>
<output>Create `.planning/phases/01-controlled-data-thin-slice/01-04-SUMMARY.md` when done.</output>
