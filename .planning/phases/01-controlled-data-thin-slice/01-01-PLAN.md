---
phase: 01-controlled-data-thin-slice
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - compose.yaml
  - frontend/.env.example
  - frontend/components.json
  - frontend/package.json
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/label.tsx
  - frontend/src/components/ui/alert.tsx
  - frontend/src/components/ui/combobox.tsx
  - backend/.env.example
  - backend/pyproject.toml
  - backend/alembic.ini
  - backend/app/main.py
  - backend/app/core/config.py
  - backend/app/core/database.py
  - backend/tests/conftest.py
autonomous: false
requirements: [ARCH-01, ARCH-03, ARCH-06]
user_setup: []
must_haves:
  truths:
    - "开发者可只用 Docker Compose 启动健康的 PostgreSQL，而前端和后端保留各自的启动命令和日志。"
    - "前端由官方 shadcn Vite + Base UI CLI 生成基础组件，不存在 Headless UI。"
    - "真实环境变量不会被提交，且开发配置不能把浏览器来源放宽到任意站点。"
  artifacts:
    - path: compose.yaml
      provides: "仅 PostgreSQL 的本地服务、固定 5432 端口和健康检查"
    - path: frontend/components.json
      provides: "官方 shadcn Vite/Base UI 初始化记录"
    - path: backend/pyproject.toml
      provides: "锁定的 FastAPI、SQLAlchemy、Alembic、Psycopg 和 pytest 工具链"
  key_links:
    - from: compose.yaml
      to: backend/.env.example
      via: "DATABASE_URL 指向 db:5432 / localhost:5432 的明确配置"
      pattern: "postgresql\\+psycopg"
    - from: frontend/components.json
      to: frontend/src/components/ui
      via: "官方 Base UI registry 生成的 primitives"
      pattern: "base"
---

<objective>
建立 Phase 1 的可审计开发骨架和受供应链验证保护的官方 Base UI 初始化路径。

Purpose: 让后续真实数据库、API 和 UI 可以在同一套独立项目、端口、配置与测试工具上实现，而不引入 Next.js、Headless UI 或隐式启动副作用。
Output: Docker PostgreSQL、独立前后端工具链、受控环境变量、FastAPI 最小应用骨架、官方 shadcn Base UI primitives。
</objective>

<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-controlled-data-thin-slice/01-CONTEXT.md
@.planning/phases/01-controlled-data-thin-slice/01-RESEARCH.md
@.planning/phases/01-controlled-data-thin-slice/01-UI-SPEC.md
@.planning/phases/01-controlled-data-thin-slice/PATTERNS.md
@.planning/phases/01-controlled-data-thin-slice/01-SOURCE-AUDIT.md

<interfaces>
The client-visible configuration contract created here is only `VITE_API_BASE_URL=http://localhost:8000`. The backend accepts `DATABASE_URL`, `TEST_DATABASE_URL`, `APP_ENV`, and a validated CORS origin list from its own environment. There is no image, model-provider, or public secret configuration in Phase 1.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Nothing is installed before this gate. The Phase 1 research audit marks `vitest` as SUS and does not audit the required official `shadcn` CLI or `lucide-react` package.</what-built>
  <how-to-verify>
    1. Verify `vitest` is the package documented at vitest.dev and exactly matches the npm package name `vitest`.
    2. Verify the official shadcn documentation identifies the `shadcn` CLI and its Vite + Base UI initialization path; verify `lucide-react` is the official Lucide React package.
    3. Confirm the executor may install only the pinned direct dependencies in `01-RESEARCH.md`, plus the official shadcn-generated Base UI dependencies recorded in the lockfile. Do not approve third-party registries or `@headlessui/react`.
  </how-to-verify>
  <resume-signal>Type "approved package sources" or identify the package that must be replaced.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Create isolated local runtime, configuration, and backend test scaffold</name>
  <files>.gitignore, compose.yaml, frontend/.env.example, backend/.env.example, backend/pyproject.toml, backend/alembic.ini, backend/app/__init__.py, backend/app/main.py, backend/app/core/__init__.py, backend/app/core/config.py, backend/app/core/database.py, backend/tests/__init__.py, backend/tests/conftest.py</files>
  <action>After the blocking package-source approval, create independent `frontend/` and `backend/` projects per D-19 and D-21–D-27. Add `.gitignore` entries for every real `.env`, Python virtual environment/cache, Node modules, Playwright output, coverage, and local database artifacts while retaining both `.env.example` files. Create `compose.yaml` with only a `db` PostgreSQL service, named/explicit data volume, port `5432:5432`, non-secret development defaults supplied without committing a real `.env`, and a `pg_isready` healthcheck; do not add pgAdmin, frontend, backend, image storage, queues, or model services. Configure `backend/pyproject.toml` for Python 3.11+, pinned approved runtime/test tooling, pytest markers for unit/integration/api, and lint/format commands selected under discretion. Create a settings object as the sole reader of `DATABASE_URL`, `TEST_DATABASE_URL`, `APP_ENV`, and CORS values; reject a missing test URL or equality between test and development database URLs in test setup. Create the SQLAlchemy synchronous engine/session dependency only; `main.py` must build the FastAPI app but must not call `create_all`, Alembic, seed code, image code, or external services. Keep the app un-routed until Plan 03, so this task cannot fabricate the product path.</action>
  <verify>
    <automated>docker compose config --quiet && cd backend && python -m pytest -q</automated>
  </verify>
  <done>Compose defines exactly one database service with a healthcheck; backend config fails safely on missing/unsafe database test configuration; FastAPI construction imports without schema or seed side effects; real secret files are ignored.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Vite and official shadcn Base UI primitives without a second primitive system</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/index.html, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/index.css, frontend/src/lib/utils.ts, frontend/components.json, frontend/src/components/ui/button.tsx, frontend/src/components/ui/card.tsx, frontend/src/components/ui/input.tsx, frontend/src/components/ui/label.tsx, frontend/src/components/ui/alert.tsx, frontend/src/components/ui/combobox.tsx</files>
  <action>Inside `frontend/`, use the official Vite React TypeScript initializer, then run exactly `npx shadcn@latest init --template vite --base base` and select the UI-SPEC-locked Slate, Inter/System sans, Lucide, 8px radius, light-only choices. Add only the official Base UI registry blocks `button`, `card`, `input`, `label`, `alert`, and `combobox`; commit the generated `components.json` and generated files rather than hand-writing fake shadcn configuration. Install the approved React, React Router, TanStack Query, Tailwind/Vite, Testing Library, Vitest, Playwright, and Lucide dependencies at researched pinned versions and commit the lockfile. Set `main.tsx` as the sole React mount and QueryClient provider, and `App.tsx` as only the Router shell; do not add calculator behavior, API fetches, a native select, `@headlessui/react`, dark mode, image handling, kcal formulas, or source/license UI. Configure Vite for the fixed 5173 development port and a test environment compatible with later Testing Library tests.</action>
  <verify>
    <automated>cd frontend && npm run build && npm run test -- --run && test -f components.json && ! npm ls @headlessui/react</automated>
  </verify>
  <done>The Vite frontend builds at the approved port, uses official committed shadcn Base UI primitives and no Headless UI dependency, and exposes only `VITE_API_BASE_URL` in its example configuration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → package registries | Package names and generated dependencies enter the repository. |
| browser origin → future FastAPI | Browser configuration may later access the local API; origin policy is established now. |
| local environment → Compose/PostgreSQL | Credentials, ports, and test databases cross from developer configuration to the database. |
| future image/multidish workflows | These are deliberately absent: no upload route/form, file parser, image storage, vision provider, or multi-item payload exists in Phase 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm package installation | mitigate | Blocking human verification for SUS/missing-audit packages; use only official shadcn registry, lockfile, pinned direct packages, and reject third-party blocks. |
| T-01-02 | Information Disclosure | `.env`, frontend build variables | mitigate | Ignore real env files; expose only `VITE_API_BASE_URL`; backend owns all DB settings and tests verify unsafe settings fail. |
| T-01-03 | Elevation of Privilege | PostgreSQL local service | mitigate | Compose runs only the DB with local port and no privileged GUI/admin sidecar; separate test URL prevents tests from operating on development data. |
| T-01-04 | Information Disclosure | CORS bootstrap | mitigate | Settings allowlist is initialized for only `http://localhost:5173`; Plan 03 adds middleware and an automated rejection test for wildcard/foreign origin. |
| T-01-05 | Spoofing / Tampering | image/upload or multi-dish attack surface | accept — out of phase | No file input, multipart endpoint, image decoder, model client, object storage, or multi-dish request exists in this deliverable; Phase 2 owns validation/deletion/recognition threats. Plan 03 API-route inventory test proves Phase 1 exposes only catalog/calculation routes. |
| T-01-SC | Tampering | package install supply chain | mitigate | Preserve this blocking human checkpoint for `[SUS]`/un-audited direct packages; no automatic approval. |
</threat_model>

<verification>
- `docker compose config --quiet`
- `docker compose up -d db` followed by `docker compose ps db` reporting healthy
- `cd backend && python -m pytest -q`
- `cd frontend && npm run build && npm run test -- --run && ! npm ls @headlessui/react`
</verification>

<success_criteria>
- Docker Compose starts only PostgreSQL on 5432 and reports a healthcheck.
- Independent frontend/backend configuration, test runners, lockfiles, and example environment contracts exist.
- Official shadcn Vite + Base UI configuration is real and committed; no Next.js or Headless UI appears.
- No automatic migrations, seed, external image/model handling, or kcal behavior exists yet.
</success_criteria>

<output>
Create `.planning/phases/01-controlled-data-thin-slice/01-01-SUMMARY.md` when done.
</output>
