---
phase: 01-controlled-data-thin-slice
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified: [.gitignore, compose.yaml, backend/.env.example, backend/pyproject.toml, backend/app/core/config.py, backend/app/core/database.py, backend/tests/conftest.py, backend/scripts/create_test_database.sh]
autonomous: true
requirements: [ARCH-01, ARCH-03, ARCH-06]
user_setup: []
must_haves:
  truths:
    - "开发者可用 Python 3.11 venv 和 Docker Compose 的 PostgreSQL 启动后端环境。"
    - "测试只能访问独立 `_test` PostgreSQL 数据库；任何 dev/prod/SQLite/缺失 URL 都在 collection 前失败。"
  artifacts:
    - path: compose.yaml
      provides: "only PostgreSQL service, healthy dev database"
    - path: backend/scripts/create_test_database.sh
      provides: "idempotent explicit creation of food_calories_test"
    - path: backend/tests/conftest.py
      provides: "fail-closed test database gate and transaction fixture"
  key_links:
    - from: backend/tests/conftest.py
      to: backend/.env.example
      via: "APP_ENV=test plus TEST_DATABASE_URL ending _test"
      pattern: "_test"
---
<objective>建立 Python 3.11、DB-only Compose 和 fail-closed 测试库前置。Purpose: 不允许所谓集成测试污染开发库或偷换 SQLite。Output: venv/tooling contract, isolated test DB command, settings/session/test fixture guards.</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/01-controlled-data-thin-slice/01-CONTEXT.md
@.planning/phases/01-controlled-data-thin-slice/01-RESEARCH.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Define Python 3.11 and fail-closed database configuration</name>
  <files>backend/pyproject.toml, backend/.env.example, backend/app/core/config.py, backend/app/core/database.py, backend/tests/conftest.py</files>
  <action>Declare Python `>=3.11,<3.13` and pinned approved dependencies. Show distinct Psycopg URLs for `food_calories_dev` and `food_calories_test`; normal runtime uses `APP_ENV=local`, tests require `APP_ENV=test`. Settings and test setup must reject missing TEST_DATABASE_URL, any SQLite/non-`postgresql+psycopg` scheme, database name not ending `_test`, non-test APP_ENV, or normalized DATABASE_URL equal to TEST_DATABASE_URL. `database.py` uses DATABASE_URL only for the application; `conftest.py` creates a separate test engine only after guards pass, applies `APP_ENV=test alembic upgrade head`, and wraps each test in outer transaction/savepoint rollback. No startup migration, seed, database creation, or fallback.</action>
  <verify><automated>cd backend && python3.11 -m venv .venv && . .venv/bin/activate && python -m pip install --upgrade pip && python -m pip install -e '.[dev]' && APP_ENV=test TEST_DATABASE_URL='sqlite:///bad.db' python -m pytest tests -q; test $? -ne 0</automated></verify>
  <done>Python 3.11 venv is explicit and every invalid test target fails before integration tests can run.</done>
</task>
<task type="auto">
  <name>Task 2: Provide DB-only Compose and explicit isolated test database creation</name>
  <files>.gitignore, compose.yaml, backend/scripts/create_test_database.sh</files>
  <action>Create exactly one Compose `db` PostgreSQL service with a volume, `food_calories_dev`, localhost 5432 and `pg_isready` healthcheck. Add a non-destructive idempotent command using `docker compose exec -T db psql` against the maintenance database to create `food_calories_test` only if absent; never drop a DB. Ignore real env files, venv/caches/node modules/coverage/browser outputs. Required order is `docker compose up -d db`, `backend/scripts/create_test_database.sh`, then test migration; do not add pgAdmin, service containers, queues, image storage or model services.</action>
  <verify><automated>docker compose config --quiet && docker compose up -d db && backend/scripts/create_test_database.sh && backend/scripts/create_test_database.sh && docker compose exec -T db psql -U postgres -d postgres -tAc "SELECT datname FROM pg_database WHERE datname = 'food_calories_test'" | rg '^food_calories_test$'</automated></verify>
  <done>One healthy PostgreSQL service and an idempotent dedicated `_test` database exist without modifying the development database.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| developer environment → settings | URLs select the target database. |
| fixture → PostgreSQL | tests obtain write access only to an isolated test DB. |
| future image/multi-dish input | no parser, route, UI or storage exists in this plan. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Tampering | test DB selection | mitigate | fail closed on URL/equality/scheme/name/APP_ENV and test negative cases. |
| T-01-02 | Information Disclosure | environment | mitigate | real `.env` ignored; no secrets/URLs logged. |
| T-01-03 | Elevation of Privilege | Compose DB | mitigate | DB-only local service and non-destructive explicit test DB creation. |
| T-01-04 | Spoofing / Tampering | image/upload/multi-dish | accept — out of phase | no multipart route, decoder, model client or multi-item contract; Phase 2 owns it. |
| T-01-SC | Tampering | supply chain | mitigate | only pinned researched Python dependencies in isolated venv; frontend gate is Plan 01-02. |
</threat_model>
<verification>- `python3.11 -m venv .venv`, `python --version` is 3.11.x.
- create the test DB twice and prove dev/test DB names differ.
- exercise all invalid URL/environment cases.</verification>
<success_criteria>- Python 3.11, Compose DB, explicit `*_test` creation and test guard are reproducible.
- Dev/prod/SQLite database usage cannot silently enter tests.</success_criteria>
<output>Create `.planning/phases/01-controlled-data-thin-slice/01-01-SUMMARY.md` when done.</output>
