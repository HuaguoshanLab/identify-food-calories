---
phase: 01-controlled-data-thin-slice
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified: [frontend/.env.example, frontend/package.json, frontend/package-lock.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/index.html, frontend/components.json, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/index.css, frontend/src/lib/utils.ts, frontend/src/components/ui/button.tsx, frontend/src/components/ui/card.tsx, frontend/src/components/ui/input.tsx, frontend/src/components/ui/label.tsx]
autonomous: false
requirements: [ARCH-01, ARCH-02]
user_setup: []
must_haves:
  truths: ["前端是独立 React/TypeScript/Vite 项目，固定 5173 且只暴露公开 API base URL。", "官方 shadcn Vite + Base UI 配置真实存在，未引入 Headless UI。"]
  artifacts:
    - path: frontend/components.json
      provides: "official shadcn Vite/Base UI initialization"
    - path: frontend/package-lock.json
      provides: "reviewed frontend dependency lockfile"
  key_links:
    - from: frontend/src/main.tsx
      to: frontend/src/App.tsx
      via: "sole React mount and QueryClient provider"
      pattern: "QueryClientProvider"
---
<objective>初始化独立 Vite 前端和唯一允许的 Base UI primitive 系统。Purpose: 后续 UI 使用真实工具链，不伪造 shadcn 配置。Output: Vite scaffold, official primitives, locked packages.</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/01-controlled-data-thin-slice/01-UI-SPEC.md
@.planning/phases/01-controlled-data-thin-slice/01-RESEARCH.md</context>
<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>No package is installed yet. Vitest is SUS; shadcn CLI and lucide-react lack audit rows.</what-built>
  <how-to-verify>Verify official Vitest, `shadcn` Vite + Base UI documentation/package, and `lucide-react`; approve only pinned direct dependencies, official registry output, and no Headless UI/third-party registries.</how-to-verify>
  <resume-signal>Type "approved package sources" or name the package to replace.</resume-signal>
</task>
<task type="auto">
  <name>Task 1: Initialize the Vite React shell and public configuration</name>
  <files>frontend/.env.example, frontend/package.json, frontend/package-lock.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/index.html, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/index.css</files>
  <action>After approval, initialize React TypeScript Vite and install only approved pinned React, Router, TanStack Query, Tailwind/Vite, Lucide and test tooling; commit lockfile. Configure 5173, `VITE_API_BASE_URL=http://localhost:8000`, a sole React mount/QueryClient provider and Router shell. This task deliberately runs only `npm run build`: there are no frontend test source files until Plan 01-08, so an empty Vitest invocation would be fabricated evidence rather than validation. Do not add calculator/API behavior, native select, Headless UI, dark mode, kcal, images or source/license UI.</action>
  <verify><automated>cd frontend && npm run build && ! npm ls @headlessui/react</automated></verify>
  <done>Separate Vite frontend builds on 5173, exposes only public API configuration, and has no product behavior.</done>
</task>
<task type="auto">
  <name>Task 2: Generate the first official Base UI primitives</name>
  <files>frontend/components.json, frontend/src/lib/utils.ts, frontend/src/components/ui/button.tsx, frontend/src/components/ui/card.tsx, frontend/src/components/ui/input.tsx, frontend/src/components/ui/label.tsx</files>
  <action>Run exactly `npx shadcn@latest init --template vite --base base` with Slate, Inter/System, Lucide, 8px radius and light-only, then use the official Base UI registry to add `button`, `card`, `input`, and `label`. Commit the generated `components.json` and primitives without hand-writing a fake registry configuration. Later plans add alert/combobox. Reject every third-party registry and `@headlessui/react`.</action>
  <verify><automated>cd frontend && test -f components.json && npm run build && ! npm ls @headlessui/react</automated></verify>
  <done>Official Base UI configuration and first generated primitives exist with no competing primitive system.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| developer shell → npm | CLI/dependencies enter lockfile. |
| future browser → API | only public base URL is exposed. |
| future image/multi-dish input | no UI/transport exists. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-05 | Tampering | npm/shadcn | mitigate | blocking source verification, official registry, pinned lockfile, reject Headless/third-party blocks. |
| T-01-06 | Information Disclosure | Vite environment | mitigate | `VITE_API_BASE_URL` only; no secrets/provider keys. |
| T-01-07 | Spoofing / Tampering | image/multi-dish | accept — out of phase | no file input/multipart/model/UI state; Phase 2 owns it. |
| T-01-SC | Tampering | supply chain | mitigate | human gate cannot auto-advance. |
</threat_model>
<verification>- `npm run build`; Vitest suite; no Headless dependency.
- inspect CLI-generated `components.json`.</verification>
<success_criteria>- independent Vite/Base UI frontend is real and limited to approved public config.
- no product flow or later-phase surface exists.</success_criteria>
<output>Create `.planning/phases/01-controlled-data-thin-slice/01-02-SUMMARY.md` when done.</output>
