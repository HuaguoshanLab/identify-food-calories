---
phase: 01-controlled-data-thin-slice
plan: "08"
type: execute
wave: 5
depends_on: [01-02, 01-07]
files_modified: [frontend/src/api/http.ts, frontend/src/api/types.ts, frontend/src/api/dishes.ts, frontend/src/api/calculations.ts, frontend/src/features/calculator/use-dish-search.ts, frontend/src/features/calculator/use-calculation.ts, frontend/src/routes/calculator-page.tsx, frontend/src/features/calculator/DishSearchCombobox.tsx, frontend/src/features/calculator/GramsField.tsx, frontend/src/features/calculator/CalculationResult.tsx, frontend/src/features/calculator/CalculatorForm.tsx, frontend/src/features/calculator/CalculatorForm.test.tsx, frontend/src/components/ui/alert.tsx, frontend/src/components/ui/combobox.tsx, frontend/src/test/setup.ts]
autonomous: true
requirements: [ARCH-02]
must_haves:
  truths: ["用户可搜索标准名/别名、得到默认克数、局部修改并明确提交计算。", "前端只显示后端 kcal，既不请求治理字段也不渲染它们。"]
  artifacts:
    - path: frontend/src/features/calculator/CalculatorForm.tsx
      provides: "single-dish explicit-submit UI state owner"
    - path: frontend/src/api/http.ts
      provides: "safe typed API boundary that discards unrendered governance metadata"
  key_links:
    - from: frontend/src/features/calculator/use-calculation.ts
      to: frontend/src/api/calculations.ts
      via: "explicit submit mutation"
      pattern: "useMutation"
---
<objective>实现只消费用户可见字段的移动端单菜计算 UI。Purpose: API D-06 数据治理字段必须存在，但第一阶段 UI 不应把它们变成产品内容。Output: typed transport, hooks, Base UI calculator and component tests.</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/01-controlled-data-thin-slice/01-UI-SPEC.md
@.planning/phases/01-controlled-data-thin-slice/01-07-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the frontend transport boundary without governance-field UI consumption</name>
  <files>frontend/src/api/http.ts, frontend/src/api/types.ts, frontend/src/api/dishes.ts, frontend/src/api/calculations.ts, frontend/src/features/calculator/use-dish-search.ts, frontend/src/features/calculator/use-calculation.ts, frontend/src/test/setup.ts</files>
  <behavior>- GET/POST endpoints are called through the configured API base URL; POST contains only dishId/grams and only mutation invocation sends it.
- wire JSON includes D-06 fields, but client-facing UI DTO/parser intentionally selects only name/alias/default grams/status and calculation dish/grams/kcal; no query parameter asks for governance data.
- non-2xx becomes safe typed `{code,message,details?}` and search/mutation states remain separate.</behavior>
  <action>Write mocked transport/hook tests first. Define wire schemas that recognize API D-06 fields, then map them to narrower UI DTOs that omit sourceReference/licenseStatus/dataVersion; this honors D-06 without requesting/rendering them. Implement the only fetch functions and debounced (250ms max) search hook plus explicit mutation hook. Do not write kcal formula, copy catalog data, POST on grams edit, or add image/multi-dish traffic.</action>
  <verify><automated>cd frontend && npm run test -- --run src/features/calculator/CalculatorForm.test.tsx && npm run typecheck</automated></verify>
  <done>Transport consumes published API safely while UI state cannot display D-06 fields or compute kcal.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement the accessible single-dish Base UI calculator</name>
  <files>frontend/src/routes/calculator-page.tsx, frontend/src/features/calculator/DishSearchCombobox.tsx, frontend/src/features/calculator/GramsField.tsx, frontend/src/features/calculator/CalculationResult.tsx, frontend/src/features/calculator/CalculatorForm.tsx, frontend/src/features/calculator/CalculatorForm.test.tsx, frontend/src/components/ui/alert.tsx, frontend/src/components/ui/combobox.tsx</files>
  <behavior>- alias `宫保` displays keyboard Base UI options with standard name and `匹配：{alias}`; Enter selects and fills typical grams.
- invalid/no dish/grams <=0 or >3000 focus visible error and make zero POSTs; valid submit prevents duplicate pending request.
- result uses backend dish/grams/kcal only, becomes visibly stale after input change; all UI-SPEC no-match/failure/loading/success copy/accessibility states pass.</behavior>
  <action>Extend user-event/Testing Library tests, add official Base UI `alert` and `combobox`, then implement the form and route exactly to UI-SPEC: Chinese semantic main/form/labels, mobile Slate/light layout, 44px controls/48px CTA, visible focus/alerts/live regions and reduced motion. Selection pre-fills grams; editing is local; only valid submit invokes `计算热量`. Do not use native select/Headless UI, dark mode, stale accent result, image/multi-dish/interval/history controls, or governance/source/license fields.</action>
  <verify><automated>cd frontend && npm run test -- --run src/features/calculator/CalculatorForm.test.tsx && npm run build</automated></verify>
  <done>The user-visible Phase 1 flow is accessible, explicit and backend-calculated; D-06 metadata is neither requested as UI data nor rendered.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| user form → transport | query/grams are untrusted and can be stale. |
| API wire DTO → UI DTO | governance metadata is received by REST but deliberately excluded from UI state. |
| future images/multi-dish | no file/array UI or request exists. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-25 | Tampering | form requests | mitigate | client validation/zero-POST tests plus authoritative API validation. |
| T-01-26 | Information Disclosure | governance metadata | mitigate | parser/UI DTO omits D-06 fields and component tests assert absent source/license/version UI. |
| T-01-27 | Repudiation | stale kcal | mitigate | stale notice and backend-only result tests. |
| T-01-28 | Denial of Service | repeat search/submit | mitigate | debounce and pending disable; Phase 5 owns server rate/cost control. |
| T-01-29 | Spoofing / Tampering | image/multi-dish | accept — out of phase | no file input/multipart/model/multi-item state. |
| T-01-SC | Tampering | supply chain | mitigate | consumes Plan 01-02 approved lockfile only. |
</threat_model>
<verification>- Vitest/Testing Library covers alias/keyboard/default grams/invalid zero-POST/pending/success/errors/stale and absence of governance UI.
- build/typecheck passes.</verification>
<success_criteria>- single-dish flow follows D-01–D-06 and UI-SPEC.
- backend retains/returns governance; frontend intentionally drops it from UI model and renders none.</success_criteria>
<output>Create `.planning/phases/01-controlled-data-thin-slice/01-08-SUMMARY.md` when done.</output>
