---
phase: 01-controlled-data-thin-slice
plan: "09"
type: execute
wave: 6
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05, 01-06, 01-07, 01-08]
files_modified: [frontend/tests/e2e/calculator.spec.ts, frontend/playwright.config.ts, README.md, backend/README.md]
autonomous: true
requirements: [ARCH-06]
must_haves:
  truths: ["开发者能按文档从 venv/Compose/空 test DB 启动真实薄切片并运行全部验证。", "浏览器 E2E 真正穿过 Vite、FastAPI 与 seeded PostgreSQL。"]
  artifacts:
    - path: frontend/tests/e2e/calculator.spec.ts
      provides: "real browser single-dish thin-slice flow"
    - path: README.md
      provides: "root local startup, test DB and data-flow runbook"
    - path: backend/README.md
      provides: "backend layering/env/migration/seed/preflight/API/test runbook"
  key_links:
    - from: frontend/tests/e2e/calculator.spec.ts
      to: /api/v1/calculations
      via: "real Vite/FastAPI/seeded PostgreSQL test"
      pattern: "计算热量"
---
<objective>以真实浏览器验证和准确运行手册关闭 Phase 1。Purpose: 启动、测试库和 preflight 不能靠口头知识。Output: Playwright smoke, concise Chinese root/backend docs.</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@.planning/phases/01-controlled-data-thin-slice/01-CONTEXT.md
@.planning/phases/01-controlled-data-thin-slice/01-08-SUMMARY.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Prove real browser-to-database calculator behavior</name>
  <files>frontend/tests/e2e/calculator.spec.ts, frontend/playwright.config.ts</files>
  <action>Install approved Chromium with `npx playwright install chromium`. Before E2E, from repository root start Compose DB, run `backend/scripts/create_test_database.sh`, then in `backend/` activate `.venv`, load local `.env` with `set -a && . .env && set +a`, run `alembic upgrade head` and `python -m app.scripts.seed_catalog --source data/catalog/v1.json` against the local development DB. Configure `frontend/playwright.config.ts` with two managed `webServer` entries and `reuseExistingServer: false`: backend command `cd ../backend && . .venv/bin/activate && set -a && . .env && set +a && uvicorn app.main:app --host 127.0.0.1 --port 8000`, waited by URL `http://127.0.0.1:8000/openapi.json`; frontend command `VITE_API_BASE_URL=http://127.0.0.1:8000 npm run dev -- --host 127.0.0.1 --port 5173`, waited by URL `http://127.0.0.1:5173`. Playwright owns and terminates both webServer child processes on completion/failure; it must not reuse a manually running server or intercept/mock endpoints. Test search `宫保`, keyboard-select `宫保鸡丁`, API default grams, local grams edit with no POST, one explicit calculate click, and backend kcal/stale state. Assert UI contains no sourceReference/licenseStatus/dataVersion, file input, image, multi-dish or interval control.</action>
  <verify><automated>docker compose up -d db && backend/scripts/create_test_database.sh && cd backend && . .venv/bin/activate && set -a && . .env && set +a && alembic upgrade head && python -m app.scripts.seed_catalog --source data/catalog/v1.json && cd ../frontend && npx playwright install chromium && npx playwright test tests/e2e/calculator.spec.ts</automated></verify>
  <done>Browser evidence proves the actual single-dish path and scope fence, not a mocked UI.</done>
</task>
<task type="auto">
  <name>Task 2: Document exact venv, test database, migration, seed, API and release commands</name>
  <files>README.md, backend/README.md</files>
  <action>Write concise Chinese D-19/D-20 runbooks. Root README must first require the blocking `python3.11 --version` 3.11.x check and list recovery through pyenv, Homebrew `python@3.11`, or official installer if it fails. Then list `cd backend && python3.11 -m venv .venv && . .venv/bin/activate && python -m pip install -e '.[dev]'`, copy `.env.example` to local `.env`, `docker compose up -d db`, `backend/scripts/create_test_database.sh`, local backend `.venv` activation plus `set -a && . .env && set +a`, local `alembic upgrade head` and explicit seed. Specify the E2E lifecycle exactly as Task 1: Playwright config starts/waits/terminates Uvicorn on 8000 and Vite on 5173 with VITE_API_BASE_URL, while migrations/seeding happen before the test. State test command uses APP_ENV=test, distinct TEST_DATABASE_URL ending `_test`, test migration/transaction fixture and `test_test_database_guards.py`; it fails closed for dev/prod/SQLite/equal URLs. Backend README explains API→Service→Repository→Model/schema direction, no-startup migration/seed, D-06 API fields returned but intentionally not rendered, docs/openapi/curl/data flow, and release preflight local/production. Document that catalog is demo_only, production intentionally fails until Phase 5 commercial audit, and local/CI runs preflight before release candidate. State image/upload/multi-dish/interval/history scope fence and no accuracy/medical claims.</action>
  <verify><automated>docker compose up -d db && backend/scripts/create_test_database.sh && cd backend && . .venv/bin/activate && set -a && . .env && set +a && APP_ENV=test python -m pytest tests/unit/test_test_database_guards.py tests/unit tests/integration tests/api -q && python -m app.scripts.release_preflight --environment local && if python -m app.scripts.release_preflight --environment production; then exit 1; fi && cd ../frontend && npm run test -- --run && npx playwright test tests/e2e/calculator.spec.ts</automated><human-check>在 390px 宽度手测搜索“宫保”、键盘选择、默认克数、编辑后过期提示和显式计算；Network 证实编辑克数无 POST，确认没有图片、多菜、区间或来源/许可/版本 UI。</human-check></verify>
  <done>新开发者可按文档安全启动本地 stack、建立隔离测试库、运行所有测试和 release gate，并理解 D-06 API/UI 的不同边界。</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| docs → developer commands | incorrect commands can bypass isolation/gates. |
| Playwright → running services | E2E must not silently substitute mock behavior. |
| future image/multi-dish | documentation and E2E keep scope absent. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-30 | Tampering | runbook/test commands | mitigate | exact Python 3.11/test URL/preflight sequence and automated replay. |
| T-01-31 | Repudiation | browser verification | mitigate | Playwright real-service rule plus explicit test evidence. |
| T-01-32 | Information Disclosure | UI/docs | mitigate | explain D-06 API fields but E2E asserts their non-rendering; do not document secrets. |
| T-01-33 | Spoofing / Tampering | upload/image/multi-dish | accept — out of phase | docs/E2E assert their absence; Phase 2 owns validation/deletion. |
| T-01-SC | Tampering | tool supply chain | mitigate | use approved lockfile/browser installation only. |
</threat_model>
<verification>- full automated venv/Compose/test DB/migration/seed/backend/frontend/Playwright run.
- required mobile human check.</verification>
<success_criteria>- operational commands are exact, safe and reproducible.
- real E2E and manual test prove Phase 1 without expanding scope.</success_criteria>
<output>Create `.planning/phases/01-controlled-data-thin-slice/01-09-SUMMARY.md` when done.</output>
