---
phase: 05-diet-planning-subgraph
plan: "03"
type: execute
wave: 5
depends_on: [05-02, 05-07]
files_modified: [frontend/src/features/README.md, frontend/src/features/plans/README.md, frontend/src/features/plans/api/README.md, frontend/src/features/plans/components/README.md, frontend/src/features/plans/api/schemas.ts, frontend/src/features/plans/api/client.ts, frontend/src/features/plans/components/ProfileGoalForm.tsx, frontend/src/features/plans/components/ProfileGoalForm.test.tsx]
autonomous: true
requirements: [PLN-01, PLN-02, PLN-06]
must_haves:
  truths: ["H5 在生成前让用户完整复核资料和现有偏好，并只发送严格 start command。", "本次不保存时仍可生成且不发生 profile 写入；偏好没有第二编辑器。"]
  artifacts:
    - path: frontend/src/features/plans/api/schemas.ts
      provides: "严格 Zod public profile/start/safe-event schemas"
    - path: frontend/src/features/plans/components/ProfileGoalForm.tsx
      provides: "RHF/Zod 复核表单与显式保存选择"
  key_links:
    - from: frontend/src/features/plans/api/client.ts
      to: /api/v1/agent
      via: "validated DietPlanningStartCommand through AuthenticatedRequest"
      pattern: "diet_planning"
---
<objective>交付首次规划的 H5 输入边界。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@frontend/AGENTS.md
@docs/ui/h5-foundation.md
@.planning/phases/05-diet-planning-subgraph/05-CONTEXT.md
@.planning/phases/05-diet-planning-subgraph/05-UI-SPEC.md
@.planning/phases/05-diet-planning-subgraph/05-PATTERNS.md
@.planning/phases/05-diet-planning-subgraph/05-02-PLAN.md
@.planning/phases/05-diet-planning-subgraph/05-07-PLAN.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 写首轮资料、偏好复核和显式保存的 RED 组件合同</name>
  <read_first>
    - AGENTS.md 与 frontend/AGENTS.md — 公开 API-only、TanStack Query/RHF+Zod、H5 与真实页面测试约束
    - docs/ui/h5-foundation.md 与 .planning/phases/05-diet-planning-subgraph/05-UI-SPEC.md — 表单、安全区、唯一滚动区和 D-01–D-04 可见文案合同
    - .planning/phases/05-diet-planning-subgraph/05-02-PLAN.md — `DietPlanningStartCommand`、`save_profile` 与安全 public command 上游合同
    - .planning/phases/05-diet-planning-subgraph/05-07-PLAN.md — profile 公开读写契约和偏好不进入 profile 的边界
    - frontend/src/features/{records/api/schemas.ts,records/api/client.ts,records/components/MealRecordEditPage.tsx,memory/api/client.ts,memory/README.md} — API、RHF/label 和只读 memory public API 模拟物
    - frontend/src/features/agent/components/AnalyzePage.test.tsx — MSW/Testing Library public-command assertion 模式
  </read_first>
  <files>frontend/src/features/README.md, frontend/src/features/plans/README.md, frontend/src/features/plans/api/README.md, frontend/src/features/plans/components/README.md, frontend/src/features/plans/components/ProfileGoalForm.test.tsx</files>
  <behavior>
    - 可见 labels/units、R-02 formula choice、五档 D-02 activity 示例、保守 enum speed 和只读 memory summary 在提交前完整出现（D-01–D-04）。
    - 未确认偏好不发送 command；`save_profile=false` 只发送 start command、从不调用 profile mutation；true 只带明确保存意图。
    - 禁止忌口/口味 input、chip delete 或第二 memory write；`管理饮食偏好` 是真实 memory link。
  </behavior>
  <action>按照 `features/records/api/{schemas.ts,client.ts}`、`MealRecordEditPage.tsx` 和 `features/memory/README.md` 建 MSW/Testing Library RED 测试与四个 README 索引。测试 public request history、label/a11y 和 server field errors；不渲染结果卡、路由或未来 Plan 09/10 测试文件。</action>
  <verify><automated>cd frontend && npm test -- --run src/features/plans/components/ProfileGoalForm.test.tsx</automated></verify>
  <done>RED 测试冻结没有静默保存的完整预生成复核。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 实现严格 public client 与 ProfileGoalForm</name>
  <read_first>
    - AGENTS.md 与 frontend/AGENTS.md — public API、feature isolation、RHF+Zod、无敏感 local storage 规则
    - docs/ui/h5-foundation.md 与 .planning/phases/05-diet-planning-subgraph/05-UI-SPEC.md — D-01–D-04 表单/只读偏好/显式保存 UI 合同
    - .planning/phases/05-diet-planning-subgraph/05-02-PLAN.md — 严格 `DietPlanningStartCommand` 和 `save_profile` 语义
    - .planning/phases/05-diet-planning-subgraph/05-07-PLAN.md — `/api/v1/planning/profile` public DTO，禁止 preferences 写入 profile
    - frontend/src/features/{records/api/schemas.ts,records/api/client.ts,records/components/MealRecordEditPage.tsx,memory/api/client.ts} — 精确 API、表单和 `listMemories` 模拟物
    - frontend/src/features/plans/components/ProfileGoalForm.test.tsx — Task 1 RED 合同
  </read_first>
  <files>frontend/src/features/plans/api/schemas.ts, frontend/src/features/plans/api/client.ts, frontend/src/features/plans/components/ProfileGoalForm.tsx</files>
  <action>依照 `records` API 模拟实现 Zod closed public DTO 和 `AuthenticatedRequest` client；RHF+Zod 表单严格映射 Plan 02 start command，使用 UI-SPEC 的可见中文标签、单位、五档活动文案、R-02 公式说明、D-03 保守速度和只读 memory summaries。保存 control 仅表达本次明确 intent，默认不因 prefill 自动写；客户端不计算目标、不推断 formula、不管理偏好、不过滤 health request。保留 server authoritative errors、44px controls、单一滚动契约和无本地存储敏感资料。</action>
  <verify><automated>cd frontend && npm run typecheck && npm run lint && npm test -- --run src/features/plans/components/ProfileGoalForm.test.tsx</automated></verify>
  <done>用户可提交精确、可审计的 transient start command，且持久化与偏好职责保持分离。</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| H5 form → public Agent API | 输入和保存意图不可信，后端仍是唯一健康/数值判断方。 |
| H5 → profile/memory APIs | 资料写入和偏好账本必须保持独立。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-14 | Tampering | form command | mitigate | RHF/Zod 关闭未知字段，服务端字段错误保留为权威。 |
| T-05-15 | Repudiation/Tampering | persistence intent | mitigate | 明确 labelled control 与 request-history test 防止 prefill 写入。 |
| T-05-16 | Information disclosure | client state | mitigate | 内存 auth、validated safe DTO；无 localStorage/provider/tool diagnostics。 |
</threat_model>
<verification>运行组件测试、typecheck/lint；浏览器验收由 UI 路由完成后的 Plan 06 执行。</verification>
<success_criteria>计划页可安全收集并复核首次资料，而没有伪造的数值、偏好或持久化行为。</success_criteria>
<output>Create `.planning/phases/05-diet-planning-subgraph/05-03-SUMMARY.md` when done.</output>
