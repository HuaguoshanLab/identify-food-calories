---
phase: 05-diet-planning-subgraph
plan: "11"
type: execute
wave: 9
depends_on: [05-10]
files_modified:
  - frontend/src/features/plans/components/PlanPage.tsx
  - frontend/src/features/plans/components/PlanPage.test.tsx
  - frontend/tests/e2e/plans.spec.ts
autonomous: true
gap_closure: true
requirements: [PLN-05]
must_haves:
  truths:
    - "用户提交单餐调整后，更新完成会被礼貌地播报，但页面不会被程序化焦点拉离当前阅读位置。"
    - "午餐调整后只有午餐标记为已调整，早餐和晚餐保持原样与既有顺序。"
  artifacts:
    - path: frontend/src/features/plans/components/PlanPage.tsx
      provides: "不夺取焦点的 polite live-region 局部调整完成提示"
      contains: 'aria-live="polite"'
    - path: frontend/src/features/plans/components/PlanPage.test.tsx
      provides: "局部调整的非焦点 live-region 组件回归合同"
    - path: frontend/tests/e2e/plans.spec.ts
      provides: "真实公开用户路径的滚动位置和局部替换回归"
  key_links:
    - from: frontend/src/features/plans/components/PlanPage.tsx
      to: frontend/src/features/plans/components/PlanPage.test.tsx
      via: "updatedSlot completion announcement"
      pattern: 'aria-live="polite"'
    - from: frontend/tests/e2e/plans.spec.ts
      to: '[data-testid="page-scroll-area"]'
      via: "pre/post adjustment scrollTop assertion on AppShell's sole scroll container"
      pattern: 'page-scroll-area'
---

<objective>
关闭 UAT-05 的局部调整滚动劫持：保留屏幕阅读器可获得的礼貌更新通知，同时不再把键盘焦点或页面滚动强制移到隐藏摘要。

Purpose: 用户提交“换清淡”等单餐反馈后可立即留在当前阅读上下文查看结果；无障碍通知不应成为破坏唯一滚动区的副作用。
Output: 无 programmatic focus 的 PlanPage live region、更新后的组件与真实浏览器回归。
</objective>

<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@frontend/AGENTS.md
@docs/ui/h5-foundation.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-diet-planning-subgraph/05-UAT.md
@.planning/phases/05-diet-planning-subgraph/05-CONTEXT.md
@.planning/phases/05-diet-planning-subgraph/05-10-SUMMARY.md

<interfaces>
From frontend/src/features/plans/components/PlanPage.tsx:
```typescript
const [updatedSlot, setUpdatedSlot] = useState<PlanMeal['slot']>()
const slotLabels = { breakfast: '早餐', lunch: '午餐', dinner: '晚餐' } as const
// Completion copy must remain: 已更新{slotLabels[updatedSlot]}，其余餐次保持不变。
```

From frontend/src/layouts/PageScrollArea.tsx:
```typescript
<main data-testid="page-scroll-area" className="min-h-0 flex-1 overflow-y-auto ..." />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先把无焦点通知与滚动保持写成回归合同</name>
  <files>frontend/src/features/plans/components/PlanPage.test.tsx, frontend/tests/e2e/plans.spec.ts</files>
  <behavior>
    - 在既有 owned-thread 午餐调整 fixture 中，完成文字仍存在于 `aria-live="polite"` 的 sr-only status，但该节点不是焦点目标，且不含 `tabindex="-1"`。
    - 点击“提交调整”后，测试保留按钮的 active element；在新餐卡与替换说明出现后，完成摘要不获得焦点。
    - 真实 Playwright 路径先记录 AppShell 唯一 `[data-testid="page-scroll-area"]` 的 `scrollTop`，提交一次“午餐换清淡一些”后等待完成文案，并断言 scrollTop 未被重置、完成摘要未获得焦点、午餐已调整而早餐/晚餐文字不变。
  </behavior>
  <action>把现有“focuses a safe replacement summary”组件测试和 `plans.spec.ts` 的 `toBeFocused()` 断言改为上述负向焦点与滚动合同。组件测试在触发调整前保存提交按钮引用/active element，禁止以只验证文案存在代替焦点断言。E2E 使用既有真实注册、Mailpit 验证、登录和公开 `/api/v1` 用户路径；只读取 `page-scroll-area` 的公开渲染状态，不写数据库、不伪造 token、不复用未知服务。此 RED 合同覆盖 D-10、D-12，并落实 H5 “唯一纵向滚动区、不得滚动劫持”约束。</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/features/plans/components/PlanPage.test.tsx</automated>
  </verify>
  <done>旧的“隐藏摘要必须获得焦点”要求被移除；测试会在任何恢复 summary focus 或将 AppShell scrollTop 拉离用户位置的实现上失败。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用非侵入式 polite live region 修复调整完成状态并验收真实页面</name>
  <files>frontend/src/features/plans/components/PlanPage.tsx</files>
  <behavior>
    - `updatedSlot` 只控制完成文案的渲染与 `aria-live="polite"` 播报；不调用该摘要节点的 `focus()`，也不把它制成可编程焦点目标。
    - 既有首次页面 `headingRef` 焦点行为不变；本修复只移除 replacement summary 的 ref/effect/tabIndex，不改变安全快照解析、线程、餐次顺序、D-12 卡片信息、限制、拒绝或持续非医疗提示。
  </behavior>
  <action>在 `PlanPage` 删除仅为 replacement summary 服务的 `summaryRef` 与依赖 `updatedSlot` 的 `useEffect`；保留现有 `updatedSlot` state 和 `<p aria-live="polite" className="sr-only">` 的准确中文状态文案，但移除 `ref` 与 `tabIndex`。不要引入新依赖、滚动 API、`scrollIntoView`、路由跳转、状态重置或后端变更。该最小修复必须保留 D-10 的未受影响餐次、D-12 的可继续调整状态，并遵守 H5 单一滚动区与禁止滚动劫持合同。</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/features/plans/components/PlanPage.test.tsx && E2E_BACKEND_PORT=8001 E2E_FRONTEND_PORT=5179 npm exec playwright test tests/e2e/plans.spec.ts</automated>
    <human-check>通过 Codex 内置浏览器连接本任务启动且自行拥有的 `http://127.0.0.1:5179`，以新建的合成测试账号走注册 → Mailpit 验证 → 登录 → `/app/plans` → 生成三餐 → 在页面底部提交一次午餐调整。记录提交前后 `page-scroll-area` 的阅读位置；预期完成提示被辅助技术礼貌播报、午餐显示“已调整”、早餐/晚餐不变，且页面没有跳回上方。只走真实页面与公开 API；不得连接、终止或复用未知端口服务。若浏览器运行环境阻止创建合成账号或提交合成资料，记录具体阻断原因，不能宣称浏览器验收通过。</human-check>
  </verify>
  <done>UAT-05 的调整完成不再劫持焦点或滚动；组件测试、隔离 Playwright 真实用户路径和 Codex 内置浏览器均提供可复现证据。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| 已验证的 planning snapshot → H5 live region | 安全快照驱动的状态文案不能变成未经验证的焦点/滚动命令。 |
| 用户操作 → AppShell 唯一滚动区 | 调整完成不得改变用户当前阅读位置或打断键盘导航。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-28 | Denial of service | `PlanPage` replacement summary | mitigate | 移除 `summaryRef.current.focus()` 和 `tabIndex=-1`，并用组件与真实浏览器的焦点/scrollTop 合同阻止回归。 |
| T-05-29 | Tampering | safe snapshot → UI completion state | mitigate | 不修改 Zod safe snapshot allowlist、owned-thread 命令或餐次 projection；live region 只复用已验证的 `updatedSlot` 和固定 slot labels。 |
| T-05-SC | Tampering | package supply chain | accept | 本计划不安装或升级 npm/pip/cargo 包；无供应链输入。 |
</threat_model>

<source_audit>
SOURCE   | ID | Feature/Requirement | Plan | Status | Notes
---------|----|---------------------|------|--------|------
GOAL | — | 可交互调整的一日三餐方案 | 05-01..10, 05-11 | COVERED | 本计划只恢复已交付调整路径的可用性，不削减目标。
REQ | PLN-01 | 资料与偏好提交 | 05-01, 05-03, 05-05 | COVERED | 已完成；本缺口不改变。
REQ | PLN-02 | 确定性目标计算 | 05-01, 05-02 | COVERED | 已完成；本缺口不改变。
REQ | PLN-03 | 受控菜谱三餐候选 | 05-02, 05-08, 05-11-SUMMARY | COVERED | 已完成；本缺口不改变。
REQ | PLN-04 | 有界确定性校验 | 05-02, 05-04, 05-10, 05-11-SUMMARY | COVERED | 已完成；本缺口不改变。
REQ | PLN-05 | 自然语言局部替换并保留约束 | 05-04, 05-10, 05-11 | COVERED | 05-11 关闭用户无法留在调整结果处阅读的 UAT-05。
REQ | PLN-06 | 非医疗边界 | 05-02, 05-09, 05-10 | COVERED | 本计划明确保持免责声明和拒绝路径。
RESEARCH | Pattern 2 / Pitfall 6 | slot-local replacement、H5 单一滚动区 | 05-11 | COVERED | 断言只更新 lunch，且唯一 `page-scroll-area` 不被劫持。
RESEARCH | 无新依赖 | 不新增第三方包 | 05-11 | COVERED | 仅删错误 focus wiring。
CONTEXT | D-01–D-09 | 资料、目标、受控三餐与安全业务状态 | 05-01..09 | COVERED | 已完成，05-11 不改变。
CONTEXT | D-10 | 仅替换受影响餐次 | 05-11 | COVERED | 组件和 E2E 固定 lunch-only 与不变的早餐/晚餐。
CONTEXT | D-11 | 明确反馈的长期偏好写入 | 05-04 | COVERED | 05-11 不改写入边界。
CONTEXT | D-12 | 展示替换依据并可继续调整 | 05-10, 05-11 | COVERED | 维持摘要与卡片，只移除强制焦点。
CONTEXT | D-13–D-18 | 放宽、上限、健康拒绝、免责声明与资料最小化 | 05-01..10 | COVERED | 05-11 明确不修改这些状态。
</source_audit>

<verification>
1. `PlanPage.test.tsx` 通过，且不再要求隐藏 summary 获得焦点。
2. 隔离 Playwright 用户路径通过；更新前后的 `page-scroll-area.scrollTop` 相同，且午餐局部替换保留其他两餐。
3. Codex 内置浏览器按真实注册/登录/API 路径复验；若其写操作被环境策略阻止，如实记录为阻断。
</verification>

<success_criteria>
- `aria-live="polite"` 的完成摘要仍存在，但没有 `tabIndex`、ref 或任何 programmatic focus 路径。
- 用户提交一次午餐调整后，页面不跳回上方；午餐显示更新，早餐与晚餐保持不变。
- 不新增依赖、不改变公开 API、后端逻辑、约束、医疗拒绝或持续免责声明。
</success_criteria>

<output>
Create `.planning/phases/05-diet-planning-subgraph/05-11-SUMMARY.md` when done. If an existing summary is present, update it with this gap-closure execution result rather than creating a duplicate.
</output>
