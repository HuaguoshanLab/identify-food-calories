---
phase: 05-diet-planning-subgraph
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/README.md, backend/app/planning/README.md, backend/app/planning/__init__.py, backend/app/planning/schemas.py, backend/app/planning/ports.py, backend/app/planning/service.py, backend/tests/README.md, backend/tests/planning/README.md, backend/tests/planning/test_planning_service.py]
autonomous: true
requirements: [PLN-01, PLN-02, PLN-04, PLN-06]
must_haves:
  truths:
    - "完整且经确认的成人资料只能得到版本化、确定性的目标区间；浏览器和模型不计算数值。"
    - "缺失资料或未确认偏好在检索前返回 NEEDS_INPUT；高风险和非保守速度在计算前被拒绝。"
  artifacts:
    - path: backend/app/planning/schemas.py
      provides: "冻结、extra=forbid 的 profile、target、validation DTO 和闭合动作枚举"
    - path: backend/app/planning/service.py
      provides: "target-policy.v1、健康边界和确定性计划校验入口"
  key_links:
    - from: backend/app/planning/service.py
      to: backend/app/planning/ports.py
      via: "只经 Protocol 获取未来 profile/recipe/nutrition 数据"
      pattern: "PlanningRepository"
---
<objective>
建立规划领域的严格、可测试合同和确定性目标政策。

Purpose: 先锁定 D-01–D-03、D-15–D-16 的安全与数值真相，后续 ORM、图和 H5 只能消费这些合同。
Output: `app.planning` 模块、`target-policy.v1`、闭合校验结果和 fake-service 测试。
</objective>
<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@backend/AGENTS.md
@.planning/phases/05-diet-planning-subgraph/05-CONTEXT.md
@.planning/phases/05-diet-planning-subgraph/05-RESEARCH.md
@.planning/phases/05-diet-planning-subgraph/05-PATTERNS.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 写确定性目标、确认和健康拒绝的 RED 合同</name>
  <files>backend/tests/README.md, backend/tests/planning/README.md, backend/tests/planning/test_planning_service.py</files>
  <behavior>
    - R-01 的五档活动因子、三个保守目标速度、`Decimal` Mifflin–St Jeor、±100 kcal 区间和 4/4/9 AMDR 宏量区间具有固定版本。
    - 缺少身高、体重、年龄、明确 `formula_variant`、活动、目标/速度或 `PreferenceReview.confirmed=true` 返回 `NEEDS_INPUT`（D-01、D-04）。
    - 19..78 之外、孕哺/疾病/用药/进食障碍/自伤/极端目标、未知速度或低于 1200 kcal 的结果，在 recipe port 调用前返回 `BLOCK_HEALTH_SCOPE` 与 D-16 安全文案（D-03、D-15、D-16）。
    - `PlanValidationResult` 仅返回 `PASS|REPLAN|RELAX|BLOCK_HEALTH_SCOPE|NEEDS_INPUT`；排除项和健康边界不可被 RELAX。
  </behavior>
  <action>创建测试目录 README 并更新 `backend/tests/README.md` 索引。参照 `backend/app/nutrition/{schemas.py,service.py,ports.py}` 的冻结 DTO、闭合动作和 Decimal 模式，先用 Fake PlanningRepository/Fake Nutrition port 编写行为测试；fixture 只用合成成年人资料，不能含真实健康数据、提示词或 Provider 输出。</action>
  <verify><automated>cd backend && uv run pytest tests/planning/test_planning_service.py -q</automated></verify>
  <done>RED 测试完整描述目标政策、首次确认、拒绝和闭合校验动作。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 实现版本化确定性规划领域合同</name>
  <files>backend/app/README.md, backend/app/planning/README.md, backend/app/planning/__init__.py, backend/app/planning/schemas.py, backend/app/planning/ports.py, backend/app/planning/service.py</files>
  <action>按 `backend/app/nutrition/{schemas.py,service.py,ports.py}` 模拟物建立 `app.planning`，并更新 `backend/app/README.md` 索引。定义 frozen/`extra="forbid"` 的 `PlanningProfileInput`、`PreferenceReview`、`DailyTarget`、`ControlledRecipe` public DTO、`PlanValidationResult` 与版本常量。实现 R-01/R-02：明确公式变体、五档活动枚举、三个保守预设、D-15 守卫优先于计算/检索、Decimal 区间和 D-16 安全拒绝。定义仅含 Protocol 的 recipe/profile/nutrition 端口及确定性 validate 入口；此计划不得创建 ORM、HTTP 路由、profile/memory 写入或 Provider 调用。</action>
  <verify><automated>cd backend && uv run pytest tests/planning/test_planning_service.py -q</automated></verify>
  <done>领域服务通过所有 RED 合同，且后续图可消费动作结果而不重写公式、校验或医疗边界。</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| transient profile input → PlanningService | 不可信身体/目标数据必须先通过闭合 DTO 与健康范围。 |
| PlanningService → future repository ports | 数值和规则只能在受版本控制的领域服务中决定。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-01 | Tampering | target input | mitigate | `extra=forbid`、枚举和范围校验拒绝自定义速度及缺失确认。 |
| T-05-02 | Elevation of privilege | health scope | mitigate | D-15 确定性 guard 在计算和 recipe 检索前终止。 |
| T-05-03 | Information disclosure | target result | mitigate | 返回版本、区间和安全文案，不返回原始输入、提示词或推理。 |
</threat_model>
<verification>运行目标/校验服务测试及后端既有 lint/type-check 命令。</verification>
<success_criteria>目标和安全判断有唯一可复算来源，且没有任何模型、浏览器或未验证数据绕过该来源。</success_criteria>
<output>Create `.planning/phases/05-diet-planning-subgraph/05-01-SUMMARY.md` when done.</output>
