---
phase: 05-diet-planning-subgraph
plan: "04"
type: execute
wave: 6
depends_on: [05-02]
files_modified: [backend/app/agent/state.py, backend/app/agent/graph.py, backend/app/agent/tools.py, backend/app/agent/service.py, backend/app/agent/schemas.py, backend/app/agent/api.py, backend/tests/unit/test_diet_planning_graph.py, backend/tests/integration/test_diet_planning_agent_api.py]
autonomous: true
requirements: [PLN-04, PLN-05, PLN-06]
must_haves:
  truths: ["明确反馈默认只替换被影响餐次，其他餐次和不可放宽约束保持不变。", "明确反馈遵循 Phase 4 审计/隔离/删除语义；三次调整后终止。"]
  artifacts:
    - path: backend/app/agent/graph.py
      provides: "安全同线程 local replacement、RELAX、limit/refusal 行为"
    - path: backend/app/agent/schemas.py
      provides: "safe adjustment snapshot/event DTO"
  key_links:
    - from: backend/app/agent/graph.py
      to: backend/app/agent/tools.py
      via: "capture_explicit_preferences and PlanningService"
      pattern: "capture_explicit_preferences"
---
<objective>在后端实现同一 planning thread 的安全恢复与有界局部调整。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@backend/AGENTS.md
@.planning/phases/05-diet-planning-subgraph/05-CONTEXT.md
@.planning/phases/05-diet-planning-subgraph/05-RESEARCH.md
@.planning/phases/05-diet-planning-subgraph/05-PATTERNS.md
@.planning/phases/04-meal-records-and-memory/04-07-SUMMARY.md
@.planning/phases/05-diet-planning-subgraph/05-02-PLAN.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 写 local replacement、memory replay、RELAX 和 limit 的 RED 后端合同</name>
  <read_first>
    - AGENTS.md 与 backend/AGENTS.md — Graph→tools-only、owner isolation、Fake Provider 与真实 PostgreSQL API 测试限制
    - .planning/phases/05-diet-planning-subgraph/05-CONTEXT.md — D-10–D-14 及不可放宽 exclusion/health 的 locked decisions
    - .planning/phases/05-diet-planning-subgraph/05-02-PLAN.md — DietPlanningState、safe event 和 owned-thread 上游接口
    - .planning/phases/04-meal-records-and-memory/04-07-SUMMARY.md — preference-first、delete-aware 和记忆审计/幂等行为
    - .planning/phases/05-diet-planning-subgraph/05-PATTERNS.md — `capture_explicit_preferences`、planning graph budget 和 terminal fourth-command 模式
    - backend/tests/{unit/test_diet_planning_graph.py,integration/test_diet_planning_agent_api.py,unit/test_agent_memory_context.py} — Plan 02 RED、memory replay 与 HTTPX 回归基线
  </read_first>
  <files>backend/tests/unit/test_diet_planning_graph.py, backend/tests/integration/test_diet_planning_agent_api.py</files>
  <behavior>
    - “午餐换清淡一些”仅改变 lunch，早餐/晚餐 identity 和仍有效 exclusion 不变；safe report 给出 changed dish、matched constraint、所有范围状态（D-10、D-12）。
    - 明确 “不吃香菜”/“换清淡” 仅经 typed tool 调 Phase 4 memory capture；重放没有重复写入，模型推测不写入（D-11）。
    - 歧义进入仅含三餐 choice 的 needs-input；无效 resume 不改 checkpoint；RELAX 仅 energy/macro，返回原范围、值、精确偏离与理由，绝不放宽排除或 health（D-13）。
    - 第三次自动调整后 `LIMIT_REACHED`，第四 command 不调用 composition/provider（D-14）。
  </behavior>
  <action>扩展 Plan 02 已创建的 Fake Provider 与 public HTTPX tests；使用合成已有 plan，断言 slot identity、safe DTO/SSE 字段和 replay 幂等，而不是调用顺序。不得读、验证或修改前端结果组件、PlanPage 测试或 Playwright：结果 UI 尚未由 Plan 09 创建，Plan 04 保持后端专属。</action>
  <verify><automated>cd backend && uv run pytest tests/unit/test_diet_planning_graph.py tests/integration/test_diet_planning_agent_api.py -q</automated></verify>
  <done>RED 测试让 D-10–D-14 的服务端安全行为独立可验证。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 实现约束保留、审计记忆和安全恢复投影</name>
  <read_first>
    - AGENTS.md 与 backend/AGENTS.md — Graph 不得直接查数据库/MemoryService、SSE 不泄露内部信息
    - .planning/phases/05-diet-planning-subgraph/05-CONTEXT.md 与 05-UI-SPEC.md — D-10–D-17 的调整、RELAX、limit/refusal 投影要求
    - .planning/phases/05-diet-planning-subgraph/05-02-PLAN.md — 现有 DietPlanningGraph/State/工具端口和 checkpoint 合同
    - .planning/phases/04-meal-records-and-memory/04-07-SUMMARY.md — explicit-only、owner-scoped、delete-aware memory authority
    - backend/app/agent/{state.py,graph.py,tools.py,service.py,schemas.py,api.py} — Task 1 测试覆盖的实现边界
    - backend/tests/{unit/test_diet_planning_graph.py,integration/test_diet_planning_agent_api.py} — Task 1 RED 合同
  </read_first>
  <files>backend/app/agent/state.py, backend/app/agent/graph.py, backend/app/agent/tools.py, backend/app/agent/service.py, backend/app/agent/schemas.py, backend/app/agent/api.py</files>
  <action>扩展 Plan 02 DietPlanningGraph，先把 freeform feedback 限为验证后的 closed intent/slot；歧义仅产生 JSON-safe breakfast/lunch/dinner choice。确定性派生 affected slots，保持未变 slots、confirmed preferences、忌口和明确排除。清晰的 allowlisted statement 只能经 `agent/tools.py` 调 `capture_explicit_preferences`，State 仅保留 replay marker/safe rationale；当前与后续检索仍用 Phase 4 preference-first、owner-scoped、delete-aware authority。由 PlanningService 判断 replacement/RELAX，RELAX 只含 energy/macro 的安全 delta/reason。把三次计数持久化进 planning state，并为 UI 定义安全 adjustment fields；不得加入前端代码、H5 tests、浏览器验证或 raw feedback/tool/provider 内容。</action>
  <verify><automated>cd backend && uv run pytest tests/unit/test_diet_planning_graph.py tests/integration/test_diet_planning_agent_api.py -q</automated></verify>
  <done>用户可经公开 Agent API 完成安全局部替换或得到透明终止，所有执行细节仍不可见。</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| freeform feedback → intent classifier | 文本可能要求医疗绕过、放宽排除或影响多餐。 |
| graph → memory ledger | 只有明确、白名单反馈可成为持久化偏好。 |
| adjustment state/SSE → eventual UI | 只允许安全业务投影。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-17 | Tampering | feedback constraints | mitigate | closed intent + deterministic validator 保留 immutable exclusions/health blocks。 |
| T-05-18 | Tampering/Repudiation | preference replay | mitigate | existing opaque idempotency key 与 state capture marker。 |
| T-05-19 | Denial of service | replan loop | mitigate | durable `replan_count<=3`、inherited budget、fourth-command guard。 |
| T-05-20 | Information disclosure | adjusted report | mitigate | 只序列化 changed-slot/rationale/range state，无 raw feedback、ledger ID 或 reasoning。 |
</threat_model>
<verification>仅运行 backend graph/API tests 和 SSE snapshot field assertions；H5 recovery browser tests 明确归 Plan 10/06。</verification>
<success_criteria>后端完成可审计、有界的调整能力，并未把未交付 UI 作为本计划依赖或验证前提。</success_criteria>
<output>Create `.planning/phases/05-diet-planning-subgraph/05-04-SUMMARY.md` when done.</output>
