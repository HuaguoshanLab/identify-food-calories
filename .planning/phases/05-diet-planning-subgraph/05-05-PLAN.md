---
phase: 05-diet-planning-subgraph
plan: "05"
type: execute
wave: 6
depends_on: [05-03, 05-07]
files_modified: [frontend/src/routePaths.ts, frontend/src/App.tsx, frontend/src/app/MePage.tsx, frontend/src/features/plans/api/profile.ts, frontend/src/features/plans/components/PersonalProfilePage.tsx, frontend/src/features/plans/components/PersonalProfilePage.test.tsx, frontend/src/App.test.tsx, frontend/tests/e2e/profile.spec.ts]
autonomous: true
requirements: [PLN-01, PLN-06]
must_haves:
  truths: ["用户能在 我的→个人资料 显式查看、编辑、删除身体资料和目标，删除后不再预填。", "资料页没有忌口/口味编辑器，记忆页仍为唯一偏好入口。"]
  artifacts:
    - path: frontend/src/features/plans/components/PersonalProfilePage.tsx
      provides: "DetailLayout profile view/empty/edit/delete modes"
    - path: frontend/src/routePaths.ts
      provides: "centralized profile protected route"
  key_links:
    - from: frontend/src/features/plans/api/profile.ts
      to: /api/v1/planning/profile
      via: "Plan 07 public CRUD contract"
      pattern: "planning/profile"
---
<objective>交付资料管理的 H5 DetailLayout 路径。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@frontend/AGENTS.md
@docs/ui/h5-foundation.md
@.planning/phases/05-diet-planning-subgraph/05-UI-SPEC.md
@.planning/phases/05-diet-planning-subgraph/05-03-PLAN.md
@.planning/phases/05-diet-planning-subgraph/05-07-PLAN.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 写资料路由、删除与偏好职责分离的 RED 合同</name>
  <read_first>
    - AGENTS.md 与 frontend/AGENTS.md — protected public API、DetailLayout、feature boundary 与公开浏览器 E2E 约束
    - docs/ui/h5-foundation.md 与 .planning/phases/05-diet-planning-subgraph/05-UI-SPEC.md — D-04/D-05/D-18 的 DetailLayout、删除、focus 和 no-preference-editor 合同
    - .planning/phases/05-diet-planning-subgraph/05-03-PLAN.md — plan profile Zod/AuthenticatedRequest 和显式保存边界
    - .planning/phases/05-diet-planning-subgraph/05-07-PLAN.md — `/api/v1/planning/profile` owner CRUD 与 delete-before-read 上游接口
    - frontend/src/{App.tsx,App.test.tsx,routePaths.ts,app/MePage.tsx,app/SettingsLinkRow.tsx,features/memory/components/MemoryEditPage.tsx,features/records/components/MealRecordEditPage.tsx} — route、DetailLayout、AlertDialog、a11y 模拟物
    - frontend/tests/e2e/agent.spec.ts — 注册→Mailpit→登录的真实公开 E2E 模式
  </read_first>
  <files>frontend/src/features/plans/components/PersonalProfilePage.test.tsx, frontend/src/App.test.tsx, frontend/tests/e2e/profile.spec.ts</files>
  <behavior>
    - 我的页使用 `routePaths.profile` 进入 protected `/app/me/profile` DetailLayout；页面有 view/empty/edit/delete 状态，无 Tab bar（D-05）。
    - view 仅显示 profile label/value；edit 有显式保存；`管理饮食偏好` link 仍指向 memory；无 avoidance/taste fields/mutation。
    - AlertDialog 的精确 D-18 文案、取消、确认删除、h1 focus 回归和删除后 cache/preflight empty state 都被验证。
  </behavior>
  <action>用 `MemoryEditPage.tsx`、`MealRecordEditPage.tsx`、`SettingsLinkRow.tsx` 和 `App.test.tsx` 的 route/a11y 模式写 MSW/Testing Library 和真实公开 E2E RED。profile API contract 只引用 Plan 07 已创建的 `/api/v1/planning/profile`，不假定 Plan 09 结果 UI 存在。</action>
  <verify><automated>cd frontend && npm test -- --run src/features/plans/components/PersonalProfilePage.test.tsx src/App.test.tsx</automated></verify>
  <done>RED 测试锁死 D-04/D-05/D-18、DetailLayout 和 memory 单一写入口。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 实现个人资料详情、路由和显式 CRUD client</name>
  <read_first>
    - AGENTS.md 与 frontend/AGENTS.md — App.tsx composition-only、计划 feature 的公开 API 边界和无 token/local storage 规则
    - docs/ui/h5-foundation.md 与 .planning/phases/05-diet-planning-subgraph/05-UI-SPEC.md — profile DetailLayout、44px、删除 copy/focus 和 memory ownership
    - .planning/phases/05-diet-planning-subgraph/05-03-PLAN.md — Zod profile types 与 `AuthenticatedRequest` 连接点
    - .planning/phases/05-diet-planning-subgraph/05-07-PLAN.md — profile GET/PUT/PATCH/DELETE 合同及 deletion semantics
    - frontend/src/{App.tsx,routePaths.ts,app/MePage.tsx,app/SettingsLinkRow.tsx,features/memory/components/MemoryEditPage.tsx} — 现有 route/link/AlertDialog 的精确模拟物
    - frontend/src/features/plans/components/PersonalProfilePage.test.tsx — Task 1 RED 合同
  </read_first>
  <files>frontend/src/routePaths.ts, frontend/src/App.tsx, frontend/src/app/MePage.tsx, frontend/src/features/plans/api/profile.ts, frontend/src/features/plans/components/PersonalProfilePage.tsx</files>
  <action>用 Plan 03 Zod types/AuthenticatedRequest 新建 `api/profile.ts`，调用 Plan 07 GET/PUT/PATCH/DELETE 并定义 TanStack Query keys/invalidation。集中加入 `routePaths.profile` 与 protected route；App.tsx 只组合 `DetailLayout title="个人资料"`，MePage 添加 UI-SPEC 的 SettingsLinkRow，保留 memory link。实现 semantic `dl/dt/dd` view、empty copy、可见标签 edit、server errors 与唯一保存 CTA；AlertDialog 使用精确不可逆删除 copy，成功后失效 profile/plan prefill cache 并 focus h1。使用语义 tokens、44px actions、一个 scroll root；绝不计算目标、存 token、重复偏好 fields 或渲染 provider/tool/reasoning。</action>
  <verify><automated>cd frontend && npm run typecheck && npm run lint && npm test -- --run src/features/plans/components/PersonalProfilePage.test.tsx src/App.test.tsx</automated></verify>
  <done>个人资料是唯一 body/goal 管理路径，且删除不会在 H5 cache 中复活。</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| settings UI → profile API | 浏览器可发起请求但后端 owner authorization 是权威。 |
| profile cache → plan prefill | 删除后 cache 不可重现敏感资料。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-21 | Information disclosure | profile client | mitigate | AuthenticatedRequest、无 user ID/local storage，后端 owner filter。 |
| T-05-22 | Tampering | profile write | mitigate | 只允许 save CTA/explicit opt-in 调 mutation。 |
| T-05-23 | Information disclosure | delete cache | mitigate | invalidate queries、success state、server soft-delete read filter。 |
</threat_model>
<verification>运行组件/路由/E2E；真实浏览器 profile path 于 Plan 06 在完整用户流可用后执行。</verification>
<success_criteria>D-18 的查看、编辑、删除与偏好权威边界在真实路由中可测试。</success_criteria>
<output>Create `.planning/phases/05-diet-planning-subgraph/05-05-SUMMARY.md` when done.</output>
