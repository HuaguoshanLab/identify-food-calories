---
phase: 05-diet-planning-subgraph
plan: "10"
type: execute
wave: 8
depends_on: [05-04, 05-09]
files_modified: [frontend/src/features/plans/api/schemas.ts, frontend/src/features/plans/api/client.ts, frontend/src/features/plans/components/PlanPage.tsx, frontend/src/features/plans/components/MealCard.tsx, frontend/src/features/plans/components/PlanningStatus.tsx, frontend/src/features/plans/components/PlanPage.test.tsx, frontend/tests/e2e/plans.spec.ts]
autonomous: true
requirements: [PLN-04, PLN-05, PLN-06]
must_haves:
  truths: ["明确反馈仅更新受影响餐次，其他餐次与排除约束保持不变。", "允许的目标放宽透明，三次后不发第四次请求，健康拒绝无绕过。"]
  artifacts:
    - path: frontend/src/features/plans/components/PlanPage.tsx
      provides: "adjustment、ambiguity、relaxation、limit 和 refusal UI"
  key_links:
    - from: frontend/src/features/plans/api/client.ts
      to: /api/v1/agent
      via: "owned-thread idempotent adjustment command"
      pattern: "command_key"
---
<objective>将 Plan 04 的安全调整 DTO 交付为受约束的真实 H5 操作。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@frontend/AGENTS.md
@docs/ui/h5-foundation.md
@.planning/phases/05-diet-planning-subgraph/05-CONTEXT.md
@.planning/phases/05-diet-planning-subgraph/05-UI-SPEC.md
@.planning/phases/05-diet-planning-subgraph/05-PATTERNS.md
@.planning/phases/05-diet-planning-subgraph/05-04-PLAN.md
@.planning/phases/05-diet-planning-subgraph/05-09-PLAN.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 写同线程调整恢复的 RED H5 合同</name>
  <read_first>
    - frontend/src/features/agent/components/AnalyzePage.tsx — recovery/status presentation 模式
    - frontend/src/features/agent/stream/useAgentEventStream.ts — existing snapshot/replay transport 模式
    - frontend/src/features/records/components/MealRecordDetailPage.tsx — card update/accessible information 模式
    - frontend/src/App.test.tsx — route/focus assertion 模式
    - frontend/tests/e2e/agent.spec.ts — public auth and owned-thread Playwright 模式
    - frontend/src/features/plans/components/{PlanPage.tsx,MealCard.tsx,PlanningStatus.tsx} — Plan 09 已交付 result interfaces
  </read_first>
  <files>frontend/src/features/plans/components/PlanPage.test.tsx, frontend/tests/e2e/plans.spec.ts</files>
  <behavior>
    - 明确 lunch feedback 只给 lunch `已调整`、old/new dish、matched constraint 和 polite focus summary；其他两卡 identity 保持（D-10、D-12）。
    - ambiguity 只显示 breakfast/lunch/dinner；RELAX Alert 展示 metric、原 range、plan value/deviation、reason 和不可放宽 exclusions；raw feedback/memory ID/tool/provider data 不进 DOM（D-13）。
    - 三次后显示精确 UI-SPEC limit 文案、new-plan/profile actions 且不提交第四 command；refusal 为 focused alert 且无 cards/bypass（D-14–D-16）。
  </behavior>
  <action>以 `AnalyzePage`、`useAgentEventStream`、`agent.spec.ts` 的现有 owned-thread/recovery 模式扩展 Plan 09 tests。MSW 与 Playwright 都从已生成的合成计划走公开 UI；断言 D-08 card fields 在 replacement 后仍存在。测试不得依赖内部 endpoint、直接数据库操作或伪造浏览器 token。</action>
  <verify><automated>cd frontend && npm test -- --run src/features/plans/components/PlanPage.test.tsx</automated></verify>
  <done>RED tests 固定 D-10–D-17 的局部替换、透明放宽、终止与拒绝 UX。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 实现严格 adjustment client 和受控恢复界面</name>
  <read_first>
    - frontend/src/features/agent/{components/AnalyzePage.tsx,stream/useAgentEventStream.ts} — Task 1 指定 transport/recovery 模式
    - frontend/src/features/records/components/MealRecordDetailPage.tsx — accessible card details 模式
    - frontend/src/features/plans/components/{PlanPage.tsx,MealCard.tsx,PlanningStatus.tsx,PlanPage.test.tsx} — Plan 09 interfaces 与 Task 1 RED
    - frontend/tests/e2e/agent.spec.ts — public thread continuation 模式
  </read_first>
  <files>frontend/src/features/plans/api/schemas.ts, frontend/src/features/plans/api/client.ts, frontend/src/features/plans/components/PlanPage.tsx, frontend/src/features/plans/components/MealCard.tsx, frontend/src/features/plans/components/PlanningStatus.tsx</files>
  <action>扩展 Zod safe adjustment DTO/client，用同一 owned thread 与 fresh idempotency command key 发送 labelled textarea 或 contained meal selection；不发送 constraint decision。实现 successful changed-slot Badge、old/new/matched constraint、focus/live summary；ambiguous 三餐 choice；UI-SPEC 四事实 RELAX Alert；精确三次 limit actions；focused refusal。保留固定餐次顺序、D-08 dish fields、persistent disclaimer、单一 scroll、existing event parser allowlist。客户端不得写 memory、放宽 exclusion/health、显示 raw feedback/internal IDs/provider/tool/reasoning，亦不得发第四次 request。</action>
  <verify><automated>cd frontend && npm run typecheck && npm run lint && npm test -- --run src/features/plans/components/PlanPage.test.tsx</automated></verify>
  <done>用户可持续安全地调整一份计划直至确定性上限，所有恢复状态符合 UI-SPEC。</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| feedback UI → Agent adjustment command | 自由文本不能成为客户端可强制执行的规则。 |
| safe report → H5 recovery state | 只有后端安全投影可呈现。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-27 | Tampering | adjustment UI | mitigate | closed client DTO；server retains constraint/health/relax authority。 |
| T-05-28 | Information disclosure | feedback rendering | mitigate | 只显示 safe report，无 raw input/ledger/provider/tool/internal IDs。 |
| T-05-29 | Denial of service | repeated adjustments | mitigate | client disables fourth submit，backend remains final hard cap。 |
</threat_model>
<verification>运行 Vitest/typecheck/lint/Playwright；内置浏览器 adjustment 流于 Plan 06 在真实完整路径执行。</verification>
<success_criteria>PLN-05 的用户调整保留约束、清晰解释且不能通过 UI 绕开 Phase 5 安全模型。</success_criteria>
<output>Create `.planning/phases/05-diet-planning-subgraph/05-10-SUMMARY.md` when done.</output>
