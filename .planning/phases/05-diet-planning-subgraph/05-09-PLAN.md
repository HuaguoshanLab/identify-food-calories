---
phase: 05-diet-planning-subgraph
plan: "09"
type: execute
wave: 7
depends_on: [05-03, 05-04, 05-05]
files_modified: [frontend/src/features/plans/format.ts, frontend/src/features/plans/format.test.ts, frontend/src/features/plans/components/PlanOverview.tsx, frontend/src/features/plans/components/MealCard.tsx, frontend/src/features/plans/components/PlanningStatus.tsx, frontend/src/features/plans/components/PlanPage.tsx, frontend/src/features/plans/components/PlanPage.test.tsx, frontend/src/App.tsx, frontend/tests/e2e/plans.spec.ts]
autonomous: true
requirements: [PLN-01, PLN-02, PLN-03, PLN-04, PLN-06]
must_haves:
  truths: ["/app/plans 显示固定早餐、午餐、晚餐和四项目标区间/计划值/文字状态。", "每个 MealCard 显示 D-08 标准菜名、受控份量、短标签和命中的偏好/忌口约束。", "用户只看到六种稳定业务阶段和持续非医疗声明。"]
  artifacts:
    - path: frontend/src/features/plans/components/MealCard.tsx
      provides: "D-08 controlled dish and matched-constraint rendering"
    - path: frontend/src/features/plans/components/PlanPage.tsx
      provides: "AppShell snapshot-first initial result route"
  key_links:
    - from: frontend/src/features/plans/components/PlanPage.tsx
      to: frontend/src/features/agent/stream/useAgentEventStream.ts
      via: "allowlisted safe business event mapping"
      pattern: "useAgentEventStream"
---
<objective>把 strict form 和后端 safe DTO 变成真实的首次 H5 一日三餐结果页。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@frontend/AGENTS.md
@docs/ui/h5-foundation.md
@.planning/phases/05-diet-planning-subgraph/05-CONTEXT.md
@.planning/phases/05-diet-planning-subgraph/05-UI-SPEC.md
@.planning/phases/05-diet-planning-subgraph/05-PATTERNS.md
@.planning/phases/05-diet-planning-subgraph/05-03-PLAN.md
@.planning/phases/05-diet-planning-subgraph/05-04-PLAN.md
@.planning/phases/05-diet-planning-subgraph/05-05-PLAN.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 写首轮结果、D-08 MealCard 和安全事件的 RED 合同</name>
  <read_first>
    - frontend/src/features/records/components/MealRecordDetailPage.tsx — Card 与 `tabular-nums` 呈现模拟
    - frontend/src/features/agent/components/AnalyzePage.tsx — snapshot-first 页面和安全业务状态模拟
    - frontend/src/features/agent/stream/useAgentEventStream.ts — snapshot/replay、sequence-gap 与事件消费边界
    - frontend/src/layouts/BottomNavigation.tsx — route-derived current Tab/`NavLink` 模拟
    - frontend/src/{App.tsx,routePaths.ts} — AppShell route composition 模拟
    - frontend/src/features/plans/components/ProfileGoalForm.test.tsx — Plan 03 已有 command form 合同
  </read_first>
  <files>frontend/src/features/plans/format.test.ts, frontend/src/features/plans/components/PlanPage.test.tsx, frontend/tests/e2e/plans.spec.ts</files>
  <behavior>
    - complete snapshot 以早餐→午餐→晚餐固定顺序渲染每卡；每道 dish 明示 standard name、受控 grams/portion、method/flavour tag 和“已遵守”matched preference/exclusion line（D-06、D-08）。
    - energy/carbohydrate/protein/fat 均为“目标 range · 计划 value · 偏低/适中/偏高”加文本/icon，不伪装医疗精度（D-07）。
    - 仅 allowlist 六个 Plan 02/04 business events；raw event 被丢弃；refusal 不显示卡片/continue，首次和结果 footer 都显示 D-17 disclaimer（D-09、D-15–D-17）。
  </behavior>
  <action>以 `MealRecordDetailPage` 和 `AnalyzePage` 模拟，用 MSW 写 formatter/组件 RED；Playwright 从注册→Mailpit 验证→登录→`/app/plans` 走公开页面填写 Plan 03 表单，不能伪造 token 或直写数据库。D-08 断言必须覆盖标准名、份量、标签和 matched constraints，不能只断言卡片标题。</action>
  <verify><automated>cd frontend && npm test -- --run src/features/plans/format.test.ts src/features/plans/components/PlanPage.test.tsx</automated></verify>
  <done>RED 测试证明受控 recipe 事实真正从后端 DTO 到用户可见 MealCard。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 实现 AppShell plan route、概览、MealCard 和安全状态</name>
  <read_first>
    - frontend/src/features/records/components/MealRecordDetailPage.tsx — Task 1 指定的 card formatting 模式
    - frontend/src/features/agent/{components/AnalyzePage.tsx,stream/useAgentEventStream.ts} — 既有 SSE hook 和 page state 模式
    - frontend/src/layouts/BottomNavigation.tsx — 当前 Tab 语义
    - frontend/src/{App.tsx,routePaths.ts} — composition-only route 模式
    - frontend/src/features/plans/components/{ProfileGoalForm.tsx,PlanPage.test.tsx} — 已有 form 与 Task 1 RED
  </read_first>
  <files>frontend/src/features/plans/format.ts, frontend/src/features/plans/components/PlanOverview.tsx, frontend/src/features/plans/components/MealCard.tsx, frontend/src/features/plans/components/PlanningStatus.tsx, frontend/src/features/plans/components/PlanPage.tsx, frontend/src/App.tsx</files>
  <action>实现 `format.ts` 的 whole-number/range grammar；以 AppShell 的唯一 scroll root 替换 `/app/plans` placeholder。PlanPage 使用既有 `useAgentEventStream`，严格 Zod parse snapshot、只映射六个安全事件；不可新建 event parser。PlanOverview 固定四项 metrics，MealCard 固定三个 slot、info-only dish rows、D-08 standard name/controlled portion/tag/matched constraint line，PlanningStatus 提供 live status、error/refusal/relaxation safe states。按 UI-SPEC 使用 h1/h2/h3、Alert、语义 tokens、44px controls、focus 与无横向溢出；不添加完整 recipe/steps/ingredients/shopping list/charts、raw feedback、provider/tool/token/cost/ID/reasoning。</action>
  <verify><automated>cd frontend && npm run typecheck && npm run lint && npm test -- --run src/features/plans/format.test.ts src/features/plans/components/PlanPage.test.tsx</automated></verify>
  <done>登录用户能从公开 DTO 得到清晰、安全、可访问且满足 D-06–D-09/D-17 的三餐结果。</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| snapshot/SSE → React | 未验证或内部服务端文本不能进入用户界面。 |
| controlled recipe DTO → MealCard | 只渲染安全展示字段，不能把 auditing/internal data 泄露。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-24 | Information disclosure | status/UI | mitigate | Zod parse、closed event allowlist、formatter 不显示 internal fields。 |
| T-05-25 | Elevation of privilege | refusal UI | mitigate | BLOCK_HEALTH_SCOPE alert 无 cards、target 或 bypass CTA。 |
| T-05-26 | Tampering | displayed constraints | mitigate | UI 只展示 backend D-08 safe report，不计算/改写 exclusions。 |
</threat_model>
<verification>运行 Vitest/typecheck/lint/Playwright；内置浏览器完整首次生成验收归 Plan 06。</verification>
<success_criteria>MealCard 的 D-08 四项行为在组件与 E2E 均有明确断言，H5 不泄露 Agent 内部。</success_criteria>
<output>Create `.planning/phases/05-diet-planning-subgraph/05-09-SUMMARY.md` when done.</output>
