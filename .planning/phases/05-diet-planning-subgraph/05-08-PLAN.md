---
phase: 05-diet-planning-subgraph
plan: "08"
type: execute
wave: 3
depends_on: [05-01, 05-07]
files_modified: [backend/app/planning/data/README.md, backend/app/planning/data/controlled-recipes.v1.json, backend/app/planning/models.py, backend/app/planning/repository.py, backend/app/planning/service.py, backend/app/planning/README.md, backend/migrations/versions/0011_controlled_recipes.py, backend/tests/planning/test_planning_service.py]
autonomous: true
requirements: [PLN-02, PLN-03, PLN-04]
must_haves:
  truths: ["只从来源、许可、审核和目录资格完整的受控菜谱组合早餐、午餐、晚餐。", "每道菜有标准名称、受控份量、短做法/口味标签和命中的约束；最终营养由合格目录克数重算。"]
  artifacts:
    - path: backend/app/planning/data/controlled-recipes.v1.json
      provides: "R-03 项目自有、可审计的受控 recipe seed"
    - path: backend/migrations/versions/0011_controlled_recipes.py
      provides: "ControlledRecipe 和 ingredient 资格 schema"
  key_links:
    - from: backend/app/planning/service.py
      to: backend/app/nutrition/service.py
      via: "qualified catalog item ID + grams 的 Decimal 重算"
      pattern: "calculate_nutrition"
---
<objective>
将 R-03 和 D-08 落地为可审核、可重算的受控菜谱与确定性组合入口。

Purpose: 图只能消费具备展示信息和数据授权链的候选，不能把持久化总计或模型文本当营养真相。
Output: controlled-recipe migration、project-authored seed、资格筛选和行为测试。
</objective>
<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@backend/AGENTS.md
@.planning/phases/05-diet-planning-subgraph/05-CONTEXT.md
@.planning/phases/05-diet-planning-subgraph/05-RESEARCH.md
@.planning/phases/05-diet-planning-subgraph/05-PATTERNS.md
@.planning/phases/05-diet-planning-subgraph/05-01-PLAN.md
@.planning/phases/05-diet-planning-subgraph/05-07-PLAN.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 写 D-08/R-03 资格、重算和三餐组合的 RED 合同</name>
  <read_first>
    - backend/app/nutrition/{models.py,schemas.py,service.py} — qualified catalog、版本和每 100g Decimal 重算模拟
    - backend/app/nutrition/data/README.md — versioned seed/data README 模式
    - backend/app/records/{models.py,repository.py} — ORM/repository SQL 查询模式
    - backend/tests/unit/test_nutrition.py — Fake repository 的数值真相测试模式
    - backend/tests/planning/test_planning_service.py — Plan 01 target/validation 合同
  </read_first>
  <files>backend/tests/planning/test_planning_service.py</files>
  <behavior>
    - 仅有 stable recipe ID、标准菜名、受控 grams/portion、短 method/flavour tags、slot eligibility、project_authored source/reference、`LicenseRef-Project-Authored-v1`、recipe/catalog/audit version、approved audit status、reviewer role/time 的 `ControlledRecipe` 可被激活（D-08、R-03）。
    - 缺任何 R-03 字段、catalog 版本不匹配、ingredient 非 qualified 或受控份量无效时被拒绝；存储的 recipe total 不可作为返回数值真相。
    - 合格候选为早餐/午餐/晚餐各提供标准名、受控份量、标签和 matched preference/exclusion summaries，营养从 ingredient catalog ID + grams 重新计算。
  </behavior>
  <action>扩展 Plan 01 的 fake-port 测试。依照 nutrition 模拟，为每个元数据门和每种拒绝情况写负例，并对三个 slot 断言完整 D-08 public recipe fields及目录重算；不得读取第三方食谱、图片、长步骤或购物清单。</action>
  <verify><automated>cd backend && uv run pytest tests/planning/test_planning_service.py -q</automated></verify>
  <done>RED 合同使缺少来源/许可/审核/版本/资格的食谱永远不可进入组合。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 实现审核 seed、recipe 存储与确定性候选查询</name>
  <read_first>
    - backend/app/nutrition/{models.py,schemas.py,service.py} — catalog reference 和重算实现模拟
    - backend/app/records/{models.py,repository.py} — Task 1 指定的 ORM 与 owner-filtered repository 模式
    - backend/migrations/versions/0007_meal_records_memory_ledger.py — migration 模式
    - backend/tests/planning/test_planning_service.py — Task 1 RED 合同
  </read_first>
  <files>backend/app/planning/data/README.md, backend/app/planning/data/controlled-recipes.v1.json, backend/app/planning/models.py, backend/app/planning/repository.py, backend/app/planning/service.py, backend/app/planning/README.md, backend/migrations/versions/0011_controlled_recipes.py</files>
  <action>创建 `data/README.md` 并更新 planning README 索引。以 R-03 只放项目自有的结构化短数据 seed；每条 `ControlledRecipe` 和其 ingredients 同时写入标准菜名、meal-slot eligibility、受控 grams/portion、短 method/flavour tags、source_kind/reference/license、recipe/catalog/audit version、approved audit status、audited_at、`nutrition_catalog_reviewer`。用 ORM/Alembic 约束和 Repository 激活查询重复强制这些字段与 qualified catalog 映射。PlanningService 只返回满足 D-08 的安全 DTO，并通过 nutrition 服务按克数重算营养；不抓取第三方内容、不持久化完整步骤、不展示 source/license 给 H5。</action>
  <verify><automated>cd backend && uv run alembic upgrade head && uv run pytest tests/planning/test_planning_service.py -q</automated></verify>
  <done>每个可选菜谱均有标准名、受控份量、标签、审核授权链和可复算营养链。</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| seed/recipe database → PlanningService | 未审核、未知许可或不可重算的内容不能进入用户计划。 |
| recipe ingredients → nutrition catalog | 最终数值必须来自合格 catalog 和受控 grams。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-07 | Tampering | recipe metadata | mitigate | seed、DTO、migration 与 active query 四层强制 R-03，并有负例测试。 |
| T-05-08 | Information disclosure | third-party recipe content | mitigate | 只允许 project-authored 短结构化 seed；无抓取、正文、图片或购物单。 |
| T-05-09 | Tampering | nutrition totals | mitigate | 以 qualified food ID 和 grams 重算，拒绝持久化总计作为真相。 |
</threat_model>
<verification>执行 migration、受控菜谱资格/重算测试与后端 lint/type-check。</verification>
<success_criteria>D-08 所需菜品展示事实在后端有受控、可审计且可计算的单一来源。</success_criteria>
<output>Create `.planning/phases/05-diet-planning-subgraph/05-08-SUMMARY.md` when done.</output>
