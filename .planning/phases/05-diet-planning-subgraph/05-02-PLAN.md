---
phase: 05-diet-planning-subgraph
plan: "02"
type: execute
wave: 4
depends_on: [05-01, 05-07, 05-08]
files_modified: [backend/app/agent/state.py, backend/app/agent/graph.py, backend/app/agent/tools.py, backend/app/agent/service.py, backend/app/agent/schemas.py, backend/app/agent/api.py, backend/app/main.py, backend/tests/unit/test_diet_planning_graph.py, backend/tests/integration/test_diet_planning_agent_api.py]
autonomous: true
requirements: [PLN-01, PLN-02, PLN-03, PLN-04, PLN-06]
must_haves:
  truths: ["现有 Agent 入口将 diet_planning 路由到独立子图并返回固定早餐、午餐、晚餐安全报告。", "规划 checkpoint 与餐食分析 checkpoint 永不互相反序列化，SSE 只公开稳定业务事件。"]
  artifacts:
    - path: backend/app/agent/state.py
      provides: "独立版本的 DietPlanningState 和 codec/namespace mapping"
    - path: backend/app/agent/graph.py
      provides: "DietPlanningGraph 和主图路由"
  key_links:
    - from: backend/app/agent/graph.py
      to: backend/app/agent/tools.py
      via: "typed PlanningToolAdapter methods"
      pattern: "calculate_daily_target|compose_daily_plan|validate_daily_plan"
---
<objective>
通过既有 owned-thread、ledger、Checkpoint 和 SSE 生命周期交付首轮一日三餐生成。

Purpose: 满足 PLN-01–04 的真正子图，同时保护图→工具边界、预算、tenant ownership 与无思维链事件合同。
Output: DietPlanningState/Graph、Agent public command、safe DTO/SSE 和 Fake Provider/HTTPX 测试。
</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@backend/AGENTS.md
@.planning/phases/05-diet-planning-subgraph/05-CONTEXT.md
@.planning/phases/05-diet-planning-subgraph/05-RESEARCH.md
@.planning/phases/05-diet-planning-subgraph/05-PATTERNS.md
@.planning/phases/05-diet-planning-subgraph/05-01-PLAN.md
@.planning/phases/05-diet-planning-subgraph/05-07-PLAN.md
@.planning/phases/05-diet-planning-subgraph/05-08-PLAN.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 写首轮 planning graph、checkpoint 和安全事件 RED 合同</name>
  <read_first>
    - AGENTS.md 与 backend/AGENTS.md — Graph 只能经 tools 调领域服务、公开 API 和测试安全边界
    - .planning/phases/05-diet-planning-subgraph/05-CONTEXT.md — D-04、D-06、D-09、D-14–D-17 的 locked behavior
    - .planning/phases/05-diet-planning-subgraph/05-01-PLAN.md — `PlanningProfileInput`、`PreferenceReview` 与确定性 action 合同
    - .planning/phases/05-diet-planning-subgraph/05-07-PLAN.md 与 05-08-PLAN.md — profile CRUD 和 controlled-recipe 上游安全 DTO
    - backend/app/agent/{state.py,graph.py,service.py,tools.py,schemas.py,api.py} — MealAnalysisGraph、owned thread、checkpoint、SSE 模拟物
    - backend/tests/{unit/test_agent_memory_context.py,integration/test_meal_records.py} — Fake Provider、真实 PostgreSQL/HTTPX owner/idempotency 测试模式
  </read_first>
  <files>backend/tests/unit/test_diet_planning_graph.py, backend/tests/integration/test_diet_planning_agent_api.py</files>
  <behavior>
    - 新 owned `diet_planning` command 只接受完整 transient `PlanningProfileInput`、safe `PreferenceReview.confirmed=true` 和显式 `save_profile`; false 不写 profile，true 使用 command key 幂等写入（D-01、D-04）。
    - 未确认资料/偏好在 target/recipe 调用前 `NEEDS_INPUT`；完成时严格返回早餐、午餐、晚餐及 D-08 dish public fields。
    - 仅 `reading_context|calculating_targets|composing_plan|validating_plan|complete|needs_input` 可进 snapshot/SSE；无 prompt、provider、candidate ranking、tool output、cost、internal ID 或思维链（D-09）。
    - three composition/replan attempts 后停止；foreign snapshot/resume 为不可用；两种 checkpoint codec 交叉反序列化失败。
  </behavior>
  <action>参照 `MealAnalysisGraph`、`AgentService.execute_run`、`NutritionToolAdapter`、`test_agent_memory_context.py` 和 `test_direct_memory_public_api.py`，用 Fake Reasoning Provider/Fake Planning Adapter 先建 unit RED，再用真实 PostgreSQL HTTPX 建 owner 与 idempotency 合同。此任务只创建自己的后端测试文件，不读取或验证任何前端文件。</action>
  <verify><automated>cd backend && uv run pytest tests/unit/test_diet_planning_graph.py tests/integration/test_diet_planning_agent_api.py -q</automated></verify>
  <done>RED 测试定义首轮可达、三槽、受限、用户隔离的后端行为。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 实现独立 State、图、工具端口与 public Agent 接线</name>
  <read_first>
    - AGENTS.md 与 backend/AGENTS.md — 独立 State/Schema/Provider DTO、Graph→tools-only 和 fail-closed checkpoint 规则
    - .planning/phases/05-diet-planning-subgraph/05-PATTERNS.md — `DietPlanningGraph` budget/codec/tool-adapter 和 safe SSE projection 模式
    - .planning/phases/05-diet-planning-subgraph/05-01-PLAN.md — PlanningService public contracts
    - .planning/phases/05-diet-planning-subgraph/05-07-PLAN.md 与 05-08-PLAN.md — profile upsert 和 recipe/query 上游边界
    - backend/app/agent/{state.py,graph.py,tools.py,service.py,schemas.py,api.py} — Task 1 要扩展的现有 Agent contracts
    - backend/tests/{unit/test_diet_planning_graph.py,integration/test_diet_planning_agent_api.py} — Task 1 RED 合同
  </read_first>
  <files>backend/app/agent/state.py, backend/app/agent/graph.py, backend/app/agent/tools.py, backend/app/agent/service.py, backend/app/agent/schemas.py, backend/app/agent/api.py, backend/app/main.py</files>
  <action>扩展既有 Agent contracts，新增 frozen `DietPlanningStartCommand`，独立 versioned `DietPlanningState`、safe interrupt、immutable breakfast/lunch/dinner slots、`replan_count<=3`、safe report 和 graph-kind→State codec→checkpoint namespace mapping。主图以 `diet_planning` 替换 not-available 分支；Graph 只经 `agent/tools.py` 调用 Plan 01/07/08 服务，Graph 不得 import ORM、Repository、MemoryService 或数值公式。保留 AgentRun command-key、owner validation、tool/model/time/cost budgets 和 checkpoint persist fail-closed；新 safe DTO/event enum 仅承载稳定业务摘要。仅在 `save_profile=true` 时调用 Plan 07 profile upsert，以 run command key 去重。</action>
  <verify><automated>cd backend && uv run pytest tests/unit/test_diet_planning_graph.py tests/integration/test_diet_planning_agent_api.py tests/unit/test_agent_memory_context.py -q</automated></verify>
  <done>认证用户能经既有 Agent API 获得三个确定性校验餐次或安全 needs-input/refusal，且 planning/meal checkpoint 完全隔离。</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| public command/resume → AgentService | command、thread 和 state kind 都不可信。 |
| graph → tool adapter | 图只可请求 runtime-validated facts，服务保留数据与规则所有权。 |
| state/event ledger → SSE | 内部执行信息必须安全投影后才可见。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-10 | Spoofing/Information disclosure | thread snapshot/resume | mitigate | owner predicate、graph kind/state-version 校验和 foreign A/B tests。 |
| T-05-11 | Tampering | command/tool input | mitigate | closed DTO、typed tool port 与 deterministic service actions。 |
| T-05-12 | Denial of service | graph loop | mitigate | inherited budgets、`replan_count<=3` 和 terminal guard。 |
| T-05-13 | Information disclosure | checkpoint/SSE | mitigate | safe enum/projection，拒绝 raw provider/tool/reasoning/IDs/cost。 |
</threat_model>
<verification>运行 Fake Provider 图、真实 PostgreSQL Agent API、既有 Agent 回归和序列化 forbidden-field 断言。</verification>
<success_criteria>后端首轮规划是独立且有界的真实 Agent 子图，不是平行执行器或前端计算。</success_criteria>
<output>Create `.planning/phases/05-diet-planning-subgraph/05-02-SUMMARY.md` when done.</output>
