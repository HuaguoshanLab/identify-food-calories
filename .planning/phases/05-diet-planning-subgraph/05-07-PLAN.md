---
phase: 05-diet-planning-subgraph
plan: "07"
type: execute
wave: 2
depends_on: [05-01]
files_modified: [backend/app/planning/models.py, backend/app/planning/repository.py, backend/app/planning/api.py, backend/app/planning/service.py, backend/app/planning/README.md, backend/app/main.py, backend/migrations/versions/0010_planning_profiles.py, backend/tests/integration/test_planning_profile_api.py]
autonomous: true
requirements: [PLN-01, PLN-06]
must_haves:
  truths: ["用户只能显式保存、读取、更新和删除自己的最小化身体资料与目标；删除后读取不可用。", "偏好不进入 profile，profile API 不接受客户端 user_id。"]
  artifacts:
    - path: backend/app/planning/api.py
      provides: "认证、所有者隔离的 /api/v1/planning/profile CRUD"
    - path: backend/migrations/versions/0010_planning_profiles.py
      provides: "PlanningProfile 最小化和软删除 schema"
  key_links:
    - from: backend/app/planning/api.py
      to: backend/app/planning/service.py
      via: "AuthenticatedPrincipal user_id"
      pattern: "PlanningProfileService"
---
<objective>
交付个人资料的唯一持久化边界和公开 API。

Purpose: 实现 D-04、D-05、D-18 的显式保存、用户隔离和删除权；`backend/app/planning/api.py` 由本计划创建并归此计划所有。
Output: migration、Profile Repository/Service/API、真实 PostgreSQL HTTPX 合同。
</objective>
<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@backend/AGENTS.md
@.planning/phases/05-diet-planning-subgraph/05-CONTEXT.md
@.planning/phases/05-diet-planning-subgraph/05-RESEARCH.md
@.planning/phases/05-diet-planning-subgraph/05-PATTERNS.md
@.planning/phases/05-diet-planning-subgraph/05-01-PLAN.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 写 owner-scoped profile CRUD 的 RED 集成合同</name>
  <read_first>
    - backend/app/records/models.py — `MealRecord` 的 user_id、soft-delete、检查约束和 partial index 模式
    - backend/app/records/repository.py — `get_record_for_user` 的 user_id SQL predicate 模式
    - backend/app/records/{service.py,api.py} — Service 事务边界、AuthenticatedPrincipal 和 404 归一化模式
    - backend/migrations/versions/0007_meal_records_memory_ledger.py — Alembic upgrade/downgrade 模式
    - backend/tests/integration/test_meal_records.py — 真实 PostgreSQL/HTTPX A/B 用户隔离模式
  </read_first>
  <files>backend/tests/integration/test_planning_profile_api.py</files>
  <behavior>
    - 未认证为 401；GET/PUT/PATCH/DELETE 仅处理 owner 的 height、weight、age、formula variant、activity、goal、speed 和版本字段。
    - foreign、missing、soft-deleted profile 均为相同的 404；删除后 GET/PATCH 和计划 prefill 查询均不可读取（D-05、D-18）。
    - request 额外字段、偏好字段、客户端 user_id、非法速度/公式均为 422，且没有行写入。
  </behavior>
  <action>使用项目隔离 PostgreSQL 与 HTTPX 写入 RED 集成测试；通过公开认证链建立 A/B 两名合成用户，不直接构造数据库行。测试只覆盖本计划将创建的 profile CRUD，不引用未来 Agent 启动命令或前端测试文件。</action>
  <verify><automated>cd backend && uv run pytest tests/integration/test_planning_profile_api.py -q</automated></verify>
  <done>RED 测试固定最小化存储、404 归一化、删除后零读取和偏好职责分离。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 实现最小化 profile 存储、API 与运行时注册</name>
  <read_first>
    - backend/app/records/{models.py,repository.py,service.py,api.py} — Task 1 指定的持久化和 HTTP 模拟
    - backend/app/main.py — router 注册和 lifespan 依赖装配模式
    - backend/app/planning/{schemas.py,ports.py,service.py} — Plan 01 的严格 DTO/领域边界
    - backend/tests/integration/test_planning_profile_api.py — Task 1 RED 合同
  </read_first>
  <files>backend/app/planning/models.py, backend/app/planning/repository.py, backend/app/planning/api.py, backend/app/planning/service.py, backend/app/planning/README.md, backend/app/main.py, backend/migrations/versions/0010_planning_profiles.py</files>
  <action>按 records 模拟实现只保存 D-18 必需字段的 `PlanningProfile`、tenant-filtered Repository 和 Service 事务边界；不存忌口、口味、原始反馈、Provider 数据或自由医疗说明。用 Alembic 建表、range/check 约束、每用户一个 active row 的 index 和 downgrade。创建 `backend/app/planning/api.py`，通过 `AuthenticatedPrincipal` 暴露 `/api/v1/planning/profile` GET/PUT/PATCH/DELETE，统一 foreign/missing/deleted 为不可用，并在 `backend/app/main.py` 注册 router。更新 `backend/app/planning/README.md` 的职责、允许依赖和文件索引；此 API 是后续 Plan 05 前端资料页唯一依赖。</action>
  <verify><automated>cd backend && uv run alembic upgrade head && uv run pytest tests/integration/test_planning_profile_api.py -q</automated></verify>
  <done>资料 CRUD 只对 owner 可见、可删除且不产生第二个偏好来源。</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| profile HTTP request → PlanningProfileService | 敏感资料必须认证、校验并以 owner 过滤。 |
| deletion → later profile read | 删除后的缓存和数据库读取均不能复活资料。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-04 | Spoofing/Information disclosure | profile CRUD | mitigate | `AuthenticatedPrincipal` 与每个 SQL 查询的 `user_id` predicate。 |
| T-05-05 | Tampering | profile persistence | mitigate | Pydantic closed DTO、DB check、Service commit/rollback 和 422 合同。 |
| T-05-06 | Information disclosure | deleted data | mitigate | soft-delete filter、404 归一化和删除后 A/B API 测试。 |
</threat_model>
<verification>执行 Alembic、真实 PostgreSQL HTTPX profile 隔离/删除测试和后端 lint/type-check。</verification>
<success_criteria>存在单一、最小化、可删除的 profile 权威存储，且 `api.py` 所有权明确归 Plan 07。</success_criteria>
<output>Create `.planning/phases/05-diet-planning-subgraph/05-07-SUMMARY.md` when done.</output>
