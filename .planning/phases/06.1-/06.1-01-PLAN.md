---
phase: "06.1"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [backend/migrations/versions/0023_managed_recipe_candidates.py, backend/app/planning/models.py, backend/app/planning/schemas.py, backend/app/planning/ports.py, backend/app/planning/repository.py, backend/app/planning/service.py, backend/tests/planning/test_managed_recipe_candidates.py, backend/tests/integration/test_managed_recipe_candidate_repository.py]
autonomous: true
requirements: [PLN-03, PLN-04, ADM-02, ARC-02, ARC-03]
must_haves:
  truths: ["成品菜候选只引用合格、可计算的营养目录条目，候选本身不保存营养真相", "早餐、午餐、晚餐是必需槽位；加餐是可选槽位", "停用或软删除的候选绝不能进入后续计划"]
  artifacts: ["backend/migrations/versions/0023_managed_recipe_candidates.py", "backend/app/planning/service.py"]
  key_links: ["managed_recipe_candidates → qualified FoodCatalogItem → deterministic nutrition calculation", "candidate status/deleted_at → planning repository eligibility query"]
---
<objective>建立管理员成品菜候选的权威数据模型和规划领域不变量。输出可线性升级的 0023 迁移、严格 DTO/port、目录资格查询和先失败后转绿的 PostgreSQL 证据。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @backend/AGENTS.md @.planning/phases/06.1-/06.1-CONTEXT.md @.planning/phases/06.1-/06.1-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 锁定候选资格和可选加餐的 RED 合同</name><files>backend/tests/planning/test_managed_recipe_candidates.py, backend/tests/integration/test_managed_recipe_candidate_repository.py</files><read_first>backend/app/planning/{models.py,schemas.py,ports.py,repository.py,service.py}; backend/app/nutrition/models.py; backend/tests/planning/test_planning_service.py; backend/tests/integration/test_catalog_publish_eligibility.py</read_first><action>先写 fake repository 与真实 PostgreSQL 失败测试：候选必须关联具备完整每 100g 营养的已发布合格 `FoodCatalogItem`；候选字段为目录条目、`breakfast|lunch|dinner|snack`、固定克数、份量说明、做法/口味标签、`pending|enabled|disabled`、revision、deleted_at。断言主餐缺失失败、加餐可省略、目录失格/停用/软删除均 fail closed，且候选营养由 `NutritionService.calculate_nutrition` 重算。</action><acceptance_criteria>测试在实现前失败，并覆盖跨目录版本、四营养值缺失、status 变化、软删除和 snack 不可作为必需餐。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/planning/test_managed_recipe_candidates.py tests/integration/test_managed_recipe_candidate_repository.py -q</automated></verify></task>
<task type="auto" tdd="true"><name>Task 2: 实现候选表、资格查询和可选餐次领域模型</name><files>backend/migrations/versions/0023_managed_recipe_candidates.py, backend/app/planning/models.py, backend/app/planning/schemas.py, backend/app/planning/ports.py, backend/app/planning/repository.py, backend/app/planning/service.py, backend/tests/planning/test_managed_recipe_candidates.py, backend/tests/integration/test_managed_recipe_candidate_repository.py</files><read_first>backend/migrations/versions/0022_meal_record_item_catalog_version.py; backend/app/planning/repository.py; backend/app/admin/models.py; backend/app/nutrition/models.py</read_first><action>新增 `ManagedRecipeCandidate`，以 FK 引用 `food_catalog_items`，持久化 meal_slot、portion_grams、portion_description、method_tags、flavour_tags、status、revision、created/updated/deleted_at；0023 的 `down_revision` 固定为 `0022`，加约束和可筛选索引。新增 frozen/extra-forbid 的候选、状态与“required main meals + optional snack” DTO；Repository 只返回 enabled、未删除、目录版本一致且目录当前合格的候选。保留旧 `controlled_recipes` seed 的审计语义，不放松其项目自有配方约束。</action><acceptance_criteria>Alembic 只有一条 `0022 → 0023` 路径；资格查询从 SQL 层排除失格条目；所有 Task 1 测试转绿。</acceptance_criteria><verify><automated>cd backend && APP_ENV=test uv run alembic upgrade head && uv run pytest tests/planning/test_managed_recipe_candidates.py tests/integration/test_managed_recipe_candidate_repository.py -q && uv run alembic heads</automated></verify></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| catalog item → managed candidate | 管理员关联不能把未发布/失格营养数据带入计划 |
| candidate status → planner | 过期、停用或删除状态不能被缓存绕过 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-061-01 | Tampering | candidate/catalog FK | mitigate | 资格联查、FK、SQL 过滤和 PG 测试。 |
| T-061-02 | Repudiation | candidate state | mitigate | revision、时间戳和后续管理审计。 |</threat_model>
<verification>Alembic、fake service 和真实 PostgreSQL 候选资格测试通过。</verification>
<success_criteria>候选池有独立、可审计的数据边界，且没有不合格目录数据能进入计划。</success_criteria>
<output>Create .planning/phases/06.1-/06.1-01-SUMMARY.md when done</output>
