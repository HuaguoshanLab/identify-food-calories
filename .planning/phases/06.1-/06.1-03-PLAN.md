---
phase: "06.1"
plan: "03"
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified: [backend/app/planning/service.py, backend/app/planning/repository.py, backend/app/planning/schemas.py, backend/app/planning/archive_schemas.py, backend/app/planning/archive_service.py, backend/app/agent/tools.py, backend/app/agent/graph.py, backend/tests/planning/test_planning_service.py, backend/tests/planning/test_plan_archive.py, backend/tests/unit/test_diet_planning_graph.py, backend/tests/integration/test_diet_planning_agent_api.py]
autonomous: true
requirements: [PLN-03, PLN-04, PLN-05, UI-03]
must_haves:
  truths: ["计划从已启用候选池可复现地轮换，不再固定取排序第一项", "三主餐总是存在；加餐只有通过整日校验时才出现", "加餐可被反馈替换并完整归档"]
  artifacts: ["backend/app/planning/service.py", "backend/app/agent/graph.py", "backend/app/planning/archive_schemas.py"]
  key_links: ["candidate eligibility query → deterministic rotation → validate_plan", "optional snack report → archive snapshot → H5-safe DTO", "feedback slot → tool adapter → replacement → revalidation"]
---
<objective>将管理员候选池真正接入规划运行时，完成轮换、按需加餐、替换与不可变计划快照，而不是只新增后台表。</objective>
<context>@AGENTS.md @backend/AGENTS.md @.planning/phases/06.1-/06.1-CONTEXT.md @.planning/phases/06.1-/06.1-RESEARCH.md @backend/app/planning/README.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 写轮换、可选 snack、归档和反馈替换 RED 矩阵</name><files>backend/tests/planning/test_planning_service.py, backend/tests/planning/test_plan_archive.py, backend/tests/unit/test_diet_planning_graph.py, backend/tests/integration/test_diet_planning_agent_api.py</files><read_first>backend/app/planning/{service.py,repository.py,schemas.py,archive_schemas.py,archive_service.py}; backend/app/agent/{tools.py,graph.py}; backend/tests/planning/test_controlled_recipe_importer.py</read_first><action>增加失败测试：同一请求上下文可重现，连续计划优先近期未使用候选；绝不跨槽位重复；主餐缺候选终止；snack 无候选或加入后超出范围时返回三餐；合格 snack 返回顺序四餐。断言归档 report/recipe_ids 接受 3–4 项且前三餐固定、第四项只能 snack；反馈文字和 input_choices 识别 snack，替换后重新校验且保存稳定 provenance。</action><acceptance_criteria>测试明确拒绝“把 snack 加入 MealSlot 后所有长度都变四”的错误实现，并覆盖候选失格后的 fail-closed 终止。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/planning/test_planning_service.py tests/planning/test_plan_archive.py tests/unit/test_diet_planning_graph.py tests/integration/test_diet_planning_agent_api.py -q</automated></verify></task>
<task type="auto" tdd="true"><name>Task 2: 接线候选轮换、可选加餐和计划快照</name><files>backend/app/planning/service.py, backend/app/planning/repository.py, backend/app/planning/schemas.py, backend/app/planning/archive_schemas.py, backend/app/planning/archive_service.py, backend/app/agent/tools.py, backend/app/agent/graph.py, backend/tests/planning/test_planning_service.py, backend/tests/planning/test_plan_archive.py, backend/tests/unit/test_diet_planning_graph.py, backend/tests/integration/test_diet_planning_agent_api.py</files><read_first>backend/app/agent/tools.py; backend/app/agent/graph.py; backend/app/planning/archive_service.py; backend/app/planning/models.py</read_first><action>让 `compose_daily_meals` 使用 `ManagedRecipeCandidate` 资格查询；按计划请求/历史使用证据定义稳定排序，先排除当前已选、最近使用、忌口和不匹配口味，再计算候选营养。主餐失败仍 REPLAN；snack 仅作为验证通过才保留的可选追加。更新 Agent adapter 移除硬编码的旧 catalog/recipe version 选择，Graph 的 `input_choices`、`_slot_from_feedback`、len 检查、报告投影和 replacement 支持 snack。归档 DTO 允许有序 3–4 餐并记录候选版本/目录版本 provenance；历史快照不读取当前 candidate status。</action><acceptance_criteria>没有对模型开放营养计算或数据库直查；所有 Task 1 场景转绿；旧 project-authored seed 仍能作为审计历史但不再导致固定首项生产选择。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/planning/test_planning_service.py tests/planning/test_plan_archive.py tests/unit/test_diet_planning_graph.py tests/integration/test_diet_planning_agent_api.py -q</automated></verify></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| candidate pool → planning graph | 未合格候选和模型指令均不能绕过确定性资格/营养校验 |
| plan snapshot → later candidate state | 后续停用/删除不能篡改历史计划 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-061-05 | Tampering | rotation/validation | mitigate | stable seed、闭合校验、fake/PG tests。 |
| T-061-06 | Repudiation | historical plan | mitigate | candidate/catalog provenance snapshot。 |</threat_model>
<verification>规划、图、归档和真实 API 测试覆盖三餐、四餐、替换、循环上限和历史快照。</verification>
<success_criteria>计划页的菜来自管理员已启用候选池，并且按需出现的加餐不会破坏安全校验或历史记录。</success_criteria>
<output>Create .planning/phases/06.1-/06.1-03-SUMMARY.md when done</output>
