---
phase: 06.1
plan: 03
subsystem: planning
tags: [recipe-candidates, snack, archive, feedback]
requirements-completed: [PLN-03, PLN-04, PLN-05, UI-03]
completed: 2026-09-07
---

# Phase 06.1 Plan 03: 候选菜与可选加餐运行时 Summary

规划服务现在优先从已启用、未删除且营养目录仍合格的管理员候选池构成三主餐；候选不全则安全终止，不回退到固定首项种子。

## Delivered

- 三主餐优先使用候选池，并继续以确定性营养工具按克数重算。
- 存档接受有序三餐或四餐，第四项仅能是加餐。
- 调整输入只允许选择当前计划实际包含的餐次；有加餐时可反馈替换加餐。

## Verification

- `cd backend && uv run pytest tests/planning/test_planning_service.py -q` — **42 passed**。
- `cd backend && uv run pytest tests/planning/test_plan_archive.py tests/unit/test_diet_planning_graph.py -q` — **16 passed**。
- `cd backend && uv run ruff check app/planning/service.py app/planning/models.py app/planning/archive_schemas.py app/agent/graph.py` — **passed**。

## Task Commit

1. **候选池优先选择、可选加餐归档与反馈调整** — `fb63836` (feat)

## Self-Check: PASSED

- 主餐缺候选会返回 REPLAN；候选营养继续走确定性营养端口。
- 历史计划快照仍只保存当时报告，不读取后续候选状态。
