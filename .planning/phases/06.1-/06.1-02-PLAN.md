---
phase: "06.1"
plan: "02"
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified: [backend/app/admin/recipe_csv.py, backend/app/admin/schemas.py, backend/app/admin/ports.py, backend/app/admin/repository.py, backend/app/admin/service.py, backend/app/admin/api.py, backend/tests/admin/test_recipe_candidate_csv.py, backend/tests/admin/test_recipe_candidate_service.py, backend/tests/unit/test_admin_recipe_candidate_api.py, backend/tests/integration/test_admin_recipe_candidate_repository.py]
autonomous: true
requirements: [ADM-01, ADM-02, ADM-05, PLN-03]
must_haves:
  truths: ["管理员可预览并原子导入候选 CSV", "批量启用、停用、软删除都需原因、幂等和审计", "普通用户不能读取或改变候选"]
  artifacts: ["backend/app/admin/recipe_csv.py", "backend/app/admin/api.py"]
  key_links: ["CSV preview → unique catalog lookup → transactional import", "admin command → current DB role → audit event + idempotent replay"]
---
<objective>把候选池交付为安全的管理员 API：列表/筛选、模板/导出、预览/导入、单条编辑和批量启用/停用/软删除。复用目录治理语义，不复制脆弱实现。</objective>
<context>@AGENTS.md @backend/AGENTS.md @.planning/phases/06.1-/06.1-CONTEXT.md @.planning/phases/06.1-/06.1-RESEARCH.md @backend/app/admin/README.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 为候选 CSV 与生命周期写 RED 服务/API/PG 测试</name><files>backend/tests/admin/test_recipe_candidate_csv.py, backend/tests/admin/test_recipe_candidate_service.py, backend/tests/unit/test_admin_recipe_candidate_api.py, backend/tests/integration/test_admin_recipe_candidate_repository.py</files><read_first>backend/app/admin/{catalog_csv.py,schemas.py,service.py,repository.py,api.py}; backend/tests/admin/test_catalog_csv.py; backend/tests/admin/test_catalog_draft_service.py</read_first><action>用目录 CSV 的中文、UTF-8、500 行、全批回滚模式写失败测试。候选模板列固定为关联目录菜品名称、餐次、单份克数、份量说明、做法标签、口味标签、状态；预览同时校验重复的“目录菜品+餐次”、目录不存在/歧义/失格、非法枚举和克数。覆盖 list/export、If-Match 编辑、批量 enabled/disabled/deleted、原因、命令重放、冲突、401/403 和审计前后差异。</action><acceptance_criteria>任何一行错误时导入不写库；普通用户的全部 recipe endpoints 返回 403；重放同 key 不产生第二条候选或审计事件。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/admin/test_recipe_candidate_csv.py tests/admin/test_recipe_candidate_service.py tests/unit/test_admin_recipe_candidate_api.py tests/integration/test_admin_recipe_candidate_repository.py -q</automated></verify></task>
<task type="auto" tdd="true"><name>Task 2: 实现受 RBAC、审计和幂等保护的候选管理 API</name><files>backend/app/admin/recipe_csv.py, backend/app/admin/schemas.py, backend/app/admin/ports.py, backend/app/admin/repository.py, backend/app/admin/service.py, backend/app/admin/api.py, backend/tests/admin/test_recipe_candidate_csv.py, backend/tests/admin/test_recipe_candidate_service.py, backend/tests/unit/test_admin_recipe_candidate_api.py, backend/tests/integration/test_admin_recipe_candidate_repository.py</files><read_first>backend/app/admin/catalog_csv.py; backend/app/admin/service.py; backend/app/admin/models.py; backend/app/planning/models.py</read_first><action>新增 `/api/v1/admin/recipe-candidates` 的严格 DTO 和端点：GET list/filter、GET export/template、POST import-preview/import、POST create、PATCH with `If-Match`、POST enable/disable/delete。Service 先 `require_role(UserRole.ADMIN)`，再用事务、advisory lock、request hash、`Idempotency-Key`、`AdminAuditEvent` 写入服务端生成的 before/after diff 和原因；批量命令仅接收显式 ID，逐项保留可重试结果，遇 401/403 停止。软删除写 deleted_at，不物理删且不允许重新启用删除项。</action><acceptance_criteria>API 不返回目录原始营养敏感字段之外的无关数据；导出防公式执行；所有 Task 1 RED 合同转绿且真实 PG 证明目录资格再次检查发生在确认导入。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/admin/test_recipe_candidate_csv.py tests/admin/test_recipe_candidate_service.py tests/unit/test_admin_recipe_candidate_api.py tests/integration/test_admin_recipe_candidate_repository.py -q</automated></verify></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| browser CSV/IDs/revisions → admin API | 上传内容、批量选择和并发版本均不可信 |
| admin mutation → future planner | 状态改变会影响新的营养建议 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-061-03 | E/T | admin candidate endpoints | mitigate | current DB RBAC、If-Match、Idempotency-Key、审计。 |
| T-061-04 | Tampering | CSV formula/partial import | mitigate | safe export、preview、全批事务回滚。 |</threat_model>
<verification>服务、HTTPX 和真实 PG 的导入、生命周期、隔离、重放测试通过。</verification>
<success_criteria>后台可安全地批量维护真正会进入规划候选池的数据。</success_criteria>
<output>Create .planning/phases/06.1-/06.1-02-SUMMARY.md when done</output>
