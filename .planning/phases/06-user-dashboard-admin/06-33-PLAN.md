---
phase: 06-user-dashboard-admin
plan: 33
type: execute
wave: 21
depends_on: [06-32]
files_modified: [backend/app/README.md, backend/app/records/service.py, backend/app/records/api.py, backend/app/records/README.md, backend/tests/README.md, backend/tests/records/test_record_service.py, backend/tests/records/README.md, backend/tests/unit/test_meal_record_api.py, backend/tests/unit/README.md]
autonomous: true
gap_closure: true
requirements: [UI-02]
must_haves:
  truths:
    - "首次有效 IANA confirmation 返回 200；同一已保存 IANA key 重试返回原 confirmation DTO 的 200，且不产生额外写入。"
    - "不同 IANA key 只返回无泄露泛化 409；唯一约束并发 loser 会重新读取后按同/异 key 返回 200/409。"
    - "absolute-path、traversal 等 ZoneInfo ValueError key 在三个公开 Records 写命令上得到既有 400、无写入、无异常泄露。"
  artifacts:
    - path: "backend/app/records/service.py"
      provides: "same-zone idempotent confirmation and complete ZoneInfo invalid-key mapping before mutation"
      contains: "DashboardTimeZoneAlreadyConfirmed"
    - path: "backend/tests/records/test_record_service.py"
      provides: "fake-repository proof for first/same/different/concurrent confirmation and no-write invalid zones"
      contains: "confirmed_at"
    - path: "backend/tests/unit/test_meal_record_api.py"
      provides: "public 400/no-leak regression for create, edit and confirmation"
      contains: "invalid-timezone"
  key_links:
    - from: "backend/app/records/service.py"
      to: "backend/app/records/api.py"
      via: "InvalidTimeZone and generic DashboardTimeZoneAlreadyConfirmed retain established HTTP 400/409 mappings"
      pattern: "InvalidTimeZone"
---

<objective>
关闭 Records confirmation 的幂等/竞争和 ZoneInfo 非法 key 缺口，为后续 dashboard 服务端当前窗口与 H5 read gate 固定安全公开契约。

Purpose: 06-31 将已存在 preference 的 409 当作继续条件，既不能区分同/异 zone，也会让 ValueError 逃逸为 500。本计划只处理 records-owned 写入边界；dashboard current-window 规则移至 06-34。
Output: tested confirmation contract、稳定 400/409 mapping 与同步的 records/test README 索引。无需 migration：现有 preference、审计及 confirmation DTO/schema 足够。
</objective>

<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@backend/AGENTS.md
@backend/app/README.md
@backend/app/records/README.md
@backend/tests/README.md
@backend/tests/records/README.md
@backend/tests/unit/README.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-user-dashboard-admin/06-CONTEXT.md
@.planning/phases/06-user-dashboard-admin/06-RESEARCH.md
@.planning/phases/06-user-dashboard-admin/06-VERIFICATION.md
@.planning/phases/06-user-dashboard-admin/06-REVIEW.md
@.planning/phases/06-user-dashboard-admin/06-32-SUMMARY.md

<interfaces>
```text
POST /api/v1/meal-records
PATCH /api/v1/meal-records/{record_id}
POST /api/v1/meal-records/dashboard-time-zone-confirmations
  InvalidTimeZone -> existing safe 400
  DashboardTimeZoneAlreadyConfirmed -> existing generic 409

DashboardTimezoneConfirmationResponse already contains the stored preference confirmation data;
no new DTO, table, field, migration or editable preference endpoint is permitted.
```
</interfaces>
</context>

<source_coverage_audit>
| Source | Required item | Plan coverage |
|---|---|---|
| GOAL/REQ | UI-02 consistent dashboard entry | Task 1 establishes the safe records-owned confirmation gate consumed by 06-34/35. |
| RESEARCH | validated IANA and server-owned data boundary | Task 1 validates ZoneInfo in Records service before persistence and keeps HTTP mapping public/safe. |
| CONTEXT | D-04, D-17, D-18 | Task 1 preserves immutable records attribution, tenant API boundary and zero sensitive disclosure. |
</source_coverage_audit>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 让 Records confirmation 同 zone 幂等、异 zone 冲突，并在写入前拒绝所有 ZoneInfo 非法 key</name>
  <files>backend/app/README.md, backend/app/records/service.py, backend/app/records/api.py, backend/app/records/README.md, backend/tests/README.md, backend/tests/records/test_record_service.py, backend/tests/records/README.md, backend/tests/unit/test_meal_record_api.py, backend/tests/unit/README.md</files>
  <read_first>AGENTS.md; backend/AGENTS.md; backend/app/README.md; backend/app/records/README.md; backend/tests/README.md; backend/tests/records/README.md; backend/tests/unit/README.md; .planning/phases/06-user-dashboard-admin/06-VERIFICATION.md; .planning/phases/06-user-dashboard-admin/06-REVIEW.md; backend/app/records/service.py; backend/app/records/api.py; backend/app/records/schemas.py; backend/app/records/ports.py; backend/app/records/repository.py; backend/tests/records/test_record_service.py; backend/tests/unit/test_meal_record_api.py</read_first>
  <behavior>
    - Test 1: first valid IANA confirmation returns 200 and creates exactly one preference/backfill audit/commit. The identical normalized IANA key returns the stored `DashboardTimezoneConfirmationResponse` as 200 with original `confirmed_at`, zero extra backfill/audit/commit.
    - Test 2: a different valid key receives only generic `DashboardTimeZoneAlreadyConfirmed`/409, with no stored key/timestamp disclosure, overwrite, record change, audit or commit.
    - Test 3: after concurrent unique `IntegrityError`, service rolls back/reloads and returns stored 200 for same key or generic conflict for different key; same-zone loser is never 409.
    - Test 4: absolute-path and traversal-shaped invalid keys raise `InvalidTimeZone`, not `ValueError`; create, edit and confirmation have no write before/after an existing preference.
    - Test 5: all three public HTTP commands return existing safe 400 with no Python/filesystem/stack/Provider/token/preference/record detail.
  </behavior>
  <action>先写 fake-repository service 与 HTTPX/TestClient 红灯。`MealRecordService.confirm_dashboard_time_zone()` 先验证 candidate IANA；现存 preference 的规范化 `ZoneInfo.key` 相同才从存储行返回原 `DashboardTimezoneConfirmationResponse`，不 backfill、不新增 audit、不 commit；不同 key 只抛既有泛化 `DashboardTimeZoneAlreadyConfirmed`。首次 insert 遇到唯一 `IntegrityError` 时 rollback 后 reread，再按相同规则返回，不能把同 key 误为 409。只在 `_validated_time_zone()` 将 `ValueError` 与既有 `TypeError`、`ZoneInfoNotFoundError` 转为 `InvalidTimeZone`；`api.py` 保持现有 400/409 mapping，即便无路由改动也要 HTTPX 锁定。用 fake repository records/preference/audit/commit 证明三写命令无写入，不得直写数据库伪造。于同一 Task/提交更新 `backend/app/records/README.md`、`backend/tests/records/README.md`、`backend/tests/unit/README.md` 以及实际父级 `backend/app/README.md`、`backend/tests/README.md` 的文件索引与公开契约；不得移交后续计划。落实 D-04、D-17、D-18。</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/records/test_record_service.py tests/unit/test_meal_record_api.py -q &amp;&amp; uv run ruff check app/records tests/records/test_record_service.py tests/unit/test_meal_record_api.py</automated></verify>
  <acceptance_criteria>same key 可安全重试且不写入；different key 泛化 409；竞态同 key 仍 200；任意 ZoneInfo 非法 shape 无 500、无部分写入、无敏感泄露。</acceptance_criteria>
  <done>Records 为 06-34/35 提供安全 confirmation read gate，目录 README 与实现同次更新。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser IANA → Records command | 不可信 zone 在 service 运行时验证，写入前拒绝非法 key。 |
| concurrent preference writes → service | 唯一约束竞争只可产生 idempotent stored 200 或泛化 conflict。 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-33-01 | T/E | confirmation | mitigate | normalize IANA, same stored key only idempotent 200, different key generic 409. |
| T-06-33-02 | T | concurrent insert | mitigate | rollback/reread then same/different decision. |
| T-06-33-03 | I | invalid-key errors | mitigate | map ZoneInfo exception family to existing safe 400 without detail. |
| T-06-SC | Tampering | dependencies | accept | existing stdlib zoneinfo/tests only; no install. |
</threat_model>

<verification>只运行公开 API/service tests；不以直写 preference、token/cookie 或 mock endpoint 证明行为。</verification>
<success_criteria>Records confirmation 和非法时区输入对所有调用路径确定、安全、可回归。</success_criteria>
<output>Create `.planning/phases/06-user-dashboard-admin/06-33-SUMMARY.md` when done.</output>
