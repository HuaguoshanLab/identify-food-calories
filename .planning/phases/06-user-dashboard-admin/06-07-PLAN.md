---
phase: 06-user-dashboard-admin
plan: 07
type: execute
wave: 4
depends_on: [06-02]
files_modified: [backend/migrations/versions/0013_weekly_review_results.py, backend/app/dashboard/models.py, backend/app/dashboard/weekly_review_dto.py, backend/app/dashboard/repository.py, backend/app/dashboard/service.py, backend/tests/dashboard/test_weekly_review_cache_service.py, backend/tests/integration/test_weekly_review_cache_repository.py, backend/tests/dashboard/test_weekly_review_facts.py]
autonomous: true
requirements: [UI-02]
must_haves:
  truths: ["低覆盖周只显示事实汇总和覆盖范围，零 Provider 调用", "相同 review key 的并发请求复用同一结果，版本/facts 改变自然失效"]
  artifacts: ["backend/migrations/versions/0013_weekly_review_results.py", "backend/app/dashboard/weekly_review_dto.py"]
  key_links: ["facts DTO → unique WeeklyReviewResult cache key", "concurrent request → reuse/wait → one AgentRun"]
---
<objective>建立 facts-first 周复盘存储、缓存和并发复用。Purpose: 实现 D-05–D-08 与既定 WeeklyReviewResult 决议。Output: 最小结果表、确定性 facts、唯一键和真实 PostgreSQL 测试。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@backend/AGENTS.md
@.planning/phases/06-user-dashboard-admin/06-AI-SPEC.md
@.planning/phases/06-user-dashboard-admin/06-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 写 facts、唯一 cache key 和并发 RED 测试</name><files>backend/tests/dashboard/test_weekly_review_cache_service.py, backend/tests/integration/test_weekly_review_cache_repository.py, backend/tests/dashboard/test_weekly_review_facts.py</files><read_first>backend/app/dashboard/repository.py; backend/app/agent/models.py; backend/app/agent/service.py; backend/tests/agent/test_agent_service.py</read_first><behavior>- facts 固定星期一开始、coverage_days/meal_count/totals/approved patterns，并在少于 4 日或 8 餐时确定性 INSUFFICIENT_COVERAGE、零 Provider 调用。
- key 精确为 user_id, week_start, facts_digest, graph_version, prompt_version, schema_version, runtime_config_version；任一变动自然 miss。
- 相同 key 并发请求锁定复用/等待一个结果；cache hit 零 Provider 调用；OUTCOME_UNKNOWN 永不重放。</behavior><action>先用 injected clock/Fake Provider 写服务 RED 测试，并以真实 PostgreSQL 竞争请求测试唯一约束、事务回滚、等待者结果和 cache hit 调用计数。测试只放合成事实，不持久 prompt、原 facts、provider body 或 reasoning。</action><acceptance_criteria>测试在存储实现前失败，且能分辨缓存命中、运行中等待、安全弃权和未知结果。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/dashboard/test_weekly_review_cache_service.py tests/integration/test_weekly_review_cache_repository.py tests/dashboard/test_weekly_review_facts.py -q</automated></verify><done>RED 测试完整锁定唯一键、低覆盖零调用、并发复用与自然失效。</done></task>
<task type="auto" tdd="true"><name>Task 2: 实现最小结果表和 facts-first cache service</name><files>backend/migrations/versions/0013_weekly_review_results.py, backend/app/dashboard/models.py, backend/app/dashboard/weekly_review_dto.py, backend/app/dashboard/repository.py, backend/app/dashboard/service.py, backend/tests/dashboard/test_weekly_review_cache_service.py, backend/tests/integration/test_weekly_review_cache_repository.py, backend/tests/dashboard/test_weekly_review_facts.py</files><read_first>backend/app/records/models.py; backend/app/agent/models.py; backend/migrations/versions/0012_planning_completion_projection.py</read_first><action>迁移新增最小 WeeklyReviewResult/等价表，唯一键严格包含 user/week/facts_digest/graph/prompt/schema/runtime-config version，只保存经语义校验后的安全建议或 abstention code、versions、result digest 和关联 AgentRun。Repository 以唯一约束和 SELECT FOR UPDATE/等价机制让同 key 请求复用或等待，绝不把运行中/OUTCOME_UNKNOWN 当可重放结果。Service 在 Provider 前计算 tenant-filtered persisted-local-date facts、coverage 与 digest；低覆盖直接返回事实汇总。</action><acceptance_criteria>表和 DTO 不含 prompt、原 facts、provider body、完整输出或 reasoning；并发和版本失效以真实 PostgreSQL 可证明。</acceptance_criteria><verify><automated>cd backend && APP_ENV=test uv run alembic upgrade head && uv run pytest tests/dashboard/test_weekly_review_cache_service.py tests/integration/test_weekly_review_cache_repository.py tests/dashboard/test_weekly_review_facts.py -q</automated></verify><done>周复盘拥有数据最小化、可并发复用、版本安全失效的持久 cache 基础。</done></task>
<task type="auto"><name>Task 3: 固定 0013 的线性迁移前驱</name><files>backend/migrations/versions/0013_weekly_review_results.py</files><read_first>backend/migrations/versions/0012_planning_completion_projection.py; backend/migrations/env.py</read_first><action>把 `0013_weekly_review_results.py` 的 down_revision 明确指向 `0012_planning_completion_projection`。06-07 经 06-02→06-03 到达 0012 后才执行，并维持最终唯一顺序 `0010 → 0011 → 0012 → 0013 → 0014 → 0015 → 0016 → 0017`，不得创建平行 revision。</action><acceptance_criteria>0013 只接续 0012，后续 admin 迁移有唯一可接续 head。</acceptance_criteria><verify><automated>cd backend && uv run alembic heads && uv run alembic history --verbose</automated></verify><done>weekly review 存储迁移不会破坏唯一链。</done></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| records SQL → facts DTO | 只能传递聚合事实 |
| cache key → concurrent runs | 错误复用或重放会重复收费 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-12 | I | WeeklyReviewResult | mitigate | minimal columns/digests only/serialization tests。 |
| T-06-13 | D/R | cache concurrency | mitigate | DB unique+lock、wait/reuse tests、OUTCOME_UNKNOWN no replay。 |
| T-06-SC | Tampering | package installs | accept | 不安装新包。 |</threat_model>
<verification>fake service、真实 PostgreSQL unique/locking、facts cache tests 通过。</verification>
<success_criteria>周复盘事实、缓存和费用边界在模型调用前已确定且可验证。</success_criteria>
<output>Create .planning/phases/06-user-dashboard-admin/06-07-SUMMARY.md when done</output>
