---
phase: 06-user-dashboard-admin
plan: 28
type: execute
wave: 2
depends_on: [06-26, 06-27]
files_modified: [admin-frontend/src/features/config/ConfigSummaryPage.tsx, admin-frontend/src/features/config/ConfigSummaryPage.test.tsx, admin-frontend/playwright.config.ts, admin-frontend/tests/e2e/admin-management.spec.ts, admin-frontend/tests/e2e/README.md, admin-frontend/tests/README.md, admin-frontend/README.md]
autonomous: true
gap_closure: true
requirements: [ADM-01, ADM-02, ADM-04, ADM-05, ARC-08]
must_haves:
  truths:
    - "在全新隔离库中，首位管理员只能在浏览器账户已验证后由受审计 CLI bootstrap；CLI 不创建 RuntimeConfig，也不能替代 Guard 或业务 API 的授权证据。"
    - "独立 admin SPA 的真实 Guard Bearer probe 200 后，管理员可仅经 RuntimeConfig 页面创建 enabled、正预算、非密钥 version 1，并观察公开 POST 201。"
    - "管理员可在同一独立后台页面完成目录草稿、服务器差异预览、审核、发布和失格；普通用户的真实 Bearer probe 返回 403 且不渲染私有后台。"
  artifacts:
    - path: "admin-frontend/playwright.config.ts"
      provides: "拥有隔离 backend、用户 SPA 与独立 admin SPA 的串行 Playwright runner"
      contains: "reuseExistingServer: false"
    - path: "admin-frontend/tests/e2e/admin-management.spec.ts"
      provides: "首位管理员、RuntimeConfig、目录生命周期与 normal-user 403 的公开 E2E"
      contains: "runtime-config"
    - path: "admin-frontend/src/features/config/ConfigSummaryPage.tsx"
      provides: "空 RuntimeConfig 的 version-0 创建 UI"
      contains: "config?.version ?? 0"
    - path: "admin-frontend/tests/e2e/README.md"
      provides: "后台 E2E 的隔离环境、bootstrap 边界与禁止捷径合同"
  key_links:
    - from: "admin-frontend/src/features/config/ConfigSummaryPage.tsx"
      to: "/api/v1/admin/runtime-config"
      via: "in-memory Bearer, If-Match: 0 and Idempotency-Key public POST"
      pattern: "saveRuntimeConfig.*config\\?\\.version \\?\\? 0"
    - from: "admin-frontend/playwright.config.ts"
      to: "backend/tests/run_pg.py -> backend/scripts/run_initialized_app.py"
      via: "validated test target, reset, migration, seed, readiness and owned FastAPI lifecycle"
      pattern: "run_pg.py.*run_initialized_app.py"
    - from: "admin-frontend/tests/e2e/admin-management.spec.ts"
      to: "/api/v1/admin/probe"
      via: "AdminRouteGuard's observed in-memory Bearer request"
      pattern: "admin/probe"
---

<objective>
建立独立后台的可重复真实 E2E 闭环：空隔离库从已验证账户、唯一受审计的首位管理员 bootstrap、Guard probe，到 RuntimeConfig UI、目录治理和普通用户拒绝。

Purpose: Phase 6 缺失的不是又一层 mock，而是能证明真实认证 cookie、内存 Bearer、公开 `/api/v1/admin/*`、数据库 RBAC、配置准入与独立 SPA 实际接通的 runner。CLI 只可处理无管理员库的首位角色，绝不能成为配置或授权的捷径。
Output: 空 RuntimeConfig 创建修复、独立后台 Playwright 配置和 admin-management spec，以及准确的测试/项目 README 运行合同。
</objective>

<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@admin-frontend/AGENTS.md
@frontend/AGENTS.md
@backend/AGENTS.md
@.planning/STATE.md
@.planning/phases/06-user-dashboard-admin/06-CONTEXT.md
@.planning/phases/06-user-dashboard-admin/06-RESEARCH.md
@.planning/phases/06-user-dashboard-admin/06-PATTERNS.md
@.planning/phases/06-user-dashboard-admin/06-UI-SPEC.md
@.planning/phases/06-user-dashboard-admin/06-VERIFICATION.md
@.planning/phases/06-user-dashboard-admin/06-20-SUMMARY.md
@.planning/phases/06-user-dashboard-admin/06-26-PLAN.md
@.planning/phases/06-user-dashboard-admin/06-27-PLAN.md
@admin-frontend/package.json
@admin-frontend/vite.config.ts
@admin-frontend/src/App.tsx
@admin-frontend/src/auth/AdminLoginPage.tsx
@admin-frontend/src/auth/AdminRouteGuard.tsx
@admin-frontend/src/features/config/ConfigSummaryPage.tsx
@admin-frontend/src/features/config/api/index.ts
@admin-frontend/src/features/catalog/CatalogDraftPage.tsx
@admin-frontend/src/features/catalog/CatalogLifecyclePage.tsx
@admin-frontend/tests/e2e/README.md
@admin-frontend/tests/README.md
@admin-frontend/README.md
@frontend/playwright.config.ts
@frontend/tests/e2e/auth-helpers.ts
@backend/scripts/run_initialized_app.py
@backend/tests/run_pg.py
@backend/app/admin/cli.py
@backend/app/admin/api.py
@backend/app/agent/service.py

<interfaces>
From `admin-frontend/src/auth/AdminRouteGuard.tsx`:
```typescript
const probeResponseSchema = z.object({ status: z.literal('ADMIN_ACCESS_GRANTED') }).strict()
// GET `${__ADMIN_API_BASE_URL__}/probe`, Authorization: Bearer <in-memory token>
```

From `admin-frontend/src/features/config/api/index.ts`:
```typescript
readRuntimeConfig(accessToken): Promise<RuntimeConfig>
saveRuntimeConfig(accessToken, values, idempotencyKey, expectedVersion): Promise<RuntimeConfig>
// saveRuntimeConfig POSTs only the non-secret allowlisted command with If-Match.
```

From `backend/app/admin/cli.py`:
```text
python -m app.admin.cli bootstrap --email <verified-email> --reason <non-empty reason>
// Operational first-role command only. It never creates RuntimeConfig.
```

From `backend/scripts/run_initialized_app.py`:
```text
guarded isolated schema reset -> Alembic -> Checkpointer -> nutrition/planning seeds -> FastAPI
// It intentionally seeds no AgentRuntimeConfigVersion.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 让空 RuntimeConfig 页面通过公共 admin 合约创建首个 enabled policy</name>
  <files>admin-frontend/src/features/config/ConfigSummaryPage.tsx, admin-frontend/src/features/config/ConfigSummaryPage.test.tsx</files>
  <read_first>AGENTS.md; admin-frontend/AGENTS.md; .planning/phases/06-user-dashboard-admin/06-CONTEXT.md; .planning/phases/06-user-dashboard-admin/06-VERIFICATION.md; .planning/phases/06-user-dashboard-admin/06-27-PLAN.md; admin-frontend/src/features/config/ConfigSummaryPage.tsx; admin-frontend/src/features/config/ConfigSummaryPage.test.tsx; admin-frontend/src/features/config/api/index.ts; admin-frontend/src/auth/AdminRouteGuard.tsx; backend/app/admin/api.py; backend/app/agent/service.py</read_first>
  <behavior>
    - Test 1: 通过 06-27 Guard 的管理员收到公开 GET runtime-config 404 时，可打开确认 dialog、填写非空理由、勾选启用和正数预算后提交。
    - Test 2: 首次提交带内存 Bearer、If-Match: 0 与 Idempotency-Key，公开 POST 返回 201 后页面只显示服务端 version 1 的非密钥摘要和 enabled 状态。
    - Test 3: 空理由、非正预算、401/403、409 和网络失败保持 fail-closed；401/403 清会话，409 保留编辑内容和可访问焦点语义。
  </behavior>
  <action>先写红灯，锁定空库 GET `/api/v1/admin/runtime-config` 返回 404 后的创建行为；随后把 `ConfigSummaryPage` 的 empty 状态作为 version 0 的合法创建前态，删除 `submit()` 对 `!config` 的静默返回。仅在 enabled、理由和每个运行预算/价格字段都为正数时，调用既有严格 Zod `saveRuntimeConfig`，以 React 内存 Bearer、随机 Idempotency-Key 和 `If-Match: config?.version ?? 0` 发送公开 POST；成功后只能以 `RuntimeConfigResponse` 刷新页面。保留现有 401/403 清 session、409 不覆盖编辑、dialog 初始取消焦点和无密钥/endpoint 回显。不得新增私有 API、环境变量写入、seed、前端角色授权或 token 持久化；`bootstrap` CLI 不得参与此动作。该任务落实 D-15、D-16、D-17、D-18。</action>
  <verify>
    <automated>cd admin-frontend &amp;&amp; npm test -- src/features/config/ConfigSummaryPage.test.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>全新隔离库中，已通过 Guard 的管理员可只用页面和公开 admin API 创建 enabled、非密钥、正预算 RuntimeConfig v1；失败路径不伪造成功或泄露敏感数据。</acceptance_criteria>
  <done>RuntimeConfig 不再是让 `/app/analyze` 在空库中无提示拒绝的隐形前置条件，且创建证据可被后续 E2E 观察。</done>
</task>

<task type="auto">
  <name>Task 2: 构建后台专属 runner，并串行验证管理生命周期与普通用户拒绝</name>
  <files>admin-frontend/playwright.config.ts, admin-frontend/tests/e2e/admin-management.spec.ts, admin-frontend/tests/e2e/README.md, admin-frontend/tests/README.md, admin-frontend/README.md</files>
  <read_first>AGENTS.md; admin-frontend/AGENTS.md; frontend/AGENTS.md; backend/AGENTS.md; .planning/phases/06-user-dashboard-admin/06-VERIFICATION.md; .planning/phases/06-user-dashboard-admin/06-26-PLAN.md; .planning/phases/06-user-dashboard-admin/06-27-PLAN.md; admin-frontend/package.json; admin-frontend/vite.config.ts; admin-frontend/src/App.tsx; admin-frontend/src/auth/AdminLoginPage.tsx; admin-frontend/src/auth/AdminRouteGuard.tsx; admin-frontend/src/features/config/ConfigSummaryPage.tsx; admin-frontend/src/features/catalog/CatalogDraftPage.tsx; admin-frontend/src/features/catalog/CatalogLifecyclePage.tsx; frontend/playwright.config.ts; frontend/tests/e2e/auth-helpers.ts; backend/scripts/run_initialized_app.py; backend/tests/run_pg.py; backend/app/admin/cli.py; backend/app/admin/api.py</read_first>
  <action>在 Task 1 的 v0 创建契约通过后，新增后台专属 `playwright.config.ts`：它独占 backend `8003`、用户 SPA `5183` 和 admin SPA `5184`（只允许 `E2E_ADMIN_BACKEND_PORT`、`E2E_ADMIN_USER_FRONTEND_PORT`、`E2E_ADMIN_FRONTEND_PORT` 覆盖），`workers: 1`、`fullyParallel: false`、`reuseExistingServer: false`、HTTP readiness、SIGTERM cleanup 与 retain-on-failure trace。backend 必须从仓库根执行 compose 测试依赖，再经 `backend/tests/run_pg.py --env-file backend/.env.test.example -- backend/.venv/bin/python backend/scripts/run_initialized_app.py` 启动；不得裸 schema reset、重写 DATABASE_URL、复用未知服务或 fixed sleep。两个 Vite preview 都显式代理该 backend，CORS 只含两个 loopback origin，admin production build 显式传 `VITE_ADMIN_API_BASE_URL=/api/v1/admin`。

在单一 serial `admin-management.spec.ts` 内，仅经用户页面注册、Mailpit 公开 HTTP 读取验证码、页面验证来建立首位账户；然后以 `execFile` 子进程调用受 `run_pg.py` 包装的 audited `python -m app.admin.cli bootstrap`，只断言无敏感 exit/audit 成功。禁止导入 ORM、Repository、Service 或业务函数，禁止 DB 直写、token/cookie 注入、浏览器持久化存储和真实模型；CLI 只可作 first-admin bootstrap，绝不创建 RuntimeConfig 或替代授权。随后从 `/admin/login?returnTo=/admin/model-configs` 页面登录，点击前监听 Guard 对 `/api/v1/admin/probe` 的内存 Bearer GET 并断言 200；通过 Task 1 的 UI 创建 enabled 正预算 non-secret v1，监听 runtime-config POST 201。接着从 `/admin/catalog` 创建草稿、核验服务器字段差异与理由、审核、发布、立即失格和审计状态（D-09..D-12）；在真实 overview/runs 路由确认只调用 `/api/v1/admin/*` 且无邮箱、原图、原文、完整 state、密钥或 reasoning（D-13、D-14、D-18）。再让独立 normal user 经同样的浏览器注册/验证/后台登录，观察 Bearer probe 403，断言 `/admin/forbidden` 且 AdminShell、catalog、overview 数据从未渲染（D-17；06-27 是其契约前置）。使用语义 locator、768px+ 布局、键盘焦点断言与 response/readiness 等待。

更新三个既有 README 的索引和运行说明：每次空库 reset 的严格顺序为“浏览器验证账户 → audited CLI first-admin → admin SPA login → probe 200 → RuntimeConfig UI POST 201 → 管理流”；明确 CLI 不创建配置、`/users/me` 仅建立身份而 probe/业务端点才是授权证据，并删除‘尚无 Playwright 配置/spec’的过期说法。不得把未实际运行的命令写为通过。</action>
  <verify>
    <automated>cd admin-frontend &amp;&amp; E2E_ADMIN_BACKEND_PORT=8003 E2E_ADMIN_USER_FRONTEND_PORT=5183 E2E_ADMIN_FRONTEND_PORT=5184 npm run test:e2e -- --grep admin-management &amp;&amp; npm run typecheck &amp;&amp; VITE_ADMIN_API_BASE_URL=/api/v1/admin npm run build</automated>
  </verify>
  <acceptance_criteria>每个后台 runner 都从守护的空测试库开始，真实页面形成“账户验证 → audited initial role → Guard 200 → RuntimeConfig UI 201 → 目录治理”；normal user 真实 probe 403 后没有私有 DOM 或缓存数据。</acceptance_criteria>
  <done>独立后台的管理/拒绝路径成为可重复的公开 E2E，而不是用角色 CLI、fixture 或前端 guard 伪造的成功。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Playwright runner → PostgreSQL | 不受保护 reset 或本地服务复用会破坏开发数据并制造假绿。 |
| verified browser account → initial admin role | 仅受审计 CLI 可完成首位角色写入；它不是 RuntimeConfig 或授权 API。 |
| in-memory Bearer → `/api/v1/admin/*` | Guard 改善 UX；每个端点仍必须由 06-26 的 replay-before-RBAC 修复和当前 DB role 强制授权。 |
| RuntimeConfig policy → Agent admission | 新运行只有在 UI 创建 enabled 正预算非密钥版本后才能经过 admission。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-28-01 | T/D | test database setup | mitigate | 仅用 `run_pg.py` + `run_initialized_app.py` 的受验证 `food_agent_test`，固定独占端口、readiness 和 `reuseExistingServer: false`。 |
| T-06-28-02 | S/E | bootstrap → admin session | mitigate | 账户先经浏览器/Mailpit 验证；CLI 仅由 audited execFile 执行首位角色，随后强制观察真实 Guard probe 200。 |
| T-06-28-03 | T/E | RuntimeConfig creation | mitigate | 空配置只能经内存 Bearer、If-Match: 0、Idempotency-Key 的公共 UI POST 201 写入；CLI、seed 和 env 均不可代替。 |
| T-06-28-04 | S/E/I | normal-user admin navigation | mitigate | 依赖 06-27 strict Bearer probe 与 06-26 DB-RBAC；E2E 断言 403、会话/cache 清理和无私有渲染。 |
| T-06-28-05 | I | admin UI and evidence | mitigate | 断言最小 DTO 与 DOM 不含邮箱、原文、原图、完整 State、Provider body、reasoning、endpoint 或密钥。 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 不安装或新增任何依赖；使用 Phase 6 已锁定 Playwright。 |
</threat_model>

<verification>
严格按 Task 1 → Task 2 执行。任何在 RuntimeConfig UI POST 201 之前开始的分析，或通过 DB、token、内部函数、fixture 或残留服务完成的‘成功’，均不计为验收证据。06-26 的全量 frontend Vitest 和 replay-before-RBAC 回归、06-27 的 identity/probe 403 回归必须先绿；本计划不替代它们。
</verification>

<success_criteria>
1. 空 RuntimeConfig UI 能以公共、非密钥、版本化合约创建 enabled 正预算 version 1，且错误/冲突/权限路径 fail closed。
2. 独立后台 runner 可在受保护的全新库中重复 admin 登录、probe 200、配置 UI、目录生命周期和审计证据。
3. 普通用户的真实 Bearer probe 为 403，后台私有页面从未渲染；CLI 不能被误称为配置或授权证明。
</success_criteria>

<output>
Create `.planning/phases/06-user-dashboard-admin/06-28-SUMMARY.md` when done.
</output>
