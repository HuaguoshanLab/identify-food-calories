---
phase: 06-user-dashboard-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/migrations/versions/0011_dashboard_time_attribution.py, backend/app/records/models.py, backend/app/records/schemas.py, backend/app/records/service.py, backend/app/records/api.py, backend/tests/records/test_record_service.py, backend/tests/integration/test_record_local_time_attribution.py, backend/tests/unit/test_meal_record_api.py]
autonomous: true
requirements: [UI-02]
must_haves:
  truths: ["餐食按用户确认的 IANA 时区和实际 consumed_at 归属本地日，补记不改变归属", "历史记录不会被伪造成可恢复的历史所在地"]
  artifacts: ["backend/migrations/versions/0011_dashboard_time_attribution.py", "backend/app/records/service.py"]
  key_links: ["create/edit request → ZoneInfo validation → persisted consumed_time_zone/consumed_local_date", "existing records → confirmed timezone backfill audit"]
---
<objective>冻结餐食统计时区事实。Purpose: 实现 D-04 与既定历史时区决议。Output: 可审计迁移、记录写入契约和真实 PostgreSQL 证明。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@backend/AGENTS.md
@.planning/phases/06-user-dashboard-admin/06-CONTEXT.md
@.planning/phases/06-user-dashboard-admin/06-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 写确认时区、本地日与回填 RED 契约</name><files>backend/tests/records/test_record_service.py, backend/tests/integration/test_record_local_time_attribution.py, backend/tests/unit/test_meal_record_api.py</files><read_first>backend/app/records/models.py; backend/app/records/schemas.py; backend/app/records/service.py; backend/app/records/api.py</read_first><behavior>- 新建/编辑只接受有效 IANA zone，按 consumed_at（非 created_at）导出 local date。
- 既有记录用户确认浏览器建议 zone 前不回填；确认后写 confirmed_timezone_backfill 来源和审计。
- 上海/洛杉矶跨午夜、补记、软删除及他人记录绝不串日。</behavior><action>先写 fake-service、HTTPX 与真实 PostgreSQL 失败测试，覆盖用户级 dashboard timezone 偏好、一次性确认、local_date_source 和审计；明确不把确认 zone 描述为历史所在地恢复。</action><acceptance_criteria>所有历史行只能由用户确认 IANA zone 计算 local date，且测试在实现前失败。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/records/test_record_service.py tests/integration/test_record_local_time_attribution.py tests/unit/test_meal_record_api.py -q</automated></verify><done>RED 测试锁定确认、回填、旅行后新增记录与跨租户隔离。</done></task>
<task type="auto" tdd="true"><name>Task 2: 持久化统计口径和餐食归属</name><files>backend/migrations/versions/0011_dashboard_time_attribution.py, backend/app/records/models.py, backend/app/records/schemas.py, backend/app/records/service.py, backend/app/records/api.py, backend/tests/records/test_record_service.py, backend/tests/integration/test_record_local_time_attribution.py, backend/tests/unit/test_meal_record_api.py</files><read_first>backend/app/auth/models.py; backend/app/records/repository.py; backend/migrations/versions/0010_planning_profiles.py</read_first><action>按 D-04 和既定决议增加用户级 IANA dashboard timezone 偏好、MealRecord submitted IANA zone、consumed_local_date、local_date_source 与确认回填审计。迁移不得静默选择默认区；已有记录必须经受保护确认 API，在同一业务事务按 consumed_at 回填并标记 confirmed_timezone_backfill。以后保存/编辑用 submitted IANA zone 持久化 local date。DTO 只能表述“当前用户确认的统计口径”，Service 负责 ZoneInfo/事务，Repository 只 flush。</action><acceptance_criteria>空库升级、确认回填、后续编辑均可重放验证；无效 zone 为 400；无虚构历史地点的字段或文案。</acceptance_criteria><verify><automated>cd backend && APP_ENV=test uv run alembic upgrade head && uv run pytest tests/records/test_record_service.py tests/integration/test_record_local_time_attribution.py tests/unit/test_meal_record_api.py -q</automated></verify><done>每条餐食都有不可漂移、可审计的 local-date 统计归属。</done></task>
<task type="auto"><name>Task 3: 固定 0011 的线性迁移前驱</name><files>backend/migrations/versions/0011_dashboard_time_attribution.py</files><read_first>backend/migrations/versions/0010_planning_profiles.py; backend/migrations/env.py</read_first><action>把 `0011_dashboard_time_attribution.py` 的 down_revision 明确指向 `0010_planning_profiles`。记录并遵守唯一最终顺序 `0010 → 0011 → 0012 → 0013 → 0014 → 0015 → 0016 → 0017`；06-01 只能创建 0011，后继迁移由其显式依赖计划串行创建，绝不生成并行 head。</action><acceptance_criteria>0011 的 revision 元数据可由 Alembic 解析，且只有一条从 0010 向前的迁移路径。</acceptance_criteria><verify><automated>cd backend && uv run alembic heads && uv run alembic history --verbose</automated></verify><done>Phase 6 迁移链的首个新节点具备唯一、可追溯前驱。</done></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| browser timezone → record API | IANA 输入和首次确认不可信 |
| confirmed timezone → historical rows | 回填形成长期统计事实 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-01 | T/R | timezone confirmation | mitigate | ZoneInfo、一次性确认、审计、真实 PG 临界时间测试。 |
| T-06-SC | Tampering | package installs | accept | 不安装新包；仅 zoneinfo。 |</threat_model>
<verification>迁移、fake service、HTTPX、真实 PostgreSQL 本地日/回填测试通过。</verification>
<success_criteria>看板统计拥有明确、持久且可解释的用户确认时区口径。</success_criteria>
<output>Create .planning/phases/06-user-dashboard-admin/06-01-SUMMARY.md when done</output>
