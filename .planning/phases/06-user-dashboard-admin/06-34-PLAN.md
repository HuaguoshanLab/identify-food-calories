---
phase: 06-user-dashboard-admin
plan: 34
type: execute
wave: 22
depends_on: [06-33]
files_modified: [backend/app/README.md, backend/app/dashboard/service.py, backend/app/dashboard/api.py, backend/app/dashboard/README.md, backend/tests/README.md, backend/tests/dashboard/test_dashboard_service.py, backend/tests/dashboard/test_weekly_review_facts.py, backend/tests/dashboard/README.md, backend/tests/unit/test_dashboard_api.py, backend/tests/unit/test_weekly_review_api.py, backend/tests/unit/README.md, docs/learning/06-dashboard-admin.md, docs/learning/README.md]
autonomous: true
gap_closure: true
requirements: [UI-02, EDU-03]
must_haves:
  truths:
    - "已确认 records-owned IANA preference 是 overview 与 default weekly-review current window 的唯一权威；browser `time_zone`、`week_start` 或部署时区都不能重定义它。"
    - "同一 UTC instant 下 stored Shanghai/browser LA 和 stored LA/browser Shanghai 都得到包含 today 的 preference-local overview week。"
    - "无 week_start current GET/refresh 为 200；explicit current Monday 为 422；仅已结束本地 Monday 在 GET/refresh 为 200，non-Monday/future 为 422。"
    - "教学文档准确使用 `DashboardTimezoneReadPort`。"
  artifacts:
    - path: "backend/app/dashboard/service.py"
      provides: "preference-owned current overview/default-review and validated ended-week domain rule"
      contains: "_local_dashboard_today"
    - path: "backend/tests/unit/test_dashboard_api.py"
      provides: "HTTP regression against browser-selected overview current range"
      contains: "America/Los_Angeles"
    - path: "docs/learning/06-dashboard-admin.md"
      provides: "accurate Chinese teaching for DashboardTimezoneReadPort and current/history boundary"
      contains: "DashboardTimezoneReadPort"
  key_links:
    - from: "backend/app/dashboard/api.py"
      to: "backend/app/dashboard/service.py"
      via: "overview accepts no effective client range; weekly delegates current/history validation to one service helper"
      pattern: "get_overview\\(user_id=principal\\)"
    - from: "backend/app/dashboard/service.py"
      to: "DashboardTimezonePreference"
      via: "DashboardTimezoneReadPort plus ZoneInfo-local injected aware clock prior to aggregates/facts"
      pattern: "get_dashboard_timezone_for_user"
---

<objective>
将 dashboard 当前 overview/default weekly 窗口收归已确认 records preference，并保留严格受域验证的历史结束周，同时校正教学 Port 名称。

Purpose: 06-30 已可读取 preference，但 overview 仍接受客户端 `week_start`，weekly current 也可由浏览器显式 Monday 旁路。06-33 固定 confirmation 后，本计划消除服务端读路径的最后授权空洞。
Output: server-authoritative dashboard contract、API/service 回归、dashboard/test README 与学习文档。无需 Alembic：只收紧现有读 API/service 规则，不变更 schema。
</objective>

<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@backend/AGENTS.md
@backend/app/README.md
@backend/app/dashboard/README.md
@backend/tests/README.md
@backend/tests/dashboard/README.md
@backend/tests/unit/README.md
@docs/learning/README.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-user-dashboard-admin/06-CONTEXT.md
@.planning/phases/06-user-dashboard-admin/06-RESEARCH.md
@.planning/phases/06-user-dashboard-admin/06-PATTERNS.md
@.planning/phases/06-user-dashboard-admin/06-VERIFICATION.md
@.planning/phases/06-user-dashboard-admin/06-REVIEW.md
@.planning/phases/06-user-dashboard-admin/06-33-SUMMARY.md

<interfaces>
```text
DashboardTimezoneReadPort.get_dashboard_timezone_for_user(user_id) -> DashboardTimezone | None
_local_dashboard_today(repository, user_id, now) validates stored IANA, converts injected aware UTC
instant to local date, or raises DashboardTimezonePreconditionError.

GET /dashboard/overview must have no effective client range.
GET/POST /dashboard/weekly-review: absent week_start is current; explicit week_start is only a completed local Monday.
Existing API mapping owns stable 409 precondition and 422 validation responses.
```
</interfaces>
</context>

<source_coverage_audit>
| Source | Required item | Plan coverage |
|---|---|---|
| GOAL/REQ | UI-02 consistent history/current dashboard | Task 1 makes all current reads one preference-local window while preserving historical completed weeks. |
| REQ | EDU-03 | Task 2 corrects public Port teaching and ties it to actual test contract. |
| RESEARCH | dashboard renders validated server projection | Task 1 derives time at service boundary, preserves tenant SQL/facts/cache order and denies corrupt/missing preference. |
| CONTEXT | D-04/D-05/D-07/D-08/D-17/D-18 | Task 1 preserves frozen local attribution, current/history rules, low coverage safety and no internal disclosure. |
</source_coverage_audit>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 让当前 dashboard 完全以 preference 定义，并收紧 weekly history 域规则</name>
  <files>backend/app/README.md, backend/app/dashboard/service.py, backend/app/dashboard/api.py, backend/app/dashboard/README.md, backend/tests/README.md, backend/tests/dashboard/test_dashboard_service.py, backend/tests/dashboard/test_weekly_review_facts.py, backend/tests/dashboard/README.md, backend/tests/unit/test_dashboard_api.py, backend/tests/unit/test_weekly_review_api.py, backend/tests/unit/README.md</files>
  <read_first>AGENTS.md; backend/AGENTS.md; backend/app/README.md; backend/app/dashboard/README.md; backend/tests/README.md; backend/tests/dashboard/README.md; backend/tests/unit/README.md; .planning/phases/06-user-dashboard-admin/06-CONTEXT.md; .planning/phases/06-user-dashboard-admin/06-VERIFICATION.md; .planning/phases/06-user-dashboard-admin/06-REVIEW.md; .planning/phases/06-user-dashboard-admin/06-33-SUMMARY.md; backend/app/dashboard/ports.py; backend/app/dashboard/repository.py; backend/app/dashboard/service.py; backend/app/dashboard/api.py; backend/app/dashboard/schemas.py; backend/app/dashboard/weekly_review_dto.py; backend/tests/dashboard/test_dashboard_service.py; backend/tests/dashboard/test_weekly_review_facts.py; backend/tests/unit/test_dashboard_api.py; backend/tests/unit/test_weekly_review_api.py</read_first>
  <behavior>
    - Test 1: at one injected aware UTC instant, stored Shanghai/browser LA and stored LA/browser Shanghai each return their own local Monday-Sunday with `today.consumed_local_date` in the seven dates; host TZ never participates.
    - Test 2: `get_overview` has no caller week_start; `GET /overview?week_start=<arbitrary/browser Monday>` cannot change its preference-current aggregate range.
    - Test 3: no-week_start current weekly GET and refresh are 200; explicit current Monday GET and refresh are 422. Old browser-derived current queries are unsupported.
    - Test 4: explicit completed local Monday GET/refresh are 200; non-Monday/future are 422 on both. Missing/corrupt preference remains 409 before aggregates/facts/cache/Provider work.
  </behavior>
  <action>先让 service/API 测试红灯。将 `DashboardService.get_overview` public signature 收紧为 `user_id`，先经 `DashboardTimezoneReadPort`、ZoneInfo 和 injected aware clock 得到 local today/current Monday 后查询七天 aggregates；API 不得传递 `week_start`、`time_zone` header/query 或 browser hint。旧 overview `week_start` 可以 HTTP 忽略，但绝不能改变范围或触达其他 aggregates。`WeeklyReviewService._review_window()` 必须共享同一 preference-local current：无参数 current，显式值仅严格早于 current Monday 的 completed Monday；GET 与 refresh 走同一 helper，explicit current/non-Monday/future 均 422。不得重归档 MealRecord、UTC fallback、schema/migration，或删除历史周。保持 preference、facts/cache/advisory lock、coverage-first/Provider 和 tenant SQL 既有顺序。于同一 Task/提交同步更新 `backend/app/dashboard/README.md`、`backend/tests/dashboard/README.md`、`backend/tests/unit/README.md` 及 `backend/app/README.md`、`backend/tests/README.md` 父索引，不能留给 Task 2。落实 D-04、D-05、D-07、D-08、D-17、D-18。</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/dashboard/test_dashboard_service.py tests/dashboard/test_weekly_review_facts.py tests/unit/test_dashboard_api.py tests/unit/test_weekly_review_api.py -q &amp;&amp; uv run ruff check app/dashboard tests/dashboard tests/unit/test_dashboard_api.py tests/unit/test_weekly_review_api.py</automated></verify>
  <acceptance_criteria>current range 仅由已确认 preference 定义；跨 zone 同 instant 的 today/week 自洽；历史仅已结束周，refresh 无独立绕过。</acceptance_criteria>
  <done>浏览器无法以 current week_start/timezone 改写 dashboard/default-review 读取，且 README 同次更新。</done>
</task>

<task type="auto">
  <name>Task 2: 修正 DashboardTimezoneReadPort 教学与真实 current/history 契约</name>
  <files>docs/learning/06-dashboard-admin.md, docs/learning/README.md</files>
  <read_first>AGENTS.md; backend/AGENTS.md; docs/learning/README.md; backend/app/dashboard/ports.py; backend/app/dashboard/service.py; backend/app/dashboard/api.py; backend/app/records/service.py; docs/learning/06-dashboard-admin.md</read_first>
  <action>在 Task 1 真实通过后，将不存在的 `DashboardTimezonePort` 精确改为 `DashboardTimezoneReadPort`，说明它是 dashboard consumer-owned、tenant-scoped 的只读边界，records 保有一次确认与写入。将契约写为 overview/default weekly 不接收 browser range，server 由 confirmed preference 选 current Monday；显式 week_start 只可访问已结束周。准确列出 Shanghai/Los Angeles 同 instant、current/history 与 absolute-path 400/no-write 的测试线索，不记录账户、时区值、餐食、Provider/State、stack 或虚构 browser evidence。于同一 Task/提交更新 `docs/learning/README.md` 索引。</action>
  <verify><automated>rg -n "DashboardTimezonePort|DashboardTimezoneReadPort|week_start" docs/learning/06-dashboard-admin.md docs/learning/README.md &amp;&amp; test "$(rg -c "DashboardTimezonePort" docs/learning/06-dashboard-admin.md)" -eq 0</automated></verify>
  <acceptance_criteria>教学 Port 与源码一致，不再暗示 browser-derived current range 安全。</acceptance_criteria>
  <done>dashboard 读边界、测试线索与中文教学无命名漂移。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser query/header → dashboard API | 当前范围不接受浏览器权威；service 从 tenant preference 派生。 |
| explicit history date → weekly service | 仅 completed Monday 可触达历史 facts/cache。 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-34-01 | T | current overview/review | mitigate | ignore/remove caller current range and derive injected-clock local Monday from preference. |
| T-06-34-02 | E | historical window | mitigate | service enforces completed Monday for GET and refresh identically. |
| T-06-34-03 | I/D | absent/corrupt preference | mitigate | existing precondition 409 before aggregate/facts/cache/Provider work. |
| T-06-SC | Tampering | dependencies | accept | existing zoneinfo and test stack only; no install. |
</threat_model>
<verification>执行 Task 1→2；禁止 host TZ、直写 preference、客户端 range 或 mock dashboard endpoint 取得假绿。</verification>
<success_criteria>服务端 current-window 权威、历史周边界、中文教学均可由自动回归验证。</success_criteria>
<output>Create `.planning/phases/06-user-dashboard-admin/06-34-SUMMARY.md` when done.</output>
