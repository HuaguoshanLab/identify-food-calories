---
phase: 06-user-dashboard-admin
plan: 04
type: execute
wave: 4
depends_on: [06-02, 06-03]
files_modified: [frontend/src/features/records/api/dashboard.ts, frontend/src/features/records/components/TodaySummaryCard.tsx, frontend/src/features/records/components/WeeklyTrend.tsx, frontend/src/features/records/components/HistoryMealList.tsx, frontend/src/features/records/components/RecordsPage.tsx, frontend/src/features/records/components/TodaySummaryCard.test.tsx, frontend/src/features/records/components/WeeklyTrend.test.tsx, frontend/src/features/records/components/HistoryMealList.test.tsx]
autonomous: true
requirements: [UI-02]
must_haves:
  truths: ["records 页首屏同一张今日摘要卡依次显示今日已记录摄入、按既有产品格式显示的总能量和当天已保存餐食数，且优先于本周趋势与周复盘", "仅 strict eligibility DTO 明确为 true 时，该卡显示蛋白质/脂肪/碳水相对目标区间状态；无资格用户仍见真实总能量和餐食数，不见目标条、百分比或猜测目标"]
  artifacts: ["frontend/src/features/records/api/dashboard.ts", "frontend/src/features/records/components/HistoryMealList.tsx"]
  key_links: ["RecordsPage → strict Zod dashboard overview eligibility/target DTO → no client-side profile lookup", "Load more → opaque server cursor"]
---
<objective>把 dashboard 事实 API 接入 Records 页，保持四 Tab 和唯一滚动区。Purpose: 实现 D-01–D-04。Output: 可访问 summary/trend/history 和 Vitest。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@frontend/AGENTS.md
@docs/ui/h5-foundation.md
@.planning/phases/06-user-dashboard-admin/06-UI-SPEC.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 建立 projection-only H5 dashboard strict-client RED 测试</name><files>frontend/src/features/records/components/TodaySummaryCard.test.tsx, frontend/src/features/records/components/WeeklyTrend.test.tsx, frontend/src/features/records/components/HistoryMealList.test.tsx</files><read_first>frontend/src/features/records/components/RecordsPage.tsx; frontend/src/features/records/api/schemas.ts; frontend/src/features/records/api/dashboard.ts; frontend/src/layouts/AppShell.tsx; frontend/src/layouts/BottomNavigation.tsx</read_first><behavior>- 页面顺序是今日→七日趋势→按日期历史，四 Tab/唯一 PageScrollArea 不变。
- TodaySummaryCard 的唯一事实来源是 06-02 `GET /dashboard/overview` 的 today `totals` 与 `meal_count`，以及 `extra=forbid` 的 strict eligibility/target DTO；卡片首屏同卡显示“今日已记录摄入”、总能量与当天已保存餐食数，三者优先于趋势和复盘，数值使用 `tabular-nums`。
- energy 沿用既有产品的能量显示格式，不显示无意义小数或伪精度；eligibility=false、缺失或 DTO 验证失败时仍显示真实 overview totals/meal_count，但不渲染任何目标条、百分比或猜测目标。
- records dashboard 只请求 `/dashboard/overview`、`/dashboard/history` 和周复盘端点；它不请求 profile/plan 端点且不以本地资料推导 eligibility 或 targets。
- DTO 拒绝 extra 字段；query key 包含 confirmed timezone、weekStart、cursor。
- SVG 有 role img、键盘点和同数据 semantic table；无资格显示 D-03 文案而非零目标。</behavior><action>用 MSW 和 Testing Library role/label 先写失败测试，06-02 overview fixture 必须以 today `totals`/`meal_count` 加 `extra=forbid` strict eligibility DTO 作为 TodaySummaryCard 唯一来源；断言首屏同卡按“今日已记录摄入→既有格式总能量→当天已保存餐食数”显示且数值有 `tabular-nums`，energy 不出现无意义小数。分别覆盖 eligibility=true 的三宏量相对目标状态，及 eligibility=false/缺失/严格 DTO 拒绝时保留真实 totals/meal_count、完全不出现目标条/百分比/猜测目标。继续覆盖成功、空态、局部错误、cursor 后页、focus、reduced-motion 和结构化 320px 无横向滚动合同；不得 mock 浏览器营养计算。</action><acceptance_criteria>实现前每一状态失败；TodaySummaryCard 的可见能量与餐食数可追溯到 06-02 overview `totals`/`meal_count`，宏量状态只可追溯到 strict eligibility DTO，数据表不是图的可选附加物。</acceptance_criteria><verify><automated>cd frontend && npm test -- --run src/features/records/components/TodaySummaryCard.test.tsx src/features/records/components/WeeklyTrend.test.tsx src/features/records/components/HistoryMealList.test.tsx</automated><human-check>通过实际 H5 `/app/records` 与公开 dashboard API（不用 MSW、伪造 token 或直接写库）验证：首屏同卡先显示“今日已记录摄入”、按既有格式的总能量和餐食数；使用 06-02 strict eligibility DTO 的有资格帐户可见三项宏量状态，无资格帐户只见真实 totals/meal_count 且无目标条或百分比。</human-check></verify><done>Vitest/MSW 测试锁定 06-02 overview totals/meal_count 与 strict eligibility DTO 来源、首屏卡片层级、`tabular-nums`、既有格式的整数/必要精度显示、无资格诚实降级、cursor 和趋势可访问性；真实页面验收路径可复现相同行为。</done></task>
<task type="auto" tdd="true"><name>Task 2: 实现 summary、trend 和 history 的 projection-only 切片</name><files>frontend/src/features/records/api/dashboard.ts, frontend/src/features/records/components/TodaySummaryCard.tsx, frontend/src/features/records/components/WeeklyTrend.tsx, frontend/src/features/records/components/HistoryMealList.tsx, frontend/src/features/records/components/RecordsPage.tsx, frontend/src/features/records/components/TodaySummaryCard.test.tsx, frontend/src/features/records/components/WeeklyTrend.test.tsx, frontend/src/features/records/components/HistoryMealList.test.tsx</files><read_first>frontend/src/features/records/api/client.ts; frontend/src/features/records/components/MealRecordEditPage.tsx; .planning/phases/06-user-dashboard-admin/06-UI-SPEC.md</read_first><action>在 records feature 创建 strict Zod API client/query-key factory。`DashboardOverview` schema 只接受 06-02 `GET /dashboard/overview` 的 today `totals`、`meal_count` 和 `extra=forbid` strict eligibility/target-ranges/target-version DTO；TodaySummaryCard 紧跟 h1，首屏同一张卡先渲染“今日已记录摄入”、由 overview totals 显示的总能量和由 overview meal_count 显示的当天已保存餐食数，三处数值使用 `tabular-nums`。总能量复用既有产品能量格式化规则，只显示必要精度、不得制造小数伪精度；仅 eligibility=true 时同卡显示三宏量低/中/高相对目标状态。eligibility=false、撤销、缺失或严格 DTO 拒绝时，仍显示真实 overview totals/meal_count，但不渲染目标条、百分比或猜测目标。严禁调用 `features/plans` profile API、读取缓存 profile 或在浏览器计算/猜测目标。WeeklyTrend 使用固定七点 SVG 加同数据 semantic table；HistoryMealList 按服务端 local-date 分组并消费 opaque next_cursor。局部 Skeleton/Alert 不替换整页；使用现有 tokens、Lucide、可见焦点、150–200ms 动效，禁止新增 dashboard/admin Tab 或滚动根。</action><acceptance_criteria>真实 API 成功时用户在趋势与周复盘之前，先在同一 TodaySummaryCard 看见可追溯到 06-02 overview `totals`/`meal_count` 的“今日已记录摄入”、既有格式总能量和餐食数；三宏量状态只在 strict eligibility DTO=true 时显示。无资格、撤销资格、缺失或错误状态不虚构目标但保留真实 totals/meal_count；记录详情链接保留且前端只有 projection-backed dashboard 链路。</acceptance_criteria><verify><automated>cd frontend && npm test -- --run src/features/records/components/TodaySummaryCard.test.tsx src/features/records/components/WeeklyTrend.test.tsx src/features/records/components/HistoryMealList.test.tsx && npm run typecheck</automated><human-check>使用 Codex 内置浏览器经实际 `/app/records` 和公开 06-02 dashboard overview API 验证两种帐户：有 strict eligibility DTO 的帐户在首屏摘要卡看见三种宏量状态；无资格帐户仍看见该 API 的真实 totals/meal_count，却没有目标条、百分比或猜测目标。确认卡片在趋势和周复盘前，且总能量没有不必要小数。</human-check></verify><done>/app/records 只消费 06-02 dashboard completion-projection 契约：TodaySummaryCard 从 overview totals/meal_count 显示首屏同卡能量与餐食数，并只从 strict eligibility DTO 显示宏量目标状态；Vitest/MSW 与真实页面均验证既有精度格式、`tabular-nums`、无资格诚实降级、H5 导航和无障碍。</done></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| dashboard HTTP → H5 | 响应可漂移或含多余字段 |
| chart → user | 视觉不能是唯一数据表达 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-06 | I | dashboard UI | mitigate | strict Zod/no raw fields/MSW tests。 |
| T-06-07 | R | trend | mitigate | semantic table、labels、keyboard points。 |
| T-06-SC | Tampering | package installs | accept | 现有 SVG/Lucide，不安装图表包。 |</threat_model>
<verification>Vitest/MSW、typecheck；Phase 17 走真实浏览器路径。</verification>
<success_criteria>用户可按确认本地日读懂今日、趋势和历史，目标不会被虚构。</success_criteria>
<output>Create .planning/phases/06-user-dashboard-admin/06-04-SUMMARY.md when done</output>
