---
phase: 06-user-dashboard-admin
plan: 10
type: execute
wave: 1
depends_on: []
files_modified: [admin-frontend/README.md, admin-frontend/AGENTS.md, admin-frontend/package.json, admin-frontend/package-lock.json, admin-frontend/index.html, admin-frontend/tsconfig.json, admin-frontend/tsconfig.app.json, admin-frontend/tsconfig.node.json, admin-frontend/vite.config.ts, admin-frontend/tailwind.config.ts, admin-frontend/postcss.config.js, admin-frontend/components.json]
autonomous: true
requirements: [ARC-08, ADM-01]
must_haves:
  truths: ["独立 admin-frontend 能以锁定依赖可重复安装、类型检查和构建", "后台绝不通过 filesystem import 复用用户 H5"]
  artifacts: ["admin-frontend/package-lock.json", "admin-frontend/vite.config.ts", "admin-frontend/components.json", "admin-frontend/README.md"]
  key_links: ["independent Vite config → fail-closed public admin API base", "package-lock → npm ci reproducible build"]
---
<objective>建立独立管理员 SPA 的供应链与 Vite 构建边界。Purpose: 落实 D-17/ARC-08 的可复现项目隔离。Output: 锁定依赖、构建配置、根目录 README 与项目级 AGENTS。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@frontend/AGENTS.md
@.planning/phases/06-user-dashboard-admin/06-RESEARCH.md
@.planning/phases/06-user-dashboard-admin/06-UI-SPEC.md</context>
<tasks>
<task type="auto"><name>Task 1: 创建锁定的独立 Vite 工程配置</name><files>admin-frontend/README.md, admin-frontend/AGENTS.md, admin-frontend/package.json, admin-frontend/package-lock.json, admin-frontend/index.html, admin-frontend/tsconfig.json, admin-frontend/tsconfig.app.json, admin-frontend/tsconfig.node.json, admin-frontend/vite.config.ts, admin-frontend/tailwind.config.ts, admin-frontend/postcss.config.js, admin-frontend/components.json</files><read_first>frontend/package.json; frontend/package-lock.json; frontend/index.html; frontend/tsconfig.json; frontend/vite.config.ts; frontend/tailwind.config.ts; frontend/components.json; .planning/phases/06-user-dashboard-admin/06-RESEARCH.md</read_first><action>按 D-17 和 RESEARCH 的 Package Legitimacy Audit 创建独立 package name、package-lock、npm scripts（test/typecheck/build/test:e2e）、index.html、TS project references、Vite admin API base/port 的生产 fail-closed 校验、Tailwind/PostCSS semantic tokens 与 Base UI components.json。根目录首次创建时同时写中文 README，说明独立部署、公开 `/api/v1/admin/*` 边界及 npm ci/typecheck/build 命令；AGENTS.md 只能细化根约束，明确 DB-RBAC 与数据最小化。不得安装 audit 外直接依赖、引用 `frontend/src`、注册用户 H5 admin 路由或静默回退 API base。</action><acceptance_criteria>根目录同次具有 README，供应链锁定，生产 API base 与用户 H5 隔离可被构建及静态扫描证明。</acceptance_criteria><verify><automated>cd admin-frontend && npm ci && npm run typecheck && npm run build && ! rg -n 'frontend/src' package.json vite.config.ts</automated></verify><done>独立后台拥有可复现的供应链和构建边界，尚未混入入口或业务页面。</done></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| build env → SPA | API base 和依赖来源不可信 |
| admin SPA → user H5 | 项目必须在部署、路由和代码层隔离 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-18 | T | build configuration | mitigate | locked package-lock、production base fail-closed、build gate。 |
| T-06-19 | E | frontend boundary | mitigate | 独立 package/router contract、import scan、DB-RBAC 保留在后端。 |
| T-06-SC | Tampering | npm installs | mitigate | 仅使用 RESEARCH 已审计的锁定依赖与 npm ci。 |</threat_model>
<verification>npm ci、typecheck、build 和 import-boundary scan 通过。</verification>
<success_criteria>admin-frontend 是可构建的独立工程，不是用户 H5 的隐蔽分支。</success_criteria>
<output>Create .planning/phases/06-user-dashboard-admin/06-10-SUMMARY.md when done</output>
