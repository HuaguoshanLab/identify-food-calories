---
phase: 06-user-dashboard-admin
plan: 14
type: execute
wave: 8
depends_on: [06-12]
files_modified: [backend/migrations/versions/0016_catalog_publication_eligibility.py, backend/app/admin/models.py, backend/app/admin/repository.py, backend/app/admin/service.py, backend/app/admin/api.py, backend/app/nutrition/repository.py, backend/app/planning/repository.py, backend/tests/admin/test_catalog_lifecycle_service.py, backend/tests/integration/test_catalog_publish_eligibility.py]
autonomous: true
requirements: [ADM-02, ADM-05]
must_haves:
  truths: ["草稿经审核发布为不可变版本，未来分析/计划仅使用合格版本", "授权撤销或关键失格立即阻断未来使用但不重写 MealRecord 快照"]
  artifacts: ["backend/migrations/versions/0016_catalog_publication_eligibility.py", "backend/app/admin/service.py"]
  key_links: ["review/publish → lock+immutable pointer+audit", "eligibility overlay → nutrition/planning SQL future queries"]
---
<objective>实现目录审核、发布和失格的后端事务协议。Purpose: 完成 D-09–D-12。Output: immutable snapshot、active pointer/eligibility overlay、真实 PG 并发测试。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@backend/AGENTS.md
@.planning/phases/06-user-dashboard-admin/06-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 写发布、失格与快照稳定 RED 测试</name><files>backend/tests/admin/test_catalog_lifecycle_service.py, backend/tests/integration/test_catalog_publish_eligibility.py</files><read_first>backend/app/nutrition/repository.py; backend/app/planning/repository.py; backend/app/admin/service.py; backend/tests/integration/test_nutrition_repository.py</read_first><behavior>- review→publish 在锁/If-Match/idempotency 下只产生一个 active immutable pointer。
- server 重算 diff/hash/impact；任何 admin 可操作且每步审计。
- revoke/disqualify 立刻排除未来 nutrition/planning 查询，历史 MealRecord snapshot 值不变。</behavior><action>先写 fake service 和真实 PostgreSQL 并发 RED 测试，覆盖 double publish、rollback、client diff 伪造、授权撤销、失格及历史稳定。</action><acceptance_criteria>两个并发请求没有双 active 版本；所有 future query 被 fail closed 证明。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/admin/test_catalog_lifecycle_service.py tests/integration/test_catalog_publish_eligibility.py -q</automated></verify><done>RED 测试锁定不可变发布、立即 overlay 和历史快照的边界。</done></task>
<task type="auto" tdd="true"><name>Task 2: 实现 immutable 发布和 eligibility overlay</name><files>backend/migrations/versions/0016_catalog_publication_eligibility.py, backend/app/admin/models.py, backend/app/admin/repository.py, backend/app/admin/service.py, backend/app/admin/api.py, backend/app/nutrition/repository.py, backend/app/planning/repository.py, backend/tests/admin/test_catalog_lifecycle_service.py, backend/tests/integration/test_catalog_publish_eligibility.py</files><read_first>backend/app/nutrition/models.py; backend/app/planning/models.py; backend/migrations/versions/0015_catalog_drafts.py</read_first><action>迁移新增 immutable published snapshot、active publication pointer 和 append-only active eligibility history。Service 以 SELECT FOR UPDATE/等价 advisory lock 验 revision，重算 typed diff/impact/hash，复制发布内容、推进 pointer、写 audit 后单次 commit；review/publish/disqualify 均需 reason/confirm/idempotency。nutrition/planning repository 的未来 SQL 同时限定 active published/eligible；MealRecord 一律读取确认时 snapshot。</action><acceptance_criteria>发布不可追溯修改；失格立即生效但历史不重算；所有 lifecycle endpoint 均 DB-RBAC/409/idempotent。</acceptance_criteria><verify><automated>cd backend && APP_ENV=test uv run alembic upgrade head && uv run pytest tests/admin/test_catalog_lifecycle_service.py tests/integration/test_catalog_publish_eligibility.py -q</automated></verify><done>目录治理形成草稿→审核→不可变发布→可逆资格 overlay 的安全闭环。</done></task>
<task type="auto"><name>Task 3: 固定 0016 的线性迁移前驱</name><files>backend/migrations/versions/0016_catalog_publication_eligibility.py</files><read_first>backend/migrations/versions/0015_catalog_drafts.py; backend/migrations/env.py</read_first><action>把 `0016_catalog_publication_eligibility.py` 的 down_revision 明确指向 `0015_catalog_drafts`。06-14 只在 06-12 后执行，并保持最终唯一顺序 `0010 → 0011 → 0012 → 0013 → 0014 → 0015 → 0016 → 0017`；0017 必须接续它而非创建第二个 head。</action><acceptance_criteria>0016 只接续 0015，资格 overlay 迁移不会产生分叉。</acceptance_criteria><verify><automated>cd backend && uv run alembic heads && uv run alembic history --verbose</automated></verify><done>发布资格迁移保留唯一连续链。</done></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| lifecycle command → catalog pointer | 并发命令可篡改未来营养真相 |
| eligibility → historical records | overlay 不得重写历史快照 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-26 | T/R | publish transition | mitigate | lock/If-Match/idempotency/audit/PG concurrency。 |
| T-06-27 | T | eligibility overlay | mitigate | SQL future guard + immutable record regression tests。 |
| T-06-SC | Tampering | package installs | accept | 不安装新包。 |</threat_model>
<verification>fake service、HTTPX/PG publish lock、future eligibility/history snapshot tests 通过。</verification>
<success_criteria>管理员能安全治理未来可用目录，已确认历史事实不可篡改。</success_criteria>
<output>Create .planning/phases/06-user-dashboard-admin/06-14-SUMMARY.md when done</output>
