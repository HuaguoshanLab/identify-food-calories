---
phase: 06-user-dashboard-admin
plan: 29
type: execute
wave: 3
depends_on: [06-28]
files_modified: [frontend/playwright.config.ts, frontend/tests/e2e/records-dashboard.spec.ts, frontend/tests/e2e/README.md, docs/verification/phase-06-browser-acceptance.md, docs/verification/README.md]
autonomous: true
gap_closure: true
requirements: [UI-02, UI-03, ADM-01, ADM-04]
must_haves:
  truths:
    - "Records E2E 在自有空隔离 runner 内先经 admin UI 创建 enabled 非密钥 RuntimeConfig，再由普通用户走 analyze → 安全 SSE → 确认保存 → `/app/records`。"
    - "Records 页面呈现今日摘要、本周趋势、按日期 history 和周复盘的真实公开响应；SSE/DOM 不泄露 Provider、节点、stack、reasoning、raw payload、密钥、原图或用户原文。"
    - "Codex 内置浏览器（不是 Playwright 或截图）记录隔离账户的 RuntimeConfig UI 创建、normal-user 完整记录路径，以及 normal-user admin probe 403/forbidden/无私有渲染。"
  artifacts:
    - path: "frontend/tests/e2e/records-dashboard.spec.ts"
      provides: "RuntimeConfig preflight 后的真实 Records/SSE/weekly-review Playwright 路径"
      contains: "确认并保存"
    - path: "frontend/playwright.config.ts"
      provides: "拥有 user + admin SPA 的隔离 Records E2E runner"
      contains: "E2E_RECORDS_ADMIN_FRONTEND_PORT"
    - path: "docs/verification/phase-06-browser-acceptance.md"
      provides: "可复查的实际 Codex 内置浏览器验收记录和未完成项"
      contains: "Codex 内置浏览器"
  key_links:
    - from: "frontend/tests/e2e/records-dashboard.spec.ts"
      to: "admin-frontend RuntimeConfig UI"
      via: "Guard probe 200 followed by public runtime-config POST 201 before user analysis"
      pattern: "runtime-config.*201"
    - from: "frontend/tests/e2e/records-dashboard.spec.ts"
      to: "/api/v1/agent"
      via: "real authenticated analyze request and observed safe SSE before confirm-save"
      pattern: "text/event-stream|开始分析"
    - from: "docs/verification/phase-06-browser-acceptance.md"
      to: "actual public product pages and APIs"
      via: "date, URL, role, observable result and explicit non-shortcut evidence"
      pattern: "不.*Playwright.*截图"
---

<objective>
在独立的 H5 Records runner 补齐真实 dashboard/SSE/周复盘 E2E，并用 Codex 内置浏览器完成不可由 Playwright 代替的实际公开页面验收与证据更新。

Purpose: 06-28 证明后台可建立授权和配置；本计划消费该前置闭环，证明普通用户在不同空库中真正越过 admission 到达分析、SSE、保存和记录看板。随后必须由内置浏览器复走关键路径，浏览器验收不是截图，也不是把 Playwright 结果换个标题抄进文档。
Output: Records 专属 runner/spec/README、实际浏览器验收记录和 verification 索引。
</objective>

<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@frontend/AGENTS.md
@admin-frontend/AGENTS.md
@backend/AGENTS.md
@.planning/STATE.md
@.planning/phases/06-user-dashboard-admin/06-CONTEXT.md
@.planning/phases/06-user-dashboard-admin/06-RESEARCH.md
@.planning/phases/06-user-dashboard-admin/06-PATTERNS.md
@.planning/phases/06-user-dashboard-admin/06-UI-SPEC.md
@.planning/phases/06-user-dashboard-admin/06-VERIFICATION.md
@.planning/phases/06-user-dashboard-admin/06-26-PLAN.md
@.planning/phases/06-user-dashboard-admin/06-27-PLAN.md
@.planning/phases/06-user-dashboard-admin/06-28-PLAN.md
@frontend/playwright.config.ts
@frontend/tests/e2e/auth-helpers.ts
@frontend/tests/e2e/agent.spec.ts
@frontend/tests/e2e/records-weekly-review.spec.ts
@frontend/tests/e2e/safe-stream-progress.spec.ts
@frontend/tests/e2e/README.md
@frontend/src/features/agent/components/AnalyzePage.tsx
@frontend/src/features/records/components/RecordsPage.tsx
@admin-frontend/vite.config.ts
@admin-frontend/src/auth/AdminLoginPage.tsx
@admin-frontend/src/auth/AdminRouteGuard.tsx
@admin-frontend/src/features/config/ConfigSummaryPage.tsx
@backend/scripts/run_initialized_app.py
@backend/tests/run_pg.py
@backend/app/admin/cli.py
@backend/app/agent/api.py
@backend/app/agent/service.py
@docs/verification/phase-06-browser-acceptance.md
@docs/verification/README.md

<interfaces>
From `frontend/tests/e2e/auth-helpers.ts`:
```typescript
export type E2eAccount = { email: string; password: string }
export async function clearMailbox(request: APIRequestContext): Promise<void>
export async function registerAndActivate(page: Page, request: APIRequestContext, account: E2eAccount): Promise<void>
export async function login(page: Page, account: E2eAccount, returnTo?: string): Promise<void>
```

From `frontend/src/features/agent/components/AnalyzePage.tsx`:
```typescript
// The user action is the visible button "开始分析".
// A completed validated report exposes "确认并保存"; saving then exposes its Records link.
```

From `frontend/src/features/records/components/RecordsPage.tsx`:
```typescript
// Render order is overview -> weekly trend -> history -> weekly review.
// All four projections use authenticated public API clients and local safe DTO parsing.
```

From 06-28's runner contract:
```text
browser-verified account -> audited first-admin CLI -> admin SPA login -> Guard probe 200
-> RuntimeConfig UI public POST 201. The CLI is not configuration or authorization evidence.
```
</interfaces>
</context>

<source_coverage_audit>
| Source | Required item | Coverage |
|---|---|---|
| GOAL | 用户看懂真实历史摄入趋势；管理员安全维护 Agent 依赖数据和配置 | 06-28 admin authorization/config/catalog E2E + this plan's user analyze/save/Records/browser route |
| REQ | UI-02 dashboard/history/trend/weekly review | 06-29 Task 1 real Records E2E, Task 2 browser record |
| REQ | UI-03 safe stream state without reasoning leakage | 06-29 Task 1 observes real SSE and safe DOM; Task 2 repeats public-path check |
| REQ | ADM-01 DB-RBAC and independent admin SPA | 06-26 replay-before-RBAC, 06-27 probe contract, 06-28 admin/normal-user E2E, 06-29 browser 403 evidence |
| REQ | ADM-02/03/04/05, ARC-08 | 06-28 catalog lifecycle, minimal runs/audit, RuntimeConfig UI, independent runner and README; 06-29 consumes config for user path |
| REQ | EDU-02/03 | Existing 06-21 documentation; 06-28/29 update only E2E/browser evidence indexes, not duplicate teaching scope |
| CONTEXT | D-01..D-08 | 06-29 preserves Records order/four Tabs, actual intake only, safe SSE, and low-coverage weekly-review result |
| CONTEXT | D-09..D-18 | 06-28 proves immutable catalog lifecycle, non-secret future-only config, DB-RBAC and minimal detail; 06-29 verifies the resulting user flow and forbidden boundary |
| RESEARCH | public-API browser acceptance, isolated runner, no DB/token shortcut | Tasks 1–2 require guarded runner, real pages/public APIs and an explicit browser-evidence record |
</source_coverage_audit>

<tasks>

<task type="auto">
  <name>Task 1: 让 Records runner 自行完成 admin configuration preflight 后的真实用户 E2E</name>
  <files>frontend/playwright.config.ts, frontend/tests/e2e/records-dashboard.spec.ts, frontend/tests/e2e/README.md</files>
  <read_first>AGENTS.md; frontend/AGENTS.md; admin-frontend/AGENTS.md; backend/AGENTS.md; .planning/phases/06-user-dashboard-admin/06-VERIFICATION.md; .planning/phases/06-user-dashboard-admin/06-28-PLAN.md; frontend/playwright.config.ts; frontend/tests/e2e/auth-helpers.ts; frontend/tests/e2e/agent.spec.ts; frontend/tests/e2e/records-weekly-review.spec.ts; frontend/tests/e2e/safe-stream-progress.spec.ts; frontend/tests/e2e/README.md; frontend/src/features/agent/components/AnalyzePage.tsx; frontend/src/features/records/components/RecordsPage.tsx; admin-frontend/vite.config.ts; admin-frontend/src/auth/AdminLoginPage.tsx; admin-frontend/src/auth/AdminRouteGuard.tsx; admin-frontend/src/features/config/ConfigSummaryPage.tsx; backend/scripts/run_initialized_app.py; backend/tests/run_pg.py; backend/app/admin/cli.py; backend/app/agent/api.py; backend/app/agent/service.py</read_first>
  <action>依赖已完成的 06-28，扩展 `frontend/playwright.config.ts`，在既有 isolated FastAPI + user Vite runner 中追加一个独立 admin Vite preview（默认 `5185`，仅 `E2E_RECORDS_ADMIN_FRONTEND_PORT` 覆盖）；显式传 `VITE_ADMIN_API_BASE_URL=/api/v1/admin`、`VITE_ADMIN_API_PROXY_TARGET=&lt;this runner backend&gt;`，并把 user/admin 两个 loopback origin 都写入 CORS。保留 `run_pg.py -> run_initialized_app.py` 的 guarded reset、migrate、Checkpointer/nutrition/planning seed、readiness、SIGTERM cleanup 与 `reuseExistingServer: false`；initializer 按设计不 seed RuntimeConfig。

新增单一 serial `records-dashboard.spec.ts`，所有 preflight 和 H5 assertions 均在它自有的空库完成：先通过用户页面注册、Mailpit 公开 HTTP 验证首位账户；再由 non-shell `execFile` 调 `run_pg.py` 包装的 audited first-admin CLI，且只断言非敏感 exit/audit 成功。通过独立 admin SPA 真实登录、观察 Guard Bearer probe 200，然后在 `/admin/model-configs` 用 UI 创建 enabled、正预算、非密钥 RuntimeConfig，观察 POST 201 与 server version；CLI 绝不写 config 或替代 Guard/API 授权证据。之后以另一 normal user 页面注册/验证/登录，在 `/app/analyze` 输入受控餐食，观察真实安全 SSE 到 completed validated 报告，点击“确认并保存”，再打开 `/app/records`，按 D-01..D-07 断言今日摘要 → SVG/语义表趋势 → 按日期 history → 低覆盖周复盘及四 Tab 不变。明确检查 SSE、公开响应和 DOM 没有 Provider、node、stack、reasoning、raw payload、密钥、原图或用户原文；所有等待只基于 response/readiness/语义 locator，禁止 sleep、DB 直写、token/cookie 注入、内部函数和真实模型。更新 E2E README，登记 Records admin-preflight、端口、每次 reset 重建 policy 与禁止捷径。</action>
  <verify>
    <automated>cd frontend &amp;&amp; E2E_FRONTEND_PORT=5182 E2E_BACKEND_PORT=8002 E2E_RECORDS_ADMIN_FRONTEND_PORT=5185 npm run test:e2e -- --grep records-dashboard &amp;&amp; npm test -- playwright.config.test.ts</automated>
  </verify>
  <acceptance_criteria>Records E2E 不借用 06-28 的数据库、账号、配置或进程：同一个 fresh runner 中 admin UI 的 public POST 201 明确发生在 normal-user analyze/SSE/save/Records 之前，且 user 页面展示真实受限数据而非 mock。</acceptance_criteria>
  <done>dashboard/SSE/weekly-review 的缺失 E2E 资产可独立、真实且可重复地运行，运行配置准入不再被 fixture 隐藏。</done>
</task>

<task type="auto">
  <name>Task 2: 用 Codex 内置浏览器复走实际公开路径，并记录可核查验收证据</name>
  <files>docs/verification/phase-06-browser-acceptance.md, docs/verification/README.md</files>
  <read_first>AGENTS.md; frontend/AGENTS.md; admin-frontend/AGENTS.md; .planning/phases/06-user-dashboard-admin/06-CONTEXT.md; .planning/phases/06-user-dashboard-admin/06-RESEARCH.md; .planning/phases/06-user-dashboard-admin/06-UI-SPEC.md; .planning/phases/06-user-dashboard-admin/06-VERIFICATION.md; .planning/phases/06-user-dashboard-admin/06-28-PLAN.md; frontend/tests/e2e/records-dashboard.spec.ts; admin-frontend/tests/e2e/admin-management.spec.ts; docs/verification/phase-06-browser-acceptance.md; docs/verification/README.md</read_first>
  <action>在 Task 1 与 06-28 的 Playwright 命令真实通过后，启动各自拥有的隔离应用，再使用 Codex 内置浏览器直接操作产品页面与公开 API；不能使用 Playwright、截图、直接数据库写入、seed、伪造/复制 token、Cookie 注入或内部函数替代。按这个顺序执行并在页面上观察结果：

1. 用浏览器创建并验证隔离首位账户，随后仅用 audited CLI bootstrap first-admin；在独立 admin SPA 登录，观察实际 Guard Bearer `/api/v1/admin/probe` 200，并从 RuntimeConfig UI 创建 enabled、正预算、非密钥 version，观察公开 POST 201 和服务器确认。CLI 不创建配置，也不构成后续授权证据。
2. 用另一 normal user 的浏览器页面注册、验证和登录；在 `/app/analyze` 提交受控餐食，观察安全 SSE 和完成报告，点击“确认并保存”，再在 `/app/records` 观察今日摘要、趋势、按日期历史和周复盘。记录四 Tab 未变和页面没有内部模型/敏感内容。
3. 以该 normal user 从 `/admin/login?returnTo=/admin/overview` 登录，观察实际 Bearer `/api/v1/admin/probe` 403、到 `/admin/forbidden` 的拒绝页及没有 AdminShell、catalog 或 overview 私有渲染；不得把 06-27 的 mock、API 单测或 Playwright 代替这次浏览器观察。

如实更新 `phase-06-browser-acceptance.md`：写入执行日期/时区、实际 URL/角色、可观察结果、三个路径的明确通过或阻塞结论、所用隔离 runner 和命令的非敏感标识，并明确声明本轮不是 Playwright/截图替代、没有 DB/token/internal shortcut；绝不记录凭据、验证码、Cookie、token、用户原文、原图、密钥、Provider body 或完整模型输出。保留仍未复验项目及其前置条件；若浏览器运行时不可用，必须记录具体阻塞原因和未完成状态，不能写 PASS。同步 `docs/verification/README.md` 的文件索引与证据边界。</action>
  <verify>
    <automated>cd admin-frontend &amp;&amp; E2E_ADMIN_BACKEND_PORT=8003 E2E_ADMIN_USER_FRONTEND_PORT=5183 E2E_ADMIN_FRONTEND_PORT=5184 npm run test:e2e -- --grep admin-management &amp;&amp; cd ../frontend &amp;&amp; E2E_FRONTEND_PORT=5182 E2E_BACKEND_PORT=8002 E2E_RECORDS_ADMIN_FRONTEND_PORT=5185 npm run test:e2e -- --grep records-dashboard</automated>
    <browser-check>使用 Codex 内置浏览器完成 action 中的三条真实页面路径；验收记录必须含日期、URL、角色、页面可观察结果和未使用捷径的事实，且明确 Playwright/截图不能替代本检查。</browser-check>
  </verify>
  <acceptance_criteria>文档包含实际内置浏览器证据：隔离 admin UI 的 RuntimeConfig POST 201、normal user 的 analyze→SSE→save→Records、以及 normal-user probe 403/forbidden/无私有渲染；浏览器不可用时文档明确为 blocked 而非虚报完成。</acceptance_criteria>
  <done>Phase 6 的用户可见/跨栈验收同时具备可重复 Playwright 和不可替代的 Codex 内置浏览器两层证据。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Records E2E runner → isolated database | 每次 reset 必须限定到 `food_agent_test`；另一 runner 的残留不构成前置条件。 |
| admin configuration → normal-user Agent admission | RuntimeConfig 只能在 Guard 200 后由 public UI/API 建立，随后才允许 analyze 到 SSE。 |
| normal-user browser → admin SPA | 用户认证不等同授权；实际 probe 403 必须清 session/cache 并阻止私有渲染。 |
| automated/browser evidence → delivery claim | Playwright、截图、mock 或内部捷径均不能冒充 Codex 实际公开页面验收。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-29-01 | T/D | Records test setup | mitigate | runner 使用 guarded `run_pg.py` reset、独占端口、HTTP readiness、SIGTERM cleanup 和 `reuseExistingServer: false`。 |
| T-06-29-02 | S/E | admin preflight | mitigate | browser-verified account 后仅 audited CLI bootstrap；Guard probe 200 和 RuntimeConfig UI POST 201 是独立公开证据。 |
| T-06-29-03 | S/E | normal-user admin access | mitigate | 依赖 06-26 replay-before-RBAC 与 06-27 strict probe；Playwright 和内置浏览器都观察 Bearer 403、forbidden 和无私有渲染。 |
| T-06-29-04 | I | SSE/dashboard/browser record | mitigate | 断言与记录均只允许安全业务内容；拒绝 Provider/node/stack/reasoning/raw payload、用户原文、原图、密钥与完整 State。 |
| T-06-29-05 | R | browser acceptance record | mitigate | 记录 URL、日期、角色、实际观察和剩余阻塞；若内置浏览器不可用如实标为 blocked，禁止以截图或 Playwright 替换。 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 不新增或安装依赖。 |
</threat_model>

<verification>
Task 1 必须在 06-28 成功后执行；Task 2 必须在两套真实 E2E 命令通过后执行。两个 runner 各自从空隔离库完成 bootstrap/configuration，不得把任何跨 runner 数据当成状态。只有内置浏览器确实操作了三个公开路径，才可更新浏览器验收为通过；否则留下精确 blocked 原因。
</verification>

<success_criteria>
1. 用户端 runner 独立建立 RuntimeConfig 前置条件，随后真实完成 analyze、安全 SSE、确认保存、今日/趋势/history/周复盘的 Records 路径。
2. 所有自动化和浏览器路径保持四 Tab、独立 admin SPA、public API、内存 token、当前 DB-RBAC 与数据最小化边界。
3. `docs/verification/phase-06-browser-acceptance.md` 对 browser evidence 诚实、可核查、与 Playwright 结果明确区分，普通用户后台拒绝已由真实浏览器确认或明确登记阻塞。
</success_criteria>

<output>
Create `.planning/phases/06-user-dashboard-admin/06-29-SUMMARY.md` when done.
</output>
