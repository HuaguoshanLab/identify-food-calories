---
phase: 06-user-dashboard-admin
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/agent/schemas.py, backend/app/agent/api.py, backend/app/agent/service.py, backend/app/planning/schemas.py, backend/app/planning/api.py, backend/app/planning/service.py, backend/tests/unit/test_safe_stream_stage_api.py, backend/tests/agent/test_safe_stream_stage_mapping.py, backend/tests/planning/test_safe_stream_stage_mapping.py]
autonomous: true
requirements: [UI-03]
must_haves:
  truths: ["餐食分析和饮食规划都展示感知、等待补充、工具计算、校验、完成", "流式事件不泄露 provider 原文、state 或思维链"]
  artifacts: ["backend/app/agent/schemas.py", "backend/app/planning/schemas.py"]
  key_links: ["analysis/planning graph lifecycle → canonical safe stage SSE DTO"]
---
<objective>盘点并补齐两个既有 Agent 流的安全业务阶段映射。Purpose: 完整实现 UI-03 与 D-18，不把周复盘状态冒充覆盖。Output: canonical stage DTO、SSE 映射和后端测试。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@backend/AGENTS.md
@.planning/phases/06-user-dashboard-admin/06-AI-SPEC.md
@.planning/phases/06-user-dashboard-admin/06-UI-SPEC.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 对两条 SSE 链写完整阶段 RED 矩阵</name><files>backend/tests/unit/test_safe_stream_stage_api.py, backend/tests/agent/test_safe_stream_stage_mapping.py, backend/tests/planning/test_safe_stream_stage_mapping.py</files><read_first>backend/app/agent/api.py; backend/app/agent/service.py; backend/app/agent/schemas.py; backend/app/planning/api.py; backend/app/planning/service.py; backend/app/planning/schemas.py</read_first><behavior>- 分析与规划均映射 perception、awaiting_input、tool_calculation、validation、completed。
- interrupt/resume 映射 awaiting_input；失败映射安全 retryable/terminal outcome；只有验证报告映射 completed。
- SSE 不含原 node/payload、reasoning、provider body、image/base64 或 full State。</behavior><action>先盘点现有 event type/graph state/API generator，以 Fake Provider/固定状态写失败覆盖矩阵；任何漏映射均失败，不能被默认文案吞掉。</action><acceptance_criteria>矩阵明确列出分析、规划、interrupt/resume、工具失败和校验失败的用户阶段。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/unit/test_safe_stream_stage_api.py tests/agent/test_safe_stream_stage_mapping.py tests/planning/test_safe_stream_stage_mapping.py -q</automated></verify><done>RED 测试证明 UI-03 的五阶段同时覆盖分析和规划。</done></task>
<task type="auto" tdd="true"><name>Task 2: 发布 canonical safe SSE stage 契约</name><files>backend/app/agent/schemas.py, backend/app/agent/api.py, backend/app/agent/service.py, backend/app/planning/schemas.py, backend/app/planning/api.py, backend/app/planning/service.py, backend/tests/unit/test_safe_stream_stage_api.py, backend/tests/agent/test_safe_stream_stage_mapping.py, backend/tests/planning/test_safe_stream_stage_mapping.py</files><read_first>backend/app/agent/state.py; backend/app/agent/graph.py; backend/app/planning/graph.py; backend/app/providers/reasoning/dto.py</read_first><action>在两个公开 SSE 边界定义同版本 strict safe business-stage DTO/mapper。只有 allowlisted lifecycle 信号映射为识别、等待、计算、校验、完成和稳定错误；保留 resume idempotency/event order。禁止直接序列化 Provider DTO、AgentEvent payload、raw exception、节点名或 State。</action><acceptance_criteria>两条 API 每个实际分支都有一致 stage 语义；未知/敏感字段被 contract 测试拒绝。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/unit/test_safe_stream_stage_api.py tests/agent/test_safe_stream_stage_mapping.py tests/planning/test_safe_stream_stage_mapping.py -q</automated></verify><done>后端为 UI-03 所有可见阶段提供唯一、安全且可验证的 SSE 来源。</done></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| graph/provider event → SSE | 内部事件可能含敏感内容 |
| SSE stage → user | 阶段不得虚报完成 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-08 | I | SSE mapper | mitigate | allowlist DTO、payload exclusion、Fake traces。 |
| T-06-09 | R | completed stage | mitigate | validated-report gate、route tests。 |
| T-06-SC | Tampering | package installs | accept | 不安装新包。 |</threat_model>
<verification>Fake Provider/graph mapping 与 SSE contract tests 通过。</verification>
<success_criteria>分析、规划进度完整可理解且不暴露推理或内部实现。</success_criteria>
<output>Create .planning/phases/06-user-dashboard-admin/06-05-SUMMARY.md when done</output>
