---
phase: 06-user-dashboard-admin
plan: 32
type: execute
wave: 3
depends_on: [06-31]
files_modified: [frontend/tests/e2e/records-dashboard.spec.ts, frontend/tests/e2e/records-weekly-review.spec.ts, docs/verification/phase-06-browser-acceptance.md, README.md, docs/learning/06-dashboard-admin.md]
autonomous: false
gap_closure: true
requirements: [UI-02, EDU-02, EDU-03]
must_haves:
  truths:
    - "两个 isolated Records Playwright spec 均通过真实公开统计时区确认建立 preference，才请求 overview/weekly review；禁止直接/未审计 DB 写入、token/cookie、Service 或 mock 捷径。唯一例外是既有 `run_pg.py` 包装的受审计 first-admin CLI bootstrap，它只写测试角色/审计记录，绝不作为配置、RBAC 或 confirmation 证据。"
    - "Records dashboard E2E 在 Shanghai 与 Los Angeles Chromium timezone contexts 中观察 confirmation 与不含 `time_zone` 旁路的 dashboard/review 请求；精确 UTC/DST/Monday 数学仅由 06-30/31 的确定性测试宣称。"
    - "真实 Codex 内置浏览器记录 normal-user analyze → save → Records 的公开路径和统计时区确认后的可见投影；不可用则如实记录 blocked。"
    - "README 与中文教学文档准确描述已存在且实际运行的 Records/admin E2E 资产、时区职责与证据范围，不再声称资产缺失。"
  artifacts:
    - path: "frontend/tests/e2e/records-dashboard.spec.ts"
      provides: "在至少两个 Chromium IANA timezone contexts 中覆盖 confirmation → analyze/save → dashboard 的 guarded public E2E。"
      contains: "timezoneId"
    - path: "frontend/tests/e2e/records-weekly-review.spec.ts"
      provides: "以真实 confirmation preference 为前置的 weekly-review public E2E。"
      contains: "dashboard-time-zone-confirmations"
    - path: "docs/verification/phase-06-browser-acceptance.md"
      provides: "真实 Codex 内置浏览器的路径、证据边界和时区观察记录。"
      contains: "Codex 内置浏览器"
    - path: "docs/learning/06-dashboard-admin.md"
      provides: "Phase 6 时区请求链、测试层级、可运行命令与常见错误的中文教学。"
      contains: "ZoneInfo"
  key_links:
    - from: "frontend/tests/e2e/records-dashboard.spec.ts"
      to: "/api/v1/meal-records/dashboard-time-zone-confirmations"
      via: "observed authenticated public POST before dashboard requests in each timezone context"
      pattern: "dashboard-time-zone-confirmations"
    - from: "frontend/tests/e2e/records-weekly-review.spec.ts"
      to: "/api/v1/dashboard/weekly-review"
      via: "real confirmed user preference before observed safe weekly-review response"
      pattern: "weekly-review"
    - from: "docs/learning/06-dashboard-admin.md"
      to: "backend/dashboard and frontend/records tests"
      via: "linked source/test evidence and runnable guarded commands"
      pattern: "test_weekly_review_api|records-weekly-review"
---

<objective>
以隔离公开 E2E、真实 Codex 浏览器和准确文档完成统计时区缺口的用户可见验收；确认两个现有 Records spec 都以真实 preference 为前置。

Purpose: 06-30/31 已分别提供 server-authoritative IANA window 和 deterministic H5 序列化。这里验证实际浏览器上下文能建立该 preference 并读到 dashboard/review，同时纠正根 README 和教学文档中“E2E 资产仍缺失”的过时说法。E2E 与浏览器只证明真实路径；UTC/DST/Monday 的精确边界由前置 deterministic tests 证明，不能混淆。
Output: 更新的两条 guarded Playwright spec、真实浏览器证据、根 README 和中文教学文档。
</objective>

<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@frontend/AGENTS.md
@frontend/tests/e2e/README.md
@docs/README.md
@docs/verification/README.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-user-dashboard-admin/06-CONTEXT.md
@.planning/phases/06-user-dashboard-admin/06-RESEARCH.md
@.planning/phases/06-user-dashboard-admin/06-UI-SPEC.md
@.planning/phases/06-user-dashboard-admin/06-VERIFICATION.md
@.planning/phases/06-user-dashboard-admin/06-29-SUMMARY.md
@.planning/phases/06-user-dashboard-admin/06-30-SUMMARY.md
@.planning/phases/06-user-dashboard-admin/06-31-SUMMARY.md
@frontend/playwright.config.ts
@frontend/tests/e2e/auth-helpers.ts
@frontend/tests/e2e/records-dashboard.spec.ts
@frontend/tests/e2e/records-weekly-review.spec.ts
@frontend/src/features/records/api/client.ts
@frontend/src/features/records/components/RecordsPage.tsx
@backend/app/records/api.py
@backend/app/dashboard/api.py
@README.md
@docs/learning/06-dashboard-admin.md
@docs/verification/phase-06-browser-acceptance.md

<interfaces>
From 06-31's frontend contract:
```text
RecordsPage sends the browser IANA zone only to POST /meal-records/dashboard-time-zone-confirmations.
Only confirmation 200 or the known one-time 409 releases overview/history/weekly-review queries.
Dashboard reads never carry a client time_zone authority.
```

From `frontend/playwright.config.ts` / 06-29:
```text
The Records runner owns a guarded fresh `food_agent_test` stack, backend 8002,
user SPA 5182 and admin SPA 5185; reuseExistingServer is false.
```

From `frontend/tests/e2e/records-weekly-review.spec.ts`:
```text
The current normal-user weekly-review E2E observes a public GET response but must
first establish the server-side confirmed timezone through the public records API.
```
</interfaces>
</context>

<source_coverage_audit>
| Source | Required item | Coverage |
|---|---|---|
| GOAL | 用户看懂真实历史摄入趋势 | Task 1 executes confirmed-user dashboard/review routes in guarded product stacks; Task 2 repeats the visible Records route in the actual built-in browser. |
| REQ | UI-02：today/week/history/trend/weekly review | Tasks 1–2 observe confirmation, overview/history/review requests and the user-visible Records projections. |
| REQ | EDU-02：README 启动/调试与证据准确 | Task 3 replaces stale missing-asset claims with exact isolated runner commands and evidence limits. |
| REQ | EDU-03：可验证教学线索 | Task 3 links preference→repository→ZoneInfo service→API→RecordsPage and the precise unit/API/E2E test files. |
| RESEARCH | public-API browser acceptance, isolated runner, no direct DB/token shortcut | Tasks 1–2 explicitly use guarded runner/real product pages/public API and reject direct/un-audited DB, token, cookie, mock and internal shortcuts; only the pre-existing audited first-admin CLI bootstrap is retained for its narrow role/audit purpose. |
| CONTEXT | D-01/D-04：Records sole entry and local natural day/week | Tasks 1–2 keep Records order/four Tabs and prove preference setup precedes the server-local read model. |
| CONTEXT | D-05–D-08：safe current-week review | Task 1 requires the weekly spec to observe the closed safe response only after preference confirmation; it does not expose facts/provider internals. |
| CONTEXT | D-17/D-18：public API and sensitive boundary | Tasks 1–3 retain independent/public boundaries and omit credentials, tokens, raw meals, images, state, provider body and reasoning from tests/docs. |
</source_coverage_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 让两个 Records E2E 都以公开统计时区 confirmation 建立真实前置条件</name>
  <files>frontend/tests/e2e/records-dashboard.spec.ts, frontend/tests/e2e/records-weekly-review.spec.ts</files>
  <read_first>AGENTS.md; frontend/AGENTS.md; frontend/tests/e2e/README.md; .planning/phases/06-user-dashboard-admin/06-CONTEXT.md; .planning/phases/06-user-dashboard-admin/06-VERIFICATION.md; .planning/phases/06-user-dashboard-admin/06-30-SUMMARY.md; .planning/phases/06-user-dashboard-admin/06-31-SUMMARY.md; frontend/playwright.config.ts; frontend/tests/e2e/auth-helpers.ts; frontend/tests/e2e/records-dashboard.spec.ts; frontend/tests/e2e/records-weekly-review.spec.ts; frontend/src/features/records/api/client.ts; frontend/src/features/records/components/RecordsPage.tsx; backend/app/records/api.py; backend/app/dashboard/api.py</read_first>
  <behavior>
    - Test 1: `records-dashboard` runs the authenticated normal-user public path in Chromium `timezoneId: Asia/Shanghai` and `timezoneId: America/Los_Angeles` contexts; each observes confirmation before dashboard/review reads, sends no dashboard `time_zone`, and renders the existing safe Records projection.
    - Test 2: `records-weekly-review` creates/logs in a normal user, observes public confirmation before its weekly-review GET, then asserts only the safe low-coverage response/DOM and no provider/error internals.
    - Test 3: both specs retain guarded fresh-stack ownership, Mailpit public verification, semantic response/DOM waits, no real model, no direct/un-audited DB write, no token/cookie injection, no internal Service/Repository call and no mock endpoint. The sole bootstrap exception is the existing `run_pg.py`-wrapped audited first-admin CLI: it writes only test role/audit state and never proves configuration, current RBAC or timezone confirmation.
  </behavior>
  <action>先让两个 spec 的新断言红灯。扩展既有 `records-dashboard.spec.ts`，通过 `browser.newContext({ timezoneId })` 分别执行 `Asia/Shanghai` 与 `America/Los_Angeles` 的正常用户路径；每个 context 必须观察 authenticated public `POST /api/v1/meal-records/dashboard-time-zone-confirmations` 的 200（或同一已确认用户的明确 409）发生在 overview/history/weekly-review 请求之前，并断言 dashboard/review URL、query、headers 和 body 没有 `time_zone` authority。继续通过真实 analyze → safe SSE → confirm save → Records 可见 DOM 验证今日/趋势/history/低覆盖 review，而不把当前墙钟日期夸大为 Monday/DST 数学覆盖。

保留既有新鲜隔离栈唯一允许的 bootstrap 例外：在浏览器完成首位账户验证后，才用 `run_pg.py` 包装的 audited first-admin CLI 写入测试角色和审计记录；它不得创建 RuntimeConfig、修改营养/餐食数据，且不能作为 Guard probe、DB-RBAC、RuntimeConfig 或 confirmation 的证据。运行配置仍必须由真实 admin UI 的公开 API 创建，normal user 的统计时区确认必须经真实用户页面/公开 records API 完成。除此以外禁止直接或未审计 DB 写入/seed、cookie/token 注入、mock endpoint、调用内部函数/Service/Repository、fixed sleep 或真实模型。

同步修订 `records-weekly-review.spec.ts`：其 normal user 在 weekly-review GET 之前必须走实际登录页面和公开 confirmation API 建立 preference，再断言闭合安全 response/DOM。保留隔离 runner、Mailpit、语义 locator/response 等待和无敏感 terms 断言；禁止直接/未审计 DB 写入或 seed、cookie/token 注入、mock endpoint、调用内部函数/Service/Repository、fixed sleep 或真实模型。此任务实现 D-04、D-05、D-07、D-08、D-17、D-18。</action>
  <verify>
    <automated>cd frontend &amp;&amp; E2E_FRONTEND_PORT=5182 E2E_BACKEND_PORT=8002 E2E_RECORDS_ADMIN_FRONTEND_PORT=5185 npm run test:e2e -- --grep 'records-dashboard|真实登录后的记录页显示低覆盖周复盘' &amp;&amp; npm test -- --run src/features/records</automated>
  </verify>
  <acceptance_criteria>两个实际 E2E 都不能绕过 preference：每一个用户的 dashboard/review 请求都有可观察的 confirmation 前置，并且在两个 IANA browser contexts 中不携带 dashboard timezone 旁路。</acceptance_criteria>
  <done>Records dashboard 与 weekly-review 的真实公开验收都消费 06-30/31 的统计时区合同，而非偶然的 server UTC 默认值。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 由用户确认真实 Codex 浏览器统计时区路径</name>
  <files>docs/verification/phase-06-browser-acceptance.md</files>
  <action>在 Task 1 自动化通过后暂停，由用户在 Codex 内置浏览器验证真实公开产品路径；不能以 Playwright、截图、直接 API、数据库或 token 捷径替代。只有用户批准或描述具体问题后，才继续更新浏览器证据文档。</action>
  <what-built>已自动完成并通过两个 guarded Playwright spec；接下来需要以 Codex 内置浏览器验证真实产品页面，而不是以 Playwright、截图或直接 API/数据库操作代替。</what-built>
  <how-to-verify>
1. 在独立产品环境用普通用户注册、邮件验证并登录，走一次“分析 → 确认保存 → 记录”。
2. 在 `/app/records` 确认“今日摘要 → 本周趋势 → 历史记录 → 周复盘”顺序、四个 Tab、低覆盖安全文案，以及没有 Provider、stack、reasoning、密钥、原图或原始输入。
3. 观察浏览器网络/页面：统计时区确认成功后才加载 dashboard；若无法执行，记录阻塞原因而不是批准。
4. 批准后，执行者才可更新 `docs/verification/phase-06-browser-acceptance.md`；记录日期/时区、URL、角色、可见结果和未用捷径的事实，绝不记录凭据、验证码、Cookie、token、原始餐食、图片、密钥、Provider body/state/reasoning。
  </how-to-verify>
  <resume-signal>输入“approved”继续文档收尾，或描述发现的问题。</resume-signal>
  <verify><automated>cd frontend &amp;&amp; E2E_FRONTEND_PORT=5182 E2E_BACKEND_PORT=8002 E2E_RECORDS_ADMIN_FRONTEND_PORT=5185 npm run test:e2e -- --grep 'records-dashboard|真实登录后的记录页显示低覆盖周复盘'</automated></verify>
  <done>用户已确认真实浏览器路径，或给出必须如实写入证据文档的阻塞/失败结果。</done>
</task>

<task type="auto">
  <name>Task 3: 如实记录内置浏览器验收，并修复 README 与教学文档的过时 E2E 说法</name>
  <files>docs/verification/phase-06-browser-acceptance.md, README.md, docs/learning/06-dashboard-admin.md</files>
  <read_first>AGENTS.md; frontend/AGENTS.md; frontend/tests/e2e/README.md; docs/README.md; docs/verification/README.md; .planning/phases/06-user-dashboard-admin/06-VERIFICATION.md; .planning/phases/06-user-dashboard-admin/06-29-SUMMARY.md; .planning/phases/06-user-dashboard-admin/06-30-SUMMARY.md; .planning/phases/06-user-dashboard-admin/06-31-SUMMARY.md; frontend/tests/e2e/records-dashboard.spec.ts; frontend/tests/e2e/records-weekly-review.spec.ts; frontend/src/features/records/components/RecordsPage.tsx; backend/app/dashboard/service.py; backend/app/dashboard/api.py; README.md; docs/learning/06-dashboard-admin.md; docs/verification/phase-06-browser-acceptance.md</read_first>
  <action>仅在前一 human-verify checkpoint 的真实浏览器结果已知后更新证据文档。若通过，`phase-06-browser-acceptance.md` 只记录执行日期/时区、产品 URL、normal-user 角色、analyze → save → Records 可见结果、统计时区 confirmation 在 dashboard 前完成、四 Tab/安全文案和明确的“非 Playwright/截图/DB/token/internal shortcut”声明；若浏览器不可用或发现问题，保留精确 blocked/failure 条目，绝不写 PASS。敏感数据（凭据、验证码、Cookie、token、原始餐食、图片、密钥、Provider body/state/reasoning）绝不进入文档。

更新根 README 与中文 `docs/learning/06-dashboard-admin.md`，删除“records/admin Playwright assets 仍缺失/未完成 E2E 门禁”的过时陈述，替换为可运行的 `records-dashboard.spec.ts`、`records-weekly-review.spec.ts` 与 `admin-management.spec.ts` 命令及其 guarded fresh-stack/public-API 边界。教学文档必须解释 records-owned one-time preference → dashboard repository read Port → ZoneInfo service local today/Monday → API validation → RecordsPage confirmation/helper 的请求链，说明 D-04 为什么不允许客户端 timezone 作为 read authority；列出上海周一凌晨、洛杉矶 DST、UTC 跨日、缺失/损坏 preference、422 与 refresh 不绕过的 deterministic tests，以及 E2E/browser 各自未覆盖的范围。不得声称未实际运行的命令或浏览器通过，也不得把时区测试扩展成营养/医疗准确性指标。该任务落实 UI-02、EDU-02、EDU-03。</action>
  <verify>
    <automated>cd frontend &amp;&amp; E2E_FRONTEND_PORT=5182 E2E_BACKEND_PORT=8002 E2E_RECORDS_ADMIN_FRONTEND_PORT=5185 npm run test:e2e -- --grep 'records-dashboard|真实登录后的记录页显示低覆盖周复盘' &amp;&amp; cd ../admin-frontend &amp;&amp; E2E_ADMIN_BACKEND_PORT=8003 E2E_ADMIN_USER_FRONTEND_PORT=5183 E2E_ADMIN_FRONTEND_PORT=5184 npm run test:e2e -- --grep admin-management</automated>
    <human-check>确认 `docs/verification/phase-06-browser-acceptance.md` 的结果与 checkpoint 实际浏览器观察完全一致；若 checkpoint 未批准或浏览器不可用，文件必须明确 blocked/failure，不能以自动化结果替代。</human-check>
  </verify>
  <acceptance_criteria>README/教学不再声称实际存在的 E2E 资产缺失；浏览器记录仅声称真实观察到的路径，并精确区分 deterministic tests、E2E 与内置浏览器证据。</acceptance_criteria>
  <done>Phase 6 的操作/教学/浏览器证据与当前时区实现和真实可执行 E2E 一致，不含敏感信息或虚假完成声明。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| isolated browser context → confirmation API | 仅后端验证/持久化的 preference 能定义统计窗口；E2E 观察前置但不能把 timezone 当 dashboard read authority。 |
| normal user → dashboard/weekly API | 所有 reads 走认证公开 API，后端 06-30 仍校验 preference、local Monday 和安全闭合周复盘。 |
| automated/browser evidence → documentation claim | Playwright、deterministic test、内置浏览器证据分别记录；任一层都不能被 DB/token/截图/内部捷径伪造。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-32-01 | T/E | preference setup | mitigate | 每个 E2E user 通过公开 confirmation 建立/复用 preference，断言 dashboard reads 不传 client timezone。 |
| T-06-32-02 | T/D | isolated test stack | mitigate | 使用既有 guarded `run_pg.py` runner、独占端口、fresh DB、readiness 与 `reuseExistingServer: false`；禁止直接/未审计 DB write、seed 与 fixed sleep。唯一 narrow exception 是浏览器验证首位账户后由 `run_pg.py` 包装的 audited first-admin CLI 写入 role/audit，绝不替代 Guard、RuntimeConfig 或 confirmation 证据。 |
| T-06-32-03 | I | weekly/browser DOM and docs | mitigate | 仅断言/记录 allowlisted public results，拒绝 token、credential、raw meal/image、Provider、state、stack、reasoning 与密钥。 |
| T-06-32-04 | R | evidence accuracy | mitigate | checkpoint 后才更新 browser claim；浏览器阻塞/失败如实写入，E2E 不替代浏览器证据。 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 复用现有 Playwright/Vitest/浏览器工具，不新增依赖。 |
</threat_model>

<verification>
必须严格在 06-31 frontend API/UI tests、typecheck 和 build 通过后执行。Task 1 同时运行 `records-dashboard` 与 `records-weekly-review`；只有它们通过后才到 human checkpoint。人类批准的真实内置浏览器观察是 Task 2 更新 browser evidence 的唯一依据；自动化不代替它。更新文档后重跑两条 Records spec 与既有 admin-management spec，确保“资产已存在且可运行”的陈述有当轮证据。
</verification>

<success_criteria>
1. 两个 Records E2E 都以公开统计时区 confirmation 为必需前置，并在真实 guarded stack 上维持安全 user-visible contract。
2. 内置浏览器实际验证结果被如实记录或明确标为 blocked，绝不由截图、Playwright、DB 或 token shortcut 冒充。
3. README/中文教学准确链接并解释当前 E2E、时区职责和可验证测试线索。
</success_criteria>

<output>
Create `.planning/phases/06-user-dashboard-admin/06-32-SUMMARY.md` when done.
</output>
