---
phase: 06-user-dashboard-admin
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified: [backend/app/README.md, backend/app/dashboard/README.md, backend/migrations/versions/0012_planning_completion_projection.py, backend/app/planning/models.py, backend/app/planning/ports.py, backend/app/planning/service.py, backend/app/planning/repository.py, backend/app/dashboard/ports.py, backend/tests/README.md, backend/tests/dashboard/README.md, backend/tests/planning/test_completion_projection_service.py, backend/tests/integration/test_planning_completion_projection.py, backend/tests/dashboard/test_dashboard_target_port.py]
autonomous: true
requirements: [UI-02]
must_haves:
  truths: ["目标对比只在完整 profile 与成功校验计划的资格存在时显示", "profile/目标失效立即撤销资格"]
  artifacts: ["backend/migrations/versions/0012_planning_completion_projection.py", "backend/app/planning/ports.py"]
  key_links: ["validated planning success → PlanningCompletionProjection transaction", "dashboard → narrow eligibility port"]
---
<objective>把完成计划做成可撤销的权威投影，而不是从 profile 猜测。Purpose: 实现 D-03 和既定 projection 决议。Output: 迁移、生产/撤销路径、dashboard port 测试。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@backend/AGENTS.md
@.planning/phases/06-user-dashboard-admin/06-CONTEXT.md
@.planning/phases/06-user-dashboard-admin/06-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 写资格生产、撤销与窄 port RED 测试</name><files>backend/app/README.md, backend/app/dashboard/README.md, backend/tests/README.md, backend/tests/dashboard/README.md, backend/tests/planning/test_completion_projection_service.py, backend/tests/integration/test_planning_completion_projection.py, backend/tests/dashboard/test_dashboard_target_port.py</files><read_first>backend/app/planning/models.py; backend/app/planning/service.py; backend/app/planning/ports.py; backend/tests/planning/test_planning_service.py; backend/app/README.md; backend/tests/README.md</read_first><behavior>- 只有成功且校验完成的计划报告创建 user-bound projection。
- projection 包含 completed run/thread、目标/profile revision、completed_at。
- 删除 profile/目标或 revision 失效在同一事务撤销；dashboard 只读 eligibility/ranges/version。</behavior><action>本 task 首次创建 dashboard port/test 目录时，同步创建目录 README 并更新 backend/app/backend/tests 父索引；随后写 fake repository、真实 PostgreSQL 与 dashboard port RED 测试，覆盖 rollback、同用户幂等、不同用户隔离及 revision 变化。禁止从 profile 本身推断完成。</action><acceptance_criteria>生产、撤销和消费均可独立失败且未完成计划永远无目标数值；新目录在 RED commit 即有 README。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/planning/test_completion_projection_service.py tests/integration/test_planning_completion_projection.py tests/dashboard/test_dashboard_target_port.py -q && test -f app/dashboard/README.md && test -f tests/dashboard/README.md</automated></verify><done>RED 测试锁定资格的权威事实来源与撤销语义，目录合同同步成立。</done></task>
<task type="auto" tdd="true"><name>Task 2: 实现可撤销 projection 和最小读取 port</name><files>backend/app/README.md, backend/app/dashboard/README.md, backend/migrations/versions/0012_planning_completion_projection.py, backend/app/planning/models.py, backend/app/planning/ports.py, backend/app/planning/service.py, backend/app/planning/repository.py, backend/app/dashboard/ports.py, backend/tests/README.md, backend/tests/dashboard/README.md, backend/tests/planning/test_completion_projection_service.py, backend/tests/integration/test_planning_completion_projection.py, backend/tests/dashboard/test_dashboard_target_port.py</files><read_first>backend/migrations/versions/0010_planning_profiles.py; backend/app/planning/repository.py; backend/app/dashboard/service.py; backend/app/README.md; backend/tests/README.md</read_first><action>由于本计划首次创建 dashboard port/test 目录，写 dashboard README、dashboard tests README，并更新 backend/app 与 backend/tests 直接父索引。增加可撤销 PlanningCompletionProjection，保存 user_id、run/thread、profile_revision、target_revision、completed_at、revoked_at/reason。规划成功且 validation 完成的 Service 事务写 projection；profile/目标删除或 revision 失效撤销。定义 planning-owned port 仅返回 eligibility、目标区间和版本；dashboard 仅依赖该 port，绝不导入 PlanningProfile ORM 或健康详情。</action><acceptance_criteria>约束防止孤儿/跨用户投影；成功计划与投影原子提交；撤销后返回 unavailable，所有新目录具备 README/父索引。</acceptance_criteria><verify><automated>cd backend && APP_ENV=test uv run alembic upgrade head && uv run pytest tests/planning/test_completion_projection_service.py tests/integration/test_planning_completion_projection.py tests/dashboard/test_dashboard_target_port.py -q && test -f app/dashboard/README.md && test -f tests/dashboard/README.md</automated></verify><done>目标资格来自可审计、可撤销、最小化的完成计划事实，且其新目录可导航。</done></task>
<task type="auto"><name>Task 3: 固定 0012 的线性迁移前驱</name><files>backend/migrations/versions/0012_planning_completion_projection.py</files><read_first>backend/migrations/versions/0011_dashboard_time_attribution.py; backend/migrations/env.py</read_first><action>把 `0012_planning_completion_projection.py` 的 down_revision 明确指向 `0011_dashboard_time_attribution`。遵守最终唯一顺序 `0010 → 0011 → 0012 → 0013 → 0014 → 0015 → 0016 → 0017`；06-03 依赖 06-01，后继计划只能接续 0012，禁止第二个 head。</action><acceptance_criteria>0012 只接续 0011，Alembic history 没有并行分支。</acceptance_criteria><verify><automated>cd backend && uv run alembic heads && uv run alembic history --verbose</automated></verify><done>0012 与 0011 形成唯一连续迁移链。</done></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| planning completion → projection | 未校验图不得授予资格 |
| dashboard → planning port | 不能泄露 profile/健康详情 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-04 | T/E | completion producer | mitigate | validated-success gate、同事务、revision revocation、PG tests。 |
| T-06-05 | I | eligibility port | mitigate | narrow DTO/import-boundary tests。 |
| T-06-SC | Tampering | package installs | accept | 不安装新包。 |</threat_model>
<verification>规划 fake service、真实 PostgreSQL projection 事务和 dashboard port 测试通过。</verification>
<success_criteria>dashboard 绝不把 profile 当作完成计划，并在资格失效时停止目标对比。</success_criteria>
<output>Create .planning/phases/06-user-dashboard-admin/06-03-SUMMARY.md when done</output>
