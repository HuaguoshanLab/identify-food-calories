---
phase: 06-user-dashboard-admin
plan: 23
type: execute
wave: 6
depends_on: [06-08]
files_modified: [backend/app/dashboard/weekly_review_graph.py, backend/app/dashboard/weekly_review.py, backend/app/providers/reasoning/ports.py, backend/app/providers/reasoning/dto.py, backend/app/providers/reasoning/fake.py, backend/app/providers/reasoning/deepseek.py, backend/tests/providers/README.md, backend/tests/dashboard/test_weekly_review_graph.py, backend/tests/providers/test_weekly_review_provider_dto.py, backend/tests/evals/test_weekly_review_eval.py]
autonomous: true
requirements: [UI-02]
must_haves:
  truths: ["模型只接收去标识聚合事实并至多调用两次/八秒", "schema、语义、预算、超时、停用和未知结果均安全弃权且未知调用不重放"]
  artifacts: ["backend/app/dashboard/weekly_review_graph.py", "backend/app/providers/reasoning/dto.py", "backend/tests/evals/test_weekly_review_eval.py"]
  key_links: ["validated facts from 06-07 → bounded graph/provider DTO → schema and semantic validator → cached safe result", "06-08 loader → fixture-driven Fake Provider → per-case model_calls and minimal ledger assertions"]
---
<objective>实现受限周复盘 graph、Provider adapter 和冻结评测。Purpose: 按 D-06–D-08 将模型限定为安全自然语言生成器。Output: 有界 graph、严格 DTO、DeepSeek/Fake 对称 adapter、fixture-driven 测试。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@backend/AGENTS.md
@.planning/phases/06-user-dashboard-admin/06-AI-SPEC.md
@.planning/phases/06-user-dashboard-admin/06-PATTERNS.md
@.planning/phases/06-user-dashboard-admin/06-08-PLAN.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 由冻结 loader 写 graph/provider/eval RED 矩阵</name><files>backend/tests/providers/README.md, backend/tests/dashboard/test_weekly_review_graph.py, backend/tests/providers/test_weekly_review_provider_dto.py, backend/tests/evals/test_weekly_review_eval.py</files><read_first>backend/tests/evals/fixtures/weekly_review/loader.py; backend/tests/evals/fixtures/weekly_review/weekly_review_cases.v1.json; backend/app/providers/reasoning/ports.py; backend/app/providers/reasoning/dto.py; backend/app/providers/reasoning/fake.py; .planning/phases/06-user-dashboard-admin/06-AI-SPEC.md</read_first><behavior>- loader 驱动的 14 cases 分别断言 expected stable code、类别、最大调用数和最小 ledger 元数据，禁止在测试内嵌案例。
- request 只含去标识 week/coverage/totals/approved patterns/versions；任何 schema、事实外数值、医疗/强制语言、隐私污染或多余 reasoning 均拒绝。
- graph 最多两次调用、八秒、360 tokens；disabled、budget、timeout、OUTCOME_UNKNOWN 均不重放并返回稳定安全弃权。</behavior><action>首次创建 tests/providers 时同一 task 写 README，说明该目录只测试 runtime-validated Provider DTO/adapter、允许依赖和文件索引。用 06-08 loader、scripted Fake Provider、注入时钟及配置快照先写 RED 测试；逐例断言 model_calls、usage/cost/config/prompt/graph/facts digest 和稳定 failure/abstention code。测试不调用真实模型、不引入 SaaS，且不得在 fixture、Fake trace 或 ledger 断言中保存 prompt、facts JSON、输出原文、Provider body 或 reasoning。</action><acceptance_criteria>14 个 frozen case 全部由 loader 驱动且在 graph/adapter 实现前失败；测试能证明零调用低覆盖、至多两次调用和未知结果不重放。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/dashboard/test_weekly_review_graph.py tests/providers/test_weekly_review_provider_dto.py tests/evals/test_weekly_review_eval.py -q</automated></verify><done>AI-SPEC 的安全、成本、隐私和评测合同成为可执行 RED 门禁。</done></task>
<task type="auto" tdd="true"><name>Task 2: 实现 facts-only graph 与对称 Provider adapter</name><files>backend/app/dashboard/weekly_review_graph.py, backend/app/dashboard/weekly_review.py, backend/app/providers/reasoning/ports.py, backend/app/providers/reasoning/dto.py, backend/app/providers/reasoning/fake.py, backend/app/providers/reasoning/deepseek.py, backend/tests/dashboard/test_weekly_review_graph.py, backend/tests/providers/test_weekly_review_provider_dto.py, backend/tests/evals/test_weekly_review_eval.py</files><read_first>backend/app/dashboard/weekly_review_dto.py; backend/app/agent/models.py; backend/app/providers/reasoning/deepseek.py; backend/app/providers/reasoning/fake.py; backend/tests/evals/fixtures/weekly_review/loader.py; .planning/phases/06-user-dashboard-admin/06-AI-SPEC.md</read_first><action>定义 weekly-review request/result DTO 与 ReasoningModelProvider 窄 port 方法；DeepSeek 与 Fake 必须使用同一运行时校验，Fake 读取脚本结果但立即丢弃 request body。Graph 不导入 ORM/repository：只接收已验证 facts，经注入 timeout/budget/config port 调 Provider，独立执行 schema+semantic validation，最多一次显式 correction retry，随后返回安全建议或稳定 abstention。eval 使用 loader 逐例读取 14-case catalog，断言 fixture 所列调用数、最小 AgentRun/Invocation versions、usage/digest/failure code；禁止持久 prompt、facts JSON、输出原文、Provider body 或 reasoning。</action><acceptance_criteria>无付费调用下所有 14 cases 通过，任何路径不超过两次调用/八秒/360 tokens，且数值与安全真相仍由确定性后端负责。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/dashboard/test_weekly_review_graph.py tests/providers/test_weekly_review_provider_dto.py tests/evals/test_weekly_review_eval.py -q</automated></verify><done>周复盘 graph 只消费受限事实和 Provider DTO，所有高风险/未知路径确定性弃权。</done></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| facts → provider | 去标识最小 DTO 是唯一允许输入 |
| provider output → review result | schema 正确不代表语义安全 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-15 | I | provider request/ledger | mitigate | DTO whitelist、Fake discard、fixture loader 和 serialization tests。 |
| T-06-16 | T/D | graph output/call loop | mitigate | semantic gate、2-call/8s/360-token cap、disabled/budget/unknown no-replay tests。 |
| T-06-SC | Tampering | package installs | accept | 复用锁定 Provider，无新包。 |</threat_model>
<verification>Fake graph/DTO、冻结 loader 驱动的 14-case eval 与敏感字段扫描通过。</verification>
<success_criteria>周复盘仅对事实支持的普通饮食参考调用模型；任何不安全或不确定路径安全弃权。</success_criteria>
<output>Create .planning/phases/06-user-dashboard-admin/06-23-SUMMARY.md when done</output>
