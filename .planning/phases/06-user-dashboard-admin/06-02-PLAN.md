---
phase: 06-user-dashboard-admin
plan: 02
type: execute
wave: 3
depends_on: [06-01, 06-03]
files_modified: [backend/app/README.md, backend/app/dashboard/README.md, backend/app/dashboard/schemas.py, backend/app/dashboard/repository.py, backend/app/dashboard/service.py, backend/app/dashboard/api.py, backend/app/main.py, backend/tests/README.md, backend/tests/dashboard/README.md, backend/tests/dashboard/test_dashboard_service.py, backend/tests/integration/test_dashboard_repository.py, backend/tests/integration/test_dashboard_overview_projection.py, backend/tests/unit/test_dashboard_api.py]
autonomous: true
requirements: [UI-02]
must_haves:
  truths: ["用户可读取今日、本周和按本地日分组的历史餐食", "今日目标对比只来自未撤销的 PlanningCompletionProjection", "历史是服务端严格 DTO 和不透明 keyset cursor"]
  artifacts: ["backend/app/dashboard/api.py", "backend/app/dashboard/repository.py"]
  key_links: ["GET /api/v1/dashboard/overview → injected PlanningCompletionProjection narrow port → strict eligibility/target DTO", "GET /api/v1/dashboard/history → service → tenant-filtered SQL keyset cursor", "overview/history → persisted consumed_local_date"]
---
<objective>交付严格依赖完成计划投影的 dashboard 读 API。Purpose: 完成 D-01–D-04，尤其是不从 profile 猜目标的 D-03。Output: overview/history、PlanningCompletionProjection 注入、严格 DTO、HTTPX 与真实 PostgreSQL 证据。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@backend/AGENTS.md
@.planning/phases/06-user-dashboard-admin/06-CONTEXT.md
@.planning/phases/06-user-dashboard-admin/06-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 编写 projection-backed overview/history cursor RED 契约</name><files>backend/tests/dashboard/test_dashboard_service.py, backend/tests/integration/test_dashboard_repository.py, backend/tests/integration/test_dashboard_overview_projection.py, backend/tests/unit/test_dashboard_api.py</files><read_first>backend/app/dashboard/ports.py; backend/app/planning/ports.py; backend/app/records/models.py; backend/app/records/repository.py; backend/tests/integration/test_meal_record_repository.py; backend/tests/unit/test_meal_record_api.py</read_first><behavior>- overview 从活跃快照返回今日 totals、餐数与七个 local-day 槽位，并只由注入的 PlanningCompletionProjection read port 返回 eligibility、目标区间和版本。
- 没有、撤销、跨用户或 revision 失效 projection 时，overview 明确 eligibility=false 且不含 target 数值；禁止 service/repository 导入或查询 PlanningProfile ORM。
- GET /api/v1/dashboard/history 返回 strict DTO，按 consumed_local_date 分组，使用稳定 DESC opaque keyset cursor。
- 无效 cursor/范围为 422；软删和其他 user_id 不进入 SQL。</behavior><action>先以 fake repository + fake planning narrow port、HTTPX 和真实 PostgreSQL 写失败测试；额外覆盖 completion projection 生成后 strict eligibility/target DTO、profile/target 撤销后目标字段消失、跨用户投影隔离、cursor 篡改、同时间戳 tiebreaker、跨周、空页和 Decimal 快照。断言 dashboard/service.py 与 repository.py 不读取 PlanningProfile 或规划表；浏览器不得推导 nutrition、目标或分页 token。</action><acceptance_criteria>测试在 API 出现前失败，且 overview 的目标资格有唯一的可撤销来源。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/dashboard/test_dashboard_service.py tests/integration/test_dashboard_repository.py tests/integration/test_dashboard_overview_projection.py tests/unit/test_dashboard_api.py -q</automated></verify><done>RED 测试锁定 strict projection DTO、SQL cursor、周界和租户过滤。</done></task>
<task type="auto" tdd="true"><name>Task 2: 注入 completion projection 并实现 tenant-filtered 聚合 API</name><files>backend/app/README.md, backend/app/dashboard/README.md, backend/app/dashboard/schemas.py, backend/app/dashboard/repository.py, backend/app/dashboard/service.py, backend/app/dashboard/api.py, backend/app/main.py, backend/tests/README.md, backend/tests/dashboard/README.md, backend/tests/dashboard/test_dashboard_service.py, backend/tests/integration/test_dashboard_repository.py, backend/tests/integration/test_dashboard_overview_projection.py, backend/tests/unit/test_dashboard_api.py</files><read_first>backend/app/dashboard/ports.py; backend/app/planning/ports.py; backend/app/records/api.py; backend/app/records/ports.py; backend/app/main.py; backend/ARCHITECTURE.md</read_first><action>按 D-01–D-04 创建 dashboard 与 backend/app、backend/tests、backend/tests/dashboard 的 README/父索引。按 API→Service→Repository 注册 overview 与 history；在 dashboard/service.py 的构造依赖中注入由 planning 实现的 narrow PlanningCompletionProjection port，overview response 使用 `extra=forbid` 的 eligibility/target-ranges/target-version DTO，eligibility=false 时省略所有目标值。不得导入 PlanningProfile ORM、查询 planning 表或从 profile 推测目标。SQL 先限定 user_id、deleted_at、persisted local date，再求 snapshot totals；history 用 consumed_local_date/consumed_at/id keyset 和 opaque cursor，只投影日期、确认快照 totals、餐数与最小 history item，不返回原文、图像、邮箱或模型数据。</action><acceptance_criteria>empty/multi-page/cross-week/soft-delete 与 projection active/revoked 均有确定 shape；真实 PostgreSQL 证明 cursor 不漏不重且前端唯一可消费 projection-backed overview。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/dashboard/test_dashboard_service.py tests/integration/test_dashboard_repository.py tests/integration/test_dashboard_overview_projection.py tests/unit/test_dashboard_api.py -q</automated></verify><done>H5 获得唯一、严格验证、由可撤销完成计划投影授权目标对比的 dashboard 契约。</done></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| H5 cursor → dashboard API | cursor/date 参数不可信 |
| repository → MealRecord | 只能读取本用户快照 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-02 | I | history query | mitigate | SQL tenant predicate、opaque cursor、PG tests。 |
| T-06-03 | T | cursor | mitigate | strict bounded schema、422 contract tests。 |
| T-06-SC | Tampering | package installs | accept | 不安装新包。 |</threat_model>
<verification>fake service、HTTPX、真实 PostgreSQL overview/history cursor 测试通过。</verification>
<success_criteria>前端只消费受隔离、严格验证、服务端分页的摄入事实。</success_criteria>
<output>Create .planning/phases/06-user-dashboard-admin/06-02-SUMMARY.md when done</output>
