---
phase: 06-user-dashboard-admin
plan: 24
type: execute
wave: 2
depends_on: [06-10]
files_modified: [admin-frontend/src/README.md, admin-frontend/src/vite-env.d.ts, admin-frontend/src/main.tsx, admin-frontend/src/App.tsx, admin-frontend/src/styles/README.md, admin-frontend/src/styles/index.css, admin-frontend/src/test/README.md, admin-frontend/src/test/setup.ts, admin-frontend/src/auth/README.md, admin-frontend/src/auth/AdminAuthProvider.tsx]
autonomous: true
requirements: [ARC-08, ADM-01]
must_haves:
  truths: ["独立后台入口固定为 BrowserRouter → QueryClientProvider → AdminAuthProvider → App", "access token 仅存在 Provider memory，身份变化或退出清 Query cache"]
  artifacts: ["admin-frontend/src/main.tsx", "admin-frontend/src/auth/AdminAuthProvider.tsx", "admin-frontend/src/styles/index.css", "admin-frontend/src/test/setup.ts"]
  key_links: ["main.tsx → BrowserRouter → QueryClientProvider → AdminAuthProvider → App", "AdminAuthProvider logout/identity change → QueryClient.clear"]
---
<objective>建立独立后台的可执行入口、Provider tree、样式与测试运行时。Purpose: 提供后续 admin feature 可安全依赖的实际 SPA runtime。Output: src 入口、memory-only auth provider、语义样式和 Vitest setup。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@frontend/AGENTS.md
@.planning/phases/06-user-dashboard-admin/06-UI-SPEC.md
@.planning/phases/06-user-dashboard-admin/06-PATTERNS.md
@.planning/phases/06-user-dashboard-admin/06-10-PLAN.md</context>
<tasks>
<task type="auto"><name>Task 1: 创建入口、memory-only Provider、样式和测试 setup</name><files>admin-frontend/src/README.md, admin-frontend/src/vite-env.d.ts, admin-frontend/src/main.tsx, admin-frontend/src/App.tsx, admin-frontend/src/styles/README.md, admin-frontend/src/styles/index.css, admin-frontend/src/test/README.md, admin-frontend/src/test/setup.ts, admin-frontend/src/auth/README.md, admin-frontend/src/auth/AdminAuthProvider.tsx</files><read_first>frontend/src/main.tsx; frontend/src/App.tsx; frontend/src/auth/AuthProvider.tsx; frontend/src/styles/index.css; frontend/src/test/setup.ts; .planning/phases/06-user-dashboard-admin/06-UI-SPEC.md</read_first><action>首次创建 src、styles、test、auth 目录时在同一 task 写中文 README，列出职责、允许依赖和文件索引，并在 src README 登记全部直接子目录。实现有明确 token 清理和 identity change Query cache 清理能力的 `AdminAuthProvider`，access token 不得写入 localStorage、sessionStorage 或 cookie。`main.tsx` 必须以精确顺序创建 `BrowserRouter → QueryClientProvider → AdminAuthProvider → App` 并导入 styles；`App.tsx` 是独立 admin 路由根，后续计划在它上面注册真实页面，绝不 import 用户 H5。styles 定义 UI-SPEC 语义 token、可见焦点和 reduced-motion 基线；test setup 配置 Vitest/Testing Library/MSW cleanup、DOM matcher 与 mock reset。</action><acceptance_criteria>入口可编译，provider 嵌套和 memory-only token 可由测试/静态检查证明，每个新目录在同一 task 有 README 和父索引。</acceptance_criteria><verify><automated>cd admin-frontend && npm test -- --run && npm run typecheck && npm run build && ! rg -n 'localStorage|sessionStorage|frontend/src' src</automated></verify><done>独立 SPA 已有可运行的 provider composition、样式和测试运行时，而不是只剩配置残片。</done></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| AdminAuthProvider → public admin API | token 与 Query cache 不得变成授权真相或持久泄露 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-40 | I/E | auth/provider bootstrap | mitigate | memory-only token、identity/logout cache reset、Vitest 与 static scan。 |
| T-06-SC | Tampering | package installs | accept | 只使用 06-10 已锁定依赖。 |</threat_model>
<verification>Vitest、typecheck、build、token/import static scan 和 README 索引检查通过。</verification>
<success_criteria>后续后台功能可建立在唯一、安全、独立的 React runtime 上。</success_criteria>
<output>Create .planning/phases/06-user-dashboard-admin/06-24-SUMMARY.md when done</output>
