---
phase: 06-user-dashboard-admin
plan: 16
type: execute
wave: 9
depends_on: [06-14]
files_modified: [backend/migrations/versions/0017_runtime_config_versions.py, backend/app/agent/models.py, backend/app/agent/service.py, backend/app/providers/reasoning/factory.py, backend/app/admin/schemas.py, backend/app/admin/repository.py, backend/app/admin/service.py, backend/app/admin/api.py, backend/tests/admin/test_runtime_config_service.py]
autonomous: true
requirements: [ADM-04, ADM-05]
must_haves:
  truths: ["管理员改变非密钥配置只影响后续运行", "停用立即拒绝新调用，已发调用按旧快照完成记账"]
  artifacts: ["backend/migrations/versions/0017_runtime_config_versions.py", "backend/app/admin/service.py"]
  key_links: ["admission → immutable config snapshot", "admin command → audit → environment-only resolver"]
---
<objective>交付非密钥运行配置与准入后端。Purpose: 实现 D-15/D-16。Output: immutable config versions、admission lock、API 与测试。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md
@backend/AGENTS.md
@.planning/phases/06-user-dashboard-admin/06-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 写配置快照和停用 RED 测试</name><files>backend/tests/admin/test_runtime_config_service.py</files><read_first>backend/app/agent/models.py; backend/app/agent/service.py; backend/app/providers/reasoning/factory.py; backend/app/admin/service.py</read_first><behavior>- command 只接受 allowlisted provider/model alias/caps/price/reason/confirm，拒绝 key/endpoint。
- 新 run 原子 snapshot active config；旧 run 不变。
- disable/budget 拒绝新调用，已发调用自然 timeout/usage/accounting，OUTCOME_UNKNOWN 不重放。</behavior><action>先用 Fake Provider/时钟写 RED 测试，覆盖 DB-RBAC、If-Match/idempotency、single/period cap、disable race 和安全 retryable result。</action><acceptance_criteria>测试在实现前失败且不依赖真实 Provider/密钥。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/admin/test_runtime_config_service.py -q</automated></verify><done>RED 测试锁定配置版本、准入快照和停用语义。</done></task>
<task type="auto" tdd="true"><name>Task 2: 实现 immutable runtime config 准入</name><files>backend/migrations/versions/0017_runtime_config_versions.py, backend/app/agent/models.py, backend/app/agent/service.py, backend/app/providers/reasoning/factory.py, backend/app/admin/schemas.py, backend/app/admin/repository.py, backend/app/admin/service.py, backend/app/admin/api.py, backend/tests/admin/test_runtime_config_service.py</files><read_first>backend/app/providers/reasoning/deepseek.py; backend/app/agent/models.py; backend/migrations/versions/0014_admin_audit_foundation.py</read_first><action>迁移增加 immutable AgentRuntimeConfigVersion 和 AgentRun/Invocation runtime_config_version、price/cap snapshot。Factory 仅从环境拿 key/endpoint；API 仅管理 allowlisted model、enabled、caps、price。admission 锁定 active config、校验 worst-case cap、写 snapshot 后才调用；disable 只阻止未来调用，不中断已发请求，并写最小安全 ledger/audit。</action><acceptance_criteria>密钥/endpoint 永不存入或返回；新旧运行配置可审计且隔离。</acceptance_criteria><verify><automated>cd backend && APP_ENV=test uv run alembic upgrade head && uv run pytest tests/admin/test_runtime_config_service.py -q</automated></verify><done>管理员可安全管理未来运行策略，账本保留启动快照。</done></task>
<task type="auto"><name>Task 3: 固定 0017 终端迁移并验证唯一 head</name><files>backend/migrations/versions/0017_runtime_config_versions.py</files><read_first>backend/migrations/versions/0016_catalog_publication_eligibility.py; backend/migrations/env.py</read_first><action>把 `0017_runtime_config_versions.py` 的 down_revision 明确指向 `0016_catalog_publication_eligibility`，完成最终唯一顺序 `0010 → 0011 → 0012 → 0013 → 0014 → 0015 → 0016 → 0017`。执行完成后 `alembic heads` 必须且只能报告 0017；不得保留并行 head、缺失前驱或未接入的 migration 文件。</action><acceptance_criteria>0017 是 Phase 6 唯一末端 revision，空库 upgrade head 和 downgrade/re-upgrade 都沿单线执行。</acceptance_criteria><verify><automated>cd backend && APP_ENV=test uv run alembic upgrade head && test "$(uv run alembic heads | wc -l | tr -d ' ')" = 1 && uv run alembic history --verbose</automated></verify><done>Phase 6 的整个 Alembic revision chain 已以唯一 head 收束。</done></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| config form → provider resolver | 任意 endpoint/key 是高风险输入 |
| config change → agent run | 并发准入可突破预算或停用 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-30 | I | config API | mitigate | allowlist/env-only secret/DTO exclusion tests。 |
| T-06-31 | D/R | admission | mitigate | lock/snapshot/cap/disable/no-replay tests。 |
| T-06-SC | Tampering | package installs | accept | 无新增包。 |</threat_model>
<verification>Fake Provider、runtime config service、Alembic tests 通过。</verification>
<success_criteria>配置只塑造未来安全运行，绝不泄露或重写已启动运行。</success_criteria>
<output>Create .planning/phases/06-user-dashboard-admin/06-16-SUMMARY.md when done</output>
