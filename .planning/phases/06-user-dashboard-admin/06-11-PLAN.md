---
phase: 06-user-dashboard-admin
plan: 11
type: execute
wave: 6
depends_on: [06-07, 06-08]
files_modified: [backend/migrations/versions/0014_admin_audit_foundation.py, backend/app/admin/models.py, backend/app/admin/schemas.py, backend/app/admin/repository.py, backend/app/admin/service.py, backend/app/admin/api.py, backend/tests/README.md, backend/tests/admin/README.md, backend/tests/admin/test_admin_rbac_audit_service.py, backend/tests/admin/test_admin_audit_service.py, backend/tests/integration/test_admin_audit_repository.py, backend/tests/unit/test_admin_rbac_api.py, backend/tests/unit/test_admin_audit_api.py]
autonomous: true
requirements: [ADM-01, ADM-05]
must_haves:
  truths: ["每个 admin API 重新从 PostgreSQL 校验 active admin role", "后台变更以 actor/time/reason/server before-after 安全 diff 原子审计", "管理员可通过 cursor 读取最小化、只读的操作审计"]
  artifacts: ["backend/migrations/versions/0014_admin_audit_foundation.py", "backend/app/admin/service.py"]
  key_links: ["/api/v1/admin/* → AuthenticatedPrincipal → DB role check", "mutation transaction → append-only AdminAuditEvent", "GET /api/v1/admin/audit → current-role RBAC → Service → Repository keyset projection"]
---
<objective>建立 admin DB-RBAC、通用审计命令及只读审计查询。Purpose: 实现 D-10/D-17/ADM-01/ADM-05。Output: append-only audit、endpoint-local role check、`/api/v1/admin/audit` cursor API、测试。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md
@backend/AGENTS.md
@.planning/phases/06-user-dashboard-admin/06-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 写 DB-authoritative RBAC/audit query RED 测试</name><files>backend/tests/README.md, backend/tests/admin/README.md, backend/tests/admin/test_admin_rbac_audit_service.py, backend/tests/admin/test_admin_audit_service.py, backend/tests/integration/test_admin_audit_repository.py, backend/tests/unit/test_admin_rbac_api.py, backend/tests/unit/test_admin_audit_api.py</files><read_first>backend/app/admin/api.py; backend/app/admin/service.py; backend/app/admin/repository.py; backend/app/auth/service.py; backend/tests/README.md</read_first><behavior>- 普通用户、过期会话、伪造 role claim 被 401/403；降级后的旧 token 立即失权。
- 每个 command 要 nonblank reason、If-Match 和 Idempotency-Key；同一事务产生 immutable audit。
- GET `/api/v1/admin/audit` 在每次读取先经数据库 current-role RBAC，按 time/action/object/actor/reason/关联 version filter，以时间/id opaque cursor 返回稳定分页。
- 审计 DTO 白名单仅为 actor 标识、time、action、object、reason、字段级 before/after diff、关联 version/command；不含邮箱、密钥、原图、原文、provider body 或完整 State。</behavior><action>首次创建 tests/admin 时，同步创建 README 并更新 tests 父索引；随后写 fake Service、HTTPX 和真实 PostgreSQL RED 测试，覆盖 role 读取发生在领域数据读取之前、rollback、不重复 command key、DTO 漏字段、白名单 filters、cursor 篡改/稳定 tiebreaker、跨管理员记录和普通/过期/降级会话拒绝。</action><acceptance_criteria>任一 API 越权、失配 revision、重复 command 或越界 audit 查询均可自动复现，测试目录同次可导航。</acceptance_criteria><verify><automated>cd backend && uv run pytest tests/admin/test_admin_rbac_audit_service.py tests/admin/test_admin_audit_service.py tests/integration/test_admin_audit_repository.py tests/unit/test_admin_rbac_api.py tests/unit/test_admin_audit_api.py -q && test -f tests/admin/README.md</automated></verify><done>RED 测试固定后台最终授权、审计原子性和只读 keyset 查询。</done></task>
<task type="auto" tdd="true"><name>Task 2: 实现 admin 角色守卫、通用审计和 audit API</name><files>backend/migrations/versions/0014_admin_audit_foundation.py, backend/app/admin/models.py, backend/app/admin/schemas.py, backend/app/admin/repository.py, backend/app/admin/service.py, backend/app/admin/api.py, backend/tests/README.md, backend/tests/admin/README.md, backend/tests/admin/test_admin_rbac_audit_service.py, backend/tests/admin/test_admin_audit_service.py, backend/tests/integration/test_admin_audit_repository.py, backend/tests/unit/test_admin_rbac_api.py, backend/tests/unit/test_admin_audit_api.py</files><read_first>backend/app/admin/models.py; backend/app/admin/service.py; backend/migrations/versions/0001_initial.py; backend/tests/admin/README.md</read_first><action>迁移新增 generic append-only AdminAuditEvent（actor、time、action/object、reason、server-computed safe field-level before/after diff、关联 version、idempotency key）及 DB checks/index。所有 `/api/v1/admin/*` 路由（包括 `GET /api/v1/admin/audit`）经既有认证后调用 AdminService.require_role，从 PostgreSQL 重读 active role；命令 schema 强制 reason/If-Match/Idempotency-Key，Service 在一次 commit 写 mutation/audit，Repository flush-only，冲突映射 409。为 audit GET 增加严格 query/response schema、bounded action/object/actor/time/reason filters、time/id opaque cursor 和白名单投影；不得加载或回传 audit payload、邮件、密钥、原文、图像、Provider body 或完整 State。</action><acceptance_criteria>普通/过期/伪造 claim 永远不能读取或写入 admin 数据；审计和命令要么一起提交要么一起 rollback，管理员可分页读取字段差异与关联版本而不见敏感数据。</acceptance_criteria><verify><automated>cd backend && APP_ENV=test uv run alembic upgrade head && uv run pytest tests/admin/test_admin_rbac_audit_service.py tests/admin/test_admin_audit_service.py tests/integration/test_admin_audit_repository.py tests/unit/test_admin_rbac_api.py tests/unit/test_admin_audit_api.py -q</automated></verify><done>所有后续 admin capability 具备 DB-RBAC、并发命令、安全审计和完整只读 audit API 基础。</done></task>
<task type="auto"><name>Task 3: 固定 0014 的线性迁移前驱</name><files>backend/migrations/versions/0014_admin_audit_foundation.py</files><read_first>backend/migrations/versions/0013_weekly_review_results.py; backend/migrations/env.py</read_first><action>把 `0014_admin_audit_foundation.py` 的 down_revision 明确指向 `0013_weekly_review_results`。本计划显式等待 06-07/06-08，既避免与 06-08 并行修改 backend/tests 索引，也确保最终唯一顺序 `0010 → 0011 → 0012 → 0013 → 0014 → 0015 → 0016 → 0017` 没有多 head。</action><acceptance_criteria>0014 在 0013 后唯一接续，backend/tests 索引不与同波计划共享写入。</acceptance_criteria><verify><automated>cd backend && uv run alembic heads && uv run alembic history --verbose</automated></verify><done>admin 审计迁移在唯一线性链上执行。</done></task>
</tasks>
<threat_model>## Trust Boundaries
| Boundary | Description |
|---|---|
| bearer claim → admin endpoint | JWT role claim 会过期或被伪造 |
| command → audit | 审计必须与变更同事务 |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-20 | E | admin endpoints | mitigate | DB active-role recheck、401/403 tests。 |
| T-06-21 | T/R | command/audit | mitigate | If-Match/idempotency/single transaction/PG rollback tests。 |
| T-06-SC | Tampering | package installs | accept | 不安装新包。 |</threat_model>
<verification>fake service、HTTPX RBAC、真实 PostgreSQL audit transaction 测试通过。</verification>
<success_criteria>后台授权永远在服务端 DB 完成，所有变更均有最小化、不可变审计证据。</success_criteria>
<output>Create .planning/phases/06-user-dashboard-admin/06-11-SUMMARY.md when done</output>
