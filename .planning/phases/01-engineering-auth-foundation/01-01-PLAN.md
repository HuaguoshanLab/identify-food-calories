---
phase: 01-engineering-auth-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md, docker-compose.yml, backend/README.md, backend/AGENTS.md, backend/pyproject.toml, backend/.env.example, backend/app/README.md, backend/app/core/README.md, backend/app/core/config.py, backend/app/core/database.py, backend/app/main.py, backend/tests/README.md, backend/tests/conftest.py, backend/migrations/README.md]
autonomous: true
requirements: [ARC-01, ARC-04, ARC-07]
must_haves:
  truths: ["pgvector dev/test 与 Mailpit 可复现启动", "后端配置对数据库、Cookie、CORS、SMTP 和生产密钥 fail closed", "根与 backend 目录契约完整"]
  artifacts: [{path: "docker-compose.yml", provides: "pgvector dev/test and fixed Mailpit"}, {path: "backend/app/core/config.py", provides: "guarded runtime settings"}]
  key_links: [{from: "backend/app/core/config.py", to: "mailpit:1025", via: "local SMTP settings"}]
---
<objective>建立根/后端工程契约、隔离数据库、固定 Mailpit 与健康运行基座。</objective>
<execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/01-engineering-auth-foundation/01-CONTEXT.md @.planning/phases/01-engineering-auth-foundation/01-RESEARCH.md @AGENTS.md</context>
<tasks>
<task type="auto"><name>Task 1: 审计并建立根与后端目录契约</name><read_first>AGENTS.md; README.md (create — missing); backend/README.md (create — missing); backend/AGENTS.md (create — missing); backend/pyproject.toml; backend/.env.example; backend/app/README.md (create — missing); backend/app/core/README.md (create — missing); backend/migrations/README.md (create — missing); backend/tests/README.md (create — missing)</read_first><files>README.md, backend/README.md, backend/AGENTS.md, backend/pyproject.toml, backend/.env.example, backend/app/README.md, backend/app/core/README.md, backend/migrations/README.md, backend/tests/README.md</files><action>按 D-01/D-02/D-03/D-04/D-05/D-29/D-30 审计旧 scaffold 与 [VERIFIED] 依赖，明确 React/Vite 与 FastAPI/Python 3.11+ 前后端分离、模块化单体；创建根/backend README+AGENTS，并为 app/core/migrations/tests 建立 `职责/允许依赖/文件索引`，父索引同任务更新。</action><verify><automated>python3.11 -c "from pathlib import Path; assert all(Path(p).is_file() for p in ['README.md','backend/README.md','backend/AGENTS.md','backend/app/README.md','backend/app/core/README.md','backend/migrations/README.md','backend/tests/README.md'])"</automated></verify><acceptance_criteria>根与 backend 的规则和索引可被执行器直接发现。</acceptance_criteria><done>目录契约建立。</done></task>
<task type="auto" tdd="true"><name>Task 2: 建立 pgvector 双库、Mailpit 与 fail-closed 配置</name><read_first>README.md; docker-compose.yml (create — missing); backend/README.md; backend/.env.example; backend/app/README.md; backend/app/core/README.md; backend/app/core/config.py; backend/app/core/database.py; backend/app/main.py (create — missing); backend/tests/README.md; backend/tests/conftest.py</read_first><files>README.md, docker-compose.yml, backend/README.md, backend/.env.example, backend/app/README.md, backend/app/core/README.md, backend/app/core/config.py, backend/app/core/database.py, backend/app/main.py, backend/tests/README.md, backend/tests/conftest.py</files><action>按 D-27/D-33 使用固定版本官方 pgvector 和 `axllent/mailpit` 镜像；dev/test DB 独立端口、卷、数据库及 healthcheck，Mailpit 固定 SMTP 1025/UI 8025 且有健康检查。配置拒绝 SQLite、同 dev/test URL、非 `_test`、弱生产 secret、生产 non-Secure cookie、wildcard CORS、生产使用 local Mailpit；本地 SMTP 不需要云凭据。仅验证 vector extension，不建业务向量表。同步根/backend/core/tests 索引并提供 `/api/v1/health`。</action><verify><automated>docker compose up -d --wait postgres postgres-test mailpit &amp;&amp; docker compose exec -T postgres-test psql -U postgres -d food_agent_test -c "CREATE EXTENSION IF NOT EXISTS vector; SELECT extversion FROM pg_extension WHERE extname='vector';" &amp;&amp; curl -fsS http://localhost:8025/api/v1/info</automated></verify><acceptance_criteria>两库、Mailpit 和健康端点稳定；生产邮件配置缺失时启动失败。</acceptance_criteria><done>本地基础设施可复现。</done></task>
</tasks>
<threat_model>| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Tampering | DB isolation | mitigate | separate service/volume/guarded URLs |
| T-01-02 | Information disclosure | SMTP/config | mitigate | no cloud credentials; production fail closed |
| T-01-SC | Tampering | images/packages | mitigate | fixed official verified images/packages |</threat_model>
<verification>Compose --wait、vector、Mailpit health 与配置 guard 均通过。</verification><success_criteria>工程运行基座可重复启动且不泄露生产秘密。</success_criteria><output>Create `01-01-SUMMARY.md`.</output>
