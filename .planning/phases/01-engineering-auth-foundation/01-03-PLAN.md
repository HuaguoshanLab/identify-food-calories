---
phase: 01-engineering-auth-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [backend/README.md, backend/app/README.md, backend/app/auth/README.md, backend/app/auth/models.py, backend/app/auth/schemas.py, backend/app/auth/ports.py, backend/app/auth/repository.py, backend/alembic.ini, backend/migrations/README.md, backend/migrations/env.py, backend/migrations/script.py.mako, backend/migrations/versions/README.md, backend/migrations/versions/0001_auth_foundation.py]
autonomous: true
requirements: [ARC-02, ARC-03, ARC-04, ARC-07]
must_haves:
  truths: ["0001 包含未验证用户、验证码 challenge、会话与 refresh 数据约束", "ORM/Schema/port/adapter 分离", "空隔离库可执行迁移"]
  artifacts: [{path: "backend/migrations/versions/0001_auth_foundation.py", provides: "auth and verification schema"}]
  key_links: [{from: "backend/app/auth/repository.py", to: "backend/app/auth/models.py", via: "request Session"}]
---
<objective>定义认证、邮箱验证和会话的数据库权威契约。</objective><execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context><context>@.planning/phases/01-engineering-auth-foundation/01-01-SUMMARY.md @.planning/phases/01-engineering-auth-foundation/01-RESEARCH.md</context>
<tasks>
<task type="auto"><name>Task 1: 定义 auth 模型、Schema 与 Repository port</name><read_first>backend/app/README.md; backend/app/auth/README.md (create — missing); backend/app/auth/models.py (create — missing); backend/app/auth/schemas.py (create — missing); backend/app/auth/ports.py (create — missing); backend/app/auth/repository.py (create — missing)</read_first><files>backend/app/README.md, backend/app/auth/README.md, backend/app/auth/models.py, backend/app/auth/schemas.py, backend/app/auth/ports.py, backend/app/auth/repository.py</files><action>按 D-06/D-18/D-19/D-20/D-31/D-32 分离 ORM、Pydantic、port/adapter。定义 users.email_verified_at、verification_challenges（purpose、context_digest、code_digest、expires_at、attempts、max_attempts、resend_available_at、consumed/invalidated）、sessions/refresh token；Repository 只 query/flush，不 commit。创建 auth README 并更新 app 索引。</action><verify><automated>cd backend &amp;&amp; python3.11 -m compileall -q app</automated></verify><acceptance_criteria>运行时类型可导入，公开 Schema 不含密码哈希、验证码摘要或 role 输入。</acceptance_criteria><done>数据接口确定。</done></task>
<task type="auto" tdd="true"><name>Task 2: 创建并验证 0001</name><read_first>backend/README.md; backend/alembic.ini (create — missing); backend/migrations/README.md; backend/migrations/env.py (create — missing); backend/migrations/script.py.mako (create — missing); backend/migrations/versions/README.md (create — missing); backend/migrations/versions/0001_auth_foundation.py (create — missing); backend/app/auth/models.py</read_first><files>backend/README.md, backend/alembic.ini, backend/migrations/README.md, backend/migrations/env.py, backend/migrations/script.py.mako, backend/migrations/versions/README.md, backend/migrations/versions/0001_auth_foundation.py</files><action>0001 建 normalized email unique、role check、email_verified_at、challenge purpose/current/expiry/attempt/consume 约束、context/code digest unique、session/family/FK/index。Alembic 在 APP_ENV=test 时只接受 TEST_DATABASE_URL。backend/migrations/versions 父索引同任务更新。</action><verify><automated>docker compose up -d --wait postgres-test &amp;&amp; cd backend &amp;&amp; APP_ENV=test DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/food_agent_dev TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:55432/food_agent_test alembic upgrade head</automated></verify><acceptance_criteria>0001 在空隔离 test DB 成功且不会连接开发库。</acceptance_criteria><done>认证迁移基线成立。</done></task>
</tasks><threat_model>| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Tampering | challenge/session schema | mitigate | DB constraints and reviewed migration |
| T-03-02 | Information disclosure | secret columns | mitigate | private ORM fields excluded from schemas |
| T-03-SC | Tampering | test URL | mitigate | exact guarded URL |</threat_model><verification>Imports 与空库 migration 通过。</verification><success_criteria>身份和验证码权威数据边界成立。</success_criteria><output>Create `01-03-SUMMARY.md`.</output>
