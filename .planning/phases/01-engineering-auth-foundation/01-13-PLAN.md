---
phase: 01-engineering-auth-foundation
plan: 13
type: execute
wave: 13
depends_on: [01-12]
files_modified: [backend/app/README.md, backend/app/accounts/README.md, backend/app/accounts/schemas.py, backend/app/accounts/ports.py, backend/app/accounts/repository.py, backend/app/accounts/service.py, backend/app/accounts/api.py, backend/app/main.py, backend/app/notifications/README.md, backend/app/notifications/ports.py, backend/app/notifications/smtp.py, backend/tests/README.md, backend/tests/accounts/README.md, backend/tests/accounts/test_account_recovery.py]
autonomous: true
requirements: [AUTH-06, ARC-02, ARC-03, ARC-04, ARC-07]
must_haves:
  truths: ["密码重置复用共享 6 位验证码与 MailProvider 策略", "发送响应不枚举账号", "密码更新、challenge 消费、session family revoke 原子", "并发消费只有一个成功"]
  artifacts: [{path: "backend/app/accounts/ports.py", provides: "AccountRecoveryRepository and SessionFamilyRevoker"}, {path: "backend/tests/accounts/test_account_recovery.py", provides: "service/API/real PG evidence"}]
  key_links: [{from: "backend/app/accounts/service.py", to: "SessionFamilyRevoker", via: "application port in same request transaction"}]
---
<objective>交付共享 MailProvider/验证码策略下的密码重置与原子会话撤销。</objective><execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context><context>@.planning/phases/01-engineering-auth-foundation/01-04-SUMMARY.md @.planning/phases/01-engineering-auth-foundation/01-10-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 实现密码恢复 Service 与端口</name><read_first>backend/app/README.md; backend/app/accounts/README.md (create — missing); backend/app/accounts/schemas.py (create — missing); backend/app/accounts/ports.py (create — missing); backend/app/accounts/repository.py (create — missing); backend/app/accounts/service.py (create — missing); backend/app/notifications/README.md; backend/app/notifications/ports.py; backend/app/notifications/smtp.py; backend/tests/README.md; backend/tests/accounts/README.md (create — missing); backend/tests/accounts/test_account_recovery.py (create — missing)</read_first><files>backend/app/README.md, backend/app/accounts/README.md, backend/app/accounts/schemas.py, backend/app/accounts/ports.py, backend/app/accounts/repository.py, backend/app/accounts/service.py, backend/app/notifications/README.md, backend/app/notifications/ports.py, backend/app/notifications/smtp.py, backend/tests/README.md, backend/tests/accounts/README.md, backend/tests/accounts/test_account_recovery.py</files><action>按 D-12/D-18/D-19/D-20/D-32/D-33 复用 0001 verification_challenges purpose=password_reset 和共享 MailProvider/code policy；accounts 不导入 auth repository，而通过 SessionFamilyRevoker port 在同一 request Session 撤销 family。Fake provider 单测统一 202、digest、10 分钟、5 次、60 秒、新码失效、single-use；密码 update+consume+family revoke 原子，任何异常全部 rollback。创建 accounts/tests/accounts README 并更新 app/tests/notifications 索引。</action><verify><automated>cd backend &amp;&amp; APP_ENV=test DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/food_agent_dev TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:55432/food_agent_test python3.11 -m pytest tests/accounts/test_account_recovery.py -q</automated></verify><acceptance_criteria>Service fake tests 证明共享验证码和原子恢复规则。</acceptance_criteria><done>恢复应用逻辑完成。</done></task>
<task type="auto" tdd="true"><name>Task 2: 暴露恢复 API 并验证真实 PG 竞争</name><read_first>backend/app/accounts/README.md; backend/app/accounts/schemas.py; backend/app/accounts/service.py; backend/app/accounts/api.py (create — missing); backend/app/main.py; backend/tests/accounts/README.md; backend/tests/accounts/test_account_recovery.py</read_first><files>backend/app/accounts/README.md, backend/app/accounts/schemas.py, backend/app/accounts/service.py, backend/app/accounts/api.py, backend/app/main.py, backend/tests/accounts/README.md, backend/tests/accounts/test_account_recovery.py</files><action>实现 forgot/context/verify-resend/reset API 的稳定 error codes 与 pending HttpOnly context；两个真实 PG 连接并发 consume 只一胜，验证过期/重复、密码更新+消费+family revoke 原子成功及注入失败 rollback。SMTP/Mailpit 仅本地开发，API/日志不回显代码。</action><verify><automated>docker compose up -d --wait postgres-test mailpit &amp;&amp; cd backend &amp;&amp; APP_ENV=test DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/food_agent_dev TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:55432/food_agent_test python3.11 -m pytest tests/accounts/test_account_recovery.py -q</automated></verify><acceptance_criteria>恢复 API、Mailpit 投递、并发单次消费和 rollback 有真实 PG 证据。</acceptance_criteria><done>AUTH-06 恢复闭环完成。</done></task>
</tasks><threat_model>| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-13-01 | Information disclosure | recovery | mitigate | uniform 202, masked context, no code logs |
| T-13-02 | Tampering | concurrent reset | mitigate | row lock and atomic transaction |
| T-13-SC | Tampering | mail adapter | mitigate | shared verified provider boundary |</threat_model><verification>Recovery service/API/real PG/Mailpit tests 通过。</verification><success_criteria>密码重置安全复用验证码基础设施且撤销旧会话。</success_criteria><output>Create `01-13-SUMMARY.md`.</output>
