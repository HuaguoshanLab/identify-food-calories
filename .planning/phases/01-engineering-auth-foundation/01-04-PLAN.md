---
phase: 01-engineering-auth-foundation
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified: [backend/app/README.md, backend/app/notifications/README.md, backend/app/notifications/ports.py, backend/app/notifications/smtp.py, backend/app/auth/README.md, backend/app/auth/schemas.py, backend/app/auth/ports.py, backend/app/auth/repository.py, backend/app/auth/security.py, backend/app/auth/service.py, backend/app/auth/api.py, backend/app/main.py, backend/tests/auth/README.md, backend/tests/auth/test_registration_verification.py]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-06, ARC-02, ARC-03, ARC-07]
must_haves:
  truths: ["注册创建未激活账号并发送 6 位码", "验证码策略由后端权威执行且只存摘要", "验证成功单次激活并返回登录流程"]
  artifacts: [{path: "backend/app/notifications/ports.py", provides: "replaceable MailProvider"}, {path: "backend/tests/auth/test_registration_verification.py", provides: "verification protocol evidence"}]
  key_links: [{from: "backend/app/auth/service.py", to: "backend/app/notifications/ports.py", via: "MailProvider injection"}]
---
<objective>交付注册→验证码→激活的后端闭环和本地 SMTP 实现。</objective><execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context><context>@.planning/phases/01-engineering-auth-foundation/01-03-SUMMARY.md @.planning/phases/01-engineering-auth-foundation/01-UI-SPEC.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 定义共享 MailProvider 与验证码策略</name><read_first>backend/app/README.md; backend/app/notifications/README.md (create — missing); backend/app/notifications/ports.py (create — missing); backend/app/notifications/smtp.py (create — missing); backend/app/auth/README.md; backend/app/auth/schemas.py; backend/app/auth/ports.py; backend/app/auth/repository.py; backend/app/auth/security.py (create — missing); backend/app/auth/service.py (create — missing); backend/tests/auth/README.md (create — missing); backend/tests/auth/test_registration_verification.py (create — missing)</read_first><files>backend/app/README.md, backend/app/notifications/README.md, backend/app/notifications/ports.py, backend/app/notifications/smtp.py, backend/app/auth/README.md, backend/app/auth/schemas.py, backend/app/auth/ports.py, backend/app/auth/repository.py, backend/app/auth/security.py, backend/app/auth/service.py, backend/tests/auth/README.md, backend/tests/auth/test_registration_verification.py</files><action>按 D-07/D-11/D-12/D-31/D-32/D-33 先用 FakeMailProvider 写 RED 测试，再实现 MailProvider port 与 SMTP adapter。注册固定 role=user、Argon2id 后创建未验证用户和当前 challenge；6 位 ASCII 数字使用 CSPRNG，保存 HMAC/digest 而非明文，10 分钟、5 次、60 秒、新码 invalidates old、成功 single-use。context 使用 HttpOnly 同源 cookie/服务端摘要，接口只返回 masked_email/cooldown；发送/重发统一 202，不记录代码。生产缺 provider/config fail closed。创建 notifications/auth/test README 并同步 app 索引。</action><verify><automated>cd backend &amp;&amp; APP_ENV=test DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/food_agent_dev TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:55432/food_agent_test python3.11 -m pytest tests/auth/test_registration_verification.py -q</automated></verify><acceptance_criteria>Fake provider 证明所有时间/次数/失效/单次规则，日志和响应无明文验证码。</acceptance_criteria><done>注册验证应用协议完成。</done></task>
<task type="auto" tdd="true"><name>Task 2: 暴露注册验证 API 并接通 Mailpit</name><read_first>backend/app/notifications/smtp.py; backend/app/auth/README.md; backend/app/auth/schemas.py; backend/app/auth/service.py; backend/app/auth/api.py (create — missing); backend/app/main.py; backend/tests/auth/README.md; backend/tests/auth/test_registration_verification.py</read_first><files>backend/app/notifications/smtp.py, backend/app/auth/README.md, backend/app/auth/schemas.py, backend/app/auth/service.py, backend/app/auth/api.py, backend/app/main.py, backend/tests/auth/README.md, backend/tests/auth/test_registration_verification.py</files><action>实现 `POST /api/v1/auth/register`、`GET /api/v1/auth/register/context`、`POST /api/v1/auth/register/verify`、`POST /api/v1/auth/register/resend`。路由只映射稳定 envelope/error code、context cookie、Origin/CORS；验证成功设置 email_verified_at、消费 challenge、清 context，并要求用户登录，不自动签发 session。SMTP local adapter 发往 Mailpit 1025，测试通过 Mailpit API 证明邮件存在但产品 API 不暴露验证码。</action><verify><automated>docker compose up -d --wait postgres-test mailpit &amp;&amp; cd backend &amp;&amp; APP_ENV=test DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/food_agent_dev TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:55432/food_agent_test python3.11 -m pytest tests/auth/test_registration_verification.py -q</automated></verify><acceptance_criteria>注册/上下文/验证/重发合约、Mailpit 投递和非枚举行为通过。</acceptance_criteria><done>AUTH-01/06 注册激活闭环完成。</done></task>
</tasks><threat_model>| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-01 | Spoofing | verification code | mitigate | digest, expiry, attempt cap, single use |
| T-04-02 | Information disclosure | registration/mail | mitigate | uniform 202, masked context, no code logs |
| T-04-SC | Tampering | SMTP/package | mitigate | verified Mailpit and provider port |</threat_model><verification>Service/API/Mailpit 验证协议测试通过。</verification><success_criteria>用户只能经邮箱验证码激活账号。</success_criteria><output>Create `01-04-SUMMARY.md`.</output>
