---
phase: 01-engineering-auth-foundation
plan: 12
type: execute
wave: 12
depends_on: [01-11]
files_modified: [backend/app/README.md, backend/app/admin/README.md, backend/app/admin/models.py, backend/app/admin/schemas.py, backend/app/admin/ports.py, backend/app/admin/repository.py, backend/app/admin/service.py, backend/app/admin/api.py, backend/app/admin/cli.py, backend/app/main.py, backend/migrations/versions/README.md, backend/migrations/versions/0003_admin_audit.py, backend/tests/integration/README.md, backend/tests/integration/test_admin_audit.py]
autonomous: true
requirements: [AUTH-05, ARC-02, ARC-03, ARC-04, ARC-07]
must_haves:
  truths: ["user 调 admin probe 为 403，admin 为 200", "create/promote admin 每次持久化完整审计事件", "公开注册不能自赋 admin", "用户 frontend 不参与后台权限"]
  artifacts: [{path: "backend/migrations/versions/0003_admin_audit.py", provides: "admin audit schema"}, {path: "backend/tests/integration/test_admin_audit.py", provides: "real PG RBAC/audit evidence"}]
  key_links: [{from: "backend/app/admin/cli.py", to: "backend/app/admin/service.py", via: "actor/target/reason command"}]
---
<objective>交付后端 admin probe、数据库权威 RBAC 与不可省略的管理员变更审计。</objective><execution_context>@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md</execution_context><context>@.planning/phases/01-engineering-auth-foundation/01-05-SUMMARY.md @.planning/phases/01-engineering-auth-foundation/01-CONTEXT.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: 建立 admin 审计模型、Service 与 probe</name><read_first>backend/app/README.md; backend/app/admin/README.md (create — missing); backend/app/admin/models.py (create — missing); backend/app/admin/schemas.py (create — missing); backend/app/admin/ports.py (create — missing); backend/app/admin/repository.py (create — missing); backend/app/admin/service.py (create — missing); backend/app/admin/api.py (create — missing); backend/app/main.py; backend/migrations/versions/README.md; backend/migrations/versions/0003_admin_audit.py (create — missing); backend/tests/integration/README.md; backend/tests/integration/test_admin_audit.py (create — missing)</read_first><files>backend/app/README.md, backend/app/admin/README.md, backend/app/admin/models.py, backend/app/admin/schemas.py, backend/app/admin/ports.py, backend/app/admin/repository.py, backend/app/admin/service.py, backend/app/admin/api.py, backend/app/main.py, backend/migrations/versions/README.md, backend/migrations/versions/0003_admin_audit.py, backend/tests/integration/README.md, backend/tests/integration/test_admin_audit.py</files><action>按 D-13/D-14/D-15/D-17/D-18/D-19/D-20 创建 admin 模块和 0003。`require_role(admin)` 对 `/api/v1/admin/probe` 每次从 DB 读取 active role；admin_role_audit 持久化 actor、target、before_role、after_role、occurred_at、非空 reason，且角色变更与 audit 同事务。D-17 只延后 UI，不允许延后日志。创建 admin README 并更新 app/versions/integration 索引。</action><verify><automated>docker compose up -d --wait postgres-test &amp;&amp; cd backend &amp;&amp; APP_ENV=test DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/food_agent_dev TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:55432/food_agent_test python3.11 -m pytest tests/integration/test_admin_audit.py -q</automated></verify><acceptance_criteria>user/inactive/admin probe 与原子 audit 模型在真实 PG 通过。</acceptance_criteria><done>后端 RBAC 与审计数据链完成。</done></task>
<task type="auto" tdd="true"><name>Task 2: 实现可审计管理员 CLI</name><read_first>backend/app/admin/README.md; backend/app/admin/service.py; backend/app/admin/cli.py (create — missing); backend/tests/integration/README.md; backend/tests/integration/test_admin_audit.py</read_first><files>backend/app/admin/README.md, backend/app/admin/service.py, backend/app/admin/cli.py, backend/tests/integration/README.md, backend/tests/integration/test_admin_audit.py</files><action>按 D-16/D-17/D-35 在 admin 模块创建 CLI：首次 bootstrap 必须显式 `--reason`，actor 固定记录 `system:bootstrap`；后续 promote 必须提供可验证的现有 admin actor 标识与 reason，禁止匿名提升、自我伪造和公开 API role 输入。真实 PG 测试断言 actor/target/before/after/time/reason 完整、失败 rollback 无孤儿审计；不创建 frontend AdminPage 或 `/admin` 路由。更新 admin/integration 索引。</action><verify><automated>docker compose up -d --wait postgres-test &amp;&amp; cd backend &amp;&amp; APP_ENV=test DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/food_agent_dev TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:55432/food_agent_test python3.11 -m pytest tests/integration/test_admin_audit.py -q</automated></verify><acceptance_criteria>bootstrap 与 promote 审计字段完整，缺 actor/reason 或非 admin actor 均失败且回滚。</acceptance_criteria><done>原 admin CLI audit blocker 闭合。</done></task>
</tasks><threat_model>| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12-01 | Elevation | admin assignment | mitigate | DB role, explicit actor, no public role input |
| T-12-02 | Repudiation | CLI role change | mitigate | atomic immutable audit with reason |
| T-12-SC | Tampering | migration/CLI | mitigate | real PG transaction tests |</threat_model><verification>RBAC probe、CLI 和审计事务测试通过。</verification><success_criteria>管理员权限变化均可追溯，用户 H5 无后台实现。</success_criteria><output>Create `01-12-SUMMARY.md`.</output>
