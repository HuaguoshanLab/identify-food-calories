---
phase: 01.1-h5-ui
plan: "05"
subsystem: ui
tags: [react, h5, public-routes, accessibility, shadcn]
requires:
  - phase: 01.1-02
    provides: MobileFrame、PublicAuthLayout 与唯一页面滚动区
provides:
  - 纯内容化的落地页与法律页
  - 单一主 CTA 与公开路由安全回归证据
affects: [01.1-06, 01.1-07, 01.1-08]
tech-stack:
  added: []
  patterns: [公开页面只提供内容，外层 H5 壳与滚动边界由 PublicAuthLayout 统一持有]
key-files:
  created: [.planning/phases/01.1-h5-ui/01.1-05-SUMMARY.md]
  modified: [frontend/src/auth/PublicPages.tsx, frontend/src/auth/PublicRoutes.test.tsx, frontend/src/auth/README.md]
key-decisions:
  - "首页只保留创建账号一个 primary CTA；登录继续作为 outline 次级动作。"
  - "落地页和法律页不创建 viewport 或滚动根，等待嵌套路由统一接入 PublicAuthLayout。"
patterns-established:
  - "公开内容页面在路由变化后聚焦其唯一 h1，并使用语义 token 与 shadcn Button。"
requirements-completed: []
duration: 6min
completed: 2026-08-28
---

# Phase 01.1 Plan 05: 公开 H5 页面迁移 Summary

**落地页和法律页已变为可放入统一 H5 壳的纯内容组件，并以单一“创建账号”主操作维护公开入口的真实能力边界。**

## Performance

- **Duration:** 6 min
- **Started:** 2026-08-28T06:09:00Z
- **Completed:** 2026-08-28T06:15:07Z
- **Tasks:** 2/2
- **Files modified:** 4

## Accomplishments

- 落地页移除自有 `min-h-dvh` 视口根，改用语义 token 与官方 shadcn `Button`，保留“创建账号”主操作和“登录”次级操作。
- 隐私说明与使用条款变为法律内容组件：具备唯一标题、普通饮食参考边界及稳定的返回首页链接。
- 为公开页面补齐 CTA、聚焦、法律页公开壳、无底部 Tab/后台/上传入口的路由测试，并同步认证目录说明。

## Task Commits

1. **Task 1: 迁移首页为单一主操作的公开 H5 内容**
   - `b574e10` — `test(01.1-05): lock public landing hierarchy`
   - `8fd4589` — `feat(01.1-05): migrate landing into public H5 content`
2. **Task 2: 迁移隐私与条款并同步认证目录职责**
   - `af9e871` — `test(01.1-05): cover public legal content`

## Verification

- `cd frontend && npm test -- src/auth/PublicRoutes.test.tsx` — 5 passed。
- `cd frontend && npm run lint` — passed。
- `cd frontend && npm run typecheck` — passed。
- Codex 内置浏览器（`http://127.0.0.1:5173/`）：首页显示主次 CTA、法律链接与“普通饮食参考”；访问 `/privacy` 后显示“隐私说明”、返回首页链接，且无“主要导航”；从首页点击“创建账号”实际进入 `/register` 并显示“创建账号”。

## Decisions Made

- 在公开入口只表达已交付的认证能力；不新增上传、示例分析、Agent 或后台入口，避免伪装未实现功能。
- 视觉层级由 `Button` 的 default/outline variant 和语义 token 表达，不在公开页散落具体色值。

## Deviations from Plan

### Planned-file overlap

**1. LegalPage was moved while refactoring the shared `PublicPages.tsx` in Task 1.**
- **Found during:** Task 1
- **Reason:** 落地页、隐私和条款共用同一旧 viewport wrapper；只迁移首页会保留法律页的竞争性页面根，与本计划的公开内容边界矛盾。
- **Resolution:** 在同一受计划授权的文件中一并把法律页拆成内容组件；Task 2 新增的公开壳测试因此首次执行即通过。
- **Impact:** 无产品范围扩张；最终路由嵌套接线仍由 Plan 07 完成。

**Total deviations:** 1 planned-file overlap. **Impact:** 保持单一滚动根合同，无额外依赖或功能。

## Known Stubs

None. 本阶段明确不提供上传、分析、记录、计划或后台入口。

## Issues Encountered

- 全局 `git diff --check` 会报告用户已有的 `.gitignore` 末尾空行；本计划的定向文件检查通过，且未暂存或提交 `.gitignore`。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 06 可把认证表单迁入同一视觉与无障碍契约。
- Plan 07 需要将公开路由实际嵌套到 `PublicAuthLayout`，使本计划的内容组件在线上路由中接收统一 H5 壳。

## Self-Check: PASSED

- `frontend/src/auth/PublicPages.tsx`、`frontend/src/auth/PublicRoutes.test.tsx`、`frontend/src/auth/README.md` 与本 SUMMARY 均存在。
- `b574e10`、`8fd4589`、`af9e871` 均可在 Git 历史中找到。
- 本计划没有删除已跟踪文件，也没有提交用户的 `.gitignore` 修改。

---
*Phase: 01.1-h5-ui*
*Completed: 2026-08-28*
