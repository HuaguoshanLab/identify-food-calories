---
phase: 01.1-h5-ui
plan: "06"
subsystem: auth-ui
tags: [react, h5, authentication, react-hook-form, zod, shadcn, accessibility]
requires:
  - phase: 01
    provides: 注册、邮箱验证、登录与密码恢复的安全 API 状态机
  - phase: 01.1-02
    provides: PublicAuthLayout 与 H5 页面壳合同
provides:
  - 五个认证表单统一使用 shadcn 语义原语
  - 注册与恢复流程的文字步骤提示和字段级无障碍关联
  - 验证码冷却、重发、恢复 context 与安全导航的回归证据
affects: [01.1-07, 01.1-08, auth]
tech-stack:
  added: []
  patterns: [认证页使用 Input、Label、Button、Alert；步骤进度由 AuthEntryPage 在标题前渲染]
key-files:
  created: [.planning/phases/01.1-h5-ui/01.1-06-SUMMARY.md]
  modified: [frontend/src/auth/LoginPage.tsx, frontend/src/auth/RegisterPage.tsx, frontend/src/auth/RegisterVerifyPage.tsx, frontend/src/auth/ForgotPasswordPage.tsx, frontend/src/auth/ResetPasswordPage.tsx, frontend/src/auth/AuthForms.test.tsx]
key-decisions:
  - "验证码和恢复 context 继续只由公开 API 与 HttpOnly Cookie 管理，展示层不复制到 React 持久状态。"
  - "步骤文字由共享 AuthEntryPage 放在 h1 前，避免每个表单各自维护标题层级。"
patterns-established:
  - "认证服务端错误放在表单顶部 Alert，Zod 字段错误紧贴输入并通过 aria-describedby 关联。"
requirements-completed: []
duration: 6min
completed: 2026-08-28
---

# Phase 01.1 Plan 06: 认证表单 H5 迁移 Summary

**五个认证表单已统一为移动端 H5 的 shadcn 语义控件，同时保留 Phase 1 的认证请求、验证码冷却、重发、恢复 context 与安全跳转合同。**

## Performance

- **Duration:** 6 min
- **Started:** 2026-08-28T06:17:00Z
- **Completed:** 2026-08-28T06:23:15Z
- **Tasks:** 3/3
- **Files modified:** 7

## Accomplishments

- 登录、注册和找回密码入口改用 `Input`、`Label`、`Button`、`Alert` 组合；保持 RHF/Zod payload、非枚举错误映射与既有安全导航。
- 注册验证步骤保留服务器冷却、重发使旧码失效、终态恢复与验证码输入归一化；增加“步骤 2/2”与 `one-time-code` 语义。
- 重置密码页继续仅经过公开 API 和 HttpOnly recovery context 完成验证/重置；不向 URL、storage 或 React 持久状态写入验证码、context 或 token。

## Task Commits

1. **Task 1: 迁移登录、注册和找回密码入口表单**
   - `8555973` — `test(01.1-06): lock entry auth form accessibility`
   - `6d3879c` — `feat(01.1-06): unify entry authentication forms`
2. **Task 2: 迁移注册验证码步骤并保留冷却/重发状态机**
   - `c17319e` — `test(01.1-06): cover verification step presentation`
   - `a614ef5` — `feat(01.1-06): unify registration verification step`
3. **Task 3: 迁移重置密码步骤并保留恢复安全边界**
   - `01f8d8b` — `test(01.1-06): cover password reset step semantics`
   - `865a889` — `feat(01.1-06): unify password reset step`

## Verification

- `cd frontend && npm test -- src/auth/AuthForms.test.tsx` — 8 passed。
- `cd frontend && npm test -- src/auth/AuthForms.test.tsx -t "register|verification|resend|cooldown"` — 4 passed。
- `cd frontend && npm run lint` — passed。
- `cd frontend && npm run typecheck` — passed。
- 真实浏览器与跨栈认证验收由 Plan 08 在路由接线完成后统一执行；当前计划未启动独立浏览器路径，避免在尚未接入 `PublicAuthLayout` 的中间状态重复验收。

## Decisions Made

- `AuthEntryPage` 增加可选 `progress`，确保“步骤 1/2”“步骤 2/2”位于标题前，且不引入视觉进度条。
- 所有认证字段继续使用可见 Label、至少 44px 的输入/操作尺寸、适当的 autocomplete 与 `aria-invalid`/`aria-describedby`，从而兼顾触控和字段级错误定位。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking integration] 将步骤进度提升到共享 AuthEntryPage。**
- **Found during:** Task 2（注册验证码步骤迁移）
- **Issue:** 计划要求步骤文字位于标题前；仅在各表单内部添加文字会落在标题之后，违反 UI-SPEC 的标题层级。
- **Fix:** 在既有 `PublicPages.tsx` 的 `AuthEntryPage` 增加可选 `progress`，由注册、找回、验证和重置页传入。
- **Files modified:** `frontend/src/auth/PublicPages.tsx`、`RegisterPage.tsx`、`ForgotPasswordPage.tsx`、`RegisterVerifyPage.tsx`。
- **Verification:** 认证表单测试 8 passed、lint 与 typecheck 通过。
- **Committed in:** `a614ef5`。

**Total deviations:** 1 auto-fixed（Rule 3: 1）。**Impact:** 仅修正共享展示层以满足已冻结的标题/进度合同；没有新增 API、依赖或敏感状态。

## Known Stubs

None. 本计划只迁移已交付的认证协议，不伪造后端能力。

## Issues Encountered

- 全局 `git diff --check` 会报告用户已有的 `.gitignore` 末尾空行；所有本计划文件的定向检查通过，`.gitignore` 未被暂存或提交。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 07 可以把公开认证路由实际嵌入 `PublicAuthLayout`，并用统一 guard 处理深链和 returnTo。
- Plan 08 应在真实 FastAPI/PostgreSQL/Mailpit 环境中完成注册、验证、登录与会话的浏览器验收和截图基线。

## Self-Check: PASSED

- `frontend/src/auth/LoginPage.tsx`、`RegisterPage.tsx`、`RegisterVerifyPage.tsx`、`ForgotPasswordPage.tsx`、`ResetPasswordPage.tsx`、`AuthForms.test.tsx` 与本 SUMMARY 均存在。
- `8555973`、`6d3879c`、`c17319e`、`a614ef5`、`01f8d8b`、`865a889` 均可在 Git 历史中找到。
- 本计划没有删除已跟踪文件，也没有提交用户的 `.gitignore` 修改。

---
*Phase: 01.1-h5-ui*
*Completed: 2026-08-28*
