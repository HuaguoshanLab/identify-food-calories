---
phase: 01.1-h5-ui
plan: "03"
type: execute
wave: 3
depends_on: ["01.1-02"]
files_modified:
  - frontend/src/app/README.md
  - frontend/src/app/PlaceholderTabPage.tsx
  - frontend/src/app/SettingsLinkRow.tsx
  - frontend/src/app/MePage.tsx
  - frontend/src/app/AccountDetailsPage.tsx
  - frontend/src/app/SessionsDetailsPage.tsx
  - frontend/src/app/AppPages.test.tsx
  - frontend/src/README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "[D-02] 分析、记录、计划页面严格只显示对应标题和“功能即将开放”。"
    - "[D-12] 我的根页只有账号资料和登录会话两个完整语义链接。"
    - "[D-13] 账号详情只读显示邮箱、账号状态和中文角色，不提供编辑或假保存。"
    - "[D-15] 账号和会话内容能被 DetailLayout 承载，不自行渲染底部 Tab。"
  artifacts:
    - path: frontend/src/app/PlaceholderTabPage.tsx
      provides: "三项诚实占位页"
    - path: frontend/src/app/MePage.tsx
      provides: "我的设置入口根页"
    - path: frontend/src/app/AccountDetailsPage.tsx
      provides: "数据库权威身份的只读映射"
    - path: frontend/src/app/SessionsDetailsPage.tsx
      provides: "会话详情内容组合入口"
  key_links:
    - from: frontend/src/app/AccountDetailsPage.tsx
      to: frontend/src/auth/useAuth.ts
      via: "AuthProvider current user"
      pattern: "useAuth"
    - from: frontend/src/app/SessionsDetailsPage.tsx
      to: frontend/src/auth/SessionList.tsx
      via: "existing TanStack Query owner"
      pattern: "SessionList"
---

<objective>
建立四个 Tab 所需的真实页面内容与“我的”两级信息结构。

Purpose: 在不实现未来业务或修改认证协议的前提下，提供可接线路由的页面组件。
Output: `app/` 目录合同、占位页、我的根页、账号/会话详情内容及测试。
</objective>

<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-h5-ui/01.1-CONTEXT.md
@.planning/phases/01.1-h5-ui/01.1-UI-SPEC.md
@frontend/src/auth/RouteGuards.tsx
@frontend/src/auth/SessionList.tsx
@frontend/src/auth/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建三个严格诚实的占位 Tab 页面</name>
  <files>frontend/src/app/README.md, frontend/src/app/PlaceholderTabPage.tsx, frontend/src/app/AppPages.test.tsx, frontend/src/README.md</files>
  <read_first>
    <file>docs/ui/h5-foundation.md</file>
    <file>.planning/phases/01.1-h5-ui/01.1-UI-SPEC.md</file>
    <file>frontend/src/auth/PublicPages.tsx</file>
    <file>frontend/src/README.md</file>
  </read_first>
  <behavior>
    - "title=分析/记录/计划 时分别输出唯一 h1 和同一句‘功能即将开放’。"
    - "DOM 中没有 button、form、示例数据、Skeleton 或上传控件。"
    - "页面进入后 h1 可被聚焦。"
  </behavior>
  <action>创建 `app/README.md` 并同次更新 `src/README.md` 索引。实现可复用但受联合类型限制的 PlaceholderTabPage，只接受三个锁定标题，输出聚焦 h1 与状态说明；不得添加 CTA、卡片、mock 数据、进度或未来功能描述。测试可见文本、唯一标题、聚焦与交互控件缺失。</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- src/app/AppPages.test.tsx</automated>
  </verify>
  <acceptance_criteria>三个未开放路由的内容严格满足 D-02，无法被误解为已可用功能。</acceptance_criteria>
  <done>诚实占位页可供路由直接复用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现我的设置列表与完整语义入口</name>
  <files>frontend/src/app/SettingsLinkRow.tsx, frontend/src/app/MePage.tsx, frontend/src/app/AppPages.test.tsx, frontend/src/app/README.md</files>
  <read_first>
    <file>frontend/src/routePaths.ts</file>
    <file>.planning/phases/01.1-h5-ui/01.1-UI-SPEC.md</file>
    <file>frontend/src/auth/PublicPages.tsx</file>
  </read_first>
  <behavior>
    - "我的页面只有账号资料、登录会话两个入口。"
    - "每行整体是 Link，含锁定 Lucide 图标、标题、辅助文案与 ChevronRight。"
    - "根页不重复显示邮箱、角色、退出按钮或未来偏好入口。"
  </behavior>
  <action>实现 SettingsLinkRow 为一个完整可点击的真实 Router Link，不用 onClick div；命中区至少 56px、语义 token、弱分割、可换行说明。MePage 渲染唯一 h1、短说明和两行入口：CircleUserRound→账号资料、MonitorSmartphone→登录会话，目的地来自 routePaths。测试链接名称/地址、没有额外设置项和键盘可达性。</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- src/app/AppPages.test.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>我的根页只承担导航职责，两个入口语义完整且没有伪功能。</acceptance_criteria>
  <done>我的信息架构根页完成。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 拆出只读账号资料与会话详情内容</name>
  <files>frontend/src/app/AccountDetailsPage.tsx, frontend/src/app/SessionsDetailsPage.tsx, frontend/src/app/AppPages.test.tsx, frontend/src/app/README.md</files>
  <read_first>
    <file>frontend/src/auth/RouteGuards.tsx</file>
    <file>frontend/src/auth/api.ts</file>
    <file>frontend/src/auth/useAuth.ts</file>
    <file>frontend/src/auth/SessionList.tsx</file>
  </read_first>
  <behavior>
    - "账号页从 useAuth().user 显示 email、is_active→正常、user/admin→普通用户/管理员。"
    - "账号页没有编辑、保存、头像或修改邮箱控件。"
    - "会话页复用 SessionList，不创建第二次 fetch 或本地会话缓存。"
  </behavior>
  <action>从 RouteGuards 的组合页面抽取账号 `dl` 到 AccountDetailsPage，保持身份真相来自 AuthProvider，经中文 UI 映射输出且邮箱可断行；若用户缺失则显示已有身份恢复错误边界，不发新 API。SessionsDetailsPage 只组合现有 SessionList，不镜像 Query 数据。两者不自行套 AppShell/BottomNavigation，交由路由层 DetailLayout。更新目录索引与测试。</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- src/app/AppPages.test.tsx &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>账号信息只读且来自权威认证状态；会话服务端状态仍只有 SessionList 一个所有者。</acceptance_criteria>
  <done>两个详情路由已有独立、可测试内容组件。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| AuthProvider user → account DOM | 服务端权威身份进入只读页面。 |
| Future feature labels → user expectation | 未实现业务名称可能造成能力欺骗。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01.1-06 | Spoofing | PlaceholderTabPage | mitigate | 锁定标题 + “功能即将开放”，测试禁止按钮、表单、假数据和 Skeleton。 |
| T-01.1-07 | Tampering | AccountDetailsPage | mitigate | 只消费 AuthProvider `/users/me` 结果，不提供本地编辑或保存路径。 |
| T-01.1-08 | Information Disclosure | MePage | mitigate | 根页不重复暴露邮箱/角色；敏感详情只在受保护详情路由接线。 |
| T-01.1-SC | Tampering | dependencies | accept | 无包安装，复用既有 Lucide/Router/Query。 |
</threat_model>

<verification>
`cd frontend &amp;&amp; npm test -- src/app/AppPages.test.tsx &amp;&amp; npm run lint &amp;&amp; npm run typecheck`
</verification>

<success_criteria>
- 三个未开放页只有标题与状态说明。
- 我的根页只有两个真实链接。
- 账号只读映射和会话 Query 所有权保持正确。
</success_criteria>

<output>
Create `.planning/phases/01.1-h5-ui/01.1-03-SUMMARY.md` when done.
</output>
