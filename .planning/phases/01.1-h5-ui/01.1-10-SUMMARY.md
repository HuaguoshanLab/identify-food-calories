---
phase: 01.1-h5-ui
plan: "10"
subsystem: auth-accessibility-regression
tags: [react, react-hook-form, vitest, fastapi, playwright, browser-verification, accessibility]

requires:
  - phase: 01.1-09
    provides: approved H5 baseline and final verification report with three focused gaps
provides:
  - registration verification context recovery for both submit and resend actions
  - classified password-recovery retry and terminal-recovery behavior
  - deterministic h1 focus after my and detail-route transitions
affects: [phase-01.1-verification, auth-ui, protected-routes, h5-accessibility]

tech-stack:
  added: []
  patterns: [auth-error-classification, retry-current-operation-only, route-entry-heading-focus]

key-files:
  created:
    - .planning/phases/01.1-h5-ui/01.1-10-SUMMARY.md
  modified:
    - frontend/src/auth/RegisterVerifyPage.tsx
    - frontend/src/auth/ResetPasswordPage.tsx
    - frontend/src/auth/AuthForms.test.tsx
    - frontend/src/app/MePage.tsx
    - frontend/src/layouts/AppHeader.tsx
    - frontend/src/App.test.tsx
    - frontend/src/app/AppPages.test.tsx

key-decisions:
  - "只有真正终态的 recovery context、验证码过期和尝试耗尽进入重新申请；网络、未知和 CSRF 错误保留原输入并仅重试刚失败的操作。"
  - "我的和详情页复用占位 Tab 的 h1 ref/effect/tabIndex 模式；详情标题以 title 为 effect 依赖，覆盖共享 Header 的路由切换。"

patterns-established:
  - "认证错误测试同时覆盖 API 阶段、字段保留、可见 Alert、精确重试调用与真实恢复路由。"
  - "页面路由测试应以 level=1 heading 的 document focus 断言验证键盘与读屏入口。"

requirements-completed: []
duration: 10 min
completed: 2026-08-28
---

# Phase 01.1 Plan 10: 验证缺口定向回修 Summary

**注册验证码、密码重置与受保护路由现在具有可恢复的错误边界和可验证的页面标题焦点。**

## Performance

- **Duration:** 10 min
- **Started:** 2026-08-28T08:04:09Z
- **Completed:** 2026-08-28T08:14:00Z
- **Tasks:** 3/3
- **Files modified:** 7

## Accomplishments

- `verifyRegistration` 与 `resendRegistrationCode` 都会将 `VERIFICATION_CONTEXT_INVALID` 收敛至同一个“返回注册”恢复界面；网络错误仍留在表单中可重试。
- 密码重置严格区分字段错误、三类终态和可恢复服务错误；验证阶段与重置阶段的重试分别只调用当次失败的公开 API。
- “分析 → 我的 → 账号资料/登录会话”及详情深链均把焦点移到唯一的 `h1`，避免键盘与读屏用户停留在旧导航入口。

## Task Commits

1. **Task 1: 用单一失效状态修复注册验证码提交与重发恢复** — `03e6505` (test), `d1e6a11` (fix)
2. **Task 2: 按真实恢复协议分类密码重置失败并保留重试输入** — `3c3cf3e` (test), `fdf9be2` (fix)
3. **Task 3: 为“我的”与详情路由补齐标题焦点和真实浏览器复验** — `12c58e6` (test), `99f1b5d` (fix)

## Verification

- `cd frontend && npm test` — 10 files / 85 tests passed.
- `cd frontend && npm run lint && npm run typecheck && npm run build` — all passed.
- `cd frontend && npm run test:e2e -- tests/e2e/auth-skeleton.spec.ts` — 1 real FastAPI/PostgreSQL/Mailpit browser flow passed.

## Browser Verification

- 通过 Codex 内置浏览器访问 `http://127.0.0.1:5173/register/verify`：无验证 context 时，真实公开 API 返回的页面显示“验证信息已失效，请重新开始。”及 `/register` 的“返回注册”链接。
- 访问 `http://127.0.0.1:5173/reset-password`：公开页面保留验证码、新密码、确认新密码、更新按钮和 `/forgot-password` 的“重新申请重置”入口；控制台无 error/warn。
- 未在浏览器提交测试账号、密码或验证码，因此没有在浏览器中重复已认证的焦点路径；该路径由路由级 Vitest 和真实全栈 Playwright 共同覆盖。FastAPI 与 Vite 临时进程已停止，Docker 基础服务未停止。

## Decisions Made

- 用明确的终态错误表维护恢复协议边界，避免把暂时故障伪装为 context 失效。
- 保持 HttpOnly recovery context 在服务端；React 只保留当前视图是否已通过验证码验证，绝不读取或持久化 context。

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- 首次 E2E 在受限环境中无法连接本机 Docker；取得本机 Docker 访问授权后，隔离测试库迁移、FastAPI、Vite 与 Playwright 全部正常通过。这是执行环境权限，不是产品缺陷。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

三条最终验证缺口均已有行为测试、生产代码、真实 E2E 和公开页面浏览器证据。可重新运行阶段 verifier；通过后再进入 Phase 2。

## Self-Check: PASSED

- 六个 `01.1-10` 的 RED/GREEN task commits 均存在，且每个实现提交之后的目标测试通过。
- 本 Summary、七个目标源/测试文件、完整前端门禁和真实 E2E 均已核对。
- 本计划没有更改 UI-SPEC、视觉基线、后端、依赖或用户持有的 `.gitignore`。
