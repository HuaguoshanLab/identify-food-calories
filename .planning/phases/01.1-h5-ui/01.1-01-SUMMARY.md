---
phase: 01.1-h5-ui
plan: "01"
subsystem: ui
tags: [react, tailwind, shadcn, routing, security, vitest]

requires:
  - phase: 01-engineering-auth-foundation
    provides: React H5、认证路由与既有 shadcn 原语
provides:
  - 无冲突的四 Tab 诚实占位页合同
  - 可消费的浅色与 dark 语义主题 token
  - 精确受保护 H5 路径白名单和 returnTo 安全回归
affects: [01.1-02, 01.1-03, 01.1-07, frontend-routing]

tech-stack:
  added: []
  patterns: [语义 token, exact route allow-list, returnTo TDD]

key-files:
  created: [frontend/src/routePaths.ts]
  modified:
    - docs/ui/h5-foundation.md
    - frontend/src/styles.css
    - frontend/src/auth/returnTo.ts
    - frontend/src/auth/ProtectedRoutes.test.tsx
    - frontend/src/README.md

key-decisions:
  - "Phase 01.1 允许四 Tab 与三个诚实占位页，但严格禁止假数据、假按钮、Skeleton 和可用性暗示。"
  - "登录 returnTo 使用 React-free 精确路径集合，而不是 /app 前缀匹配。"
  - "主题只建立 shadcn 语义 token 和 .dark 预留，不增加主题切换功能。"

patterns-established:
  - "路由声明与认证重定向从 routePaths.ts 共享精确路径合同。"
  - "业务组件消费语义 utility；主题变量集中在 styles.css。"

requirements-completed: []
duration: 5 min
completed: 2026-08-28
---

# Phase 01.1 Plan 01: H5 合同、主题与受保护路径 Summary

**同步四 Tab 诚实占位页合同，补齐 shadcn 浅色/dark 语义 token，并用 TDD 收紧登录 returnTo 到七个精确 H5 路径。**

## Performance

- **Duration:** 5 min
- **Started:** 2026-08-28T05:33:51Z
- **Completed:** 2026-08-28T05:38:17Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- 解除跨阶段文档冲突：Phase 01.1 可展示四个 Tab，未开放页只能诚实说明“功能即将开放”。
- 主题层补齐 popover 等官方原语所需 token，并保留未启用的 `.dark` 映射。
- 登录回跳只接受 `/app`、四个 Tab 和两个“我的”详情路径；外域、hash、反斜杠和相似前缀全部回退。

## Task Commits

1. **Task 1: 同步诚实占位页的跨阶段文档合同** - `0fd1de1` (docs)
2. **Task 2: 完整映射 shadcn 语义主题并预留深色 token** - `a5b32b7` (style)
3. **Task 3 RED: 扩充精确路径攻击矩阵** - `16e91b7` (test)
4. **Task 3 GREEN: 抽取精确路径表并收紧 returnTo** - `a35a576` (feat)

## Files Created/Modified

- `docs/ui/h5-foundation.md` - 统一四 Tab 占位页的范围和禁止伪功能边界。
- `frontend/src/styles.css` - shadcn 默认浅色 token、完整 popover 映射与 `.dark` 预留。
- `frontend/src/routePaths.ts` - 不依赖 React 的用户 H5 精确路径集合。
- `frontend/src/auth/returnTo.ts` - 复用精确集合解析受信登录回跳。
- `frontend/src/auth/ProtectedRoutes.test.tsx` - 覆盖合法深链和相似/外部攻击输入。
- `frontend/src/README.md` - 更新 `src/` 文件索引。

## Decisions Made

- 认证重定向不从 Router 组件导入路径，也不接受 `/app` 前缀匹配，避免未来 admin 或未知子路径被意外开放。
- 深色模式当前仅是 CSS token 能力，不提供 Provider、偏好监听、localStorage 或切换按钮。

## TDD Gate Compliance

- RED `16e91b7`：新深链测试在旧实现下失败 7 项，失败原因是 allow-list 仅含 `/app`。
- GREEN `a35a576`：新增精确路径集合后，`ProtectedRoutes.test.tsx` 21 项测试全部通过。

## Verification

- `npm test -- src/auth/ProtectedRoutes.test.tsx` — 21 passed。
- `npm run lint`、`npm run typecheck`、`npm run build` — 全部通过。
- 文档 grep 门禁确认保留“功能即将开放/假数据/假按钮/四个 Tab”，移除了冲突表述。

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Known Stubs

None - 本计划只建立合同、主题和受保护路径基础，不新增用户可见业务占位实现。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

`01.1-02` 可以复用 `routePaths.ts` 建立四 Tab 路由和页面壳，同时直接消费完整语义主题 token。

---
*Phase: 01.1-h5-ui*
*Completed: 2026-08-28*
