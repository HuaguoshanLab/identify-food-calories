---
phase: 01.1-h5-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ui/h5-foundation.md
  - frontend/src/styles.css
  - frontend/src/routePaths.ts
  - frontend/src/auth/returnTo.ts
  - frontend/src/auth/ProtectedRoutes.test.tsx
  - frontend/src/README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "[D-06] 跨阶段 H5 规范先于实现承认三个未开放 Tab 的诚实占位页，并继续禁止假数据、假按钮和伪功能。"
    - "[D-03] 所有受保护 H5 路径来自同一精确路径表，登录返回地址不能放宽为 /app 前缀匹配。"
    - "新 H5 样式只使用完整 shadcn 语义 token；.dark 只预留 token，不出现主题开关。"
  artifacts:
    - path: docs/ui/h5-foundation.md
      provides: "与 D-02/D-06 一致的跨阶段占位页规则"
      contains: "功能即将开放"
    - path: frontend/src/routePaths.ts
      provides: "React-free 的 /app 路径常量和受保护路径集合"
    - path: frontend/src/styles.css
      provides: "浅色语义 token、完整 popover 映射和 .dark 预留"
  key_links:
    - from: frontend/src/auth/returnTo.ts
      to: frontend/src/routePaths.ts
      via: "exact allow-list import"
      pattern: "protectedRoutePaths"
---

<objective>
同步 H5 权威文档并建立主题、路径与安全返回地址的共享基础。

Purpose: 后续页面不能在旧文档、缺失 token 或分散路径字符串上继续实现。
Output: 已同步的 H5 合同、完整语义主题、单一受保护路径表及攻击回归测试。
</objective>

<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-h5-ui/01.1-CONTEXT.md
@.planning/phases/01.1-h5-ui/01.1-UI-SPEC.md
@.planning/phases/01.1-h5-ui/01.1-RESEARCH.md
@docs/ui/h5-foundation.md
@frontend/src/styles.css
@frontend/src/auth/returnTo.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 先同步诚实占位页的跨阶段文档合同</name>
  <files>docs/ui/h5-foundation.md</files>
  <read_first>
    <file>docs/ui/h5-foundation.md</file>
    <file>.planning/phases/01.1-h5-ui/01.1-CONTEXT.md</file>
    <file>.planning/phases/01.1-h5-ui/01.1-UI-SPEC.md</file>
  </read_first>
  <action>按 D-02/D-06 定向修订第 4 节和实施顺序：Phase 01.1 允许立即展示四个 Tab，但分析、记录、计划只能各含页面标题与“功能即将开放”；删除“对应阶段才允许实现路由/导航”和“不要为未来 Tab 做页面”的冲突表述。保留禁止假数据、CTA、Skeleton、样例结果和可用性暗示的边界。不要改写未来业务职责。</action>
  <verify>
    <automated>rg -n "功能即将开放|假数据|假按钮|四个 Tab" docs/ui/h5-foundation.md &amp;&amp; ! rg -n "不是.*空壳 Tab.*授权|路由与导航只在对应阶段" docs/ui/h5-foundation.md</automated>
  </verify>
  <acceptance_criteria>文档明确覆盖 D-06，且不再与 D-02 冲突。</acceptance_criteria>
  <done>后续代码可引用同一份无冲突的占位页合同。</done>
</task>

<task type="auto">
  <name>Task 2: 完整映射 shadcn 语义主题并预留深色 token</name>
  <files>frontend/src/styles.css</files>
  <read_first>
    <file>frontend/src/styles.css</file>
    <file>frontend/src/components/ui/alert-dialog.tsx</file>
    <file>.planning/phases/01.1-h5-ui/01.1-UI-SPEC.md</file>
  </read_first>
  <action>补齐 `@custom-variant dark`、`@theme inline`、`:root` 与 `.dark` 中组件实际使用的 background/foreground、card、popover、primary、secondary、muted、accent、destructive、border、input、ring 配对。保持系统字体、320px 最小宽度、focus-visible 与 reduced-motion 基线。不要增加主题 Provider、localStorage、系统偏好监听、切换按钮或品牌 Hex；业务类将只消费语义 utility。</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>`bg-popover` 等现有官方原语 utility 有完整 token 来源，浅色构建通过，`.dark` 仅为可消费预留。</acceptance_criteria>
  <done>主题层不再依赖缺失变量或散落业务颜色。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 抽取精确路径表并收紧 returnTo 回归</name>
  <files>frontend/src/routePaths.ts, frontend/src/auth/returnTo.ts, frontend/src/auth/ProtectedRoutes.test.tsx, frontend/src/README.md</files>
  <read_first>
    <file>frontend/src/auth/returnTo.ts</file>
    <file>frontend/src/auth/ProtectedRoutes.test.tsx</file>
    <file>frontend/src/README.md</file>
    <file>.planning/phases/01.1-h5-ui/01.1-PATTERNS.md</file>
  </read_first>
  <behavior>
    - "精确接受 /app、四个 Tab 和两个我的详情路径。"
    - "拒绝外域、协议相对 URL、反斜杠、hash、/app/admin、/application 与未登记子路径。"
    - "合法 query 可保留；非法输入回退到 /app。"
  </behavior>
  <action>先扩充 ProtectedRoutes 测试形成 RED，再创建不导入 React/Lucide 的 `routePaths.ts`，导出命名路径常量和 exact `protectedRoutePaths`。让 `returnTo.ts` 导入该集合，保留同源解析、hash/反斜杠/协议相对拒绝和合法 query 行为，禁止 `startsWith('/app')`。同步 `src/README.md` 文件索引。</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- src/auth/ProtectedRoutes.test.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>所有登记深链可安全返回，所有 lookalike/外部目标被拒绝，路径字符串有单一来源。</acceptance_criteria>
  <done>路由接线可复用精确路径合同而不扩大开放跳转面。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| URL/query → SPA navigation | 未可信 returnTo 输入进入登录后导航。 |
| CSS token → official primitives | 缺失语义变量会使弹窗/焦点状态不可见。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01.1-01 | Spoofing / Elevation | `parseReturnTo` | mitigate (HIGH) | 精确路径集合、同源解析、拒绝 hash/反斜杠/前缀 lookalike，并以攻击矩阵测试阻断开放跳转及未来 admin 表面。 |
| T-01.1-02 | Tampering | UI contract | mitigate | 先同步 D-06，grep 门禁防止执行器依据冲突旧文档实现伪功能。 |
| T-01.1-SC | Tampering | npm supply chain | accept | 本计划不安装或升级任何包；只使用已锁定依赖。 |
</threat_model>

<verification>
`cd frontend &amp;&amp; npm test -- src/auth/ProtectedRoutes.test.tsx &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run build`
</verification>

<success_criteria>
- H5 文档与 D-02/D-06 无冲突。
- 精确路径集合覆盖七个受保护地址且拒绝前缀扩大。
- 主题 token 支撑浅色实现与 `.dark` 预留，不增加主题功能。
</success_criteria>

<output>
Create `.planning/phases/01.1-h5-ui/01.1-01-SUMMARY.md` when done.
</output>
