---
phase: 01.1-h5-ui
plan: "05"
type: execute
wave: 3
depends_on: ["01.1-02"]
files_modified:
  - frontend/src/auth/PublicPages.tsx
  - frontend/src/auth/PublicRoutes.test.tsx
  - frontend/src/auth/README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "[D-07] 首页、隐私和条款成为 PublicAuthLayout 内的内容页面，不再各自拥有 viewport/滚动壳。"
    - "[D-08] 首页只有一个明确主操作：创建账号为 primary，登录为次级。"
    - "[D-09] 公开入口页呈现品牌入口，并保留确定的法律页返回首页链接。"
  artifacts:
    - path: frontend/src/auth/PublicPages.tsx
      provides: "首页、隐私和条款的内容组件"
    - path: frontend/src/auth/PublicRoutes.test.tsx
      provides: "公开页层级、语义与禁止伪功能证据"
  key_links:
    - from: frontend/src/auth/PublicPages.tsx
      to: frontend/src/components/ui/button.tsx
      via: "primary/outline actions"
      pattern: "Button"
---

<objective>
把 landing 与法律页面迁移成统一 H5 公开页内容，同时保持用户确认的 CTA 层级。

Purpose: 公开入口应与认证流程共享设备容器、语义 token 和滚动规则，而不伪造餐食功能。
Output: 迁移后的 PublicPages、结构测试和认证目录索引。
</objective>

<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-h5-ui/01.1-UI-SPEC.md
@frontend/src/auth/PublicPages.tsx
@frontend/src/auth/PublicRoutes.test.tsx
@frontend/src/layouts/PublicAuthLayout.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 迁移首页为单一主操作的公开 H5 内容</name>
  <files>frontend/src/auth/PublicPages.tsx, frontend/src/auth/PublicRoutes.test.tsx</files>
  <read_first>
    <file>frontend/src/auth/PublicPages.tsx</file>
    <file>frontend/src/auth/PublicRoutes.test.tsx</file>
    <file>frontend/src/layouts/PublicAuthLayout.tsx</file>
    <file>.planning/phases/01.1-h5-ui/01.1-UI-SPEC.md</file>
  </read_first>
  <behavior>
    - "层级固定为品牌由 layout 提供、h1、参考说明、创建账号主按钮、登录次级按钮、隐私/条款链接。"
    - "没有上传、示例结果、Agent 按钮或未来功能假入口。"
    - "h1 路由进入后可聚焦，链接语义和地址正确。"
  </behavior>
  <action>先改测试锁定文案/链接/交互缺失，再移除 PublicPages 自有 `min-h-dvh` wrapper，让 LandingPage 成为 PublicAuthLayout 的内容。使用现有 shadcn Button 的 primary 与 outline/secondary 视觉层级，关键动作高至少 44px；所有颜色改语义 token。保持结果仅作普通饮食参考的说明，不增加最多三个能力营销块之外的内容，当前阶段直接不新增营销块。</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- src/auth/PublicRoutes.test.tsx</automated>
  </verify>
  <acceptance_criteria>首页与用户确认截图保持主次结构，同时进入统一 H5 内容边界。</acceptance_criteria>
  <done>Landing 不再控制 viewport，且只有创建账号一个 primary CTA。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 迁移隐私与条款并同步认证目录职责</name>
  <files>frontend/src/auth/PublicPages.tsx, frontend/src/auth/PublicRoutes.test.tsx, frontend/src/auth/README.md</files>
  <read_first>
    <file>frontend/src/auth/PublicPages.tsx</file>
    <file>frontend/src/auth/README.md</file>
    <file>.planning/phases/01.1-h5-ui/01.1-CONTEXT.md</file>
  </read_first>
  <behavior>
    - "隐私/条款各有唯一 h1、现有核心说明和返回首页的真实链接。"
    - "法律页不显示底部导航、上传控件或后台入口。"
    - "长内容由父 PublicAuthLayout 的 PageScrollArea 滚动。"
  </behavior>
  <action>将 PrivacyPage/TermsPage 变为内容组件，保留现有业务语义与非医疗边界，用短段落和语义 token 排版，不重复 MobileFrame/main。扩展测试覆盖标题、返回链接、无 Tab/无 admin。更新 auth README 中 PublicPages 的新职责；不要声称后续 Agent 页面已经完成。</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- src/auth/PublicRoutes.test.tsx &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>两个法律页可在统一公开壳中滚动并稳定返回首页，目录文档与代码一致。</acceptance_criteria>
  <done>全部非表单公开页完成 H5 迁移。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public route → user expectation | 未登录用户依据 landing 文案判断产品能力与风险。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01.1-12 | Spoofing | LandingPage | mitigate | 禁止上传/结果/未来 CTA，测试只允许已交付认证入口，避免伪装 Agent 已可用。 |
| T-01.1-13 | Information Disclosure | public/legal pages | mitigate | 不渲染账号、会话、验证码、token 或后台入口；只保留公开说明。 |
| T-01.1-14 | Repudiation | health disclaimer | mitigate | 保留“普通饮食参考”可见说明，不宣称医疗/精准效果。 |
| T-01.1-SC | Tampering | dependencies | accept | 无依赖安装或 registry 使用。 |
</threat_model>

<verification>
`cd frontend &amp;&amp; npm test -- src/auth/PublicRoutes.test.tsx &amp;&amp; npm run lint &amp;&amp; npm run typecheck`
</verification>

<success_criteria>
- landing CTA 层级与 D-08 一致。
- landing/legal 不再拥有自己的 viewport 或硬编码色。
- 公开页面没有未来业务、后台或敏感表面。
</success_criteria>

<output>
Create `.planning/phases/01.1-h5-ui/01.1-05-SUMMARY.md` when done.
</output>
