---
phase: 01.1-h5-ui
plan: "08"
type: execute
wave: 5
depends_on: ["01.1-07"]
files_modified:
  - frontend/tests/e2e/auth-helpers.ts
  - frontend/tests/e2e/auth-skeleton.spec.ts
  - frontend/tests/e2e/h5-visual.spec.ts
  - frontend/tests/e2e/h5-visual.spec.ts-snapshots/README.md
  - frontend/tests/e2e/h5-visual.spec.ts-snapshots/
  - frontend/tests/e2e/README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "[D-16] 430×932 保存视觉基线；320px 和桌面尺寸只做 overflow、遮挡、设备容器与排布检查。"
    - "[D-17] Git 基线覆盖首页、登录、注册、分析占位、我的、账号资料、登录会话和撤销弹窗。"
    - "[D-18] 受保护截图与内置浏览器验收通过真实 FastAPI/PostgreSQL/Mailpit 公开链准备，不写 DB、不伪 token、不使用 MSW。"
    - "[D-19] 默认 E2E 命令不含快照更新；基线进入 Git，后续差异必须进入 Plan 09 人工审查。"
  artifacts:
    - path: frontend/tests/e2e/h5-visual.spec.ts
      provides: "真实数据视觉、响应式、历史和键盘测试"
    - path: frontend/tests/e2e/auth-helpers.ts
      provides: "公开注册/Mailpit/登录/第二设备复用链"
    - path: frontend/tests/e2e/h5-visual.spec.ts-snapshots/
      provides: "八张 430px Git 基线和目录合同"
  key_links:
    - from: frontend/tests/e2e/h5-visual.spec.ts
      to: http://127.0.0.1:8025
      via: "Mailpit public HTTP read after UI registration"
      pattern: "readCode"
    - from: frontend/tests/e2e/h5-visual.spec.ts
      to: http://127.0.0.1:5173/app/me/sessions
      via: "real browser authenticated flow"
      pattern: "me/sessions"
---

<objective>
用真实跨栈链建立行为回归、响应式断言、八张视觉基线和一次 Codex 内置浏览器验收。

Purpose: 证明新 H5 不只是组件测试通过，而是在真实 Cookie/API/Mailpit 生命周期中可用。
Output: 复用 E2E helper、更新认证 skeleton、视觉 spec、Git 基线和浏览器验收记录。
</objective>

<execution_context>
@/Users/mina/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mina/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-h5-ui/01.1-UI-SPEC.md
@.planning/phases/01.1-h5-ui/01.1-RESEARCH.md
@frontend/playwright.config.ts
@frontend/tests/e2e/auth-skeleton.spec.ts
@frontend/tests/e2e/README.md
@frontend/AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 复用真实认证 fixture 并迁移既有全栈行为路径</name>
  <files>frontend/tests/e2e/auth-helpers.ts, frontend/tests/e2e/auth-skeleton.spec.ts, frontend/tests/e2e/README.md</files>
  <read_first>
    <file>frontend/tests/e2e/auth-skeleton.spec.ts</file>
    <file>frontend/playwright.config.ts</file>
    <file>frontend/tests/e2e/README.md</file>
    <file>frontend/AGENTS.md</file>
  </read_first>
  <behavior>
    - "从前端注册→Mailpit 读真实验证码→验证→登录进入 /app/me。"
    - "第二 browser context 真实登录产生其他会话，再在 /app/me/sessions 撤销。"
    - "退出当前设备后回登录；后退仍不能看 protected 页面。"
    - "helper 接受调用方显式传入账号资料；视觉 spec 使用独立固定邮箱，既有 auth-skeleton 继续使用自己的隔离账号。"
  </behavior>
  <action>把 clearMailbox/readCode/register/verify/login 等只通过页面和公开 Mailpit HTTP 的 helper 抽到 `auth-helpers.ts`，账号、密码等 fixture 数据必须由调用方显式传入，helper 内不得使用 `Date.now()`、随机邮箱或共享可变账号。更新既有 auth-skeleton 的路径、标题和设置入口点击，使它继续验证注册、恢复、第二设备会话、撤销与退出。保留 serial、隔离 `food_agent_test`、两 webServer、reuseExistingServer=false 和可观察等待；不使用 waitForTimeout。同步 e2e README 索引。</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run test:e2e -- tests/e2e/auth-skeleton.spec.ts</automated>
  </verify>
  <acceptance_criteria>既有认证安全链在新路径上全绿，测试没有直接 DB、token、storage 或内部 Service 准备。</acceptance_criteria>
  <done>真实跨栈行为回归已迁移到新 H5 信息架构。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 430 基线与非截图响应式/键盘门禁</name>
  <files>frontend/tests/e2e/h5-visual.spec.ts, frontend/tests/e2e/h5-visual.spec.ts-snapshots/README.md, frontend/tests/e2e/h5-visual.spec.ts-snapshots/, frontend/tests/e2e/README.md</files>
  <read_first>
    <file>frontend/tests/e2e/auth-helpers.ts</file>
    <file>.planning/phases/01.1-h5-ui/01.1-UI-SPEC.md</file>
    <file>frontend/playwright.config.ts</file>
    <file>frontend/tests/e2e/README.md</file>
  </read_first>
  <behavior>
    - "430×932/light/reduced-motion 下八个显式命名截图与真实受保护状态匹配。"
    - "h5-visual 固定使用独立账号 `h5-visual@example.test`，依赖 Playwright 每次命令启动前清空 `food_agent_test`；不得使用 Date.now() 或随机邮箱。"
    - "只 mask `session-dates` 动态日期；固定邮箱、卡片、DOM、按钮和状态保持可见。"
    - "320px 无横滚、按钮不遮挡且会话操作纵向；768/1024/1440 居中 430px frame、单滚动。"
    - "skip link、Tab 顺序/Enter、Escape 关弹窗并恢复焦点、Tab push/back history 全部可观察。"
  </behavior>
  <action>新增 serial `h5-visual.spec.ts`，固定使用仅属于本 spec 的 `h5-visual@example.test` 和固定测试密码，复用真实 helper 创建账号和第二会话；其可重复性必须依赖 `playwright.config.ts` 的 FastAPI webServer 在每次 Playwright 命令前 DROP/CREATE 隔离 `food_agent_test` schema，不得回退到随机邮箱、直接写库或清理开发库。固定 viewport/colorScheme/reducedMotion，使用网络/可见状态等待而非 sleep。保存 landing-430、login-430、register-430、analyze-430、me-430、account-430、sessions-430、revoke-dialog-430 八个逻辑基线。为 snapshot 目录同次创建 README（职责、允许依赖、文件索引）。320 和桌面只断言 scrollWidth/clientWidth、frame bounding box、按钮几何和单滚动 owner。首次生成前先断言 snapshot 目录不存在任何 PNG；若已有任一 PNG，立即停止且不得删除或更新，交由 Plan 09 人工审查。只有全空时允许一次 `--update-snapshots=missing`，生成后必须不带任何 update 参数连续普通运行两次；不得把任何 snapshot update 参数写进 package.json、默认脚本或 CI。</action>
  <verify>
    <automated>cd frontend &amp;&amp; test "$(find tests/e2e/h5-visual.spec.ts-snapshots -type f -name '*.png' | wc -l | tr -d '[:space:]')" -eq 0 &amp;&amp; npm run test:e2e -- tests/e2e/h5-visual.spec.ts --update-snapshots=missing &amp;&amp; test "$(find tests/e2e/h5-visual.spec.ts-snapshots -type f -name '*.png' | wc -l | tr -d '[:space:]')" -eq 8 &amp;&amp; npm run test:e2e -- tests/e2e/h5-visual.spec.ts &amp;&amp; npm run test:e2e -- tests/e2e/h5-visual.spec.ts</automated>
  </verify>
  <acceptance_criteria>八张基线入库，默认普通测试稳定通过；小屏/桌面/键盘/历史不靠复制截图验证。</acceptance_criteria>
  <done>视觉和响应式合同具备持续回归证据，等待 Plan 09 人工审查。</done>
</task>

<task type="auto">
  <name>Task 3: 跑完整门禁并用 Codex 内置浏览器走真实用户路径</name>
  <files>frontend/tests/e2e/h5-visual.spec.ts</files>
  <read_first>
    <file>AGENTS.md</file>
    <file>frontend/AGENTS.md</file>
    <file>README.md</file>
    <file>backend/README.md</file>
    <file>frontend/README.md</file>
    <file>frontend/playwright.config.ts</file>
    <file>.planning/phases/01.1-h5-ui/01.1-UI-SPEC.md</file>
  </read_first>
  <action>先用只读端口检查确认 5173/8000 的监听归属；若存在未知进程则停止本任务并报告，绝不终止未知进程或把 reuseExistingServer 改为 true。运行前端完整门禁和两份 E2E，并确认 Playwright webServer 已按配置关闭。随后按 README 显式建立内置浏览器验收生命周期：记录 Docker 服务和端口的启动前状态；从仓库根目录运行 `docker compose up -d --wait postgres postgres-test mailpit`，在独立且可追踪的任务会话中分别运行后端 `.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000` 和前端 `npm run dev -- --host 127.0.0.1 --port 5173`。分别等待 `http://127.0.0.1:8000/api/v1/health`、`http://127.0.0.1:8025/` 和 `http://127.0.0.1:5173/` 成功响应后，才调用 `browser:control-in-app-browser` skill。用真实页面和公开 API 走 `/`→注册→Mailpit 取码→验证→登录→四 Tab→我的→账号资料→会话→第二设备撤销→退出，并额外观察一个相关错误/空态；不得直接写 DB、复制 token 或把截图当交互替代。结束时只停止本任务创建并记录 PID/session 的 FastAPI、Vite 进程；Docker 服务仅在启动前未运行且由本任务启动时停止，任何预先存在的服务保持不动。把启动命令、readiness URL、拥有的进程、清理结果、浏览器路径、关键观察、错误/空态和未覆盖项写入本计划 SUMMARY 的 browser verification 段。</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run test:e2e -- tests/e2e/auth-skeleton.spec.ts tests/e2e/h5-visual.spec.ts</automated>
    <human-check>执行器必须实际调用 Codex 内置浏览器并在 SUMMARY 记录路径；浏览器不可用时明确失败，不得声称完成。</human-check>
  </verify>
  <acceptance_criteria>自动化门禁全绿，内置浏览器通过真实公开路径验证成功和相关错误/空态，未覆盖项有明确记录。</acceptance_criteria>
  <done>Phase 01.1 有真实跨栈、视觉和内置浏览器三层证据。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| E2E browser → test FastAPI/PostgreSQL/Mailpit | 测试凭据和 Cookie 进入真实隔离基础设施。 |
| Dynamic production-like UI → Git screenshots | 页面可能把验证码、token 或不必要敏感值写入基线。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01.1-21 | Information Disclosure | visual baselines | mitigate (HIGH) | 使用固定测试账号；截图不含 Mailpit/验证码/token/cookie；只在业务页面截屏，人工审查八张基线。 |
| T-01.1-22 | Tampering | E2E environment | mitigate | 只使用隔离 `food_agent_test`、reuseExistingServer=false、固定端口和 readiness；禁止未知进程复用/直接 DB seed。 |
| T-01.1-23 | Repudiation | snapshot updates | mitigate | 默认命令绝不更新；显式初建后普通重跑，后续差异由 Plan 09 阻塞人工审查。 |
| T-01.1-SC | Tampering | Playwright/npm | accept | 不安装包，使用锁定 Playwright；`npx` 只解析本地已安装二进制，禁止 `--yes`/latest。 |
</threat_model>

<verification>
`cd frontend &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run test:e2e -- tests/e2e/auth-skeleton.spec.ts tests/e2e/h5-visual.spec.ts`
</verification>

<success_criteria>
- 真实认证/会话链在新 H5 路由通过。
- 八张 430px Git 基线与 320/desktop/keyboard/history 门禁就位。
- Codex 内置浏览器真实验收有明确记录。
- 默认命令不会覆盖快照。
</success_criteria>

<output>
Create `.planning/phases/01.1-h5-ui/01.1-08-SUMMARY.md` when done, including the browser verification path and observations.
</output>
