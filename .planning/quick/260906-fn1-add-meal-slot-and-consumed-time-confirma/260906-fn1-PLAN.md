---
mode: quick
status: complete
---
# 餐次与补录时间

## 已批准范围
分析确认保存选择早餐、午餐、晚餐、加餐及用餐时间；记录列表、详情、编辑贯通。历史记录为 null（未分类），不按保存时刻回填。不扩展餐次统计与规划关联。遵循 meal-slot.v1：仅初始按时间预选，用户修改时间不覆盖已选餐次。

## 任务
1. records schema/service/model + Alembic 0020：可空四值餐次；历史兼容；API 输入校验；dashboard history 投影。保持用户隔离、营养快照与保存幂等。
2. 前端公开 records API 增加餐次、时间与时区，RHF/Zod 表单接入分析及编辑；列表和详情显示餐次。失败保留输入，保存中禁用。
3. Service、API、PostgreSQL、组件和 E2E 验证补录、编辑、旧记录、非法值和重复保存。内置浏览器实际验收；中文教学与 README 索引同步。

## 计划检查
依赖和落点已核对 ARCHITECTURE、H5 合同与现有源码；不引入新依赖。旧 API 省略餐次保持兼容，修改时间时省略餐次保留原值。测试与实际浏览器证据完成后记录 SUMMARY/VERIFICATION 和 STATE。
