---
type: quick
task: 260905-jr2
status: planned
---

# 营养目录筛选表格与导入导出

用户要求按参考图二改为上方过滤条件、下方表格，支持新增、导入、导出。Phase 7 暂停，本任务独立执行。

用户已选择 CSV（Excel 可打开）；导入只新增草稿，UTF-8、500 条/1 MB，先校验后确认；导出最多 10000 条匹配结果。

## Task 1 — 列表及文件公开接口

- files: backend/app/admin/{api,schemas,service,ports,repository}.py 与所属 tests/README。
- action: 新增按名称/别名、来源、授权状态筛选的稳定分页列表及总数；复用持久草稿。文件导入先验证再确认，批量创建草稿并写审计，不直接发布或覆盖已有数据；导出按已应用筛选条件。所有接口 DB-RBAC，导入有大小/行数限制，重试幂等，不新增 schema。
- verify: Service fake-repository、HTTP 合约及 PostgreSQL 筛选分页集成测试。
- done: 列表、文件解析/校验/提交与导出使用真实数据库及服务事务。

## Task 2 — 表格页面与编辑入口

- files: admin-frontend/src/features/catalog/、src/App.tsx、相关 README。
- action: 桌面紧凑筛选栏（查询/重置）；表格工具栏（新增、导入、导出、模板、刷新），营养值明确为每100g；服务端分页、空态/失败状态、行编辑/审核发布。新增编辑复用已实现表单并按需打开，保留服务器预览与并发校验。导入显示校验结果、错误行号及确认；导出使用筛选条件而非当前页。
- verify: 组件/API 测试、类型检查、构建。
- done: 不再默认展示长表单；筛选与操作可用；保持键盘可达及独立后台架构。

## Task 3 — 实际路径验证与收尾

- files: admin-frontend/tests/e2e/、docs/learning/、本任务 SUMMARY、STATE。
- action: 补充真实公开 E2E；内置浏览器验证当前本地后台列表、筛选、新增入口、导入导出。同步中文使用/维护文档和索引，记录准确证据及残余限制。
- verify: 相关 E2E、真实浏览器与 diff check。
- done: 改造结果在当前 5179 环境可查看，任务与提交可追溯。
