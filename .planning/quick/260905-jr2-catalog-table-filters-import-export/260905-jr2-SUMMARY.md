---
task: 260905-jr2
status: complete
implementation: complete
browser_verification: completed_with_download_observability_limit
date: 2026-09-05
---

# 营养目录表格与 CSV 导入导出

## 已交付

- 营养目录首页改为上方筛选、下方分页表格；查询名称/别名、来源、授权状态，支持重置、刷新、每页条数。
- 新增、编辑使用侧边窗口；保存保留服务器字段预览和并发检查，行操作可进入既有审核发布。
- 用户选择 CSV 格式。模板为带 BOM 的 UTF-8 中文表头，营养单位每 100g；导入先校验/提示错误行号再确认，最多 500 条/1 MB，只新增草稿。
- 导入所有行通过后单事务创建和逐行审计，失败整批回滚；同一批请求可幂等重试。导出按已应用筛选获取全部匹配数据，上限 10000 条，文本单元格防公式执行。
- 新增端点复用现有 DB-RBAC 与草稿账本，无数据库迁移、依赖或 Phase 7 工作。

## 提交

- `84eb0b7`：列表、CSV、服务/HTTP/数据库测试与模块文档。
- `e4aafc8`：表格、侧边编辑、CSV 界面、组件/E2E 与中文教学。

## 已运行验证

- Backend：CSV/service/HTTP 24 项通过；Ruff 通过。
- PostgreSQL：草稿持久化、中文/字面筛选和稳定分页、第二行失败整批回滚，3 项通过。仅使用 guarded `food_agent_test`。
- Frontend：目录组件 3 files / 10 tests 通过；类型检查通过；生产构建通过（大 chunk 提示不影响成功）。
- Playwright：`npm run test:e2e -- --grep admin-management`，1 项完整公开跨栈场景通过（14.3s），包含新增、审核发布失格、CSV 预览/提交、筛选导出真实下载内容、空态/重置、编辑、普通用户拒绝。
- 中文 JSON 别名查找使用 `json_array_elements_text` 解码后匹配，最终修改经 PostgreSQL 回归验证。
- `git diff --check` 通过。

## 修复过程中发现的问题

- 批量新增审计使用有意义的初始 revision 0，满足既有非空标量 diff 契约。
- 编辑表单只映射可编辑字段；不能把只读 ID/revision 复制到 strict form schema。
- 导出验证直接读取浏览器下载文件；不以附件响应正文的读取行为替代真实下载证据。

## 内置浏览器验收

已检查用户指定的 `http://127.0.0.1:5179/admin/catalog`。刷新加载新构建时，现有会话失效，页面跳至后台登录；未读取或注入任何凭据，未创建本地业务数据。已通过异步问题请用户重新登录。

用户回复已登录后，本次浏览器截图实际处于操作审计页。直接导航至目录触发整页加载并再次跳转登录。源码确认 `AdminAuthProvider` 仅将会话保存在 React 内存，未实现刷新后恢复；因此此次不能把跳转判断为服务端会话过期。后续应在用户登录后通过页面内部链接切换，不再整页导航或刷新。登录恢复能力不纳入本次目录改造。

用户再次登录后，使用同一内置浏览器与站内控件完成：

- 目录页显示真实白米饭记录；查询中文别名“白饭”命中，查询不存在的名称显示空态，重置恢复记录。
- 新增打开空白侧边窗口；编辑回填白米饭、营养值、来源与授权状态，关闭均未写入数据。
- 导入窗口显示 UTF-8、500 条/1 MB、别名分隔规则和只新增草稿说明；未选择文件时确认按钮禁用。未向用户开发库导入测试数据，实际提交由上述隔离 E2E 覆盖。
- 点击下载模板、导出，页面分别显示成功提示。内置浏览器等待 download 事件超时，因此不据此声称已验证该浏览器的文件落盘；下载内容由上述 Playwright 真实文件验证覆盖。
- 验证当前窄屏和临时 1440×1000 桌面断点；桌面恢复侧边导航，筛选在表格上方。DOM 确认文档宽 1440、主体宽 1136，没有整页横向溢出；表格容器宽 1086、内容宽 1111，仅表格内部需横向滚动。截图工具只返回可见面板裁切，未把裁切图当完整宽屏截图。已恢复临时视口设置。

目录改造交付完成；内置浏览器下载落盘仍可由用户手动确认。后台刷新后登录恢复为已有独立问题，本任务未修改认证机制。Phase 7 按用户要求暂不启动。
