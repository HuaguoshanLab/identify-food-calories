---
task: 260906-lz0
status: complete
commit: bef6edc
---

# 第 3 项完成，等待用户检查

固定 Tab 页头与详情返回栏，统一标题所有权、导航配置、安全区和唯一滚动区；设备容器按设计稿居中。只移动标题及其聚焦职责，保留业务内容、路由、API、数值与状态。

## 验证

- 29文件、167条前端测试通过（单worker）；类型检查、构建通过。
- lint仍为基线两项问题，未增加新报错。已有构建大包提示未处理。
- 独立框架E2E 1/1通过；首轮暴露用例依赖截图测试预先创建账号的问题，已使其自行注册隔离账号。旧截图未更新。
- 内置浏览器正常登录后验证四Tab、记录长滚动、餐食详情返回、320px末尾操作与空输入错误、430px边距、768/1024/1440px居中设备容器。
- 非零安全区、实机键盘、200%缩放和完整业务E2E未覆盖；详见 `docs/ui/h5-page-shell.md`。

## 停止点

代码提交 `bef6edc`。本项已完成；等待用户检查，不进入第4项公共组件。先前未提交的基线与基座文档继续保留。
