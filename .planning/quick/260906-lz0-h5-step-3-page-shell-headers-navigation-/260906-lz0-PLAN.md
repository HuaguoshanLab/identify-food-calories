---
task: 260906-lz0
status: complete
---

# 页面框架对齐

用户已检查全局主题并授权继续；完成第 3 项后必须停止。GSD quick 内联执行，沿用明确设计决策，无新增业务需求。

1. 提取四 Tab 的统一导航配置，新增布局拥有的 TabHeader；56px 主体 + 顶部安全区，图标20px、标题20px；详情页48px主体、标题18px、44px返回命中区，保留历史返回行为。
2. 移除真实页面重复 h1 和旧路由聚焦逻辑；保留字段、说明、链接与所有业务状态。餐食详情顶栏用本地名称“餐食详情”。
3. 设备容器桌面高度对齐100dvh减32px，阴影及边框沿用主题；内容唯一滚动，Tab 内容边距常规28px/窄屏16px，详情16px。导航64px主体另加安全区，固定栏是flex同级，不重复留160px空白。
4. pathname变化时重置主滚动区并聚焦页头；query/hash变化不重置，保留填写、追问、版本及跳转入口语义。公开认证保留标题归属、流程与返回目标。
5. 回归组件与路由焦点测试、全量前端测试、类型、构建和lint。内置浏览器验证长内容滚动时页头/Tab固定、窄屏/桌面几何、Tab切换、详情返回和输入错误。已有视觉快照不直接覆盖。

代码评审重点：不改API、数值、认证或模型；不引入DOM隐藏标题补丁；一页一个h1和main；不在组件重渲染时抢走输入焦点。已有lint问题仍按基线单列。
