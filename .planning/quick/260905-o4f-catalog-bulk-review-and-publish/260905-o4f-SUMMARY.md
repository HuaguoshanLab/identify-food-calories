---
status: complete
verification: passed
commit: 535ec14
---

# 营养目录批量操作完成

- 当前页勾选/全选/半选与清空，最多 100 条；翻页、查询、每页条数变更清除选择。审核完成保留勾选方便继续发布。
- 居中确认弹窗统一原因；逐项读取服务端预览、展示版本和可展开差异/影响，使用原公开 API 顺序审核/发布。没有新增后端批量接口或降低 RBAC/授权/审核/审计要求。
- 每个命令独立幂等键和 If-Match。成功/明确失败不再提交，未知结果仅在当前弹窗冻结原因/原键重试。401/403 停止后续请求；已发布/失格/未授权发布/版本变化阻止提交。请求超时有界，操作期间不可关闭弹窗。
- 按 ui-ux-pro-max 采用可访问名称、半选状态、实时文字反馈与稳定居中布局；保留现有样式，没有引入推荐中不适合当前页面的暗色主题或远程字体。

## 验证证据

- Vitest：13 文件、52 项通过；覆盖批量成功、原因必填、独立幂等键、原键重试、成功项不重做、401/403 停止、不可发布项、选择范围。
- TypeScript 检查通过；生产构建通过，仍有既有大于 500 kB 的包体提示（当前 530.49 kB）。
- Playwright `admin-management.spec.ts`：1 passed，15.7s。隔离 food_agent_test 经公开 UI 导入两条已授权测试菜品，批量审核和发布各成功两条，再次发布显示跳过。保留原单条操作、CSV、权限、刷新/退出测试。
- 内置浏览器当前 5179 后台：已有 20 条勾选，打开批量发布，服务端预览显示未授权麻婆茄子与已发布担担饭跳过、其余 18 条待处理；DOM 几何确认弹窗中心偏移 [0,0] 且完全在视口内。点击取消关闭，保留原 20 条选择。没有审核或发布用户业务数据。

## 边界

- 非跨页后台任务；浏览器刷新后不继续队列，已发送请求仍可能已在服务端完成。执行时不要刷新。未知结果应在当前弹窗重试，关闭后需核对状态。
- 当前预览 DTO 不包含审核状态，重复审核可能返回 409；显示核对提示，不猜测为成功。已审核的菜品可直接批量发布。
- 部分失败不回滚已成功项。Phase 7 继续暂停。
