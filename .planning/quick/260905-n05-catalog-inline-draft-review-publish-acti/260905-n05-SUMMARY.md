---
status: complete
date: 2026-09-05
commit: 7668209
---

# 目录操作居中弹窗

## 交付

- 按用户后续指示，将中途实现的表格内展开操作改为居中弹窗，而非右侧抽屉。
- 每行保留编辑、审核、发布；详情入口保留原有审计和失格能力。
- 新增/编辑直接保存草稿，不再套二次预览确认。仍调用公开服务端预览、校验打开时的 revision，冲突保留输入，不静默覆盖最新草稿。
- 审核/发布先读取真实生命周期投影，默认只显示菜名、版本、影响、操作原因与确认。字段差异可选展开；错误出现在当前弹窗，不臆称其他管理员修改。
- 同一审核/发布命令重试复用幂等键，提交期间关闭、输入和重复点击受保护。未授权或当前版本已经发布时，不提供错误的再次发布入口。
- CSV 共享弹窗统一居中，导入、导出逻辑不变。
- ui-ux-pro-max 用于确认焦点、标签、居中与滚动边界；沿用项目样式，不引入新字体或依赖。gsd-quick 内联完成计划、实施、验证和提交，没有推进 Phase。

## 验证

- 后台全量 Vitest：11 文件、41 测试通过。最后命名清理后目录定向19测试再次通过，TypeScript 检查通过。
- 生产构建：设置项目要求的 `VITE_ADMIN_API_BASE_URL=/api/v1/admin` 后通过；首次缺少该变量触发预期 fail-closed 拒绝。保留现有体积告警（约522KB chunk），本次未做无关拆包。
- Playwright：1 条隔离完整跨栈流程通过，包括注册/验证、受审计首位管理员建立、真实登录、配置、新增居中保存、行审核/发布居中位置、详情失格、CSV导入筛选下载编辑、普通用户403。
- E2E 只 reset 受保护的 food_agent_test，不改开发库。移除测试清空共享 Mailpit 全部邮件的步骤，改为已有的唯一邮箱精确读取。
- 内置浏览器5179：审核弹窗在717×986视口中为512×399，中心与视口中心一致；差异折叠，空原因确认显示必填提示。新增和编辑窗口居中、长表单内部滚动、保存按钮在页脚；编辑正确回填担担饭数据。未为验收发布开发库菜品。
- 浏览器中用户也在操作，后续一次编辑校验点击时弹窗已关闭，故不把该次操作声称为验证通过；成功保存/审核/发布由隔离E2E验证。
- `git diff --check` 通过。

## 范围与残余

- 不修改后端审核发布规则或营养数据；不自动发布。
- 后端尚未提供细分409原因或列表审核状态，因此错误提示只给出可能的前置条件，不伪造精确原因。
- 原详情页仍保留旧的通用409提示；本次优化的是表格入口的精简弹窗。
- 原先未提交的 debug 记录原样保留，不纳入此次代码提交。
