# 独立重量解析与追问恢复

用户已确认：纯数字默认克；支持 g/克/公克、kg/千克/公斤、斤/市斤、两/市两（大陆标准），换算后大于 0 且最多 2000g。完整匹配，不截取未知单位中的数字；不支持体积/模糊份量。

1. 独立 `agent/weight.py` 命令解析纯函数，使用 Decimal；补充/修正入口复用，非法输入先拒绝且保持 checkpoint。修复 meal resume 无效输入丢失等待状态。覆盖单位、非法/超限和真实 PostgreSQL 恢复路径。
2. H5 原样传完整重量，显示后端安全错误并保留表单；修正重量不再截取数字丢失单位；统一文字/图片错误文案。补组件测试。
3. 跑针对性测试、类型检查、真实浏览器对照（100kg 拒绝后 0.1kg 成功），同步中文文档、诊断和 STATE，原子提交。不改营养目录数据、数据库结构或 Phase 路线。
