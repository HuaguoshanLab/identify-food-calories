# 第 4 项：共享组件
1. 保留 Base UI API、业务数据与回调，调整现有按钮、输入、卡片、标签、提示及确认弹窗。默认操作至少 44px；紧凑显式尺寸保留；输入 16px；白色卡片弱边框轻阴影；弹窗 360px 上限与至少16px边距。
2. 保留现有局部加载及空态文案，不提前抽象业务组件或改各页布局。Skeleton 继续依赖全局减少动态规则。
3. 执行组件/全量串行测试、类型检查、构建与 lint；内置浏览器检查表单、确认取消、移动宽度。文档记录已验证与未验证范围。
4. 单独提交本项并更新 STATE；停止等待检查，不进入第5项。
参考：docs/ui/h5-foundation.md、frontend/ARCHITECTURE.md、设计稿源码。技能检索的黑金与超大字号不适合本项目，采用已批准森林绿设计。
