# H5 第7项：计划与历史

> 最新用户要求覆盖下文临时资料表单约定：资料与目标在“我的”统一维护，计划页只展示摘要。

## 最新流程修正

缺资料时提示“去填写”并禁用生成；读取失败同样禁止生成。我的资料支持首次创建、编辑及删除。计划提交使用已保存资料和复核偏好，save_profile=false，不再提供临时资料输入和保存开关。重新生成时隐藏旧报告，取消后恢复。

170项前端测试、类型与构建通过，原有两处lint保留。新增隔离E2E验证缺资料禁用→我的首次保存→返回计划摘要→未复核阻止提交，320px无横向溢出。内置浏览器检查只读摘要布局和未复核提示；助手未修改开发资料或主动调用模型。真机键盘、安全区和深色模式未验证。

## 实现

保留完整临时资料表单、可选保存、偏好复核及真实Agent行为。设计稿摘要不能取消本地填写能力：用身体资料、活动、目标、偏好白卡分组，选中项浅绿底。输入16px和44px触控区延续共享组件规范。

计划页历史入口采用日历图标、白卡与向右箭头。目标概览浅绿块保留目标范围、计划值、偏高/低/适中及说明；三餐卡片明确区分餐次、能量、菜名、份量、口味与已遵守条件。调整表单白卡化，历史列表使用日历圆标和右箭头；详情日期、版本按钮与删除统一视觉。

未引入模拟数据、汇总计算规则、第三方图表或新的请求；时区确认、自动保存、历史分页、版本切换、删除范围与放宽提示保持原实现。

## 验证

- 29文件167测试、类型与构建通过。已有2处lint错误及构建大包提示保留。
- 隔离Playwright响应式、滚动、键盘和导航测试1项通过。
- 内置浏览器读取开发服务现有今日计划；进入重新生成表单、切换活动项后取消，保留原计划；未提交生成或调整。
- 320px表单与保存详情文档/main均320px；输入、单选、偏好复核与可选保存仍可达。
- 历史入口→真实计划详情→删除确认→取消验证通过；当前仅第1版，上一版/下一版均按原规则禁用。跨版本切换由已有SavedPlanPages测试覆盖，未创建新版本。
- 已检查窄屏三餐卡片和标签换行。真机软键盘、非零安全区、200%缩放、深色模式未验收；失败及空态由既有测试覆盖。

完成后停止等待用户检查，下一项分析页尚未开始。
