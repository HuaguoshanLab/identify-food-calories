# 组合餐逐项份量适配验收

2026-09-16，策略 `planning-selection.v10`。本记录补充分类核查结果，不覆盖适配前证据。

每个角色以原份量为基准，默认只在 75%–125% 内取整数克，重算每项营养。保留原份量，调整失败或预算耗尽不放宽边界。午晚餐每餐最多额外计算 12 项（默认每角色保留 4 项）；原版与适配版共享 64 次组合预算。

真实目录只读试算五档目标 1400、1600、1800、2000、2200 kcal 全部通过确定性校验，午晚餐均为组合。无偏好、无近期历史；具体结果见 `portion-adaptation-verification.json`。不能推导总体生成成功率。

真实 H5 验收：

- 第 3 版生成成功，全天 1481.8 kcal；晚餐白米饭 174g、龙井虾仁 188g、问政山笋 188g。
- 提交“晚餐换一份”后第 4 版保存成功，主食改为冠头蒸馒头 113g，早餐和午餐页面文本逐字相同；全天 1515.5 kcal，全部指标在目标范围内。
- 历史第 3 版仍保留白米饭 174g；第 2 版仍保留原固定份量组合午餐（200g/200g/250g，690 kcal）。

回滚：在未提交配置中设置 `PLANNING_PORTION_ADJUSTMENT_ENABLED=false` 并重启，停止新份量适配，历史照常读取。本次无新增迁移。

自动化验证：规划测试 204 项、Agent/API 合约和配置测试 48 项、隔离 PostgreSQL API 测试 8 项、组合餐 Playwright 端到端 1 项通过；Ruff、定向 Mypy、H5 类型检查、定向 ESLint 通过。端到端验证了必须适配才满足目标、营养逐项计算、连续同文换餐、固定餐次、历史冻结和缺少候选的失败保留。遥测接收端未启动，导出出现告警，不影响业务断言；遥测链路未验证。

限制：分类与来源质量问题仍以 `核查结果.md` 为准。没有逐道换菜或手动调克数界面；并非全库最优搜索；未评测真实模型或 Mem0 效果。
